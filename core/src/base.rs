//! Re-export module for backward compatibility.
//! Core types are directly under `uix_core::`, this module mirrors the
//! old `src/base/` structure for transitional compatibility.

pub use crate::animation;
pub use crate::geometry;
pub use crate::types;
