//! Color token trait — brand, neutral, semantic, shadow, and link colors.
//!
//! `ShadowToken` 与 `IColorTokens` 定义在 `crate::render::painting`。

pub use crate::render::painting::{IColorTokens, ShadowToken};
