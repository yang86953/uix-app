[package]
name = "uix-framework-pipeline"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = "symbols"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
