# 变更日志

[← 返回 UIX Lang 索引](../README.md)

> **文档角色**：变更记录——只记录已经发生的语言、兼容和文档治理变化，不持有当前完整契约。
>
> **接口**：声明 UIX Lang 语言面的变更记录。依赖：[版本策略](版本策略.md)。导出：无。

## 0.0.3（2026-08-30）

- 语言版本随根 crate 升级到 `0.0.3`；本版本没有新增语法、组件登记或生成映射，既有 `0.0.2`
  语言基线保持兼容。

## 0.0.2（2026-08-30）

- 语言版本随根 crate 升级到 `0.0.2`；本版本没有新增语法、组件登记或生成映射，既有 `0.0.1` 语言基线保持兼容。

## 0.0.1（2026-08-29）

基线版本（2026-08 文档体系重构时建立）。

### 已实现能力基线

- **入口宏**：`uix!`（内嵌源码与 `.uix` 文件入口）、`uix_app!`（`<App>` 根）、`uix_items!`（`<Record>` 模块级结构体与 `<Visual>` 静态常量）。
- **语法面**：顶层声明区（样式类、`@theme`、`<Widget>`、`<Record>`、`<Visual>`、`@import` / `@export`）；`<If>` / `<ElseIf>` / `<Else>` 相邻条件链；`<For>` 循环（索引、`key` 身份）；受限表达式子集；数据类型构造链与对象字面量；`setState` / `setTheme` / `setStyle` 内置操作；句柄位绑定。
- **样式与主题**：样式类、内联样式、`extends` 组合；`:hover/:disabled/:checked` 差异叠加；`@theme` 完整 `DesignTokens` 白名单；类型化 `#token` 引用；`setTheme` 与 `setStyle` 切换。
- **组件矩阵**：通用、布局、输入、展示、反馈、导航、图表七类共 108 个标签已登记（含仅作为 `Row` / `Grid` 直接子项使用的 `Col`；完整分类见 [README](../README.md#状态声明) 与各组件参考）。
- **模块系统**：`@import` / `@export`（具名/全量、递归依赖、增量编译）。
- **诊断模型**：未登记能力编译期诊断（位置 + 修复建议）；解析错误 SourceSpan。

### 变更记录

| 2026-08-21 | 编译工具链 | 共享 Compiler System、schema v3、SourceGraph/无损 CST/AST/TypedUiIr/Rust Emitter 与同名 `.uixmap.json` 源码映射落地；新增 `uix` 的 check/fmt/compile/query/lsp，LSP 支持内存 overlay 与递归 import；生产仍为纯 AOT，不携带解释器、监听器或 LSP 会话 | [编译器架构](../设计/编译器架构.md) |

| 日期 | 范围 | 说明 | 关联 |
|---|---|---|---|
| 2026-08-27 | 测试面 / 门禁 | 建立消费者层门禁证据基建：评估 trybuild / ui_test 后未引入外部框架，根包 `tests/uix_lang_gate_public_api.rs` 经公开 check 入口锁定诊断码、阶段、主 SourceSpan 与文案快照；新增 derive-only 工作区成员 `uix-lang-gate-consumer`，以真实子进程失败编译驱动成员混用双写（UIX1000）与 CascaderValue 值类型门禁（UIX2000），失败输出行同时携带码、阶段与行列；组件 / 属性级 UIX2001 关闭场景留待后续批次 | [语言完善计划](语言完善计划.md) |
| 2026-08-27 | 工具链 | 完成公开宏文件入口消费者证据补全：新增 `tests/uix_lang_public_api.rs` 消费 `uix!` 递归导入闭包、`uix_app!` 未运行 builder 与 `uix_items!` Visual 静态项并断言单一静态地址（五个此前无引用的 fixture 资产归位）；`uix scaffold visual` 骨架输出锁定单测落地；消费者层 compile-fail 证据基建与 capability 关闭场景验证登记为下一目标 | [语言完善计划](语言完善计划.md) |
| 2026-08-27 | Widget / 语法 | 新增 `@props` / `@state` / `@computed` / `@actions` 块级成员声明，与字符串属性形式解析等价（双写拒绝、仅在 `<Widget>` 内、不参与 Slot 归位）；成员列表统一允许尾随逗号 | [语法 · Widget 成员](../规范/语法.md#2-widget-成员) |
| 2026-08-27 | Schema / 能力门禁 | 组件专属状态值类型（`CascaderValue`、`Vec<UploadFile>`）从核心白名单迁入 `UiProjectionSchema` 按拥有组件登记并挂接 capability 门禁；未启用时在生成前定向诊断，check 与 AOT 一致；诊断建议清单改为 schema 生成 | [类型语法](../规范/语法.md#3-类型语法) |
| 2026-08-27 | 工具链 | `uix scaffold visual <rust文件> <StructName>` 从同模块 Rust 结构体生成可编译的 `<Visual>` 骨架（已知标量类型给占位初值，复合类型输出 TODO 提示），消除 Rust/`.uix` 双侧手写字段搬运 | [编译契约](../规范/编译契约.md#4-编译模式边界) |
| 2026-08-27 | 编译器内部 | 54 个生成器模块中逐字复制的 `find_attribute` 收敛为共享单源实现（归一化指纹全等比对迁移）；`required_attribute` 等其余复制实现登记为后续收敛批次 | [编译器架构 · 迁移顺序](../设计/编译器架构.md#8-迁移顺序) |
| 2026-08-27 | 编译器内部 | 完成 `required_attribute` 收敛批次：39 个生成器模块副本统一为带组件标签与修复建议参数的共享单源实现，缺失诊断与修复建议逐字不变；清理漏迁的本地 `find_attribute`；`optional_boolean` 三份等价副本并入值映射、`is_renderable_node` 两组内去重；同名不同义辅助不合并的判定依据登记于迁移顺序 | [编译器架构 · 迁移顺序](../设计/编译器架构.md#8-迁移顺序) |
| 2026-08-27 | 设计治理 | 新增[边界成本度量](../设计/边界成本度量.md)：启发式脚本量化处理器分类与回调密度（基线：96 文件 6287 行，纯界面处理器 43、转发 3、混合 0），实证校准语言边界画点 | [语言边界](../设计/语言边界.md) |
| 2026-08-24 | 编译契约 / Widget | 新增顶层 `<Visual>` 具名静态视觉记录与 `uix_items!` 静态项生成，使构造、测量、绘制和组合根共享 UIX 唯一事实源；固定数组保持 const 初始化语义，Items 资源可省略视图根，并清零双源默认表与巨型位置参数壳债务 | [组件规范](../规范/组件.md#5-静态视觉记录visual) |
| 2026-08-21 | 文档治理 | 按事实类型建立唯一文档所有者，分离当前规范、登记参考、长期设计、实施计划与历史记录；补齐闭合结构文法、State 所有权和组件级事件载荷契约，无语言语义变更 | [语言首页](../README.md) |
| 2026-08-21 | 产品方向 / 职责边界 | 撤回“应用开发语言”方向，明确 UIX Lang 只描述 UI，业务逻辑、领域状态、服务、异步流程与生命周期由 Rust 持有；既有同步 action 收敛为局部 UI 交互编排 | [语言边界](../设计/语言边界.md) |
| 2026-08-21 | Widget / action | 为无参数同步 action 增加独立 `do { ... }` 语句面，支持块级局部、赋值、条件与返回；保持事件位置纯 AOT 静态内联、调用顺序、实际 View 的 `setStyle` 绑定和全图递归诊断 | [组件规范](../规范/组件.md#32-同步-action) |
| 2026-08-20 | Widget / action | 实现 `actions` 无参数单表达式同步 action，在事件位置静态内联为 Rust；补齐名称、参数、作用域、事件载荷与递归诊断 | [组件规范](../规范/组件.md#32-同步-action) |
| 2026-08-20 | 产品方向 / 编译模式 | 曾将 UIX Lang 定位为应用开发主语言；该方向已于 2026-08-21 撤回，保留本条作为历史记录 | [语言边界](../设计/语言边界.md) |
| 2026-08-17 | Widget / For | 实现 For 内 props、private state、computed、事件、状态样式与 Slot 投影的逐实例语义；key/位置完整路径同时约束组件状态复用和卸载清理 | Gitea #22 |
| 2026-08-17 | 样式 / 阴影 | 为 `boxShadow` 登记可选正负 spread，保持既有四段语法的零 spread 兼容语义 | Gitea #5 |
| 2026-08-16 | 组件 / 图表 | 为 `ScatterChart` 登记互斥的 `bubbleData`、`BubbleData` 构造器与 `bubbleScale`，保持动态集合精确类型检查 | #1097 |
| 2026-08-15 | 组件 / 图表 | 为十二类图表登记 `legend`、`animation`、`interactive`、`brush` 与 `tooltip` 共同配置，并保留运行时图表组件的交互状态唯一所有权 | #1096 |
| 2026-08-15 | 样式 / 背景 | 实现本地单层背景图、固定方向双色渐变、剩余空间定位与四种图片重复方式；接入 Style、ImageService、状态差异和真实宏消费者 | #1068 |
| 2026-08-15 | 样式 / 布局 | 将与 Flex/Grid 架构冲突的 float/clear 从伪规划项收敛为明确非目标，并提供属性级编译诊断与可执行替代建议 | #1069 |
| 2026-08-15 | 语法 / Widget / 事件 | 实现相邻 If/ElseIf/Else 短路链、按事件登记的 `$event` 字段校验、prop 默认值与必填诊断，以及 Vec<number>/Vec<Record>、Some 和嵌套 record 集合初始值 | #1014 |
| 2026-08-15 | 样式 / 主题 | 实现 hover、disabled、checked 状态伪类差异叠加；`@theme` 对齐完整运行时设计 token 字段并增加类型化 `#tokenName` 校验 | #1010 |
| 2026-08-15 | Widget / Slot | 实现默认与具名内容插槽、调用方作用域投影、嵌套转发及 For 内纯组件展开；补充主演示容器复用 | #1013 |
| 2026-08-15 | 表达式 / Widget | 实现不可变数组操作扩展、单参数受限闭包与按声明顺序求值的 computed 派生值；补充真实宏消费者和主演示 | #1012 |
| 2026-08-15 | 编译契约 / Widget | `uix!` 与 `uix_app!` 写入确定性生成文件并以来源注释回映表达式；实现 `external` 外部依赖白名单与未声明标识符诊断 | #1011 |
| 2026-08 | 文档 | 文档体系按「规范 / 参考 / 指南 / 变更」重构；规范拆分九篇，新增术语表、事件参考、版本策略 | #2157-#2161 前置 |

### 历史设计记录

组合与表达式改进的设计 1–7 已实现并回填规范正文；设计理由、当时的依赖顺序与 SMC 边界保存在[历史设计记录](目标设计.md)。当前契约以规范和参考正文为准。
