# 进度

[← 文档中心](README.md)

> **接口**：本文档组件是进度文档的路由索引，记录当前交付状态和活跃差距。依赖：[产品](产品.md)（需先了解目标）、[架构](架构.md)（需了解分层）。导出：按分类路由到 `进度/` 下的各文档组件。

## 适用线索

当需要了解"当前做到哪了、还差什么、瓶颈在哪"时，从本文档进入。

## 文档入口

| 需要的事实 | 权威入口 |
|---|---|
| 产品目标、能力与边界 | [产品](产品.md) |
| 当前公开用法与示例 | [使用](使用.md) |
| 内部边界、依赖与不变量 | [架构](架构.md) |

## 权威范围

本文档及 `进度/` 子目录下的文档组件共同构成 UIX 的交付真相。以下事实以本组件为准：

- 版本发布状态与交付物清单
- 功能逐项实现状态与证据
- 组件稳定性与已知缺陷
- 性能基线与优化方向
- 平台工程与运行保障进度
- 门禁、批次和验证结果的带时点记录；产品目标、公开用法与内部机制分别引用对应角色文档

## 进度总览

| 状态 | 事项 |
|------|------|
| ✅ 已完成 | 功能实现 110/110 项、迁移前 Rust fence 214/214 通过、稳定帧 ~4.7ms、42 项组件修复与真窗验证、Platform 公开面定义、Diagnostics 核心落地、S0-01～S0-05 基线审计、S1-01～S1-06 平台回调入队、S2 旧日志迁移与 native trait 失败语义清理（12 个 trait 全部改 typed Result） |
| ✅ 已完成 | 消除 graphics→ui 反向依赖（主题令牌分层收口）：`ThemeSnapshot` 删除、`draw::PaintContext` 移除主题、UI-owned `PaintContext` 接管 `tokens()` 与 scope，`src/draw` 零 `crate::ui` 引用 |
| 🔄 进行中 | 内存优化（~142 MiB Vulkan，瓶颈：驱动+字体）、G5 长稳失败归因、GFX-R5 mixed-DPI 与 AMD / Intel 证据、功能账本 8.7～8.12 六项映射补全、联系表最终审美确认 |
| ✅ 已完成 | **SMC 模式改造（最高优先级，主人 2026-07-31 决定）**：SMC-01～SMC-07 全部落地（core 定级、platform / graphics / ui / data / app 五 System 迁移、跨层强制规则全量闭合） |
| ✅ 已完成 | **SMC EventBus 基础设施与治理（2026-08-03，主人决定按通用 SMC 全面改造）**：新增 `src/bus/` 进程内同步类型化 EventBus（`EventBus` / `Subscription` / `Publisher<F>` 窄发布端口 / `DispatchReport`，含不可重入、幂等注销、空壳句柄、深度上限 64，G-08 验证矩阵 17 项）；窗口事件总线治理化（移除 `-> bool` 命令式返回语义，8 项测试）；阶段 D 试点：app 主题变更事实 `ThemeApplied`（SystemEvent(SMC)，事实建立点 = 主题替换完成后，遥测订阅接入）；路由清单（G-05）落地并登记；UIX 应用映射 v1.1 废弃，以[通用 SMC 文档](<System Module Component 设计模式.md>)为唯一权威 |
| 🔄 待修复 | **全量审核发现（2026-08-03）**：① ~~P1 架构违规：G5 遥测引入 `event_loop`/`window` → `agent` Module 具体类型依赖（`WindowAgentState`/`AgentCommandQueue`/`AgentWindowRegistration`，含 `window_semantics` 依赖 agent 私有类型），违反通用 SMC 共同依赖规则~~ ✅ **已修复（2026-08-04）**：`WindowAgentState`/`AgentCommandQueue`/命令协议契约上移 System 私有边界 `queues/`（`agent_command_queue`/`window_agent_state` 两新模块），`agent` 只实现执行契约 `AgentCommandExecutor` 与语义发布端口 `AgentSemanticsPort`（契约归 `window_semantics`），组合根 `session_runtime` 组装期注入；rg 审计兄弟 Module 与 System 私有边界对 agent 引用全 0，默认 / agent-control / test-harness / all-features 编译 + doctest 47/47 + 全量测试通过；② **P2 文件行数超限**：13 个文件超 900 行（`draw/painting/encoder.rs` 2601、`draw/backend/gpu/mod.rs` 1941、`draw/painting/recorder.rs` 1759 等，全部既有遗留）——已拆 3 批 11 个：第一批 5 个（encoder 2601 / recorder 1759 / gpu mod 1941 / gpu backend 1582 / table 2638，2026-08-03）；第二批 4 个（misc 1432→4 文件、form_binding 1478→5 文件、layer_tree 1331→3 文件、modal 1271→3 文件，2026-08-04，commit `975212b`）；第三批 3 个（advanced 3295→6 文件、fields 1333→2 文件、cascader 1265→3 文件，2026-08-05，commit `f3c5e24`/`d8b8b6d`/`bfef7e1`）；第四批 4 个（tree 1248→3 文件、tree_layout 1235→4 文件、tree_core 1022→4 文件、select 1159→6 文件，2026-08-05，commit `da47b98`/`fbda212`/`951454c`/`a6f8c79`）；第五批 4 个（notification 1123→3 文件、calendar 995→2 文件、bar_chart 992→3 文件、animated 987→2 文件，2026-08-05，commit `a826a24`），剩余 src 超限文件 30 个；③ ~~P3 clippy all-targets：11 处测试代码 `expect`/`unwrap`~~ ✅ **已修复（2026-08-04）**：全部改为 `let-else` panic 断言，clippy all-targets 零错误；④ ~~P4 unsafe 无 SAFETY 注释：`event_loop.rs:555/582` 裸指针解引用~~ ✅ **已修复（2026-08-04）**：两处补 SAFETY 注释（主线程、无重叠可变借用、瞬时 publish）；⑤ **P5 编译警告 176 个**（unused imports 等，多与 feature 门控相关，既有遗留） |
| ✅ 已完成 | **理想使用演进 E-01～E-09 全部落地（2026-08-01）**：类型化表单 `Form::model`、声明式虚拟化 `Table::virtualized`、受控浮层 `Modal::builder`、全局反馈门面 `ui::message()`/`ui::notify()`、声明式无障碍动作 `semantic_actions`、路由 key 派生宏 `#[derive(Display)]`、富文本链接回调 `RichText::on_link`、全局服务注册 `App::register` 与 i18n key 宏 `t!`、主题品牌种子 `Theme::custom`；使用文档「目标写法」示例全部升级为已落地 rust fence；下一步 S4 真窗视觉验收 |
| ✅ 已完成 | **SMC 完成度独立审计（2026-08-01）**：跨 System 依赖与系统列表「允许依赖」列一致（platform→graphics 零依赖、data 仅 core、ui 无 app/data）；五 System 兄弟 Module 反向边 rg 全量扫描全 0（native/draw/ui/app/core）；doctest 47/47（含 29 个 compile-fail deny 契约）、7 组公开 API 契约、workspace all-targets 与 Windows GNU check 通过；同时修复 `diagnostics::crash` panic hook 测试与 `wnd_proc`/TSF thunk 故意 panic 测试的全局 hook 竞态（新增 `PANIC_HOOK_TEST_LOCK` 共享测试锁，lib 测试 12 次连续通过），详见[运行保障](进度/运行保障.md) |
| ✅ 已完成 | **S4-01 真窗组件视觉验收（2026-08-01）**：clean commit `276657343c14` 上真窗视觉门禁首次通过——92 组件 / 663 声明状态 / 847 行 PASS 全部在 clean 环境生成（AMD RX 5700 / 96 DPI / build 26100），329 PNG + 22 联系表 + `graphics-auto-vulkan.log`，报告见 `test-reports/0.0.1-276657343c14-20260801/component-visual/report.md`（test-reports 不入库）；替代历史 dirty UNVERIFIED 证据 |
| ✅ 已完成 | **S4-03 无障碍证据 schema 与收集器（2026-08-01）**：固化 `manifest.json`（`uix.accessibility.manifest.v1`）+ 逐项 JSON（`uix.accessibility.item.v1`）schema（见[无障碍证据](进度/无障碍证据.md)）；新增真窗收集器（`tests/support/agent_gui_windows/accessibility.rs`：组件 QA 92 case + charts 场景两个测试，提取 role/name/state/value/actions/focus/keyboard）与组装器（`scripts/build_accessibility_manifest.py`）；ChartPlaceholder 补齐语义节点（role=image + title 名，对齐 Bar/Line/Pie）；clean commit 上生成 110 行 manifest：104 captured + 6 ledger-gap（8.7～8.12 账本缺口，不冒充）；pending-scene 已清零 |
| ✅ 已完成 | **F 批次「使用落地」（2026-08-01）**：使用文档最后 19 处「目标写法」`text` fence 全部落地为 `rust uix-compile` 示例（16 个新锚点）；配套 API：`Transition` + `View::enter/leave`（含 stagger 延迟接入播放管线）、`Header::title` / `Content::child` / `Grid::children`、`App::diagnostics_runtime`；`Platform` 便捷查询已存在（`uix::platform` 公开面）无需新增；契约测试 `tests/usage_facade.rs` 5 项通过 |
| 📋 规划中 | **使用方按需编译**：未选择且非传递必需的 UIX 能力不进入使用方编译图，已选择但不可达的代码不进入 release 目标程序；当前只有部分 backend / serde / Agent feature 与 release LTO，尚未形成完整能力分区和使用方证据矩阵 |
| 📋 规划中 | **性能与资源持续优化（未排期）**：未来继续降低启动、首帧、交互与稳定帧成本，以及 CPU、GPU、内存、线程、I/O 和空闲能耗；当前只登记长期目标，不承诺开始时间、执行顺序或交付版本 |
| 📋 规划中 | 四平台同等视觉矩阵、窄 present、复杂脚本 shaping、移动端 |

## 内容索引

| 要回答的问题 | 权威文档 |
|---|---|
| 当前交付目标、优化方向与瓶颈 | [交付方向](进度/交付方向.md) |
| 版本号、发布状态与交付物 | [首发状态](进度/首发状态.md) |
| 功能到源码和证据的逐项映射 | [功能账本](进度/功能账本.md) |
| 组件行为能力的活跃差距 | [组件功能缺口](进度/组件功能缺口.md) |
| 逐组件风险、回归与验证等级 | [组件稳定性](进度/组件稳定性.md) |
| 当前未关闭缺陷 | [已知缺陷](进度/已知缺陷.md) |
| 无障碍证据 schema、收集与状态语义 | [无障碍证据](进度/无障碍证据.md) |
| 平台公开面、硬件覆盖与跨平台状态 | [平台工程](进度/平台工程.md) |
| 错误处理、恢复、日志与崩溃边界 | [运行保障](进度/运行保障.md) |
| 帧耗时、内存与图形瓶颈 | [性能与内存](进度/性能与内存.md) |
| 主题令牌跨层迁移状态 | [主题令牌分层](进度/主题令牌分层.md) |
| 理想 API 的演进记录与完成条件 | [理想使用演进](进度/理想使用演进.md) |
| 使用方按需编译的当前差距、阶段与门禁 | [使用方按需编译](进度/使用方按需编译.md) |
