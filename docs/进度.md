# 进度

> 缺口与未关闭项；落地以源码 / 测试为准。

## 现状

- P0–P5（零闲置）：已落地
- P6（Windows 生产可用）：未闭合（真实 Vulkan、帧/恢复 GPU 真机、typed 终态收尾、真机 IME/证据）
- 其后：移动端 / 网络·同步 / 读屏桥（先产品裁决）

**P6 gate**（Linux/macOS/移动端不计）：

1. `cargo test --all-targets` 全过  
2. **图形契约闭合**：GFX-R0–R3 全关  
3. 失败路径具备 **typed** 诊断；不得静默换 GPU API / 伪成功  
4. 微软拼音（系统 IME）前台 composition → commit 真机过关（RISK-001）  
5. 上层无 OS 分叉  

推进：`R0→R1→R2→R3→R5→R6`。Metal `GpuNative` / D3D12 扩展不得早于图形契约闭合。

## 未关闭

| ID | 内容 | 下一动作 |
|----|------|----------|
| GFX-R0 · BUG-001 | Win/macOS 真实 Vulkan（WSI/portability+present）；替 Planned stub | 落地 Win32 / MoltenVK adapter |
| GFX-R1 · BUG-004 | Additive/Scroll IR+recording+参考执行+NativeGpu readback/**replace upload** 已落地（D3D11 WARP；**WGL/EGL/D3D12/Vulkan 均声明 `upload_surface_pixels`**；D3D12 WARP replace 实测）；圆角 Additive 仍 typed NotImplemented；Vulkan 无 native readback（destination-dependent IR 在该后端仍 NotImplemented）；无独立 GPU blend/copy 原语 | 可选 GPU blend/copy；Vulkan readback 若要闭合 NativeGpu Additive/Scroll |
| GFX-R2 · BUG-005 | device/surface-loss FSM **已有**；真机 loss / 同 API recipe 切换 / Software 终态 present 证据未闭合 | 真机 fault 注入与 Software present 矩阵 |
| GFX-R3 · RISK-004 | `IGraphicsContext` / `GraphicsEngine` / `RenderBackend` **均已去掉 void `shutdown`**（仅 `try_shutdown`）；session teardown fault 注入已有；`RenderSession` / `WindowSession` 保留 concrete `shutdown()` 作日志便利 | 闭合（无真机条件下） |
| GFX-R5 · BUG-006/007/009 · RISK-002/006 | 真机 parity/fault/resize/多驱动/稳定性；前台 capture | 真机矩阵 |
| GFX-R6 · RISK-005 | 共享 `GraphicsDevice` + 逐窗 `WindowSurface`；长跑 profile | 正确性闭合后做 |
| RISK-001 | 微软拼音前台 composition（**P6 门禁**） | 先真实会话，再裁准则 |
| RISK-003 | macOS 仅 cross-compile | 先裁验收标准，再 smoke |
| BUG-002 | Input/Tab 焦点（待验证） | 真机 Tab；IME→RISK-001 |
| BUG-003 | 最小化后再最大化曾黑屏（待验证） | 实机确认铺满重绘 |

## 待决

| 问题 | 时机 |
|------|------|
| macOS 真机验收标准 | 有真机与范围后 |
| 移动端首批 OS / 图形栈 | P6 后 |
| 网络/同步：`data` 或新域 | 有真实用例后 |
| 读屏桥范围与平台顺序 | 产品单独立项 |
| 微软拼音 production 细则 | 有最小真实会话后 |
| Metal GpuNative vs D3D12 顺序 | 图形契约闭合后 |
| `Computed` capture / 连续指针 / `from_primaries` | 接受则写入[`使用.md`](使用.md)，否则改代码 |
