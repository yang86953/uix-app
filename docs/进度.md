# 进度

> 当前重心：**Windows 生产可用（P6）**。契约见[架构](架构.md)；Linux / macOS / 移动端 / Agent 不计本 gate。

## 目标

把 Windows 桌面路径做到可交付生产：默认 Vulkan 真机可用、失败 typed、系统 IME（微软拼音）过关、**空闲真休眠/遮挡可停帧**、无 busy retry。上层无 OS 分叉。

**未闭合。** 已落地：DeepIdle、逐窗 one-shot、隐藏/最小化零唤醒、D3D11 bitblt 遮挡休眠、其余 Windows recipe typed `Unsupported` + 生命周期休眠、默认 Vulkan PixelUpload 真窗 present 与最小化/恢复、`TerminalFailure` 吸收态。

### Gate（仅 Windows）

1. `cargo test --all-targets` 全过
2. 图形契约生效（[架构·图形](架构.md#图形-api与帧提交硬约束)）；下表图形证据关完
3. 失败 **typed**；禁静默换 GPU API / 伪成功
4. 微软拼音 composition → commit 真机过关（RISK-001）
5. 上层无 OS 分叉
6. one-shot；隐藏 / **遮挡** / zero extent / 持续失败无 layout、present 或 busy retry（含默认 Vulkan 路径的遮挡停帧或诚实的 typed unsupported + 生命周期休眠）

### 推进顺序

`RISK-001` → `GFX-R2` → `BUG-002` → `GFX-R5` → `GFX-R6`。

`GFX-R1` 圆角 Additive 先裁决；Metal/D3D12 扩展不进本 gate。

## 未关闭（挡 P6）

| 优先级 | ID | 缺口 | 下一动作 |
|--------|-----|------|----------|
| P0 | RISK-001 | 微软拼音前台 composition 未过 | 真实前台会话 → 裁准则 |
| P1 | GFX-R2 · BUG-005 | 可控集成已贯穿 DeviceLost → Software CPU 帧 → 唯一 Presenter 成功并重置；缺 Win 真机 loss / recipe 切换 / Software 终态 present | 真机 fault 注入 + Software present 矩阵 |
| P1 | BUG-002 | 真实 HWND `WM_KEYDOWN/UP` Tab 映射已过；缺前台物理系统键盘 + 隐藏 / 浮层组合证据 | 系统键盘验收；衔 IME |
| P2 | GFX-R5 · BUG-006/007/009 · RISK-002/006 | Win parity / fault / resize / 多驱动 / 稳定性 / 前台 capture | 真机矩阵 |
| P2 | GFX-R6 · RISK-005 | 共享 device + 逐窗 surface 长跑 | 正确性闭合后 profile |
| 裁决 | GFX-R1 · BUG-004 | 圆角 Additive 仍 NotImplemented；`GpuNative` / GPU blend 未裁决 | 产品/架构裁决是否挡发布 |
| P2 | BUG-008 · DRAW-SYNC | 绘制裁剪同步：清空区（`begin_frame`）与录制管线裁剪区独立计算无编译期同步；不一致时父背景可越过 clip 覆盖未标脏兄弟节点像素 | 统一 dirty 并集来源，加编译期或运行时同步 |

## P6 后 / 不计本 gate

<a id="附能力不计-p6"></a>

| 类 | 项 | 时机 |
|----|-----|------|
| 三端债 | Wayland occlusion v6+、macOS/Wayland 真机（BUG-010–015、RISK-003、非 Win GFX-R0） | P6 后按需 |
| 附能力 | AGENT-R1：异账号 / 异 uid 拒绝证据 | 不挡 P6 |
| 暂缓 | 移动端 backend；Metal `GpuNative` / D3D12 完整扩展 | 图形契约闭合且 P6 后 |
| 不做 | 网络 / 同步；读屏桥（除非单独立项） | — |
| 性能 | PERF-001 · SCROLL-COPY | `ScrollCopy` 未接入 `render_frame`：当前滚动视口整窗清空重绘而非仅 exposed strip；接线后仅重绘暴露条带 | 图形契约闭合后 |

## 待裁决

以下项需产品/架构决定是否纳入公开使用文档（[`使用.md`](使用.md)），或从公开面移除：

- **`Computed<T>`**（已在 `prelude`，未写入使用文档）——自动追踪依赖的派生状态。
- **连续指针脏区优化**——同一 hit 目标上连续 `PointerMove` 不每帧全脏（仅目标变化时标脏；测试 `debug_pointer_move_dirties_only_when_hover_target_changes` 已覆盖）。需裁决是否提升为公开优化契约。
- **`DesignTokens::from_primaries()`**（`DesignTokens` 已在 `prelude`，本方法未写入使用文档）——从 12 色原色数组构造主题色板。

裁决规则：接受则写入使用文档；否则从公开面移除。有最小证据或决策时执行。

> 微软拼音 production 细则已由 [RISK-001](#未关闭挡-p6) 覆盖，此处不再重复。
