# 进度

> 缺口与未关闭项；落地以源码 / 测试为准。

## 现状

- P0–P5（零闲置）：已落地
- P6（Windows 生产可用）：未闭合（真实 Vulkan、帧/恢复、真机证据）
- 其后：移动端 / 网络·同步 / 读屏桥（先产品裁决）

**P6 gate**：`cargo test --all-targets` 全过；下表开项关闭；微软拼音与关键真机过关；上层无 OS 分叉。Linux/macOS/移动端不计。顺序：`R0→R1→R2→R3→R5→R6`；Metal `GpuNative` / D3D12 扩展不得早于图形契约闭合。

## 未关闭

| ID | 内容 | 下一动作 |
|----|------|----------|
| GFX-R0 · BUG-001 | Win/macOS 真实 Vulkan（WSI/portability+present）；替 Planned stub | 落地 adapter |
| GFX-R1 · BUG-004 | Additive/scroll 等价 IR；跨 API parity；原生 offscreen | [`frame_recording`](../src/draw/pipeline/frame_recording.rs) |
| GFX-R2 · BUG-005 | device/surface-loss；同 API recipe 切换；Software 终态 present | [`recovering`](../src/draw/engine/recovering.rs) |
| GFX-R3 · RISK-004 | 去 legacy void shutdown；错误线程 Drop；teardown 故障注入 | 改 shutdown 路径 |
| GFX-R5 · BUG-006/007/009 · RISK-002/006 | 真机 parity/fault/resize/多驱动/稳定性；前台 capture | 真机矩阵 |
| GFX-R6 · RISK-005 | 共享 `GraphicsDevice` + 逐窗 `WindowSurface`；长跑 profile | 正确性闭合后做 |
| RISK-001 | 微软拼音前台 composition | 先真实会话，再裁准则 |
| RISK-003 | macOS 仅 cross-compile | 先裁验收标准，再 smoke |
| BUG-002 | Input/Tab 焦点（待验证） | 真机 Tab；IME→RISK-001 |
| BUG-003 | 最小化后再最大化曾黑屏（待验证） | 实机确认铺满重绘 |

## 待决

| 问题 | 时机 |
|------|------|
| macOS 真机验收标准 | 有真机与范围后 |
| 移动端首批 OS / 图形栈 | P6 后 |
| 网络/同步：`data` 或新域 | 有真实用例后 |
| 读屏桥范围与平台顺序 | 产品单独立项 |
| 微软拼音 production 准则 | 有最小真实会话后 |
| Metal GpuNative vs D3D12 顺序 | 图形阶段闭合后 |
| `Computed` capture / 连续指针 / `from_primaries` | 接受则写入[`使用.md`](使用.md)，否则改代码 |
