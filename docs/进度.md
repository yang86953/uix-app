# 进度

> 这里只保留活跃差距。Windows P6 交付基线已闭合；产品范围见[产品](产品.md)，运行时与图形契约见[架构](架构.md)。
> 共 **29** 组件已完成 Windows Vulkan/NVIDIA 真窗视觉闭环，其余差距按平台/性能/内存/Agent 分类追踪。

## 更小 · 更快 · 更稳

功能面已收敛。后续只围绕三个方向：

- **更小**：缩小内存占用。当前 ~100MB RSS 中 GPU 驱动/交换链占比过半不可控，其余来自字形缓存、CPU 中转缓冲、Picture 离屏池、槽位数组膨胀。目标是逼近同体量 GPU-only 框架的内存水平。
- **更快**：降低每帧耗时。当前瓶颈在 CPU→GPU 中转（文字光栅化上传、圆角 SDF、阴影模糊）和首帧 layout 尖峰。目标是稳定 60Hz。
- **更稳**：跨平台真机矩阵闭环。当前仅 Windows Vulkan/NVIDIA 验证；Wayland、macOS、AMD、Intel 均未覆盖。Agent 安全在 Linux/macOS 上缺异 uid 拒绝路径。

## 其他活跃差距

### 组件稳定性

| 类 | 项 | 依赖 / 边界 |
|----|-----|------|
| 组件稳定性 | 【修复】Typography：单字体长段不折行、CJK 测量少行、显式换行少高、闭合标点落行首。Label：多行按单行高、CJK 量宽偏小、Style 单位未进测量。Button：CJK 宽按 `0.55em` 欠量、字号回退不一致。Tag：UTF-8 字节量宽、关闭符号退化。Badge：按 count 量宽、固定字符估算。Tooltip：UTF-8 字节量宽、dirty damage padding 不一。Checkbox：UTF-8 字节量宽。Switch：滑块纵偏 2/4px。Radio：UTF-8 字节量宽、无焦点图形、方向键首尾停。【规格】Typo 112px=4 行；Label 三行=54px；Button 六字 CJK 14px=114px；Tag 三字 12px 可关闭=72×20；Badge 新消息=45×20 / 99+=30×20；Tooltip 四字=64×26；Checkbox 中号=74×32 空=16×32；Switch S/M/L=28×24/40×32/52×40 滑块 φ12/18/24；Radio 双字 S/M/L≈135×24/168×32/200×40 三项组=120×96。【验证】9 项 Windows 真窗 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Typography/Label/Button/Tag/Badge/Tooltip/Checkbox/Switch/Radio 当前回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Avatar） | 【修复】长 fallback 固定字号溢出、受限 frame 按名义尺寸绘、默认文字对比不足、低分辨率先遮罩后放大→边缘退化、方形未沿用 fallback 圆角。【规格】文字按实际居中正方形测量缩放，图片按 DPR 居中裁切后抗锯齿遮罩，位图复用 generation 递增防旧 handle 复效。【验证】32/48/64px、CJK、长文本、加载失败、1×1 图片、方形及 64×24 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Avatar 当前专项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Calendar） | 【修复】绘制/命中用名义 `cell_size`→外溢误触、标题/星期/日期固偏未居中、标题硬编码中文、箭头争空间、首日遗漏边框、星期低对比色、焦点环与填充同色。【规格】标题/星期/日期按实际几何测量缩放，全程裁剪，命中复用最近渲染几何。【验证】154×172 选中、140×160 紧凑、9999 年跳转、140×100 受限、方向键翻月 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Calendar 当前 6 项专项回归、3 项 Component QA 门禁与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Card） | 【修复】标题/action 固定字号越界、正文缺 body 裁剪、窄卡负宽装饰条、过大 padding 负几何、短卡标题 action 争空间、`AfterChildren` 重绘背景覆正文。【规格】标题/action 保留 11/10px 下限按实宽省略，卡片/标题/action/正文分别按实 frame/body 裁剪。【验证】210×128 正文、180×110 长标题、elevation/hover、双 action、120×64 受限、92×72 大 padding、方向键+Enter ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Card 当前 14 项筛选回归、4 项 Component QA 门禁与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Carousel） | 【修复】非当前幻灯仅归零直接子 frame→后代仍参与绘制/命中/焦点、交互幻灯遮后绘控件、子控件焦点时方向键误冒泡切页、窄视口命中失配、reconcile 误读运行态。【规格】父级可见性 gate 隐藏非当前子树，控件区先捕获再聚焦 Carousel，箭头/圆点/命中/裁剪共用安全几何，提供 `.size()` 首选视口。【验证】560×190 默认/指针切页/键盘切页、140×88 八项紧凑、180×88 隐藏控件、120×88 单项 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Carousel 当前 12 项专项回归、3 项 Component QA 门禁与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Collapse） | 【修复】固有宽 0、正文只按显式换行计高、长标题/正文越界、字符箭头、绘制/命中分叉、按下即切换且移出仍激活、缺 hover/pressed、动画完未请求布局收缩、reconcile 误读焦点/展开状态。【规格】按受限宽测量换行，标题省略/正文/箭头/焦点/命中共用裁剪几何，完整点击序列才激活，动画后传播布局失效。【验证】240×142 默认/hover/指针展开/键盘折叠、140×180 手风琴长标题/三行正文 ✅、lib 2262 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 Collapse 当前 17 项筛选回归、完整 lib 回归与 Windows Vulkan 真窗场景 |
| 组件稳定性（Descriptions） | 【修复】宽不足仍强制列数、标签可占满格、标题/label/value 不裁剪不换行、固定行高测量、零宽标签区 1px 虚增高。【规格】每列 ≥140px 自适应收敛列数保留 span 契约，标题省略，字段按各自可用宽换行以同行最大内容高测量，bordered/非 bordered 全程裁剪。【验证】360×130 双列 bordered、220×180 三列→单列长标题/CJK/email ✅、lib 2265 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 Descriptions 当前 7 项筛选回归、完整 lib 回归与 Windows Vulkan 真窗场景 |
| 组件稳定性（Empty） | 【修复】固定 160×100 测量→长文本单行越界、图标/文字百分比定位受限偏移、纯文本被压下区、四级色对比不足、手绘图标与 Lucide 分叉。【规格】按本地化描述在 160–320px 首选宽内测量，受限换行随内容计高，内容块按实 frame 居中，复用公共 Lucide 图标。【验证】160×100 默认纯文本、160×120 inbox 图标、120×150 search 预设 ✅、lib 2269 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 Empty 当前 5 项组件相关回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Image） | 【修复】加载后 `alt` 绘到 frame 外、占位/fallback/焦点/预览缺统一裁剪、长 fallback 不换行、四级色对比不足、emoji 预览与关闭图标、非有限尺寸/圆角污染几何、`radius` 只作用背景位图保持直角。【规格】`alt` 保持语义名称，fallback 实 frame 内居中换行，缩略图按 DPR/尺寸/圆角/fit 生成缓存抗锯齿圆角位图。【验证】128×88 fit 加载、128×88 长中文失败、72×40 `fit(false)`/10px 圆角、全窗模态预览 ✅、lib 2273 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 Image 当前 25 项筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（List） | 【修复】空列表无 empty→整块空白、header/item/footer/load-more 受限越界、窄 frame 负分隔线、换行破坏固定行高、静态条目未入语义快照。【规格】保留 ConfigProvider 自定义空态优先并回退 Empty，固定高度行按 Unicode 单行省略，文字/分隔线/边框统一裁剪于归一化 frame。【验证】260×200 header/三项/footer、默认空态、150×120 长 CJK/英文受限 ✅、lib 2277 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 List 当前 41 项筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（ResultView） | 【修复】图标/标题/副标题/按钮按 400×300 固定偏移受限外绘、长文本/按钮不换行不省略、按钮命中依赖缓存重排前后错位、框外释放仍提交、404 低对比四级色、默认本地化未入语义。【规格】按实 frame 为图标/标题/副标题和 36px 操作区分配共享裁剪，窄高优先保留标题/副标题各一行，按钮 Unicode 省略，命中同步当前 frame 操作区，完整点击才提交。【验证】172×190 成功含副标题/操作、警告/错误、140×180 受限及键盘焦点 ✅、lib 2281 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 ResultView 当前 23 项筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（SelectableList） | 【修复】行命中只查纵坐标、按下即提交且移出仍激活、缺 pressed 反馈、滚轮在 header/footer 被吞、受限 frame 负几何、长文字越界、固定图标偏移窄行溢出、reconcile 按旧索引粘到重排后项。【规格】同一归一化几何驱动 header/视口/footer/二维命中/滚动，仅同一目标内完成按下释放后提交，Unicode 单行省略/图标缩放/绘制/焦点框统一受实 frame 裁剪，非受控选择按 item id 跨重排保留。【验证】220×190 三项含 header/footer hover/pressed/指针提交、132×120 长 CJK/英文、180×150 十二项 End 滚至末项 ✅、lib 2284 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 SelectableList 当前 13 项筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Skeleton） | 【修复】负数/NaN/无穷尺寸进入测量与快照、Text 按配置高而非实 frame 计行高受限外溢、极窄 Rect 圆角和 Circle 半径未收敛。【规格】Rect 圆角/Circle 半径/Text 两行高与间距全部按真实可用尺寸计算并统一裁剪。【验证】240×18 Rect、48px Circle、200×48 Text、72×12 受限 Text ✅、lib 2287 passed | 继续按组件审查渲染与文字问题；本结论仅覆盖 Skeleton 当前 4 项筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Table · 受限渲染） | 【修复】非法列宽/视口尺寸/行高进入测量几何与虚拟滚动、短表头 frame 虚增一行表体视口可命中、长列标题/分组标题/单元格/空态跨列或跨 frame 绘、外焦点框被裁、展开行用字符箭头。【规格】规范化普通/typed/expandable 三条路径尺寸，表头/单元格/空态/表体/边框/焦点框统一按实 frame/列区裁剪，固定单行按 Unicode 省略，展开指示复用 Lucide。【验证】405×190 18 行带选择排序、135×190 长空态、150×90 长 CJK/英文受限、焦点/选中/排序 ✅、lib 2290 passed | 交互闭环见下行；本结论覆盖 Table 当时 85 项筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Table · 指针交互） | 【修复】排序表头/全选/行复选/普通行/展开按钮 PointerDown 立即提交→移出仍激活、缺 pressed 反馈、header/frame 外滚轮仍滚表体、无变化 PointerMove 吞事件。【规格】统一目标枚举驱动二维命中/按下/释放；同一目标完整序列才改变排序/勾选/当前行/展开，移出/PointerLeave/FocusOut 取消；行与排序表头分别 pressed 背景，hover 仅目标变化时重绘，滚轮只真实表体生效。【验证】行按下未提前选中/释放后蓝色选中、表头按下局部反馈/释放后排序箭头切换、浅深色 ✅、lib 2292 passed | 继续按组件审查渲染、交互与语义问题；本结论仅覆盖 Table 87 项筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Tag） | 【修复】主体/关闭区在 PointerDown 立即提交、移出仍激活、关闭命中比可见槽宽 8px、重复按键可能重复切换、缺 hover/pressed、窄 frame 产生负文字区并让文字/图标/焦点框越界、浅色自定义背景固定白字。【规格】统一实际几何驱动主体、14px 勾选槽、精确 20px 关闭槽、绘制与二维命中；指针和 Enter/Space 仅匹配释放时提交，离开/失焦取消；Unicode 单行省略、全部内容与内嵌描边统一裁剪，自定义背景按明暗选择黑/白前景。【验证】72×20 CJK 可关闭、可勾选 hover/pressed/完整释放/focus、80×16 长中英文受限、浅色自定义背景、20px 字号 ✅、Tag 33 项、lib 2295 passed | 继续按组件审查渲染、交互与语义问题；本结论仅覆盖 Tag 当前筛选回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Timeline） | 【修复】固定 400×N×60 坐标直接绘制导致受限 frame 内文字、节点和连接线越界，长标签/说明不省略，节点外环固定白色在深色容器中分叉，reverse 每帧分配引用 Vec，组件语义退化为 Generic。【规格】归一化实际 frame 统一计算 28–60px 行距、节点/连接线/文字槽和可见行，标签与说明按 Unicode 单行省略并双层裁剪，节点外环使用容器背景；reverse 零临时分配，List `value_text` 按视觉顺序发布说明并把 pending 保持末尾。【验证】220×150 三项语义色、132×120 四行长中英文受限、reverse+pending、浅/深色 ✅、4 项组件专项回归、lib 2297 passed | 继续按组件审查渲染与语义问题；本结论仅覆盖 Timeline 当前回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Tree） | 【修复】勾选/展开/选择 PointerDown 立即提交且移出仍激活、绘制与命中槽位分叉、横向越界仍命中、缺 hover/pressed、滚轮不校验 frame、深层缩进/长标题硬裁、极小 frame 焦点框外溢、展开未请求布局、无障碍把业务 key 当用户值、flatten 每次克隆整棵节点树。【规格】统一实际 frame 行几何驱动有上限缩进/勾选/Lucide 展开/图标/标题/二维命中，完整同目标释放才提交，离开/失焦取消；Unicode 单行省略与全部绘制统一裁剪，展开行数变化发一次 Layout，请求语义发布可见标题与位置，flatten 直接构建临时行表。【验证】200×190 三十二子项滚动至第 13 项、132×84 长标题/禁用、浅深色、键盘展开/折叠/勾选、指针按下未提交且释放提交 ✅、Tree 10 项筛选回归、lib 2301 passed | 继续按组件审查渲染、交互与语义问题；本结论仅覆盖 Tree 当前回归、Windows Vulkan/NVIDIA 真窗语义交互与浅/深色后台精确窗口证据 |
| 组件稳定性（RichText） | 【修复】链接与代码复制 PointerDown 立即提交且移出仍激活、框外按下启动选择、绘制/代码复制区未统一裁剪、无效 frame 仍布局、默认固定灰色导致浅色正文低对比、主题切换复用旧色布局缓存、QA 缺代码/折行/受限/完整释放覆盖。【规格】归一化实际 frame 统一裁剪字形背景/文字 run/选择/链接/代码按钮与二维命中；链接/复制使用稳定 segment 目标并仅同目标释放提交，离开/失焦/reconcile 取消；默认正文随 `color_text`，显式 `.color(...)` 保持覆盖，解析后颜色进入缓存键。【验证】约 283.5×82 中英文链接/粗体/代码与约 116.9×64 窄宽折行，首末链接键盘语义、浅深色 ✅、RichText 5 项筛选回归、lib 2303 passed | 继续按组件审查渲染、选择与语义问题；本结论仅覆盖 RichText 当前回归、Windows Vulkan/NVIDIA 真窗语义交互与浅/深色后台精确窗口证据 |
| 组件稳定性（Alert） | 【修复】PointerDown/Enter/Space 立即关闭且移出无法取消、关闭命中与绘制分叉、未归一化/裁剪导致极窄负几何和文字越界、消息使用状态色对比不稳、关闭无 hover/pressed、关闭后 measure 未回收、说明未入无障碍值。【规格】归一化实际 frame 统一分配强调线/图标/消息/说明/关闭槽，消息与说明 Unicode 单行省略，正文随 `color_text`；同一关闭目标完整释放才提交，离开/失焦/reconcile 取消，关闭返回零测量并请求布局。【验证】240×54 可关闭说明、240×36 无图标、四状态色、132×54 长中英文受限、浅深色 ✅、Alert 6 项筛选回归、lib 2306 passed | 继续按组件审查反馈组件的渲染、交互与语义问题；本结论仅覆盖 Alert 当前回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Drawer） | 【修复】闭合触发器/关闭按钮 PointerDown 立即提交且不可键盘访问、拉伸 frame 的触发视觉与命中错位、标题/extra/关闭槽固定坐标重叠、超 surface 与非法尺寸产生负几何、内容子树打开态不裁剪、关闭无 hover/pressed、闭合态仍发布 Dialog、空标题触发器缺可访问名称。【规格】遮罩面板归一化并收敛至真实 surface，header/body/footer/子树共享非负几何和裁剪，标题/extra Unicode 单行省略；闭合 Button 与打开 Dialog 发布 expanded 和非空名称，触发器和关闭槽完整释放才提交，离开/失焦或含 mask 在内的交互几何改变时取消，overlay focus trap/Escape 保持。【验证】360px 右侧长标题/extra/双按钮、132×90 超限紧凑、空标题与闭合/打开语义、浅深色与焦点陷阱 ✅、Drawer 13 项组件回归、16 项筛选回归、lib 2311 passed | footer 内置确认仍是视觉槽，业务提交继续由内容子树真实 Button 承担；本结论仅覆盖 Drawer 当前回归、Windows Vulkan/NVIDIA 真窗右侧面板与现有 overlay 性能证据 |
| 组件稳定性（Input） | 【修复】CJK addon 按 UTF-8 字节分配宽、textarea 丢末尾空行与滚动命中错行、CRLF/CR 破坏行索引、空 placeholder→越界光标、密码按明文宽定位光标、clearable 无动作、附件按钮坐标分叉。【规格】双字 CJK addon=122×32；clearable=100×32；password=104×32；两行 textarea=80×60；S/L=80×24/80×40。【验证】CJK addon/掩码/清除/CRLF 双行/末尾空行焦点 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Input 当前回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（InputNumber） | 【修复】小数固定两位→聚焦丢精度、连续步进累积尾差、重复点击/步进丢弃缓冲、受限高度仍按名义判上下区、光标固定 7px 宽、禁用复用普通背景、焦点描边遗漏步进区、步进区无外框、三档总宽固定 80px 挤压 Large。【规格】S/M/L=104×24/112×32/120×40。【验证】`1.2345` 精度、三次 0.1 步进→0.3、文本提交 7.25、上下往返、完整焦点框/禁用色/负号/超长裁切 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 InputNumber 当前回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Select） | 【修复】可搜索方向键提前提交、空选项/无匹配不显空态、下拉视口外仍可命中、Large 受限仍按名义高命中、底部空不足不上翻、光标固定字符宽、Unicode 按 UTF-8 估算、长文字覆箭头、多选标签溢出缺关闭槽、快速重开残留查询、下拉被后绘覆盖。【规格】S/M/L 高=24/32/40；下拉行高 28、视口上限 280。【验证】搜索浏览不提交 Enter 提交、多选保持展开、本地化空态、长文字裁切、禁用态、三档、十行上翻后点末项 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Select 当前 25 项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Segmented） | 【修复】CJK 按 UTF-8 字节分配宽、长文字/选中块越出覆盖相邻、Large 20px 受限仍按 40px 绘/命中、整体禁用选中块与容器同色、方向键首尾停、实 frame 宽于固有→不可点尾部、窄 frame 按名义排挤残片、边界仍可命中、无变化 hover 吞事件、options 重排按旧索引变非受控值。【规格】四双字 CJK Medium=200×32；S/M/L 高=24/32/40。【验证】CJK 紧凑、单项禁用跳过、整体禁用选中、长文字两段可选、180px 无空白尾、20px 高度、三档+键盘首尾循环 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Segmented 当前 14 项专项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Form / FormItem） | 【修复】标签/帮助文字未裁剪越界覆控件、Vertical 帮助与子控件共区、无帮助 Horizontal 浪费 16px、Inline 遗漏状态条与帮助、带帮助自然高未含帮助行、NaN/无穷 gap→重叠归零。【规格】Horizontal/Inline 无帮助=44px 有帮助=60px；Vertical=56/72px；帮助行=16px。51 项测试+4 项 QA 通过。【验证】Vertical/Horizontal 表单、220px 长中文、160px 窄字段、Inline error、required/success/warning/error ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Form/FormItem 当前回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Slider） | 【修复】方向键从离格值直加减未对齐区间起点、十进制步进→尾差、受限宽高用名义轨道/滑块→溢出、框外按下仍启拖动、`PointerLeave` 提前终止捕获。【规格】S/M/L=200×24/200×32/200×40。【验证】离格 42 按 5 递增→45、0.3 按 0.1 递增→0.4、三档、80×12 受限、完整焦点框+语义数值 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Slider 当前 9 项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Rate） | 【修复】半星左半用空态色且右半被背景擦除、自定义 Unicode 固定 24px 格宽互相覆盖、受限仍用名义字号/命中格→溢出、框外按下误选首项、半星无障碍最大值按整星发布。【规格】五颗星 S/M/L=96×24/120×32/144×40。【验证】2.5 星左右半色、两格文字宽、半星值 5/最大 10、80×12 受限、三档、禁用/清零/语义增量 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Rate 当前 11 项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（DatePicker） | 【修复】星期标题与首行日期共垂直区绘制/命中错位、月份标题区误触翻月、窄触发框七列压至不可读、固字号/图标受限外溢、长占位覆图标、月历被后绘覆盖截获点击、Tab 不进入焦点态、无障碍缺展开状态、清除 hover 不请求重绘、无关按键被吞。【规格】S/M/L=160×24/160×32/160×40；最小弹层宽 160。【验证】长占位裁切、月/星期/日期分区、边缘翻月、弹层覆盖后续、首行选中 2026-07-01、80×12 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 DatePicker 当前 11 项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（DateRangePicker） | 【修复】月历与预设按普通树序被后绘覆盖截获点击、范围文字/图标按名义高受限外溢、长占位覆图标、长预设标签越出 footer、Tab 不进入焦点态、无障碍缺展开状态、清除 hover 不请求重绘、翻月残留 hover、无关按键被吞。【规格】S/M/L=280×24/280×32/280×40；最小月历宽 160。【验证】首击保持展开、7/1–3 hover 预览、次击提交 2026-06-29/07-03、弹层覆盖、80×12 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 DateRangePicker 当前 9 项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（TimePicker） | 【修复】分钟列五分步长把实际分钟误作行索引、晚时间不滚到当前/指针无法到 23:59、滚轮无效、键盘只滚小时不能切列/提交、固字号/图标受限外溢、长占位覆图标、滚动后半行/高亮越出弹层、列文字偏未居中、弹层被后绘覆盖、Tab 不进入焦点态、无障碍缺展开、离开未清预览、无关按键被吞。【规格】S/M/L=120×24/120×32/120×40。【验证】14:30 打开居中、键盘提交 14:31、23:59 末行可见、长占位裁切、两列居中、弹层覆盖、80×12 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 TimePicker 当前 12 项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（ColorPicker） | 【修复】受限仍强制 ≥8px 色块描边越出 frame、触发命中只查纵坐标用名义高、色板右 padding/边界错预览下行色、透明色显示空白、预设无选择标记、浅色 hover 白框不可辨、键盘只能开关不能导航/提交、弹层被后绘覆盖、无障碍缺展开、无变化移动/离开吞事件。【规格】S/M/L=24×24/32×32/40×40。【验证】半透明蓝棋盘格、深色白勾浅色黑勾、二维键盘提交 `#B37FEBFF`、色板覆盖后续、12×6 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 ColorPicker 当前 11 项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Cascader） | 【修复】只绘末级→鼠标无法返回/切换祖先、弹层命中忽略横坐标、长列表仅前六项可达无滚轮、触发框按名义 32px 命中、长占位/选项覆箭头或越界、弹层被后绘覆盖、缺 hover/FocusIn/Home/End/展开状态/叶子 Change、reconcile 重置打开中子级到根列。【规格】触发框=120×32；弹层每列 ≥200×200。【验证】北京/海淀两列键盘提交、保留祖先鼠标切换上海/徐汇、10 项 End 提交"区域 9"、超长中文裁切、弹层覆盖、80×12 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Cascader 当前 12 项专项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（TreeSelect） | 【修复】触发框/弹层命中只查纵坐标、视口下不可见行可点击、受限高仍按名义 32px 绘、长占位/值覆箭头、窄触发器层级压至不可读、长/深标题缺可见文字、空树不绘空态、弹层被后绘覆盖、无变化移动吞事件、hover 与键盘高亮互清、缺 Home/End、reconcile 重置高亮、退出动画跳回顶部、重开长列表未定位选中项。【规格】触发框=200×32；弹层 ≥200 宽/28 行高/视口 ≤280。【验证】六行层级缩进/End 提交 Go/指针提交 Vue、禁用保持展开、空态、超长父子裁切、40 节点提交、弹层覆盖、80×12 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 TreeSelect 当前 14 项专项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（AutoComplete） | 【修复】触发框/弹层命中只查纵坐标、受限高仍按名义 32px 绘、长文字越界、光标固定字符宽只能末尾编辑、查询提交不发 Change、长候选无滚动、过滤空无空态、缺候选 hover、弹层被后绘覆盖、reconcile 重置键盘候选到首项。【规格】触发框=200×32；弹层 ≥200 宽/28 行高/视口 ≤280。【验证】输入"上"过滤提交"上海"、光标回开头编辑为"新"显空态、超长裁切+hover、30 项提交"候选 20"、弹层覆盖、80×12 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 AutoComplete 当前 10 项专项回归与 Windows Vulkan/NVIDIA 真窗场景 |
| 组件稳定性（Mentions） | 【修复】光标固定字符宽只能末尾编辑、活动查询只取最后 `@` 不看光标、提交截断查询后正文、输入提交不发 Change、候选只绘前五项键盘可入不可见、缺滚轮空态、命中忽略二维边界、长占位/正文/候选越界、缺 hover、弹层被后绘覆盖、reconcile 重置候选。【规格】触发框=200×32；弹层 ≥200 宽/28 行高/视口 ≤280。【验证】"你好 @Al 后文"光标移回查询提交"你好 @Alan 后文"、后文前插 `X`、超长中文水平滚动、空态、超长裁切+hover、30 项提交"@候选 20"、弹层覆盖、80×12 受限 ✅ | 继续按组件审查渲染与文字问题；本结论仅覆盖 Mentions 当前 12 项专项回归与 Windows Vulkan/NVIDIA 真窗场景 |

### 平台与硬件

| 类 | 项 | 依赖 / 边界 |
|----|-----|------|
| 跨平台 / 多硬件 | Windows Vulkan/NVIDIA 的 88 组件、412 状态逐组件视觉矩阵已闭合；Wayland fault / resize / parity / occlusion v6+、macOS / Wayland 真机及 AMD / Intel 同等视觉/图形矩阵未闭合 | 仍需对应平台与硬件环境 |
| Agent 附能力 | AGENT-R1：Windows 当前 `TokenUser` SID 准入与合成异账号 SID 拒绝已有 Authz 证据；Linux / macOS 尚缺真实异 uid 进程拒绝路径 | 仍需双账号或隔离的异 uid 环境 |

### 性能与内存

| 类 | 项 | 依赖 / 边界 |
|----|-----|------|
| 性能 | Modal / Drawer 关闭子树残留、重新打开布局与全屏遮罩录制已修复；可读 retained CPU 后端的全屏 overlay 帧复用干净背景，只重放浮层。圆角填充 / 描边改为像素等价的边界扫描，整幅透明目标背景恢复走精确行拷贝。1200×800 Windows Vulkan/NVIDIA + CPU raster 真窗连续开关 12 轮：Drawer 首 / 末 4 轮中位数 21.7 / 26.9ms（单次 76.6ms 离群），Modal 22.4 / 19.3ms；观察到的 backdrop 帧 `paint_cpu` 最大 11.7ms，未见累积 | 端到端首帧仍受约 4–7ms layout 与偶发调度尖峰影响，尚不宣称稳定 60Hz；GPU-only / 不可读后端保守完整重绘，仍需跨后端 / 硬件复测 |
| 内存 | 字形缓存从 8192 项 / 32 MiB 降至 **2048 项 / 8 MiB**（对齐 Chrome/Flutter 默认）；LayerTree 构建消除 DisplayList 双 clone（`collect_picture_handles` + `build_node_cached`），改为所有权转移；ImageService 新增 `compact()` 截断尾部无效槽位；CpuOffscreenPool 新增 `compact()` 并在每次 `destroy_offscreen` 后截断尾部空槽。当前 GPU 路径上 `FrameRecordingCanvas::scratch` 与 `NativeGpuCanvas2D::soft_fallback` 两份 CPU 缓冲（合计 ~16 MB at 1920×1080）因生命周期不同无法合并；`soft_fallback` 惰性创建后永不释放。消除它们的正确路径是 GPU 原生字形纹理图集、GPU SDF 圆角与 GPU 模糊——见[架构§图形](架构.md#图形-api与帧提交硬约束) | GPU 原生字形 / 圆角 / 模糊尚未实施；GPU 后端 `NativeGpuBackend::offscreens` 未 compact |
| 暂缓 | 移动端 backend；Metal `GpuNative` / D3D12 完整扩展 | 另行立项 |
