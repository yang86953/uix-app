# 进度

## 当前状态

| 工作流 | 状态 | 依据 |
|--------|------|------|
| P0–P5 零闲置主体 | 已落地；全量自动化零失败 | [已落地阶段](#已落地阶段p0p5) · [当前验证证据](#当前验证证据) |
| **P6 生产级** | Executable 设计基线；REQ-006 图形契约整改与 Windows 生产 gate 均未闭合 | [P6.9](#p69-图形契约整改) · [P6 gate](#p6-退出门槛默认工程-gate) |
| 验证基线 | 全量自动化、doctest 与文档门禁通过 | [当前验证证据](#当前验证证据) |
| backlog | P6 细项与跨阶段汇总均在本文件维护 | [P6 优先项](#生产级优先项) · [后续工作](#后续工作) |

## 里程碑与工作域

| 里程碑 | 内容 | 下一步判定 |
|--------|------|------------|
| P0–P5 维护 | 主循环 / 热更新 / App API / 窄路径 / 多窗 / Handle | 改动同步架构硬约束/决策 + 最小测试 |
| **P6** | **Windows 优先**生产可用；可组合渲染轴 [#169](决策.md#d169)；Linux/macOS 随后 | [P6 退出门槛](#p6-退出门槛默认工程-gate) · [优先项](#生产级优先项) |
| **P7+ 移动端** | iOS/Android backend；复用上层 trait | P6 后切片；须新决策 |
| **全栈扩展** | `data` 之上网络/同步 | 产品边界决策后排期 |
| 无障碍（读屏桥） | v1 快照/键盘已落地；平台桥待后续 | 新决策或阶段切片 |
| 文档 | 架构硬约束 / 进度 / 决策同步 | 决策 → [决策](决策.md) 声明的下一编号 |

## P6 退出门槛（默认工程 gate）

Windows「生产可用」至少同时满足（细节与证据 → [当前验证证据](#当前验证证据) · [证据矩阵](#平台与后端证据矩阵)）：

1. `cargo test --all-targets` 零失败；测试总数与结果只在[当前验证证据](#当前验证证据)维护。
2. Windows 图形与输入矩阵达到 production：已达成能力见[需求证据索引](#需求证据索引)与[证据矩阵](#平台与后端证据矩阵)；图形首先关闭 [P6.9](#p69-图形契约整改) 的 Critical/High 缺陷，再补微软拼音 GUI、`ITfTextEditSink` 属性路径、多 GPU/驱动矩阵、前台 capture，以及最小化→最大化实机复验。
3. 架构边界扫描无真实违规、无注释假阳性（[#171](决策.md#d171)）。
4. P0 生产阻塞归零；帧顺序、线程亲和、失败帧 dirty 保留、typed recovery、probe 诊断与 shutdown 均有自动化证据；EGL/Vulkan/DPI 等平台项另须适用平台真机证据。
5. 文档严格检查通过；公开示例与实现状态一致。

Linux/macOS/移动端 **不属于** Windows gate；不得用上层 `#[cfg]` 换取 Windows 通过。

**下一步（P6 · [#167](决策.md#d167)）**：先按 [P6.9](#p69-图形契约整改) 从 GFX-R0/R1/R2/R3 关闭 Critical 正确性与线程安全问题；其后复验 [BUG-20260712-003](问题.md#bug-20260712-003)，再补微软拼音 GUI、GPU/驱动矩阵与前台 capture。Metal `GpuNative` / D3D12 能力扩展不得早于图形契约闭合。改公开 API → [架构 · 术语与公开面](架构.md#术语与公开面)。

## 风险与阻塞

交付风险、阻塞和待裁决边界由 [`问题.md`](问题.md#活跃风险与阻塞) 维护；本文件只在 P6 gate、优先项或 backlog 中引用已排期工作。平台边界与零闲置约束分别以 [#167](决策.md#d167) 和 [#105](决策.md#d105) 为准。

---

## 实现落地

**P0–P5 主体已落地**（[#154](决策.md#d154) · [#157](决策.md#d157)）。证据级别：`designed` → `coded` → `automated` → `compiled:<platform>` → `hardware:<platform>` → `production`。`✅` ≠ 跨平台/生产；平台交付以 [证据矩阵](#平台与后端证据矩阵) 为准。源码 → [架构 · 源码入口](架构.md#源码入口)。

---

<a id="分阶段路线图"></a>
<a id="已落地阶段p0p5"></a>

## 已落地阶段（P0–P5）

设计（#154）— 下列阶段 **主体接线已完成**；按域能力清单见 [实现进度总览](#实现进度总览)。

```text
P0 基础零闲置          P1 热更新           P2 App 运行时 API      P3 渲染窄路径
├─ 三态主循环 #106     ├─ reconcile 接入   ├─ Timer #132          ├─ PicturePolicy #122
├─ Registry #115       ├─ 帧内合并 #118    ├─ post_to_ui #133     ├─ Composite #107
├─ wait_until #127     ├─ reconcile合并#153├─ MainThreadQueue #137  └─ PointerMove #109
└─ Effect DeepIdle     ├─ handler重绑#123  ├─ on_start #140
                       └─ StateSlotId #143 └─ TestClock #139

P4 多窗                P5 组件 Handle 体系
├─ WindowSession #116  ├─ AppState #145
├─ open_window #148    ├─ Snapshot #146 #151 #152
├─ update_view #149    ├─ Handle emit #147
└─ State fan-out #150  └─ ComponentId #101

P6 图形后端            ← #162 #163，详见下文
├─ GraphicsBackend 枚举 + registry probe
├─ Windows D3D11/12
├─ Linux Vulkan
└─ macOS Metal + AppKit SoftwareEngine bootstrap
```

| 阶段 | 目标 | 关键决策 | 验收（已达成） |
|------|------|----------|----------------|
| **P0** | DeepIdle 真休眠 | #106 #115 #127 #105 | 无事件无 present；DeepIdle 不 `tick_effects` |
| **P1** | 热更新不上全量 build | #118 #153 #123 #143 | State 批次单次 reconcile；handler 智能重绑；显式 capture API 与 `semantic_handler!` 宏可生成 fingerprint，普通闭包仍保守重绑（#159/#160） |
| **P2** | App 公开定时/投递 | #132–#137 #140 #139 | Timer cancel 回 DeepIdle；TestClock 测 drain / wait_until |
| **P3** | L2 最小脏区 | #122 #129 #107 #109 | 滚动 Composite；Picture 自适应；PointerMove 窄路径 |
| **P4** | 多窗编排 | #116 #148 #149 #150 | 副窗独立 tree/session；State fan-out 仅 wake 有关窗 |
| **P5** | Handle 零维护 | #145–#152 #147 | mount 自动 snapshot；emit 走 dispatch_semantic |

---

<a id="实现进度总览"></a>

## 实现进度总览

| 域 | 证据级别 | 已落地要点 |
|----|----------|------------|
| `core` | `automated` | 几何 / Damage / `ComponentId`·`WindowId` / 错误·日志·诊断（#89） |
| `native` | `coded` + Win `automated` | Win/Linux/macOS backend；FakePlatform；`EventLoopWaker`；`WindowOps` Result-only；GPU context（[#167](决策.md#d167)·[#162](决策.md#d162)–[#169](决策.md#d169)） |
| `draw` | `automated` + 部分 `hardware` | Gpu/PresentUpload/Software engine；Invalidation/Composite；PicturePolicy；AnimationRegistry；焦点环已取消（[#177](决策.md#d177)） |
| `app` | `automated` | 三态主循环；WindowSession；Timer / `post_to_ui` / `set_theme`；多窗路由；Timer cancel/zero 间隔、窗口 teardown 与终态 session 清理回归；TestClock；Settings opt-in |
| `ui` | `automated` | View/Reconciler；State/Computed/Effect；Handle+Snapshot（86 widget）；Overlay；Theme/StyleSet；布局两阶段（[#174](决策.md#d174)） |
| `data` | `automated` | Settings KV opt-in；不进 UI 热路径 |

Widget Snapshot 覆盖与 demo 页矩阵 → [`demo/README.md`](../demo/README.md) · `src/ui/widgets/` · `demo/src/demos/gallery.rs`。

<a id="需求证据索引"></a>

## 需求证据索引

本表是产品需求的状态与证据入口；证据级别沿用上文定义，未取得 `hardware` / `production` 时不得由 `automated` 外推。

| 需求 | 当前证据级别 | 直接证据 | 未闭合项 |
|------|--------------|----------|----------|
| <a id="evidence-req-001"></a>[REQ-001](产品.md#req-001) | `automated` | [`src/prelude.rs`](../src/prelude.rs)；[`combinators`](../src/tests/ui/view/combinators.rs)、[`adapter`](../src/tests/ui/view/adapter.rs) 与 [`low_level_widget_loop_stays_off_prelude`](../src/tests/architecture/boundaries.rs) | 公开门面继续随 API 变更维护 |
| <a id="evidence-req-002"></a>[REQ-002](产品.md#req-002) | `automated` + 部分 `hardware` | 六域测试由 `cargo test --all-targets` 覆盖；原生 GUI 交互见 [综合报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；运行时不包含 WebView 依赖 | 平台/后端生产差距见[证据矩阵](#平台与后端证据矩阵)与[活跃风险](问题.md#活跃风险与阻塞) |
| <a id="evidence-req-002a"></a>[REQ-002a](产品.md#req-002a) | `automated` + 部分 GUI | [`ime_dispatch`](../src/native/backends/windows/ime_dispatch.rs)、[`tsf_session`](../src/native/backends/windows/tsf_session.rs)、[`tsf_text_store`](../src/native/backends/windows/tsf_text_store.rs)；Tab→文本输入与 preedit/commit 回归 | 系统 IME 前台 composition → [RISK-20260712-001](问题.md#risk-windows-ime)；准则 → [OPEN-005](问题.md#open-ime-production) |
| <a id="evidence-req-003"></a>[REQ-003](产品.md#req-003) | `automated` | `deep_idle_waits_without_fixed_timeout_or_extra_present`、`key_event_after_first_frame_does_not_force_layout_without_invalidation`（[`event_loop`](../src/tests/app/event_loop/event_loop.rs)）与 scroll composite（[`scroll_view`](../src/tests/ui/widgets/other/scroll_view/mod.rs)） | 生产功耗/长跑随平台矩阵补证 |
| <a id="evidence-req-004"></a>[REQ-004](产品.md#req-004) | `automated` | [`app_handle`](../src/tests/app/app_handle.rs)、[`app_timer`](../src/tests/app/app_timer.rs)、[`event_loop`](../src/tests/app/event_loop/event_loop.rs)、[`session_runtime`](../src/tests/app/session_runtime.rs) 覆盖跨线程投递、Timer cancel/teardown、State reconcile 与多窗终态清理 | 无生产阻塞；新增活动源须过 #105 gate |
| <a id="evidence-req-005"></a>[REQ-005](产品.md#req-005) | Windows `automated`；Linux/macOS `compiled`（本轮 cross-check） | [`architecture/boundaries`](../src/tests/architecture/boundaries.rs) 守卫上层无 OS 分叉；`cargo check --target x86_64-unknown-linux-gnu --all-features` 与 `cargo check --target x86_64-apple-darwin --all-features`；桌面源码入口见[架构](架构.md#源码入口) | macOS 真机 → [RISK-20260712-003](问题.md#risk-macos-hardware)；移动端 → [OPEN-002](问题.md#open-mobile-stack) |
| <a id="evidence-req-006"></a>[REQ-006](产品.md#req-006) | 部分 `automated` + 部分 `hardware`；**未达验收** | `FrameRenderer` 现由私有 `FrameRecordingEngine` 将 root、非缓存 direct、overlay 与 Picture 的每个可视操作按 painter order 写入 checked main `FrameEncoder`，并在唯一 `end_frame`/Presenter 前执行；CPU 与 D3D11 WARP 已分别验证其实际执行，后者 readback 在最终 present 前与参考像素一致。其余既有证据：`FrameReady`/`PresentPending`/最终 `Present` 生命周期分离、已缓存 Picture 的 CPU/NativeGpu/GL 路径、GL source-crop/WGL 自动化、Windows logical/drawable DPR real-window caps、typed failure/recovery、recipe、thread-affinity 与 D3D11 WARP crop/scissor；单次 D3D12 hardware context 仅覆盖 clear/draw/readback/present；EGL/Vulkan 本轮仅 `compiled` | [AC-GFX-01..06](产品.md#req-006-acceptance) 未闭合；非整数/有状态操作仍以 full-extent CPU source 记录，且完整 recording 每帧重建；原生 device-loss、API 边界、EGL/Vulkan 真机、hybrid damage upload、高 DPI 与真机矩阵仍未完成 → [P6.9](#p69-图形契约整改) |
| <a id="evidence-req-007"></a>[REQ-007](产品.md#req-007) | opt-in `automated`；热路径 `coded` | [`settings` 回归](../src/tests/data/settings/settings.rs)验证显式 load/save；[`app_settings_is_opt_in` / `app_settings_load_registers_settings_service`](../src/tests/app/shell/application.rs)验证 App 默认不加载；实现入口 [`data/settings`](../src/data/settings/mod.rs) | “不进入 UI 热路径”尚无独立边界回归 |
| <a id="evidence-req-008"></a>[REQ-008](产品.md#req-008) | `automated` + 部分 `hardware` | 平台边界：[`architecture/boundaries`](../src/tests/architecture/boundaries.rs)；无窗/GPU：[`native/test_harness`](../src/native/test_harness/mod.rs)；主循环/事件/布局/绘制：对应 `src/tests/` 分层；公开 API：doctest + View 回归 | 真实平台证据按[矩阵](#平台与后端证据矩阵)扩充 |
| <a id="evidence-req-009"></a>[REQ-009](产品.md#req-009) | `automated`（v1 范围） | `accessibility_snapshot_derives_static_aria_metadata`（[`component_snapshot`](../src/tests/ui/component_snapshot.rs)）与 `focus_manager_drives_tab_navigation`（[`tree_core`](../src/tests/ui/core/widget/tree_core.rs)）；源码入口 [`component_snapshot.rs`](../src/ui/component_snapshot.rs) | 读屏平台桥 → [OPEN-004](问题.md#open-accessibility-bridge) |

---

<a id="当前验证证据"></a>
<a id="当前验证增量-2026-07-13"></a>

## 当前验证增量（2026-07-13）

记录时工作树包含截至 2026-07-13 的 P6.9 R4 factory-owned context 初始化、native-owned `OpenGlRasterPipeline`、API-neutral DTO、shader source 所有权与 opaque bootstrap surface 整改，以及 R5 EGL 仅 GLES3 的 typed 拒绝、D3D11/WGL premultiplied soft tile 与 WARP/WGL parity scripts；在 Windows `x86_64-pc-windows-gnu`、Rust 1.96.0 上验证。真实 demo 报告仍停在 `97c968a`，因此只证明此前的页面与交互场景；本轮 WARP/WGL 为 Windows 自动化，不证明多驱动或 production 硬件，EGL/Vulkan/Metal 仅按下列 `compiled` 证据记录。本节是当前易漂移测试总数与门禁结果的唯一事实源。

| 门禁 | 结果 | 证据范围 |
| --- | --- | --- |
| `cargo test --all-targets` | **通过** | lib **1293/1293**；demo **24/24**；含 P6.9 `FrameReady`/external `PresentPending`/最终 `Present` 防倒置、main `FrameEncoder` 在 root/direct/overlay 后且 final present 前执行、其 typed failure 不进入 present 且 recovery 在下一帧触发、FrameRecording producer 的 `Clear → Native → CPU segment → Picture` 顺序、完整 recorder 对 partial dirty/多 dirty/scroll 均强制 full redraw/full present、Additive typed 拒绝及不再产生 main full-image carrier、D3D11 WARP main/已绑定 offscreen mixed readback，以及含 CPU fallback 的 `FrameRenderer` direct/Picture 生产流真实窗口 readback；WGL `FrameRenderer` direct/Picture/root-level overlay 回归在唯一 encoder 执行后、swap 前读回并确认最终 present；CPU/NativeGpu main 与 checked Picture path clip 以及 GPU scroll copy 以 typed `NotImplemented` 拒绝，旧 CPU/GPU `RenderBackend::present` 也在 native 提交前消费 deferred failure，架构回归禁止 `Canvas2D::scroll_region` 重获默认 no-op；checked Picture destroy 的 native error、handle 保留与 orphan retry、`IGraphicsContext::read_pixels` typed 单入口与错误线程/native `GraphicsDeviceLost` 传播、D3D12 public checked shutdown 的 terminal fence drain/幂等与 WGL checked shutdown 的真实窗口成功路径，EGL/Vulkan checked shutdown 与 Vulkan typed readback 均仅 Linux target `compiled`，Metal PixelUpload typed readback 及 Vulkan/Metal PixelUpload legacy swapchain-present typed reject 均仅对应 Linux/macOS target `compiled`、紧凑 `SoftFallbackTile` payload/destination 校验、D3D11 WARP 非零落点与连续 segment 防重采样、cached CPU/NativeGpu/GL complete `DisplayList` Picture 及 clip/圆角/非矩形/Drawer animation 回归、premultiplied 半透明与 replace clear、NativeGpu/GL Picture 顺序与失败、WGL bounded soft upload/source crop/active Picture→Picture 画序、WGL/D3D11/D3D12 shared logical/drawable DPR 与 D3D real-window caps、Software/WGL premultiplied soft-tile 与 hybrid/clip/bounded-tile parity、WGL 与 D3D11-WARP bound offscreen encoded Picture、D3D11/D3D12 `SoftFallbackTile`、D3D11 WARP factory、Software/D3D11-WARP/D3D12-WARP hybrid/clip/tile parity、Software/D3D11-WARP offscreen crop/post-blit clip parity、typed resize/present/recovery、factory context 错误线程 `InvalidState`、native shader source legacy re-export、draw 无 API GL object、Metal identity CPU PixelUpload 命名与 raw GPU factory 仅限 native factory bridge 的架构扫描、factory-owned context 不会被 null surface 二次初始化或在 GpuNative startup 重建 swapchain、D3D device-lost/OOM HRESULT 分类、GDI resize failure、bootstrap initialize/checked teardown 原因链、recovery teardown-before-replace、recipe fallback、EGL 不成立 GLES2 fallback 防回归 |
| `cargo test --no-default-features --all-targets` | **通过** | lib **1245/1245**；demo **24/24**；feature-off 的 graphics API 模块及其专属测试不再编译 |
| feature 依赖裁剪 | **通过** | `cargo check --no-default-features`、所有单 API feature Windows check、Linux no-feature/all-feature check；无默认 feature 依赖树不含 `glow`、`ash`、`khronos-egl` 或 Direct3D feature，API 模块也不再声明 |
| Linux 图形交叉编译 | **通过** | `cargo check --target x86_64-unknown-linux-gnu --all-features --tests`；覆盖 EGL 仅 GLES3 config/context 与 typed reject、终止、Vulkan fence/swapchain 与 out-of-date/suboptimal typed-error 分类测试源码，只有既有 Linux dead-code warning，不是运行或硬件证据 |
| macOS 图形交叉编译 | **通过** | `cargo check --target x86_64-apple-darwin --all-features --tests`；覆盖本轮 Metal PixelUpload payload 校验、CALayer CFData/provider/colorspace/image typed failure 与 AppKit 路径类型检查，不是运行、硬件或 production 证据 |
| Linux 图形测试链接 | 环境缺口 | `cargo test --target x86_64-unknown-linux-gnu --all-features --lib --no-run` 编译测试对象后由 Windows host `lld` 拒绝 Linux linker 参数 `--eh-frame-hdr` / `-z`；未将其记为 Linux 测试或硬件结果 |
| `cargo test --doc` | **通过** | **5 passed / 24 ignored**；`NativeSurfaceHandle` compile-fail 证明 bootstrap/recovery surface 不能安全跨线程 |
| Rust 格式 | P6.9 改动文件 **通过**；全工作区基线未通过 | 本轮 P6.9 `.rs` 改动以 `rustfmt --edition 2021 --check --config skip_children=true` 通过；`cargo fmt --all -- --check` 仍报告未纳入本整改的既有工作区格式差异，未将其混入本整改 |
| 编译 / CLI demo | **通过** | `cargo build --bin uix-demo`；`cargo run --bin uix-demo -- --cli` |
| `cargo clippy --all-targets` | **通过** | exit 0；仅既有非阻断 warning。额外 `-D warnings` 被仓库范围既有 86 项 lint 阻断，未作为本切片已清零的质量证据 |
| 文档与差异门禁 | **通过** | `python scripts/check_docs_links.py`；`git diff --check` |

<a id="当前验证基线-2026-07-11"></a>

## 历史能力验证快照（2026-07-11）

记录时源码为 `HEAD f4ebec9` 加当时工作树的 BUG-20260711-001、003、005、008、010–014、018–024 修复与新增回归；主机/目标为 Windows `x86_64-pc-windows-gnu`；工具链为 `rustc 1.96.0`、`cargo 1.96.0`。本节保留能力证据的历史快照；当前测试总数以[当前验证证据](#当前验证证据)为准。

| 门禁 | 结果 | 证据范围 |
|------|------|----------|
| `cargo test --all-targets` | **通过** | lib **1181/1181**；demo **23/23** |
| `cargo test --doc` | **通过** | **3 passed / 24 ignored** |
| Windows compile | **通过（本轮 default/no-default）** | `cargo test --all-targets`；`cargo build --bin uix-demo --no-default-features`；`--all-features` 为历史通过 |
| Linux / macOS cross-check | **历史通过；本次未复跑** | `x86_64-unknown-linux-gnu` / `x86_64-apple-darwin` `--all-targets`（default + all-features） |
| `cargo clippy --all-targets` | **通过** | `NativeGpuCanvas2D::ensure_soft` 以 `get_or_insert_with` 保持惰性分配并移除 denied `clippy::expect_used`；仍有既有非阻断 warnings |
| 文档门禁 | **通过** | `python scripts/check_docs_links.py`；`architecture_document_stays_in_the_qualified_docs_tree`；`git diff --check` |

| 能力簇 | 证据级别 | 要点（链） |
|--------|----------|------------|
| 布局两阶段 / ScrollView / 窄父约束 | `automated` | [#174](决策.md#d174)；Container/Grid/ScrollView/Space/Form/Card pass-local `LayoutChild` |
| FrameRenderer / Picture / DisplayList | `automated` | 首帧 build；无 offscreen 时直绘；clip/glyph 录制与 replay |
| CPU / D3D11 fill·stroke path | `automated` + 部分 `hardware` | D3D11 identity mesh + soft fallback；真实 HWND readback |
| D3D11 Present / 内存 | `automated` + `hardware`（单 GPU） | Present 前释 RTV；39 秒 15 帧与 15 分钟资源/显存采样稳定；[综合报告](../test-reports/20260711-190000-uix-comprehensive/report.md) |
| Software / D3D11 GUI smoke | `hardware`（单 GPU） | 首帧/点击/普通最大化·还原/关闭、窄窗滚动条与副窗主题；最小化→最大化仍待 [BUG-20260712-003](问题.md#bug-20260712-003)，多驱动矩阵待补 |
| OpenGL ES / WGL | `automated` + `hardware`（单 GPU） | real-window draw readback + **post-SwapBuffers** 多帧 present（`opengles_gpu_engine_*` · `factory_create_wgl_*`）；显式 OpenGL ES 主窗 tick 33/44/56 与副窗完整；驱动矩阵仍待 |
| IME / Input | `automated` + 部分 GUI | IMM32 + `ime_dispatch`；TSF **AssociateFocus** + 最小 **`ITextStoreACP`** + **`ITfContextOwnerCompositionSink`**（`tsf_text_store` / `tsf_session`，共享事件队列；TSF 活跃时跳过 IMM32 去重）；微软拼音 GUI / `ITfTextEditSink` 仍待 → [RISK-20260712-001](问题.md#risk-windows-ime)；真实 GUI Tab 复验 → [BUG-20260711-002](问题.md#bug-20260711-002) |
| 全局主题 `#175` | `automated` + Software GUI | `set_theme` 合并 wake；palette-only；**D3D11 双 HWND 主题色板 present/readback**（`dual_hwnd_theme_palette_*`）已补；前台 capture 仍待 |
| 多窗 HWND 路由 | `automated` + Software GUI | 每 HWND 独立 `WindowState`；**D3D11 双窗联合 present** 已补（同上） |
| Linux / macOS 真机 | `compiled` only | 未做真机运行 |

---

<a id="平台与后端证据矩阵"></a>

### 平台与后端证据矩阵

| 平台 / 路径 | coded | automated | compiled | hardware / GUI | production |
|-------------|:-----:|:---------:|:--------:|:--------------:|:----------:|
| Windows D3D11 `GpuNative × Swapchain` | 是 | 单元/集成 + real-window Hardware/WARP readback + 双 HWND 主题色板 present；WARP 自动化已覆盖非零 crop/scissor 恢复及 bound encoded Picture，仍未覆盖 root 的统一 painter order/fatal present | 单 GPU 配置历史通过 | 单 GPU hardware + 主副窗 backbuffer GUI；前台 capture / 多驱动待补 | 否；[BUG-004/005/009](问题.md#已确认图形架构缺陷) · [RISK-006](问题.md#risk-graphics-platform-evidence) |
| Windows D3D12 bounded `GpuNative × Swapchain` | 是（`feature=d3d12`） | WARP/Hardware context + soft-blit；本次 real-window test 在 NVIDIA GeForce RTX 4070 Ti SUPER 上通过 hardware clear/draw/readback/present，且 D3D11/D3D12 caps 现从 shared logical/drawable DPR 取值；未覆盖统一命令流、fatal present 与反复 device fault 回收 | `--features d3d12` 历史通过 | 单个 hardware 自动化，不等于完整 engine GUI / 高 DPI / 多驱动矩阵 | 否；[P6.9](#p69-图形契约整改) |
| Windows OpenGL ES / Software | 是 | ES3 + soft readback + SwapBuffers 多帧 present；WGL 真实窗口已自动化验证预乘 soft-tile parity 与编码 Picture 执行，线程亲和由通用 context 边界回归覆盖；实际 Software 终态 fallback 仍待 | Windows 配置历史通过 | Software GUI smoke；ES 前台 capture / 多驱动矩阵待验 | 否；[RISK-004](问题.md#risk-graphics-thread-affinity) |
| Linux Vulkan / EGL | 是 | EGL 禁用未证明 partial、完整 swap 与 display 终止；Vulkan fence/reset、swapchain/extent 错误边已编码；尚无目标平台运行测试 | 本轮 `cargo check --target x86_64-unknown-linux-gnu --all-features` 通过；链接测试受本机 Linux linker 缺口阻断 | 待 Wayland/GPU 真机 | 否；[BUG-006/007](问题.md#已确认图形架构缺陷) |
| macOS Metal identity `Cpu × PixelUpload` / Software | 是 | 当前无 context 错误路径测试 | 本轮 `cargo check --target x86_64-apple-darwin --all-features --tests` 通过 | 待 AppKit 真机；不得外推为 Metal GPU raster | 否；[RISK-006](问题.md#risk-graphics-platform-evidence) |
| iOS / Android | 否 | — | — | — | — |

---

<a id="p6-生产级框架"></a>

## P6：生产级框架

Windows 优先（[#167](决策.md#d167)）；策略 → [产品 · 开发策略](产品.md#开发策略)；设计 → [#162](决策.md#d162)–[#169](决策.md#d169) · [#172](决策.md#d172)。

<a id="生产级优先项"></a>

### 生产级优先项

| 优先 | 项 | 状态 | 说明 |
|------|-----|------|------|
| P0 | **Windows 生产可用** | 进行中 | 主验证环境（[#167](决策.md#d167)） |
| P0 | 全量测试零失败 | `automated` | 见[当前验证证据](#当前验证证据) |
| P0 | 图形帧/错误/线程契约 | **阻塞** | [P6.9](#p69-图形契约整改)；Critical 缺陷未修复 |
| P0 | probe 诊断与资源关闭 | `coded` + 部分 `automated` | 初始化候选/阶段诊断已有覆盖；运行期 recovery、平台失败清理与真实 Software 终态未闭合 |
| P0 | 平台层抹平差异 | `coded` + `automated` | Result-only、依赖/cfg 守卫；上层无 OS cfg |
| P1 | Linux / macOS parity | `coded` + `compiled`（历史） | 历史 cross-check 见[快照](#当前验证基线-2026-07-11)；真机待验 |
| P1 | macOS 原生运行验证 | 待验证 | AppKit/Metal/IME 已接 |
| P1 | demo / docs 同步 | `automated` 基线 | 持续维护 |
| P2 | 无障碍 v1 | 部分 | 快照/键盘已落地；读屏桥待后续 |
| P1 | D3D11 GPU native raster | **阻塞** | 实现存在，但未满足画序、fallback、offscreen 与失败语义；见 [P6.9](#p69-图形契约整改) |
| P2 | D3D12 / Metal GPU 光栅 | D3D12 bounded `coded`；Metal 未实现 | 契约整改闭合前不扩能力；排期由 [OPEN-006](问题.md#open-gpu-roadmap) 裁决 |
| P2 | WebGPU 评估 | backlog | 远期 |

### 图形后端里程碑

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| P6.0 设计 | 抽象分层、平台矩阵、回退链、术语 | 历史基线；由 #181–#186 修订 |
| P6.1 基线 | `GraphicsBackend` 枚举；registry 单条目创建 | `coded`；recipe 模型待整改 |
| P6.2 Windows | D3D11 `GpuNative` × `Swapchain` + WGL `GpuNative` × `Swapchain` | `coded` + 部分 hardware；正确性阻塞 |
| P6.3 Linux | Vulkan `Cpu` × `PixelUpload` + EGL `GpuNative` × `Swapchain` | `coded` + 历史 compiled；Critical/High 阻塞 |
| P6.4 macOS | AppKit + Metal `Cpu` × `PixelUpload` context；SoftwareEngine 回退 | 部分 — 原生验证待完成 |
| P6.5 配置 | App builder / env / Settings opt-in | `automated` |
| P6.6 WebGPU | 远期评估 | backlog |
| P6.7 可插拔 registry | registry、probe、present 入口 | `coded`；recipe/lifecycle 契约不完整 |
| P6.8 可组合渲染轴 | `RasterMode` × `PresentMode` 表驱动装配；D3D11 与 D3D12 bounded GpuNative | 部分 `coded`；caps/fallback 语义不完整 |
| P6.9 图形契约整改 | 有序帧、typed recovery、线程亲和、recipe/caps、平台资源事务 | **部分 `coded` / `automated`；未闭合** — 见 [P6.9](#p69-图形契约整改) |

<a id="p68-可组合渲染轴"></a>
<a id="p67-图形后端架构"></a>
<a id="p67-迁移验收-g1g11--m2m7"></a>

### P6.8 可组合渲染轴（#169）· P6.7 registry

状态对照 [#168](决策.md#d168) · [#169](决策.md#d169) · [#172](决策.md#d172)。

P6.7 的 registry/probe 形态已编码：`draw::bootstrap_graphics_engine` 是唯一 probe 入口，`native/graphics/<api>/` 是 API 对等实现根；但 registry 仍以 API identity 取单行、context 生命周期与 present 成功语义不统一，因此不能标为契约完成。

| 优先 | 项 | 状态 | 说明 |
|------|-----|------|------|
| P0 | 正交轴类型与 caps | 部分 `coded` | 轴类型存在；offscreen/partial/crop 等 caps 仍有耦合或过度承诺 → [BUG-006/010](问题.md#已确认图形架构缺陷) |
| P0 | D3D11 GPU native raster | **阻塞** | 已有 native/soft 操作，WARP 已验证 soft-tile blend、Picture crop/scissor 与 bound encoded Picture；统一 painter order、复杂 Picture 与真实 fatal present 仍未满足 [AC-GFX](产品.md#req-006-acceptance) |
| P1 | 表驱动 factory / engine | 部分 `coded` | registry 行含 `raster`+`present` 且同 API 多 recipe 已落地；API 中立边界仍未完成 → [BUG-008](问题.md#bug-20260712-008) |
| P1 | `BackendKind` 统一 | `coded` | `Cpu`/`Gpu`/`Auto`/`Null` 已存在；不得替代 recipe key |
| P2 | D3D12 bounded GpuNative | 部分 `coded` | clear/rounded + soft-blit；其余 soft；契约整改前不扩能力 |
| P2 | Metal native raster | backlog | 下一 native raster 切片 |
| P2 | D3D12 扩展能力 | backlog | stroke/glyph/gradient/path/shadow 等仍按 caps 走 soft |

当前装配源码入口 → `src/draw/engine/` · `src/draw/backend/` · `src/native/factory/` · `src/native/graphics/`。目标硬约束 → [架构 · 图形 API 与帧提交](架构.md#图形-api与帧提交硬约束)。

<a id="p69-图形契约整改"></a>

### P6.9 图形契约整改（#181–#186）

状态：**部分 `coded` / `automated`；未闭合**。本节只拥有排期、验收门槛和证据要求；缺陷事实与源码证据由[问题台账](问题.md#已确认图形架构缺陷)唯一维护。阶段仍按依赖推进；只有原始复现与至少一个相邻场景通过，且没有超出证据级别的声明，才可关闭关联 issue。

| 阶段 | 优先级 / 状态 | 实施边界 | 退出门槛 | 关联 |
|------|---------------|----------|----------|------|
| GFX-R0 契约锁定 | P0 / 部分 `automated` | 已补 FrameEncoder 参考帧、typed present failure、recipe/caps 与 thread-affinity compile-fail；EGL partial 与 D3D11 offscreen caps 曾先收紧 | 其余 Critical/High 原始复现仍待覆盖；能力只能按已证明行为开启 | [AC-GFX-01..06](产品.md#req-006-acceptance) · [BUG-004..009](问题.md#已确认图形架构缺陷) |
| GFX-R1 有序帧与唯一 present | P0 / 部分 `coded` + `automated` | `FrameRenderer` 现由私有 `FrameRecordingEngine` 直接生成 main `FrameEncoder`：每个 Canvas 可视操作按 painter order 降为 `Clear`、严格受限的 `Native(FillRect)`、精确 source-over `CpuSegment` 或 `PictureBlit`；真实 backend canvas 不再接受 root/direct/overlay 绘制。缓存 Picture 仍使用私有 CPU offscreen 存储，主 target blit 进入同一流；受 clip/offset/opacity 影响的 Picture 降为 CPU 段。CPU target 逐命令执行，`NativeGpuBackend` main 与已绑定 Picture target 逐项消费该子集，只在 segment/blit 边界上传紧凑 alpha-visible tile，自己不 present。因 producer 仍全帧重建，所有 partial dirty、多 dirty 与 scroll 输入均强制 `FullRedraw` + 完整 final damage；D3D11 WARP real-window readback 已覆盖 main/已绑定 offscreen 的 mixed 命令、离屏 target viewport，以及含 CPU fallback 的 `FrameRenderer` direct/Picture producer 到唯一 present 的实际路径；Windows WGL 自动化在同一真实路径的唯一 main `FrameEncoder` 执行后、`SwapBuffers` 前捕获 drawable，确认 direct/Picture/root-level overlay CPU fallback 的 top-left 像素画序并确认随后最终 `Present`；测试显式处理 WGL bottom-left readback 与交换后 back buffer 不保证可读。CPU/NativeGpu legacy main 与 checked Picture 路径的 path clip，以及 GPU legacy main/canvas 的 scroll copy，不再静默 no-op：它们在最终 present 或 checked flush 前返回 `NotImplemented`，直接 `RenderBackend::present` 亦先消费 deferred failure；公开 `Canvas2D::scroll_region` 已无默认 no-op，所有实现必须在编译期选择精确 copy、typed reject 或仅 Null/test 的显式 no-op。direct-root 命令流、Additive typed 拒绝、executor failure、cached Picture、Drawer animation 与既有 `FrameReady`/`PresentPending`/最终 `Present` 分离均已自动化 | destination-dependent Additive 与 scroll copy 尚无等价 IR，现会 typed failure；非整数/有状态 Canvas 操作仍为每操作 full-extent CPU source，跨 API 完整像素 parity、原生 offscreen render 与 retained/damage-aware recording 均待完成。因此 #181 与 BUG-004 仍未关闭，性能优化留给 R6；WGL 仅为当前 Windows host 的 `automated` 证据，不等同于多驱动 hardware 或 production 验收 | [#181](决策.md#d181) · [#188](决策.md#d188) · [#196](决策.md#d196) · [#197](决策.md#d197) · [#199](决策.md#d199) · [#200](决策.md#d200) · [#201](决策.md#d201) · [BUG-004](问题.md#bug-20260712-004) |
| GFX-R2 typed failure 与恢复 | P0 / 部分 `coded` + `automated` | `GraphicsFailure`、失败帧不伪报成功/不清 dirty、有限恢复状态机已编码；`IGraphicsContext` 与 `GraphicsEngine::resize`、make-current/swap/default present 均可传播错误，窗口事件失败时保留 dirty；Picture 的 bind/flush/restore/blit 与 main `FrameEncoder` execute 均由 checked `Result` 边界传播，失败时 `LayerTree` 保持/恢复 dirty，`FrameRenderer` 不调用 `end_frame`/final present；main encoder `SurfaceLost` 注入回归确认 `RecoveringGraphicsEngine` 在下一 `begin_frame` 触发 surface rebuild。external presenter 失败同样经 hook 送入 recovery，并按 surface→same recipe→next recipe→Software 逐步替换；Vulkan out-of-date/suboptimal 现重建后返回 `GraphicsSurfaceLost`，Vulkan device/OOM result 也分类为对应 typed failure；Vulkan/Metal 的 PixelUpload recipe 现明确拒绝不提交像素的 legacy swapchain-present hook，只接受 `PresentFrame::PixelBuffer`，Linux/macOS 均仅 target `compiled`；D3D11/12 的 device-removed/reset/hung 与 OOM HRESULT 已归类为对应 typed failure 并有 Windows 自动化回归；GDI resize、Wayland SHM I/O 与 macOS CALayer allocation/image failure 已送入同一 presenter error hook | 未注入实际原生 device-loss/surface-loss 端到端场景；Wayland/Vulkan Linux 与 Metal/AppKit macOS 仅 target `compiled`，平台资源重建仍待 | [#182](决策.md#d182) · [#188](决策.md#d188) · [#190](决策.md#d190) · [BUG-005](问题.md#bug-20260712-005) · [BUG-007](问题.md#bug-20260712-007) |
| GFX-R3 线程与生命周期所有权 | P0 / 部分 `coded` + `automated` | 已移除 `RenderBackend`/native context 的无依据 `Send/Sync`，并以 doctest 阻止安全跨线程移动；registry 与 D3D12 WARP 测试工厂返回的 context 已统一包装为 `ThreadBoundGraphicsContext`（`!Send + !Sync`），所有 `Result` 图形操作先验证创建线程并以 `InvalidState` 拒绝错误线程；caps/native caps/width/height/DPR 在 owner thread 缓存并仅在成功 initialize/resize 后刷新，错误线程 metadata 查询不再触达 native object；`IGraphicsContext::read_pixels` 已收为唯一 typed `Result`，WGL/EGL/D3D11/D3D12 不再吞失败为空像素，错误线程与 inner `GraphicsDeviceLost` 回归均覆盖；D3D12 的 public `try_shutdown` 现直接返回 terminal fence drain 的 `shutdown_result`，不再落回 legacy void hook，WARP real-window fault 回归经该入口验证收尾与幂等；WGL 的 public `try_shutdown` 现逐项传播 make-current、解绑、delete-context 与 ReleaseDC 失败，真实窗口自动化验证其成功收尾；D3D11 的 checked offscreen destroy 现先恢复 swapchain target、失败时保留 offscreen slot，架构回归禁止再吞 restore failure，WARP 自动化验证成功路径；EGL 的同一入口现逐项传播 make-current、unbind、destroy-context/surface 与 terminate 失败，并为已成功销毁的 context/surface/display 保留独立状态以允许 retry，当前仅 Linux target `compiled`；Vulkan 的 public `try_shutdown` 现先以 checked `vkDeviceWaitIdle` 返回 typed failure、失败前保留资源拓扑供 retry，且不支持的 native readback 返回 `NotImplemented` 而非空成功；同样仅 Linux target `compiled`；Metal PixelUpload context 的 native readback 也返回 `NotImplemented` 而非空成功，但仅 macOS target `compiled`；`RenderBackend`/`GraphicsEngine` 的 checked offscreen destroy 现贯通 native backend、engine wrapper、Picture resize 与 LayerTree orphan sweep，失败时保留 native slot/handle 并允许 retry；`NativeGpuBackend` shutdown 现依次检查 offscreen/swapchain restore 与 context-specific checked teardown，任一步失败均保留 backend 以供 shutdown/Drop retry；factory/engine/backend 的失败清理与 staged context 替换使用 thread-bound 边界，后者在旧 context shutdown 失败时保留旧所有权并关闭 replacement；`RenderSession::shutdown` 同样只经 `try_shutdown` 释放 staged context，失败时保留所有权供下一次 shutdown/Drop retry；公开 `PlatformWindowCore::with_gpu` 已收为 crate 内部，不能从公开窗口 API 绕过 factory 注入裸 context；`PresentUploadEngine` 的 shutdown 现先经 `try_shutdown`，失败时不标记完成以供 Drop/retry 重试；`RecoveringGraphicsEngine` 的 shutdown/Drop 已幂等 | shutdown 及部分 native teardown 仍有旧无返回值兼容 hook；错误线程 Drop、WGL/EGL/Vulkan teardown 的故障注入和其他平台 native lifecycle 仍缺目标平台证据 | [#183](决策.md#d183) · [#187](决策.md#d187) · [#191](决策.md#d191) · [#195](决策.md#d195) · [#198](决策.md#d198) · [RISK-004](问题.md#risk-graphics-thread-affinity) |
| GFX-R4 recipe 与 API 边界 | P1 / 部分 `coded` + `automated` | registry/bootstrap 已以 `(backend,raster,present)` recipe 多行 probe，并覆盖同 API 首 recipe 失败→后项成功；feature-off API 模块不再声明，`glow`、`ash`、`khronos-egl` 与 Windows Direct3D feature 随 Cargo feature 裁剪，disabled registry 行拒绝直接创建；raw entry/API create、Wayland `GpuPresenter` 与 draw 的 raw context constructor/`RenderSession` 注入已收为 crate-private，`native::create_gpu_context_with_backend` 与 registry raw context create 也仅 native factory bridge 内可见。bootstrap 与 runtime recovery 复用单一 engine assembly/清理路径并保留 create/initialize 阶段诊断；其 surface 改为 `NativeSurfaceHandle`（`!Send + !Sync`），仅 app 装配点显式 unsafe 包装窗口 raw pointer；factory 已成为生产路径唯一的 `IGraphicsContext::initialize` 调用方；`PresentUploadEngine` 仅使用 factory 的实际 drawable 尺寸创建 CPU session，回归证明不会以 null surface 二次初始化；GpuNative startup 不会重建已创建的 swapchain，init-failure 回归从 native make-current 失败验证 context 清理。GL loader/context/shader source 与 raster program/buffer/texture/FBO 已由 `native/graphics/opengl::OpenGlRasterPipeline` 独占；WGL/EGL 仅声明其真实 DTO caps，`NativeGpuBackend` 为 OpenGL ES 与 D3D 共用装配路径，runtime lease、`GpuBackend`/`GpuCanvas2D` 与 draw 侧 `glow` 已删除；WGL 自动化覆盖 bounded soft upload、source crop、active Picture→Picture 画序、DPR scissor，架构回归扫描禁止 draw 重引 API object 或越过 factory bridge 创建 raw GPU context | factory bridge 外的历史 platform raw-pointer API、WGL/EGL 以外 API 的同等 native raster 闭合与真实平台 lifecycle 证据仍待完成 | [#184](决策.md#d184) · [#187](决策.md#d187) · [#192](决策.md#d192) · [#194](决策.md#d194) · [#195](决策.md#d195) · [BUG-008](问题.md#bug-20260712-008) |
| GFX-R5 fallback 与平台正确性 | P1 / 部分 `automated` + `compiled` | CPU Additive 在纯 CPU 路径有回归；不具备 destination 的 GL/NativeGpu soft segment 现 typed 拒绝而非 alpha-over 近似；D3D11 与 GL source crop、raster state/画序回归已自动化，BUG-010 仅按 `automated` 关闭；`SoftFallbackTile` 现携带紧凑、premultiplied source payload 与独立 destination，NativeGpu 按 alpha-visible bounds 打包，D3D11/12 与 WGL/EGL 分别以 source 纹理原点和 destination viewport/scissor 执行，D3D11 WARP 自动化验证非零落点和连续不同 segment 不重采样；未实现 tile 的 context typed 拒绝而不退回全帧。D3D11 与 GL soft tile 以 premultiplied blend 组合，Software/D3D11 WARP/D3D12 WARP 共用 hybrid/clip/bounded-tile probe script，Software/D3D11 WARP 共用 Picture source-crop→post-blit clip script，Software/WGL 共用半透明 soft tile probe 均已通过，D3D12 明确不声明 offscreen cap；Windows WGL/D3D11/D3D12 现共享 logical→drawable DPI 计算，创建、resize 与 caps 的 DPR 一致，纯 192 DPI、架构守卫及 D3D11/D3D12 real-window caps 回归已通过；本次 D3D12 自动化还在 NVIDIA GeForce RTX 4070 Ti SUPER 上跑过单次 hardware context。graphics feature 隔离已验证；native EGL raster 明确仅支持 GLES3，无法获取相应 config/context 时 typed 拒绝，架构回归防止再引入无等价 shader pipeline 的 GLES2 fallback；macOS `MetalPixelUploadContext` 名称与 CPU PixelUpload recipe 已由架构回归锁定，不宣称 Metal native raster；EGL/Vulkan 事务路径仍仅有 Linux target `compiled` | 其他 API parity、EGL/Vulkan、高 DPI/Metal 适用真机验证以及运行期端到端错误注入仍待完成；WARP/WGL 自动化与单次 D3D12 hardware test 均不得外推为 production 或多驱动矩阵 | [#185](决策.md#d185) · [#186](决策.md#d186) · [#189](决策.md#d189) · [#196](决策.md#d196) |
| GFX-R6 device/surface 与性能 | P2 / 待实施 | 在正确性闭合后拆共享 `GraphicsDevice` 与逐窗 `WindowSurface`，复用 shader/cache/scratch，消除整帧 soft upload与可避免临时分配 | 多窗/4K/15 分钟 profile 无每窗重复 device、无 steady-state 全帧 fallback upload、资源/显存稳定；数据归档到测试报告 | [RISK-005](问题.md#risk-graphics-resource-topology) |

**跨阶段质量门禁**：`cargo test --all-targets`、目标 feature/cross-check、文档门禁必须通过；涉及 EGL/Vulkan/WGL/Metal 的阶段没有对应真实平台证据时最多标 `automated` / `compiled`，不得标 `hardware` 或 `production`。

---

## 后续工作

| 项 | 说明 | 文档 |
|----|------|------|
| **生产级框架（P6 优先）** | **Windows 优先**生产可用；先完成图形契约整改，再扩 D3D12/Metal 能力；Linux/macOS parity 与真机验证随后 | [P6.9](#p69-图形契约整改) · [P6 生产级框架](#p6-生产级框架) |
| **移动端（P7+ backlog）** | **未实现**。目标 iOS / Android 原生 backend；复用上层 trait 与零闲置主循环 | [产品 · 开发策略](产品.md#开发策略) · [里程碑](#里程碑与工作域) |
| **全栈扩展（backlog）** | **部分**：`data` 仅 Settings KV（opt-in）。**未实现**：网络层、HTTP、同步、服务端集成 | [#166](决策.md#d166) |
| 无障碍（屏幕阅读器桥） | #99：v1 基线已落地；屏幕阅读器平台桥待后续 | [架构 · 源码入口](架构.md#源码入口) |
