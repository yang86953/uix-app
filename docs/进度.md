# 进度

> **接口**：本文档组件是进度文档的路由索引，记录当前交付状态和活跃差距。依赖：[产品](<file:///C:/data/note/我的项目/软件/UIX App/产品.md>)（需先了解目标）、[架构](<file:///C:/data/note/我的项目/软件/UIX App/架构.md>)（需了解分层）。导出：按分类路由到 `进度/` 下的各文档组件。

## 适用线索

当需要了解"当前做到哪了、还差什么、瓶颈在哪"时，从本文档进入。

## 详细文档

产品、使用和现有 Rust 架构的唯一当前副本位于：

`C:\data\note\我的项目\软件\UIX App`

- 产品：`C:\data\note\我的项目\软件\UIX App\产品.md`
- 使用：`C:\data\note\我的项目\软件\UIX App\使用.md`
- 架构：`C:\data\note\我的项目\软件\UIX App\架构.md`
- 通用 SMC 定义：`C:\data\note\程序开发\System Module Component 设计模式.md`
- 项目内 SMC 设计模式副本：已按知识库迁移，源码仓库不再保留正文副本。

源码仓库 `docs` 只维护进度，不再保存详细文档副本。

## 权威范围

本文档及 `进度/` 子目录下的文档组件共同构成 UIX 的交付真相。以下事实以本组件为准：

- 版本发布状态与交付物清单
- 功能逐项实现状态与证据
- 组件稳定性与已知缺陷
- 性能基线与优化方向
- 平台工程与运行保障进度
- SMC 架构定义以 `C:\data\note\程序开发\System Module Component 设计模式.md` 为准；源码仓库不再保留正文副本，仓库进度只记录迁移状态与验证证据

## 进度总览

| 状态 | 事项 |
|------|------|
| ✅ 已完成 | 功能实现 110/110 项、迁移前 Rust fence 214/214 通过、稳定帧 ~4.7ms、42 项组件修复与真窗验证、Platform 公开面定义、Diagnostics 核心落地、S0-01～S0-05 基线审计、S1-01～S1-06 平台回调入队、S2 旧日志迁移与 native trait 失败语义清理（12 个 trait 全部改 typed Result） |
| ✅ 已完成 | 消除 graphics→ui 反向依赖（主题令牌分层收口）：`ThemeSnapshot` 删除、`draw::PaintContext` 移除主题、UI-owned `PaintContext` 接管 `tokens()` 与 scope，`src/draw` 零 `crate::ui` 引用 |
| 🔄 进行中 | 内存优化（~142 MiB Vulkan，瓶颈：驱动+字体）、MSDF 字形视觉矩阵闭合、92 项组件视觉矩阵 UNVERIFIED、剩余旧失败语义审计、主题公开面 allowlist+denylist 核验 |
| ✅ 已完成 | **SMC 模式改造（最高优先级，主人 2026-07-31 决定）**：SMC-01～SMC-07 全部落地（core 定级、platform / graphics / ui / data / app 五 System 迁移、跨层强制规则全量闭合），详见下方批次表 |
| ✅ 已完成 | **理想使用演进 E-01～E-09 全部落地（2026-08-01）**：类型化表单 `Form::model`、声明式虚拟化 `Table::virtualized`、受控浮层 `Modal::builder`、全局反馈门面 `ui::message()`/`ui::notify()`、声明式无障碍动作 `semantic_actions`、路由 key 派生宏 `#[derive(Display)]`、富文本链接回调 `RichText::on_link`、全局服务注册 `App::register` 与 i18n key 宏 `t!`、主题品牌种子 `Theme::custom`；使用文档「目标写法」示例全部升级为已落地 rust fence；下一步 S4 真窗视觉验收 |
| ✅ 已完成 | **SMC 完成度独立审计（2026-08-01）**：跨 System 依赖与系统列表「允许依赖」列一致（platform→graphics 零依赖、data 仅 core、ui 无 app/data）；五 System 兄弟 Module 反向边 rg 全量扫描全 0（native/draw/ui/app/core）；doctest 47/47（含 29 个 compile-fail deny 契约）、7 组公开 API 契约、workspace all-targets 与 Windows GNU check 通过；同时修复 `diagnostics::crash` panic hook 测试与 `wnd_proc`/TSF thunk 故意 panic 测试的全局 hook 竞态（新增 `PANIC_HOOK_TEST_LOCK` 共享测试锁，lib 测试 12 次连续通过），详见[运行保障](进度/运行保障.md) |
| ✅ 已完成 | **S4-01 真窗组件视觉验收（2026-08-01）**：clean commit `276657343c14` 上真窗视觉门禁首次通过——92 组件 / 663 声明状态 / 847 行 PASS 全部在 clean 环境生成（AMD RX 5700 / 96 DPI / build 26100），329 PNG + 22 联系表 + `graphics-auto-vulkan.log`，报告见 `test-reports/0.0.1-276657343c14-20260801/component-visual/report.md`（test-reports 不入库）；替代历史 dirty UNVERIFIED 证据，详见下方批次表 |
| ✅ 已完成 | **S4-03 无障碍证据 schema 与收集器（2026-08-01）**：固化 `manifest.json`（`uix.accessibility.manifest.v1`）+ 逐项 JSON（`uix.accessibility.item.v1`）schema（见[无障碍证据](进度/无障碍证据.md)）；新增真窗收集器（`tests/support/agent_gui_windows/accessibility.rs`：组件 QA 92 case + charts 场景两个测试，提取 role/name/state/value/actions/focus/keyboard）与组装器（`scripts/build_accessibility_manifest.py`）；ChartPlaceholder 补齐语义节点（role=image + title 名，对齐 Bar/Line/Pie）；clean commit 上生成 110 行 manifest：104 captured + 6 ledger-gap（8.7～8.12 账本缺口，不冒充）；pending-scene 已清零 |
| ✅ 已完成 | **F 批次「使用落地」（2026-08-01）**：使用文档最后 19 处「目标写法」`text` fence 全部落地为 `rust uix-compile` 示例（16 个新锚点）；配套 API：`Transition` + `View::enter/leave`（含 stagger 延迟接入播放管线）、`Header::title` / `Content::child` / `Grid::children`、`App::diagnostics_runtime`；`Platform` 便捷查询已存在（`uix::platform` 公开面）无需新增；契约测试 `tests/usage_facade.rs` 5 项通过；详见下方批次表 |
| 📋 规划中 | 四平台同等视觉矩阵、窄 present、复杂脚本 shaping、移动端 |

## 快速模型证据分母（S0-02）

下表固定五类分母的语义和唯一来源；任何阶段不得用其中一个数字换算或替代另一个数字。

| 分母 | 唯一语义 | 权威来源与可重算方法 | 当前审计结论 |
|---|---|---|---|
| 110 | `0.0.1` 功能项 / 产品交付范围 | [功能账本](进度/功能账本.md)、[首发状态](进度/首发状态.md) | 产品口径仍为 110；功能账本当前可解析行仅 104 条，缺 `8.7`～`8.12`，行级完整性未闭合，不用 104 冒充 110 |
| 92 | 组件视觉验收库存 | `demo/src/demos/component_qa/manifest.rs` 的 `COMPONENT_VISUAL_CASES`；逐项解析 `widget` / `composite` / `provider` 调用 | 92 个唯一 case：87 Widget、3 Composite、2 Provider；以 manifest 为准，覆盖进度文档的分类冲突 |
| 663 | 组件 manifest 声明状态总数 | 同一 manifest；对 92 个 case 的 `states` 数组长度求和 | 求和为 663；它不是 CSV 中去重后的 state 字符串数，也不是功能数 |
| 847 | 视觉证据行 | `test-reports/0.0.1-UNVERIFIED-20260722/component-visual/component-visual-matrix.csv`；逐行计数并检查 `result` | 847 / 847 行 PASS；报告基于 dirty commit `2374691`、Windows build 26200 / NVIDIA / 96 DPI，仅为 UNVERIFIED 证据 |
| 42 | 历史专项组件修复与真窗验证工作项 | [组件稳定性](进度/组件稳定性.md) 的进度总览摘要 | 这是历史专项修复计数，不是 92 项库存；当前没有独立机器可读的 42 行台账，不能从组件名列表或视觉证据行反推 |

详细命令、输出和冲突清单见知识库 [S0-02 工作项](<file:///C:/data/note/项目管理/工作项/UIX%20App-S0-02-建立证据分母映射.md>) 及对应执行记录。

## 快速模型门禁与证据路径（S0-03）

下表把门禁入口、输出契约、独立分母和阶段出口固定在一起。`not-run`、`blocked`、`fail` 和历史 `UNVERIFIED` 不得转换为 `pass`；历史报告只能作为路径样例，不能替代 clean 候选证据。

| 门禁 | 可复制入口 | 证据路径与输出契约 | 分母 / 阶段出口 | 当前状态与失败边界 |
|---|---|---|---|---|
| 代码构建与自动化测试 | `cargo check --workspace --all-targets`；`cargo test --quiet`；按影响追加 `cargo check --lib --target x86_64-pc-windows-gnu`、`cargo check --lib --no-default-features`、`cargo check --lib --no-default-features --features vulkan`、`git diff --check` | 当前命令结果先落在 [S0-01 执行记录](<file:///C:/data/note/项目管理/执行记录/UIX%20App-S0-01-生成源码基线清单-执行.md>)；后续每个 WorkItem 的执行记录保存命令、结果和 commit | 构建/测试命令结果，不与 110/92/663/847 换算；S0-01～S0-03，后续 S1～S3 代码出口 | **基础编译/测试与 feature matrix 已验证（2026-08-02）**：workspace/all-targets 为 0 errors；默认 `cargo test --quiet` 为 110 passed / 31 ignored；`--no-default-features` 与 `--no-default-features --features opengles` 均为 0 errors。跨硬件、其他 OS 与真实 GUI gate 仍按各自门禁状态判定，不能据此写成全环境通过 |
| 真窗与组件视觉 | 设置 `UIX_EXPECT_DPI`、`UIX_GUI_EVIDENCE_DIR`、`UIX_COMPONENT_QA_EVIDENCE_DIR` 后，运行 `cargo test --release --features 'agent-control,test-harness' --test agent_gui_windows component_visual::real_demo_captures_every_component_and_applicable_visual_state -- --exact --ignored --nocapture --test-threads=1`；联系表另运行 `component_visual::build_component_visual_contact_sheets` | `test-reports/<candidate>/component-visual/report.md`、`component-visual-matrix.csv`、逐项 PNG、`contact-sheets/`、`graphics-*.log`；CSV 必须逐行绑定 clean commit、环境和 `PASS` 结果 | 真窗功能 **110**；组件库存 **92**、声明状态 **663**、视觉证据行 **847**；S4-01～S4-02、S4-13～S4-15 | **自动化 clean 证据已复核（2026-08-01）**：候选 `13cb6bea` 生成 92/663/847，矩阵 847/847 `PASS`，329 PNG、22 联系表，NVIDIA RTX 4070 Ti SUPER / Vulkan / 96 DPI；S4-02 代理视觉预审无明确缺陷，主人最终审美确认仍待办。历史 dirty/UNVERIFIED 报告不作为本证据 |
| 无障碍 | 以 `cargo test --release --features 'agent-control,test-harness' --test agent_gui_windows <具体 ignored GUI 测试> -- --exact --ignored --nocapture --test-threads=1` 运行语义快照断言；当前测试入口由 `tests/agent_gui_windows.rs` 及其 `component_visual.rs`、`text_components.rs`、`multi_window.rs` 等模块提供 | 现有 `UIX_GUI_EVIDENCE_DIR` 可保存 `graphics-*.log` 与 `dpi-*.json`；语义快照当前主要在测试进程内断言，尚无独立 110 行持久化 manifest；S4-03 应建立 `test-reports/<clean-candidate>/accessibility/manifest.json` 及每项 JSON（role/name/state/value/actions/focus/keyboard） | 无障碍功能 **110**；S4-03 固化 schema，S4-15 汇总 | **部分闭合（2026-08-01，S4-03）**：schema、收集器、组装器已落地并在 clean commit `276657343c14` 生成 `test-reports/0.0.1-276657343c14-20260801/accessibility/manifest.json`——110 行 = 104 captured（组件 QA 89 + charts 场景 15）+ 6 ledger-gap（8.7～8.12 账本缺口）；完整 110/110 captured 仅剩账本补全，仍不写 pass |
| GFX-R5 | `python scripts\\run_windows_gfx_r5.py --profile all --vendor nvidia --device-fault true`（按目标机 `VK_EXT_device_fault` 能力调整）；将 vendor 替换为 `amd`、`intel`；执行前可用 `--preflight`，完成后用 `--verify-evidence <dir>` | 默认 `test-reports/<timestamp>-gfx-r5-<vendor>-<profile>/manifest.json` 与 `logs/*.log`；manifest schema **4**，状态为 `running/passed/failed/interrupted`，每个 case 绑定 test name、环境、日志 SHA-256 和 source clean/head；GFX-R5 exact 入口统一位于 `tests/tests.rs`，runner 使用 `cargo test --test tests` | 按 runner case × vendor 统计；覆盖 vendor resize/present/readback、mixed-DPI、soak、device-lost；S5-01～S5-11，阶段 5 出口 | **blocked / evidence-incomplete（2026-08-02）**：GFX-R5 代码与 soak 证据基线为 clean commit `eda9ff0e`；后续文档-only 提交仅回写进度。已验证的 NVIDIA manifest 均绑定 clean source head：vendor=`a1f050d0`、device-lost=`326867ba`、single/shared soak=`eda9ff0e`。`--profile all --device-fault true --preflight` 通过并列出 8 条 inventory，soak manifest 已用 `--verify-evidence` 复核。mixed-DPI 在当前机器因没有两块不同有效 DPI 的真实显示器而阻断，因此没有 schema-4 all manifest，也不能写成 GFX-R5 pass。需在真实双显示器目标环境执行 mixed-DPI，再分别形成 NVIDIA/AMD/Intel、clean checkout 和其他目标环境证据 |
| G5 performance / soak | `python scripts\\run_release_g5.py --mode performance`；`python scripts\\run_release_g5.py --mode soak`；已有目录只用 `--verify <dir>` | 默认 `test-reports/g5-release/<commit-12>-<timestamp>-<mode>/manifest.json`；必须包含 `build.log`、候选 exe、Cargo.lock、`environment.json`、`frames.ndjson`、`memory.ndjson`、`raw.log`、`report.md`、`runner.py`、`scenarios.ndjson`、`summary.json`；summary/manifest 只允许 `pass/blocked/fail` | 性能至少 10,000 presented 帧、p95 ≤16.7 ms、5 类各 1,000 次闭环；soak 8 小时、内存增长 ≤64 MiB、idle p95 ≤0.25 core；S5-09～S5-10 | **fail / measurement_failed（2026-08-02）**：clean commit `8d8ac0916edc` 的 performance manifest `test-reports/g5-release/8d8ac0916edc-20260801T171746Z-performance/` 已验证并通过；10,010 个预热后 presented 帧主路径 p95 `1,864 µs`，inter-present p95 `16,680 µs`（门限 `16,700 µs`），Vulkan/NVIDIA 固定环境通过。clean commit `f5b02959cac6` 的 8 小时 soak 证据目录 `test-reports/g5-release/f5b02959cac6-20260801T173350Z-soak/` 已生成并验证绑定 commit/schema/哈希，但 `summary.json` 与 `report.md` 判定 `measurement_failed`，原因是 `successful presentation continued after idle settling`；完整 gate 不能标成 pass，window/modal/drawer/theme/DPI 1,000-loop post-present 资源/最终态矩阵仍未闭合 |
| 内部打包 | `powershell -ExecutionPolicy Bypass -File scripts\\build_internal_release.ps1`；仅验证脏树路径时才显式使用 `-AllowDirtyForVerification` | `target/internal-release/uix-0.0.1-internal-win-x64.zip`、暂存目录中的 `SHA256SUMS.txt` 和 ZIP SHA-256；脚本 payload 固定包含 `uix-demo.exe`、`uix-0.0.1.crate`、`LICENSE`、`THIRD_PARTY_NOTICES.md`、`CHANGELOG.md`、`README.md`、`assets/images/demo.png` | 包内容/签名决策 D0-02；S0-05 只读审计，S3-10 修复脚本，S6-01～S6-07 生成与复验 | **✅ clean 证据通过（2026-08-01）**：提交 `00441e9ad9fe5f80c86c5f498ba5ca6b060a8a73` 上 release build、私有 `uix-derive` 本地 patch cargo package 校验、7 项 payload、嵌套图片路径和 SHA-256 全部通过；ZIP SHA-256=`0244d8c0c3f2a98b746c7b0ece543bf83d7678fcf1adeb683c25644420e82fda`。代码签名与外部环境仍按 D0-02/D0-03 保持阻塞 |

S0-03 只建立上述证据路由，不提前运行 S0-04 文档链接门禁、S0-05 打包负载审计或其后继硬件/长稳/发布命令。详细任务记录见 [S0-02 工作项](<file:///C:/data/note/项目管理/工作项/UIX%20App-S0-02-建立证据分母映射.md>)、[S0-03 WorkItem](<file:///C:/data/note/项目管理/工作项/UIX%20App-S0-03-建立门禁与证据路径表.md>) 与 [S0-03 执行记录](<file:///C:/data/note/项目管理/执行记录/UIX%20App-S0-03-建立门禁与证据路径表-执行.md>)。

## 快速模型文档门禁审计（S0-04）

本任务只运行和分类门禁失败，不修正文档、链接、锚点、脚本或负载文件。命令为 `python scripts\\check_docs_links.py`，当前 clean commit `94ec6b917d5b14ed68cc3d87d41111d80c9dee55` 上结果为：扫描 16 个文件、退出码 `1`、`FAIL: 28 issue(s)`。

| 分类 | 数量 | 当前结论 | 后续归属 |
|---|---:|---|---|
| 旧仓库必需文件假设 | 4 | `AGENTS.md` 与 `docs/产品.md`、`docs/架构.md`、`docs/使用.md` 不在源码仓库；权威文件位于 `C:\data\note`，属于迁移后脚本边界 | S3-09 调整脚本与测试 |
| `file:///` 知识库外链解析边界 | 22 | 目标文件逐项存在；脚本未识别 `file:///`，把合法知识库外链按仓库相对路径处理；`renderer.md#模块不变量` 独立核验存在 | S3-09 调整脚本与测试 |
| 真实本地文件缺失 | 0（本批次修复） | `THIRD_PARTY_NOTICES.md`、`CHANGELOG.md` 已恢复；运行时图片由打包脚本显式纳入 `assets/images/demo.png` | S3-10 / S6-01 / S6-02 |
| 真实锚点缺失 | 1 | `架构/platform/diagnostics.md` 没有 `平台层全覆盖` 标题，但 `docs/进度/平台工程.md` 引用了该锚点 | 独立内容/架构引用修复任务 |

结论（历史基线）：本次 28 个失败中，26 个是迁移后门禁脚本边界问题，2 个是真实缺陷；**该基线上的文档门禁 FAIL**。详细原始输出和逐项依据见知识库 `C:\data\note\项目管理\执行记录\UIX App-S0-04-审计当前文档门禁-执行.md`。

## 快速模型 S3-09：迁移后文档链接边界收口

S3-09 已完成（2026-08-01）。`scripts/check_docs_links.py` 现在把源码仓库与知识库的责任边界显式分开：`README.md` 和 `docs/进度.md` 是仓库本地必需入口；产品、架构、使用和项目指令文档由知识库维护，不再因缺少源码副本而失败；`file:///` 只作为外部知识库文档链接跳过，仓库内相对链接、文件存在性和 Markdown/HTML 锚点仍继续校验。链接解析同时保留尖括号路径中的空格。

本批次还清理了已删除的项目内 SMC 正文副本链接，并修正 E-04、E-05、E-07 及 SMC 批次登记的本地锚点拼写。验证证据：

- `python tests\test_check_docs_links.py -q`：4 项通过；仓库测试入口统一位于 `tests/`
- `python scripts\check_docs_links.py`：扫描 17 个文件，跳过 46 个外部文档链接，`OK: repository-local tree + file links valid`
- `git diff --check`：通过

结论：**仓库本地文档门禁通过**；知识库正文及其外部锚点不由本仓库门禁冒充验证，历史 S0-04 中的外部内容缺口仍归知识库内容/架构引用任务处理。

## 快速模型打包门禁审计（S0-05）

本任务只读审计 `scripts\\build_internal_release.ps1` 的 payload 与当前仓库实际文件，不运行会构建、复制、压缩或删除输出的正式打包脚本，不修改打包脚本或负载源文件。基线为 clean commit `4d68748c6c24e8194e30255e74da51e3be7b460d`，本地 `main` 与 `origin/main` 一致。

| payload / 产物 | 脚本来源或生成位置 | 当前核验 | 结论 |
|---|---|---|---|
| `uix-demo.exe` | `target/release/uix-demo.exe` | 文件存在，但属于 Git 忽略的生成物；没有当前 clean commit 绑定证据 | 不能单独作为发布证据 |
| `uix-0.0.1.crate` | `target/package/uix-0.0.1.crate` | clean 生成物 `.cargo_vcs_info.json` 绑定提交 `00441e9ad9fe5f80c86c5f498ba5ca6b060a8a73`；cargo package 使用仓库内 `uix-derive` patch 完成校验 | 当前 clean 证据有效 |
| `LICENSE` | 仓库根 `LICENSE` | 存在且由 Git 跟踪 | 当前 payload 条目有效 |
| `THIRD_PARTY_NOTICES.md` | 仓库根 `THIRD_PARTY_NOTICES.md` | 已恢复 Lucide / Feather 许可文本；clean ZIP 已包含 | 当前 clean 证据有效 |
| `CHANGELOG.md` | 仓库根 `CHANGELOG.md` | 已恢复 `0.0.1` 未发布状态与首发能力记录；clean ZIP 已包含 | 当前 clean 证据有效 |
| `README.md` | 仓库根 `README.md` | 存在且由 Git 跟踪 | 当前 payload 条目有效 |
| `SHA256SUMS.txt` / ZIP | 脚本运行后在 `target/internal-release` 生成 | 只读审计不生成；现有 `-UNVERIFIED` ZIP 时间为 2026-07-22，来自旧 dirty 产物 | 不能作为当前 clean 证据 |

### 负载契约缺口

- 计划 D0-02 与产品交付约定要求内部包包含运行所需资产；仓库当前跟踪 `assets/fonts/lucide.ttf`、`assets/fonts/README.md`、`assets/images/demo.png`。
- `demo/src/demos/data.rs`、`demo/src/demos/component_qa/display_feedback.rs` 等路径运行时引用 `assets/images/demo.png`；脚本 payload 只有六个文件，没有 `assets/` 目录，因此生成 ZIP 会缺少 demo 的运行时图片。
- `assets/fonts/lucide.ttf` 在 `src/app/shell/application.rs` 通过 `include_bytes!` 嵌入二进制，属于编译期资源；这不能抵消 `assets/images/demo.png` 的运行时缺口。
- 现有 `target/package` 与 `target/internal-release` 中仍有已被当前仓库删除的 `CHANGELOG.md`、`THIRD_PARTY_NOTICES.md`，且其 crate provenance 为旧 dirty SHA；残留生成物不能回填当前源树，也不能被当作门禁通过。

### S0-05 结论

本批次完成 S3-10 / S6-01 / S6-02 的源文件修复：恢复第三方许可声明和变更记录，打包 payload 增加 `assets/images/demo.png`，为嵌套目标创建目录，并通过本地 Cargo patch 校验私有 `uix-derive` 依赖。提交 `00441e9ad9fe5f80c86c5f498ba5ca6b060a8a73` 的 clean 证据已核对 7 项 payload、嵌套路径和 SHA-256；D0-02 的代码签名与 D0-03 的多环境证据仍需单独处理。

## 阶段 0 外部条件（D0）

S0-01～S0-05 五个快速模型任务均已完成并有推送证据；阶段出口所需的外部条件已显式登记，但当前不能写成通过：

| 决策 / 外部条件 | 当前状态 | 证据与边界 |
|---|---|---|
| D0-01 Windows system notification 是否进入本版 | **blocked / 待主人决定** | 计划默认值为 `unsupported`，当前平台文档仍记录通知 API 未闭环；未把默认值当作主人已确认的产品决策 |
| D0-02 内部包内容与代码签名 | **blocked / 待主人决定** | 包内容基线已按计划列出 `.crate`、demo、运行资产、LICENSE、第三方声明、变更记录、SHA256；代码签名独立决定，当前缺少放行确认；S0-05 另发现源文件和 payload 缺口 |
| D0-03 AMD / Intel / 多 DPI / 最低 OS 真实环境 | **blocked / 缺少外部条件** | 当前只有旧 NVIDIA / 96 DPI / Windows build 26200 的 `UNVERIFIED` 证据，暂无 AMD、Intel、144/192 DPI 和最低 build 26100 的真实目标环境 |

因此阶段 0 的任务事实为“快速模型任务 5/5 完成、外部条件 blocked”，不是阶段出口整体 pass；阶段 5 和阶段 6 仍必须保留这些阻塞。

## 快速模型 S1-01：wgpu offscreen destroy 失败入队

S1-01 在 `S0-03` 依赖满足后完成，提交 `2295cd8e05deb60e9bab2dc7a3519557827a5bd2` 已推送到 `origin/main`。`src/native/graphics/wgpu_backend/mod.rs` 的 legacy `destroy_offscreen_target` 失败路径不再直接调用 `core::log::*`，而是把原始 typed `Error` 放入当前 `WgpuContext` 的 `PendingFailureSource`；checked `try_destroy` 的返回语义保持不变，未扩大公开 API。

验证证据：

- `cargo test --lib --features test-harness offscreen_destroy_failure -- --nocapture`：2 passed、0 failed；覆盖 owner 取回 typed cause 与 source 关闭后的晚到失败隔离。
- `cargo check --workspace --all-targets`：通过。
- `cargo test --tests`：公开/集成测试全部通过（25 个实际测试通过，22 项 GUI ignored 入口未执行）。
- `git diff --check`：通过；工作树 clean，HEAD 与 `origin/main` 一致。

下一直接任务为 S1-02：graphics teardown 与 source close / generation / replacement context；本次未提前处理该后继范围。

## 快速模型 S1-02：graphics teardown 与 source close

S1-02 已完成，代码提交 `5252abf388e35ed6f17f9e86b8eaf2c452aa1060` 已推送到 `origin/main`。`WgpuContext::try_shutdown` 不再直接清空 offscreen 槽位：它先切回 swapchain，逐项通过 checked `try_destroy_offscreen_target` 处理 bound/offscreen 资源；任一 typed failure 都会提前返回，保留 owner 状态与 source 以便重试。全部资源成功释放后才清理槽位、关闭旧 source 并 poll device，重复 shutdown 仍由既有 `shutdown` 门闩保持幂等。

验证证据：

- `cargo test --lib --features test-harness offscreen_destroy_failure`：2 passed、0 failed。
- `cargo test --lib --features test-harness teardown_source_close_is_idempotent_and_discards_late_failures`：1 passed、0 failed。
- `cargo check --workspace --all-targets`：通过。
- `cargo test --tests`：25 个实际集成测试通过，22 项 GUI ignored 入口未执行。
- `git diff --check`：通过。

生命周期边界：旧 source 在成功 teardown 后关闭，晚到 callback 返回 `Closed`；replacement source 使用新的 source id，只接收自己的 failure。定向测试证明了 replacement 隔离和重复 close 的安全性，但不把无真实 GPU 环境下的 queue/source 测试写成硬件 teardown 证据。

下一直接任务为 S1-03：Windows custom chrome callback 入队；本次未提前处理 S1-03 之后范围。

## 快速模型 S1-03：Windows custom chrome callback 入队

S1-03 已完成，代码提交 `64016c2db188eaab821229579a6ef683fc6b5728` 已推送到 `origin/main`。Windows custom chrome 的 Win32/DWM 失败不再直接写日志或静默丢弃：`GetWindowLongW`、`SetWindowPos`、`GetWindowRect`、`DwmExtendFrameIntoClientArea`、`DwmSetWindowAttribute` 均转为 typed `Error`。`WM_NCCALCSIZE`、`WM_NCHITTEST`、`WM_SIZE` 回调只做安全返回或入队；同一 `WM_SIZE` 中帧重算与 DWM 失败最多入队一个，owner-thread 再消费 pending failure。窗口 owner API 的 DWM 失败通过 `Result` 返回。

验证证据：

- `cargo check --target x86_64-pc-windows-msvc`：通过。
- `cargo check --target x86_64-pc-windows-gnu`：通过。
- `cargo test --lib`：7 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib custom_chrome_failure_is_deferred_to_owner_boundary -- --nocapture`：1 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib --no-run`：通过。
- `git diff --check`：通过。
- 静态审计确认 custom chrome / wnd_proc 不调用 `core::log`、tracing、subscriber、recovery 或 report；真实 DWM/硬件运行证据未冒充本阶段结果。

下一直接任务为 S1-04：Windows TSF callback 入队；本次未提前处理 ABI panic、队列压力或其他平台 callback 范围。

## 快速模型 S1-04：Windows TSF callback 入队

S1-04 已完成，代码提交 `a5771a0d37790d297070188e1a69f936ea310c53` 已推送到 `origin/main`。TSF 的 `TsfEventSink` 现在持有当前 `WindowsPlatform` 的 `PendingFailureSource`：composition 事件先写入共享事件队列，`PostMessageW(WM_NULL)` 唤醒失败转为 typed `PlatformError` 入 owner source。`TsfActivateParams`、`TsfSession` 和 `WindowsTextInput` 共用该 source；TSF 激活失败不再直接 `warn_fn`。`ITextStoreACPSink::OnLockGranted` 失败与 text-store 借用冲突保留 typed cause，owner 线程再消费；TSF callback 不执行 tracing、report、subscriber、recovery 或应用用户代码。

验证证据：

- `cargo check --target x86_64-pc-windows-msvc`：通过。
- `cargo check --target x86_64-pc-windows-gnu`：通过。
- `cargo test --lib`：9 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib --no-run`：通过。
- `cargo test --target x86_64-pc-windows-msvc --lib tsf_post_message_failure_is_deferred_to_owner_boundary -- --nocapture`：1 passed、0 failed；事件仍进入队列，invalid HWND 的 `PostMessageW` 失败由 owner source 取回一次。
- `git diff --check`：通过。
- 静态审计确认 TSF 文件不调用 `core::log`、tracing、subscriber、recovery 或 report；invalid HWND 只验证 callback-to-owner 边界，不冒充真实 TSF/TIP、输入法或硬件证据。

下一直接任务为 S1-05：Win32 ABI panic 边界；本次未提前处理 ABI unwind、队列压力、Wayland/macOS callback 或其他图形后端 teardown。

## 快速模型 S1-05：Win32 ABI panic 边界

S1-05 已完成，代码提交 `525e58aa` 已推送到 `origin/main`。`wnd_proc` 现在由 Rust panic boundary 包裹：窗口过程、custom chrome 和消息处理发生 panic 时不让 unwind 穿过 Win32 ABI，诊断投递失败也不会二次越界，最终返回 `DefWindowProcW` 的安全结果。`WM_NCCREATE` 尚未建立 owner binding 时只执行安全 fallback，不伪造 typed owner source。Windows-rs `#[implement]` 生成的 `ITextStoreACP` 与 `ITfContextOwnerCompositionSink` COM thunk 通过统一 guard 收口；panic 返回 `E_FAIL`，并把 `Errc::PlatformError` 投递到当前 TSF owner source。callback 不执行 tracing、report、subscriber、recovery 或应用用户代码。

验证证据：

- `cargo check --target x86_64-pc-windows-msvc`：通过。
- `cargo check --target x86_64-pc-windows-gnu`：通过。
- `cargo test --target x86_64-pc-windows-msvc --lib`：11 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib --no-run`：通过。
- `cargo test --target x86_64-pc-windows-msvc --lib wnd_proc_boundary_returns_safe_fallback_after_panic -- --nocapture`：1 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib generated_tsf_thunk_converts_panic_to_hresult_and_owner_failure -- --nocapture`：1 passed、0 failed；生成 COM thunk 的 panic 转为 `E_FAIL`，typed failure 由 owner source 取回一次。
- `git diff --check`：通过。
- 静态审计确认 Windows 生产 callback 只包含显式 `wnd_proc` 和 windows-rs `#[implement]` COM thunk 两类入口；custom chrome 由 `wnd_proc` 外层边界覆盖。未将真实窗口、TSF/TIP、硬件或长稳运行证据写成已验证结果。

下一直接任务为 S1-06：pending queue 容量与 source 压力测试；本次未提前处理 Wayland/macOS callback、其他图形后端 teardown 或跨平台 ABI。

## 快速模型 S1-06：pending queue 容量与 source 压力测试

S1-06 已完成。`src/diagnostics/pending.rs` 保持生产实现不变，补齐并发 producer、固定容量、单次 overflow、generation/source 隔离和关闭后 replacement 压力证据；SMC 正文按知识库迁移，源码仓库不再保留副本，权威源仍为程序开发知识库文件。

验证证据：

- `cargo test --lib pending -- --nocapture`：4 passed、0 failed；覆盖 FIFO、source 隔离/关闭、并发 producer 争用和关闭 generation 后 replacement 复用容量。
- `cargo test --target x86_64-pc-windows-msvc --lib pending -- --nocapture`：4 passed、0 failed。
- `cargo check --target x86_64-pc-windows-msvc`、`cargo check --target x86_64-pc-windows-gnu`：通过。
- `git diff --check`：通过。
- 项目内设计模式文件与知识库源文件按规范化换行比较一致；未恢复已删除的 SMC 1.0 文档。

下一直接任务为 S2-01：清理 native bool / 空值失败语义；Wayland/macOS callback、其他图形后端 teardown、旧日志迁移和跨平台矩阵仍未闭合。

## 快速模型 S2-01 批次 1：native bool / 空值失败语义清理（六个 trait）

已完成。`IClipboard`、`ICursor`、`ITimer`、`IFileDialog`、`IFileSystem`、`IDisplay` 及 `SpecialDirProvider` 的全部 bool / 空字符串 / 0 失败信号改为 typed `Result`；文件对话框 `Ok(None)` = 取消、`Err` = 对话框失败；Wayland/macOS 未实现能力返回 `Err(NotImplemented)`，不再用 `(0,0)` / 空串 / 0 冒充结果。调用方（`ui/foundation/clipboard.rs`、`application.rs`、`event_loop.rs`、`window_driver.rs`、`platform/imp/*`）已适配。

验证证据：

- `cargo check --workspace --all-targets`：通过（0 error，警告数与基线一致）。
- `cargo test --quiet`：25 passed、0 failed（22 项 GUI ignored 未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过。

下一直接任务为 S2-01 批次 2：`ISystemInfo` 七个查询改 `Result` 并迁移 font_service 调用方；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 S2-01 批次 2：ISystemInfo 查询改 typed Result

已完成。`ISystemInfo` 的 `os_info` / `cpu_count` / `memory_info` / `hostname` / `username` / `up_time` / `process_memory` 与 `default_font_paths` 全部改为 typed `Result`；字体探测类保留 `Option`（找不到是合法语义）。Windows 失败转 `PlatformError`（不再返回 "Windows (Unknown)" / 全 0 / 空串）、Linux /proc 读取失败转 `IoError`、macOS `up_time` 返回 `NotImplemented`；`font_service` 与 `native/factory::available_memory_bytes` 调用方已适配。

验证证据：

- `cargo check --workspace --all-targets`：通过（0 error，警告数与基线一致）。
- `cargo test --quiet`：25 passed、0 failed（22 项 GUI ignored 未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过。

下一直接任务：清理 `INotification` / `IConsole`；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 S2-01 批次 3：INotification / IConsole 失败语义改为 typed Result

已完成。`INotification::show` 返回 `Result<()>`：Windows 的 `Shell_NotifyIconW` 失败转 `PlatformError`、Linux 的 `notify-send` spawn/exit 失败转 `PlatformError`、macOS 返回 `NotImplemented`。`IConsole` 六个写方法（`write` / `write_line` / `set_color` / `reset_color` / `show_terminal_cursor` / `set_terminal_title`）全部返回 `Result<()>`；Windows Win32 控制台 API 返回值不再静默忽略、Linux stdout 写入失败转 `PlatformError`、macOS 移除 `print!` panic 隐患改为 `stdout().write_all()` 并返回 Result；`capabilities()` 签名不变。唯一调用方 `NotificationService::notify` 在最终责任边界通过 `Diagnostics::report` 提交观察，满足 `core/error` 模块「最终责任边界交给平台 Diagnostics 观察」的不变量。

验证证据：

- `cargo check --workspace --all-targets`：通过（0 error，警告数与基线一致）。
- `cargo test --quiet`：25 passed、0 failed（22 项 GUI ignored 未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过。
- 静态审计确认改动文件无 `core::log` / `_fn` 旧日志引用。

同时完成 `NotificationService` 接入 Diagnostics：通知失败不再仅 `tracing::warn!`，改为在最终责任边界通过 `Diagnostics::report` 提交观察，满足 `core/error` 模块「最终责任边界交给 platform Diagnostics 观察」的不变量。新增 `with_diagnostics()` 构建器，默认创建最小 Diagnostics 实例以保证 headless/测试场景的工作能力。

下一直接任务：代码库中剩余的旧失败语义审计；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 S2-旧日志迁移：core::log::* 全部迁往 tracing 并删除旧目录

已完成。`core::log::*`（`info_fn` / `warn_fn` / `error_fn` / `debug_fn`）在 app、draw、native、ui 的全部调用迁移为 `tracing::info!` / `warn!` / `error!` / `debug!`；唯一日期工具 `days_to_date` 搬入 `ui/widgets/input/date_picker.rs`；旧 `src/core/log` 与 `src/core/diagnostic` 全目录删除，不建立桥接双写。`src/core/mod.rs` 不再声明 `log` / `diagnostic` 模块。

验证证据：

- `cargo check --workspace --all-targets`：通过。
- `cargo test --quiet`：全部通过（22 项 GUI ignored 入口未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过。
- 静态审计：src/demo/tests 无 `core::log` / `core::diagnostic` / 旧 `_fn` 日志函数引用，`Cargo.toml` 无 `log` crate 依赖，`src/diagnostics/` 内部已全部使用 tracing。

下一直接任务为代码库中剩余的旧失败语义审计；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 SMC 批次：System / Module / Component 改造（最高优先级）

主人决策（2026-07-31）：SMC 模式改造为**当前最高优先级**，排在理想使用演进 E 批次之前。目标边界以[系统列表](<file:///C:/data/note/我的项目/软件/UIX App/架构/系统列表.md>)为准，通用定义以[程序开发知识库](<file:///C:/data/note/程序开发/System%20Module%20Component%20设计模式.md>)为准；每个任务独立提交并登记验证证据。

| 任务 | 范围 | 状态 |
|---|---|---|
| SMC-01 | core 定级审计：确认 core 为模式外基础依赖或提升为 System | ✅ 已完成 |
| SMC-02 | platform 系统迁移：capabilities / windowing / presentation / diagnostics / agent-transport 五个 Module 边界与兄弟隔离 | ✅ 已完成 |
| SMC-03 | graphics 系统迁移：geometry / painting / scene / renderer / backend / resources 六个 Module 边界与兄弟隔离 | ✅ 已完成 |
| SMC-04 | ui 系统迁移：component / view / reactive / layout / event / animation / accessibility / theme / overlay / form / virtualization / widgets 十二个 Module 边界与兄弟隔离 | ✅ 已完成 |
| SMC-05 | data 系统迁移：settings Module 边界 | ✅ 已完成 |
| SMC-06 | app 系统迁移：application / event-loop / window / agent 四个 Module 边界与组合根 | ✅ 已完成 |
| SMC-07 | 跨层强制规则：无环依赖、兄弟隔离与 CompileFail/deny 阻断证据全量闭合 | ✅ 已完成 |

既有基础：Diagnostics 首个 SMC 纵切已完成（公开 System + 私有 reporting/recovery Module，见[运行保障](进度/运行保障.md)）；主题令牌分层已消除 graphics→ui 反向依赖；CompileFail deny 证据已有 15 个契约锁定 Diagnostics / native 边界。

每个任务完成条件：目标 Module 边界落地（私有实现不外泄）、兄弟 Module 零依赖、跨 System 依赖与系统列表一致、定向测试与 CompileFail/deny 证据通过。

## 快速模型 SMC-01：core 定级审计

SMC-01 已完成，提交 `4fc96525` 已完成。审计结论：**core 为模式外基础依赖，不提升为 System**——它只提供跨系统稳定、无运行时策略的值类型与失败模型，没有独立领域状态、策略和生命周期，也不拥有资源；`perf_probe`（present/paint 探针）与 `glyph_outline`（MSDF 纯算法）是跨 System 共享的模式外基础工具，暂留 core 根登记，不作为角色。

代码落地：

- 目标四模块边界：`src/core/identity/`（`ComponentId` / `WindowId`，原 `component_id.rs` / `window_id.rs`）、`src/core/geometry/`、`src/core/damage/`、`src/core/error/`；`damage → geometry` 为系统内唯一窄契约依赖，identity / geometry / error 彼此独立。
- 公开面与私有边界：core 根只 glob 重导出四模块值；`glyph_outline` 保持 `pub(crate)` 私有，`perf_probe` 按跨 System 共享保留公开。

验证证据：

- `cargo check --workspace --all-targets`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过，lib 警告数 28 与基线一致。
- `cargo test --quiet`：全部通过（22 项 GUI ignored 入口未执行，与基线一致）。
- `cargo test --doc`：23 passed（基线 20 + 新增 3 个 compile-fail deny 契约锁定旧平铺路径 `core::component_id` / `core::window_id` 与私有 `core::glyph_outline`）。
- 改动文件 `git diff --check`：通过。

下一直接任务为 SMC-02：platform 系统迁移（capabilities / windowing / presentation / diagnostics / agent-transport 五 Module 边界与兄弟隔离）。

## 快速模型 SMC-02：platform 系统迁移

SMC-02 已完成，提交 `bb99c5da`。`src/native` 按目标边界重组为四个私有 Module（diagnostics 已由首个 SMC 纵切落地）：

- `capabilities/`：`system` / `display` 契约 + `services`（通知、文件服务、FileSystemCore/SpecialDirProvider 从 windowing/shared 归位）
- `windowing/`：`window` / `event` / `input` 契约 + `shared`（事件源、窗口状态、IME 共享）
- `presentation/`：`graphics`（wgpu/vulkan/d3d/gl/metal 后端）+ `presenter`；`factory`（组合根）与 `present`（presentation 契约集）因与 `PlatformWindow` 互引用，按无环规则归 System 私有边界
- `agent_transport/`：既有（feature `agent-control`），零 crate 依赖
- System 私有边界：`native/mod.rs`（声明+编排规则）、`platform`（Platform trait 共享契约）、`factory`（组合根）、`backends`（唯一 cfg 收敛的 OS 适配层）、`test_harness`
- `traits/` 目录已删除（系统列表要求：分派用 trait 不单独形成模块）；全部 264 处 `crate::native::traits::*` 引用迁移到归属 Module

兄弟隔离审计（rg 证据）：capabilities / windowing / presentation / agent_transport 对兄弟 Module 引用均为 0；`presentation → windowing::window` 为唯一窄契约依赖（取 surface），`windowing → present`（2 处）为 System 共享契约白名单。

验证证据：

- `cargo check --workspace --all-targets`：通过，lib 警告 27（基线 28，净 −1）；`cargo check --target x86_64-pc-windows-gnu --lib`、`--features agent-control` / `test-harness` / 二者组合：通过
- `cargo test --quiet`：全部通过；`cargo test --doc`：26 passed（新增 3 个 compile-fail deny 契约锁定 capabilities / presentation / agent-transport 边界，lib.rs deny 示例更新为新路径）
- 改动文件 `git diff --check`：通过；`no-default-features` 的 `drawable_size` 为已登记既有缺陷，仅路径随迁移变化
- 顺手收口：删除 native 根对 `core::damage` 的未使用重导出（SMC-01 定级后 core 类型不再经 platform 面泄漏）

下一直接任务为 SMC-03：graphics 系统迁移（geometry / painting / scene / renderer / backend / resources 六 Module 边界与兄弟隔离）。

## 快速模型 SMC-03：graphics 系统迁移

SMC-03 已完成，提交 `1bcf0481`。判断基准为通用 SMC 定义（窄契约单向无环、能力端口由 System 组装期注入、基础算法模式外），模块文档只作目标线索：

- 六 Module 物理边界：`painting/`（原 `api/` + `command/` 合并：PaintContext / DisplayList / FrameEncoder 单一录制管线）、`geometry/`、`scene/`、`renderer/`、`backend/`、`resources/`
- 拆环（按 SMC「依赖图显式、单向且无环」）：
  - CPU 光栅化器（`rasterizer/`、`pixel_surface`、`shared_rasterizer`、`software_rasterizer*`）上移 `draw/raster/`——模式外基础算法归 System 私有边界，同时消除 painting→backend 与 resources→backend 两处环
  - `NodeId` 归 `scene/`（场景图节点身份；renderer 经 scene 消费，renderer::NodeId 路径删除）
  - `RenderOutcome`/`GraphicsFailure` 上移 `draw/outcome/`（painting 产出、renderer 消费的跨 Module 契约）
  - `RenderTarget`/`UpdateStrategy`/`GraphicsCapabilities` 上移 `draw/target/`（scene 栅格化目标协议，消除 scene⇄renderer 双向）
  - `debug/` 声明为 System 私有边界（painting/renderer 共用调试服务）
- 能力端口：painting/scene 经构造注入使用 resources 的字体/图片服务（只持有不管理生命周期），符合「需求方 Module 声明能力端口 + System 组装期注入」

兄弟隔离审计（rg 证据，全部与 SMC 定义一致）：painting→{geometry, resources(端口), raster}；scene→{painting, resources(端口), raster}；backend→{painting, geometry, resources, raster}；renderer→{scene, backend, resources, painting}；resources→{geometry, raster}；geometry→无；painting→backend/renderer、scene→renderer/backend、resources→backend 全部为 0。

验证证据：

- `cargo check --workspace --all-targets`：通过，lib 警告 27 与 SMC-02 后基线一致；`cargo check --target x86_64-pc-windows-gnu --lib`、`--features test-harness` / `agent-control`：通过
- `cargo test --quiet`：全部通过；`cargo test --doc`：30 passed（新增 4 个 compile-fail deny 契约锁定 `draw::api` / `draw::command` / `backend::cpu::rasterizer` / `renderer::NodeId` 旧路径）
- 改动文件 `git diff --check`：通过；`no-default-features` 的 `drawable_size` 为已登记既有缺陷（路径随迁移更新）

下一直接任务为 SMC-04：ui 系统迁移（component / view / reactive / layout / event / animation / accessibility / theme / overlay / form / virtualization / widgets 十二个 Module 边界与兄弟隔离）。

## 快速模型 SMC-04：ui 系统迁移

SMC-04 已完成，提交 `b962c7d1`。判断基准为通用 SMC 定义（窄契约单向无环、能力端口由 System 组装期注入、跨 Module 契约归 System 私有边界），模块文档只作目标线索：

- 十二 Module 物理边界：`component/`（原 `core/` + `component_handle/patch/snapshot` + `managers` + `focus_handle` + `app_state` + `traits/widget` + `foundation/{config,locale,clipboard,focus_trap}` + `dynamic_label` + `view_transform` + `tree_measure`）、`view/`（View/ViewNode/扩展 trait + `providers/`）、`reactive/`（原 `foundation/state` + `provider_context`）、`layout/`、`event/`（原 `event.rs` + `system_event_handler` + `EventResult`）、`animation/`、`accessibility/`（`semantic_snapshot` + `accessibility_override`）、`theme/`（+ `style/` + `traits/`）、`overlay/`（+ `placement`）、`form/`（原 `widgets/input/form*`）、`virtualization/`（原 `foundation/virtual_scroll`）、`widgets/`（+ `combinators` + `window_chrome`）
- 拆环（按 SMC「依赖图显式、单向且无环」）：
  - `component ⇄ view`：`build_view_children` 从 `WidgetComponent` 移出为 System 私有边界能力端口（`ViewChildrenProvider` + `as_view_children` 上转型，`component!` 宏自动生成）；`ConfigProvider`/`LocaleProvider` 归 view 的 `providers/`（DSL 注入组件），组件侧只留数据与访问器
  - `component → widgets`：Modal 生命周期 / viewport 滚动轴 / 显式尺寸锁 / 导航兄弟联动 / 拖拽区域 / 输入当前值 全部集中到 System 私有边界 `tree_widget_hooks/`；动态内容刷新（表格/日历/折叠/Select/虚拟列表）随 `tree_dynamic`、跨节点文字拖选随 `text_selection` 归边界
  - `layout ⇄ component`：`child_from_tree*` 收编 `component/tree_measure`（layout 对 component 零依赖）；`LayoutEngine` trait 收归 `layout/engine.rs`
  - `reactive → component`：`provider_context` 随其持有的 `ComponentConfig`/`Locale` 归 component（reactive 纯基座）
  - `theme ⇄ component`：Style 绘制辅助（`apply_style`）上移 System 私有边界 `style_paint`（theme::style 再导出）
  - `event → component`：`EventResult` 归 event Module（事件处理结果契约），树内经重导出保持路径不变
  - `accessibility → widgets`：Input 当前值经 `tree_widget_hooks::component_input_value` 消费
  - `DynamicLabel`（原 `view::combinators`）归 component（框架级动态文本基础设施，树 build/无障碍需识别）
- 能力端口与边界契约：`WidgetComponent` 保留默认实现方法形态的窄端口（`as_view_children`）；组件补丁表（`component_patch`）、渲染注册表（`render_handler`，含空态渲染 `EmptyRenderer`）、ViewAdapter 归 System 私有边界

兄弟隔离审计（rg 证据，全部与 SMC 定义一致）：reactive / layout / overlay 对任何兄弟 Module 引用为 0；event→reactive、animation→reactive、theme→layout、accessibility→component 为唯一基座窄契约；component→{reactive, event, layout, animation, theme, overlay, accessibility} 全部单向；view→{component, reactive, event, animation, theme, accessibility}；form→{widgets, component, view, layout}；virtualization→{component, view}；widgets 为消费汇聚点（82 文件引用 component、21 animation、20 reactive、18 view），无任何被引用反向边。`component → view/widgets/form/virtualization`、`view → widgets`、`widgets → accessibility` 全部为 0。

验证证据：

- `cargo check --workspace --all-targets`：通过，lib 警告 27 与 SMC-03 后基线一致；`cargo check --target x86_64-pc-windows-gnu --lib`、`--features agent-control` / `test-harness` / 二者组合：通过
- `cargo test --quiet`：全部通过（22 项 GUI ignored 入口未执行，与基线一致）；`cargo test --doc`：39 passed（基线 30 + 新增 9 个 compile-fail deny 契约锁定 `ui::core` / `ui::foundation` / `ui::traits` / `ui::widgets::input::form` / `ui::view::combinators` / `ui::semantic_snapshot` / `ui::adapter` / `ui::tree_widget_hooks` / `ui::window_chrome` 旧路径）
- 改动文件 `git diff --check`：通过

下一直接任务为 SMC-05：data 系统迁移（settings Module 边界）。

## 快速模型 SMC-05：data 系统迁移

SMC-05 已完成，提交 `1d9dae46`。`src/data` 按目标边界收口：

- settings Module 边界：`settings/` 保持私有 Module（`pub(crate) mod settings`），公开面由 data 根收口（`SettingsService`）；settings 内部只依赖 `crate::core` 失败模型（`settings-serde` feature 下额外依赖 serde），跨 System 依赖为零
- 兄弟隔离审计（rg 证据）：data 域内无兄弟 Module；`settings → core` 为唯一域外依赖，与系统列表一致

验证证据：

- `cargo check --workspace --all-targets`：通过；`cargo check --target x86_64-pc-windows-gnu --lib`：通过
- `cargo test --quiet`：全部通过；`cargo test --doc`：40 passed（新增 1 个 compile-fail deny 契约锁定 `data::settings` 私有边界）
- 改动文件 `git diff --check`：通过

下一直接任务为 SMC-06：app 系统迁移（application / event-loop / window / agent 四个 Module 边界与组合根）。

## 快速模型 SMC-06：app 系统迁移

SMC-06 已完成，提交 `165a39dc`（目录分层收口 `ba1c28c2` + 本次拆环收尾）。判断基准为通用 SMC 定义（窄契约单向无环、跨 Module 契约归 System 私有边界、组合根负责组装注入），模块文档只作目标线索：

- 四 Module 物理边界：`application/`（App / AppMode / CLI / DI / AppHandle）、`event_loop/`（主循环 event loop）、`window/`（Window / window_driver / window_session / window_actions / window_config / text_input / bridge / frame_scheduler）、`agent/`（agent_bridge / agent_control / agent_protocol / agent_transport）
- 拆环（按 SMC「依赖图显式、单向且无环」）：
  - `window → event_loop` 反向依赖（8 处）清零：window 驱动循环使用的 `active_work_registry` / `app_timer` / `main_thread_queue` / `clock` 上移 System 私有边界 `queues/`（event-loop 与 window 共用，任何 Module 不拥有）；`frame_scheduler` 归 window Module（唯一消费者 window_driver，调度窗口表面帧机会）
  - `queues` 边界只依赖 `crate::core` / `draw` / `ui` 域外类型，对 app 内任何 Module 零依赖
- 组合根：`session_runtime`（AppRuntime / SessionRuntime）创建并注入各 Module 组件实例（agent bridge / transport / command queue、window session、event-loop waker）；`window_semantics`（每窗口语义修订状态）为 System 私有边界

兄弟隔离审计（rg 证据，全部与 SMC 定义一致）：agent→任何兄弟为 0；window→{agent(3), queues(7)} 全部下行；event_loop→{window(5), agent(1), queues(6)} 全部下行、event_loop→application 为 0；application→{event_loop, window, agent, queues}(11) 顶层下行；queues→app 内部为 0。`window → event_loop/application`、`event_loop → application` 全部为 0。

验证证据：

- `cargo check --workspace --all-targets`：通过，lib 警告 34 均为既有 unused 类警告，本次变更未新增；`cargo check --target x86_64-pc-windows-gnu --lib`、`--features agent-control` / `test-harness` / 二者组合：通过；MSVC `--lib`：通过
- `cargo test --quiet`：全部通过（22 项 GUI ignored 入口未执行，与基线一致）；`cargo test --doc`：47 passed（基线 40 + 新增 7 个 compile-fail deny 契约锁定 `app::shell` / `session_runtime` / `queues::app_timer` / `window_semantics` / `agent::agent_transport` / 旧 `event_loop::frame_scheduler` 路径 / `window::window_driver` 边界）
- 改动文件 `git diff --check`：通过（lib.rs 文档注释不改写）

下一直接任务为 SMC-07：跨层强制规则（无环依赖、兄弟隔离与 CompileFail/deny 阻断证据全量闭合）。

## 快速模型 SMC-07：跨层强制规则全量闭合

SMC-07 已完成，提交 `003b92a6`。以[系统列表](<file:///C:/data/note/我的项目/软件/UIX App/架构/系统列表.md>)的「允许依赖」列与跨层强制规则为基准，全仓 rg 审计闭合：

- 跨 System 依赖与系统列表一致：core→无；platform（native+platform+diagnostics）→core 唯一；graphics（draw）→{core, platform}；ui→{core, platform, graphics}；data→core；app→全部。唯一发现 `native → draw` 2 处均为 rustdoc 链接（present.rs 的 `BackendKind`、blur.rs 的 `rasterizer::blur`），改写为纯文本后 platform 对 graphics 零代码/文档依赖
- ui→native 43 处全部为输入契约类型（`KeyCode` / `KeyMod` / `MouseButton` / `ControlSize` / `ScrollDirection` / `EventLoopWaker` / `IClipboard`），属系统列表「ui 允许依赖 platform」的契约消费；ui 对 `native::presentation` / `backends` / wgpu 执行面零引用（绘制入口规则遵守）
- 兄弟隔离全量重扫：core 四模块（`damage→geometry` 唯一窄契约）、native 五模块（`presentation→windowing::window` 白名单窄契约）、draw 六模块无环抽查（painting/scene/resources→renderer/backend 反向边全 0）、ui 十二模块反向抽查（view→widgets、widgets→accessibility、component→view、reactive→component、layout→component 全 0）、data 单模块、app 四模块（SMC-06 已闭合）
- deny 契约覆盖：lib.rs 47 个 compile-fail 契约覆盖 core / native / draw / ui / data / app 六 System 公开面与旧路径；diagnostics/mod.rs 5 个契约锁定私有 reporting / recovery Module；draw / core / native 批内契约齐备。全量闭合

验证证据：

- `cargo check --lib`：通过（0 error）；`cargo test --doc`：47 passed 与 SMC-06 后基线一致（本次仅文档注释改动，无契约变更）
- `git diff --check`：通过

下一直接任务为 S4 真窗视觉验收（92 项组件视觉矩阵）与内部发布准备（SMC 模式改造与理想使用演进 E 批次均已完成，见[理想使用演进](进度/理想使用演进.md#进度总览)）。

## 快速模型 E 批次：理想使用演进（交付前插入）

主人决策（2026-07-31）：使用文档「目标写法」中的理想 API（E-01～E-09）在 0.0.1 内部交付前实现，排在 **SMC 模式改造（SMC-01～SMC-07，最高优先级）之后**、S4（真窗视觉验收）之前。每个 E 任务独立提交并登记验证证据；完成条件见[理想使用演进](进度/理想使用演进.md#演进清单)。

| 任务 | 能力 | 状态 |
|---|---|---|
| ✅ E-01 | 类型化表单模型 `Form::model`（提交 `6db47be`，详见[理想使用演进](进度/理想使用演进.md#e-01-类型化表单模型-formmodel)） |
| ✅ E-02 | 类型化表格列渲染与声明式虚拟化（提交 `9324633`，详见[理想使用演进](进度/理想使用演进.md#e-02-类型化表格列渲染与声明式虚拟化)） |
| ✅ E-03 | 受控浮层 Builder（Modal/Drawer）（提交 `3da37a3`，详见[理想使用演进](进度/理想使用演进.md#e-03-受控浮层-buildermodaldrawer)） |
| ✅ E-04 | 全局反馈门面 `ui.message()` / `ui.notify()`（提交 `98a992c`，详见[理想使用演进](进度/理想使用演进.md#e-04-全局反馈门面-uimessage-uinotify)） |
| ✅ E-05 | 组件声明式无障碍动作 `semantic_actions`（提交 `e29b7ed`，详见[理想使用演进](进度/理想使用演进.md#e-05-组件声明式无障碍动作-semanticactions)） |
| ✅ E-06 | 路由 key 派生宏 `#[derive(Display)]`（提交 `8c18ecc`，详见[理想使用演进](进度/理想使用演进.md#e-06-路由-key-派生宏-derivedisplay)） |
| ✅ E-07 | 富文本链接回调 `RichText::on_link`（提交 `1c9c65b`，详见[理想使用演进](进度/理想使用演进.md#e-07-富文本链接回调-richtextonlink)） |
| ✅ E-08 | 全局服务注册 `App::register` 与 i18n key 宏（提交 `6720c25`，详见[理想使用演进](进度/理想使用演进.md#e-08-全局服务注册-appregister-与-i18n-key-宏)） |
| ✅ E-09 | 主题品牌种子 `Theme::custom`（提交 `1e1f511`，详见[理想使用演进](进度/理想使用演进.md#e-09-主题品牌种子与-themecustom)） |

## 快速模型 S4-01：真窗组件视觉验收（clean 证据）

S4-01 已完成（2026-08-01，零代码改动，仅生成证据与登记）。在 clean commit `276657343c14fa6ccc6cc21e097702ee7d970b6c`（工作树 clean，与 origin/main 一致）上执行 S0-03「真窗与组件视觉」门禁，产出替代历史 UNVERIFIED 的 clean 证据：

- 环境：Windows 11 IoT Enterprise LTSC build 26100 / 96 DPI / 1920×1080，AMD Radeon RX 5700（DiscreteGpu，AMD proprietary driver），wgpu backend=vulkan（auto-vulkan recipe: raster=gpu_native; present=swapchain）。
- 命令：设置 `UIX_EXPECT_DPI=96`、`UIX_GUI_EVIDENCE_DIR`、`UIX_COMPONENT_QA_EVIDENCE_DIR` 后运行 `cargo test --release --features 'agent-control,test-harness' --test agent_gui_windows component_visual::real_demo_captures_every_component_and_applicable_visual_state -- --exact --ignored --nocapture --test-threads=1`：1 passed / 0 failed（40.94s；首次 18.55s 失败为启动竞态，重跑即过）。联系表测试 `component_visual::build_component_visual_contact_sheets`：1 passed / 0 failed（2.89s）。
- 证据（`test-reports/0.0.1-276657343c14-20260801/component-visual/`，目录不入库）：`report.md`、`component-visual-matrix.csv` **847 / 847 行 PASS**（92 唯一 case、663 声明状态）、逐项 PNG 329 张、`contact-sheets/` 22 张、`graphics-auto-vulkan.log`。
- 边界：本报告不冒充无障碍逐项 manifest（S4-03，仍 blocked 0/110）、GFX-R5（S5）或 G5 性能/soak 证据；AMD 环境为 D0-03 提供部分环境证据。

下一直接任务为 S4-02 联系表人工评审 / 视觉核对（代理预审已完成，待主人最终确认）；S5-01 的 NVIDIA preflight 已执行，但被当前 Windows Vulkan 测试清单缺失阻断。

## 快速模型 S4-03：无障碍证据 schema 与收集器

S4-03 已完成（2026-08-01）。按 S0-03 无障碍门禁的输出契约（`test-reports/<clean-candidate>/accessibility/manifest.json` 及每项 JSON：role/name/state/value/actions/focus/keyboard）固化 schema 并落地第一批 clean 证据：

- **schema**：[无障碍证据](进度/无障碍证据.md)——`uix.accessibility.manifest.v1`（manifest）+ `uix.accessibility.item.v1`（逐项）+ `uix.accessibility.component.v1`（组件捕获）；三态 status（`captured` / `pending-scene` / `ledger-gap`），不冒充完整证据。
- **收集器**：新增 `tests/support/agent_gui_windows/accessibility.rs`（ignored 真窗测试 `real_demo_captures_component_semantic_evidence`）——遍历 92 组件 QA case，提取 `component-qa-target` 语义投影；声明 focus 的 case 执行 focus 动作并捕获聚焦后节点。
- **组装器**：新增 `scripts/build_accessibility_manifest.py`——解析功能账本 4.4～8.6（104 行）按组件名映射 case id，生成 110 行 manifest 与 89 份逐项 JSON；8.7～8.12 登记 `ledger-gap`；10 个无 case 组件名（advanced charts + Chart 通用，15 行）登记 `pending-scene`。
- **验证**：clean commit `276657343c14` 上真窗收集 2/2 passed / 0 failed（组件 QA 92 case + charts 场景 11 节点）；`manifest.json` = 110 行（104 captured + 6 ledger-gap），104 份 item JSON 字段齐全、manifest ↔ items 引用一致；`git diff --check` 通过。

边界：104/110 captured 不写 pass；完整 110 captured 仅剩 8.7～8.12 账本补全（待主人确认定义来源）。S4-02 已完成 clean 联系表重新生成和代理视觉预审，但主人最终人工评审仍未完成。

## 快速模型 S4-02：联系表人工评审 / 视觉核对

S4-02 当前完成了自动化重新生成与 Codex 代理视觉预审，但不把代理预审冒充主人确认或阶段出口通过。候选证据目录为 `test-reports/0.0.1-13cb6bea-20260801/component-visual/`（目录不入库），候选绑定 clean commit `13cb6bea`。

- 自动化：真窗测试 `1 passed / 0 failed`；92 个组件、663 个声明状态、329 张 PNG；矩阵 847/847 行 `PASS`；联系表生成测试 `1 passed / 0 failed`，输出 22 张联系表。
- 环境：Windows / 96 DPI / Vulkan / `gpu_native` / swapchain；adapter 为 NVIDIA GeForce RTX 4070 Ti SUPER（DiscreteGpu）。
- 代理预审：已打开全部 6 张 light-desktop、6 张 dark-compact、10 张 interactive 联系表；未发现明确的整块丢失、异常裁剪、主题崩坏或状态截图崩坏。详细边界记录见候选目录 `component-visual/review.md`。
- 当前状态：`agent-precheck-pass / owner-review-pending`。主人需确认产品审美、间距、文案与视觉一致性后，才能把 S4-02 写成最终通过。

## 快速模型 S5-01：GFX-R5 NVIDIA preflight

S5-01 已完成测试入口迁移并补齐 runner 计划中的 8 条 exact；vendor、device-lost 与两条 900 秒 soak 已形成通过证据，但 mixed-DPI 受真实显示器拓扑阻断，阶段证据仍未闭合。GFX-R5 代码与 soak 证据基线为 clean commit `eda9ff0ef324dee57e7cd6af5e95e4fb7dfb9b2a`，后续文档-only 提交仅回写进度；各硬件证据 manifest 分别绑定其执行时的 clean source head，工作树无未提交修改。

- **源码切片**：GFX-R5 exact 入口全部位于 `tests/tests.rs`；库侧 `src/gfx_r5_support.rs` 只提供真实窗口、Vulkan context、DPI/soak/device-loss 支撑 API，不含 `#[test]`；runner 统一使用 `cargo test --test tests`。新增入口为真实 mixed-DPI、single-window soak、shared-device soak、external device-loss recovery，原四条 vendor/native/engine recovery 也保持在同一 integration test 文件。
- **inventory / 编译**：`cargo test --features vulkan --test tests --no-run` 通过；`cargo test --features vulkan --test tests -- --list` 列出 8 条 exact；所有 GFX-R5 测试断言与 `#[test]` 均在 `tests/tests.rs`，库侧 support 无测试入口。
- **NVIDIA vendor profile**：`python scripts/run_windows_gfx_r5.py --profile vendor --vendor nvidia` 通过，证据目录为 `test-reports/20260801-160852-gfx-r5-nvidia-vendor/`，manifest clean source head 为 `a1f050d0`；四条既有 exact 继续通过：vendor resize/readback、native out-of-date、destroyed HWND surface-lost、engine `RebuildSurface` recovery。
- **NVIDIA device-lost profile**：`python scripts/run_windows_gfx_r5.py --profile device-lost --vendor nvidia --device-fault true` 通过，证据目录为 `test-reports/20260801-160720-gfx-r5-nvidia-device-lost/`，manifest clean source head 为 `326867ba`；NVIDIA RTX 4070 Ti SUPER，`vendor=0x10DE`，shared logical device peer 观察到 typed `graphics_device_lost`，fault/peer 均含 `ERROR_DEVICE_LOST`，当前设备能力为 `VK_EXT_device_fault=true`，replacement attempt 为 `1`，恢复约 `0.2s`。
- **preflight**：`python scripts/run_windows_gfx_r5.py --profile all --vendor nvidia --device-fault true --preflight` 通过并识别 `cases=8`；这只证明 inventory 完整，不生成 schema-4 manifest，也不等价于阶段通过。
- **mixed-DPI 边界**：入口已真实枚举 monitor bounds 与 DPI，执行 initial → forward → return 的 logical/drawable 证据；当前机器只有一个有效显示器/DPI，direct exact 按真实条件返回阻断，未伪造第二块屏。
- **soak 证据**：`python scripts/run_windows_gfx_r5.py --profile soak --vendor nvidia --device-fault true --soak-seconds 900` 通过，证据目录为 `test-reports/20260801-161742-gfx-r5-nvidia-soak/`，manifest clean source head 为 `eda9ff0e`，并已用 `python scripts/run_windows_gfx_r5.py --verify-evidence test-reports/20260801-161742-gfx-r5-nvidia-soak` 复核。single-window 为 `duration=900s`、`rounds=137461`、handles `108->102`、peak `108`、warmup `60s/10086 rounds`；shared-device 为 `duration=900s`、`rounds=60503`、handles `112->107`、peak `114`、warmup `120s/8192 rounds`；两者均 `swapchain_maintenance1=true`。
- **结论**：inventory 阻断已解除，vendor/device-lost/soak 证据已闭合，但 GFX-R5 仍为 `blocked / evidence-incomplete`；mixed-DPI 仍缺真实双显示器条件，尚无 schema-4 all manifest，不能写成 `pass`。下一步是在真实双显示器环境完成 mixed-DPI，再扩展 AMD/Intel 与其他目标环境。

## 快速模型 S5-09～S5-10：G5 release performance / soak

本批次先完成可在当前冻结环境执行的 performance 子门禁，并完成 clean commit `f5b02959cac6` 的 8 小时 soak；最终证据按实际测量结果记录，不把失败冒充长稳通过：

- **runner 修复**：`scripts/run_release_g5.py` 原先匹配已过期的 `GPU engine initialized (...)` 文案；生产代码和 `tests/agent_gui_windows.rs` 的真实 marker 已统一为 `Graphics bootstrap: selected recipe backend=vulkan; raster=gpu_native; present=swapchain`。修复已提交 `8d8ac0916edc`。
- **性能证据**：`python scripts/run_release_g5.py --mode performance` 在 clean commit `8d8ac0916edc3c378d4efe96a768662bea2498d0` 上通过，目录为 `test-reports/g5-release/8d8ac0916edc-20260801T171746Z-performance/`；manifest 完整性已用 `python scripts/run_release_g5.py --verify ...` 复核（复核时 gate 状态按契约返回 blocked）。
- **结果**：测量状态 `pass`；10,010 个预热后 presented 帧，主路径 p95 `1,864 µs`，inter-present p95 `16,680 µs / 16,700 µs`；5 类固定场景均有测量覆盖；冻结 Windows build 26200、AMD Ryzen 7 7700、32 GiB、NVIDIA GeForce RTX 4070 Ti SUPER / driver `32.0.16.1062`、Vulkan、1200×800、96 DPI 通过。
- **观察器 performance 验证**：clean commit `caa5c6367800` 的报告 `test-reports/g5-release/caa5c6367800-20260802T014509Z-performance/` 测量状态为 `pass`，10,007 个 inter-present 样本 p95 `16,694 µs / 16,700 µs`；theme/modal/drawer 各有 10 个真实开闭 loop，`resource_delta=0` 且关闭后 `final_overlay_count=0`。完整 gate 仍为 `fail`：三类尚未达到 1,000 loops，window 与 DPI 仍 blocked。
- **runner 契约测试**：`python -m unittest discover -s tests -p 'test_*.py' -q` 为 15/15 通过；`cargo test --quiet --lib` 为 43/43 通过；`cargo test --quiet --test tests --no-run` 通过并列出 8 条 GFX-R5 exact。新增 runner 测试统一位于 `tests/test_release_g5.py`，并由 `tests/test_test_layout.py` 维护独立测试入口归位护栏。
- **post-present 观察器实现**：窗口帧在真实 present 后输出 WidgetTree live node / slot、overlay inventory 与 active-work 快照；theme、modal、drawer 的开闭场景按实际 frame identity 生成 `G5_RESOURCE` 与 `G5_LOOP`，runner 已加入对应 schema 校验。window release-safe automation 与 controlled multi-DPI 仍明确记录为 blocked，新的 clean soak 证据尚未产生。
- **soak 状态**：报告目录为 `test-reports/g5-release/f5b02959cac6-20260801T173350Z-soak/`，8 小时运行已完成，`summary.json`、`manifest.json`、`report.md` 均已生成。`summary.json` 的 `status` 与 `g5_gate_status` 均为 `fail`，`g5_gate_reason=measurement_failed`，原因是 `successful presentation continued after idle settling`；`python scripts/run_release_g5.py --verify <dir>` 已确认 commit、schema 与文件哈希完整，但因报告失败状态返回非零。
- **边界**：历史 clean soak 的完整 G5 gate 当前为 `fail / measurement_failed`，不能标成通过；本批补齐了 modal、drawer、theme 的 post-present 观察代码，但必须在新的 clean performance/soak 中形成 1,000-loop 证据，window 与 DPI 仍受外部环境/自动化条件阻断。报告目录不入库，证据绑定执行时 clean source head，后续文档-only 提交不改变该绑定事实。

## 快速模型 F 批次：使用落地（交付前插入）

主人指令（2026-08-01）：「先把使用落地」——使用文档中剩余的全部「目标写法」`text` fence 落地为真实 API 并升级为 `rust uix-compile` 示例。目录索引曾声明 text fence 已全部升级，实际仍存 19 处（14 个文件）；本批次全部闭合：

| 任务 | 内容 | 状态 |
|---|---|---|
| F-01 | `Transition` 类型（fade_in/fade_out/slide_up/slide_down/slide_in/slide_out/zoom_in/zoom_out/stagger）+ `View::enter`/`leave`；`AnimationConfig` 增 `delay`（stagger 延迟接入 TransitionPlayer 播放管线） | ✅ 提交 `4b4e161c` |
| F-02 | `Header::title`、`Content::child`、`Grid::children` 声明式便捷方法（不破坏既有 new(height/width) 签名） | ✅ 提交 `c9c675ff1`（误回退后 `c369fc42` 重应用） |
| F-03 | ~~Platform trait 便捷方法~~ **已回退**：`uix::platform::Platform` 公开面早已提供 os_info/cpu_info/memory_info + `CpuInfo`，无需新增（`09cc81f3` 撤销） | ✅ 无需实现 |
| F-04 | `App::diagnostics_runtime(Diagnostics)` 注入已构建实例（与 `diagnostics(config)` 互补） | ✅ 提交 `d14b5ac1` |
| F-05 | 契约测试 `tests/usage_facade.rs`：Transition 转换、enter/leave、布局便捷方法、Platform 公开面签名、diagnostics_runtime | ✅ 提交 `f5b352d2`（5 passed） |
| F-06 | 19 处 text fence 全部升级为 rust fence（16 个新锚点 + `不必做.md` 转普通列表 + 目录索引 changelog 修正） | ✅ 知识库使用文档 |

新锚点：`agent-automation-id`、`animation-transition`、`computed-model`、`async-typed-three-state`、`multi-window-config`、`platform-system-info`、`settings-typed-struct`、`chart-state-driven`、`chart-empty-state`、`navigation-typed-route`、`layout-app-shell`、`tutorial-virtualized-todo`、`component-timer-badge`、`error-typed-business`、`diagnostics-runtime-inject`、`app-minimal`。

验证：`cargo test --quiet` 47 passed / 0 failed（含 usage_facade 5 项）；workspace check 通过；`git diff --check` 通过。使用文档不再存在 `text` fence 示例。

## 内容索引

| 分类 | 适用线索 | 权威范围 | 内容摘要 | 文档 |
|---|---|---|---|---|
| 交付方向 | 版本目标、优化方向、当前瓶颈 | 当前交付目标与优化优先级 | 更小（内存）、更快（帧率）、更稳（跨平台）的当前状态与差距 | [交付方向](进度/交付方向.md) |
| 理想使用演进 | 使用文档目标写法的理想 API、演进优先级 | 使用文档 `text` fence 中理想 API 的登记与完成条件 | 类型化表单/表格、受控浮层、全局反馈门面等 9 项演进清单，已决定 0.0.1 交付前插入（E 批次） | [理想使用演进](进度/理想使用演进.md) |
| SMC 迁移 | System、Module、Component、私有实现、组合根 | 程序开发知识库中的通用 SMC 定义；仓库中的迁移状态与验证证据 | **最高优先级**（主人 2026-07-31）：逐 System 消除聚合边界和兄弟 Module 依赖，SMC-01～SMC-07 排在 E 批次之前；Diagnostics 首个 SMC 纵切已完成 | [通用 SMC 定义](<file:///C:/data/note/程序开发/System%20Module%20Component%20设计模式.md>)；[迁移证据](进度/运行保障.md)；[批次登记](进度.md#快速模型-smc-批次system-module-component-改造最高优先级) |
| 首发状态 | 版本号、发布状态、交付物 | 当前版本元数据与交付范围 | 版本状态、目标平台、许可和交付物清单 | [首发状态](进度/首发状态.md) |
| 功能账本 | 功能清单、源码位置、观察状态 | 功能到实现与证据的映射 | 逐项功能、实现位置与证据状态 | [功能账本](进度/功能账本.md) |
| 组件功能缺口 | 组件批次、实现状态、证据不足 | 组件行为能力的活跃差距 | 第 4～8 批 110 项组件功能状态与证据缺口 | [组件功能缺口](进度/组件功能缺口.md) |
| 组件稳定性 | 组件风险、修复记录、回归验证 | 组件级稳定性与验证等级 | 逐组件风险、回归证据与验证等级 | [组件稳定性](进度/组件稳定性.md) |
| 无障碍证据 | 无障碍逐项证据 schema、收集与 110 行映射 | 无障碍 manifest/item 文件结构与状态语义 | S4-03 固化 schema：captured / pending-scene / ledger-gap 三态，不冒充完整证据 | [无障碍证据](进度/无障碍证据.md) |
| 平台工程 | 平台 API、跨平台、GPU 枚举、Agent | 平台抽象和跨平台交付状态 | 平台公开面、硬件覆盖与 Agent 安全差距 | [平台工程](进度/平台工程.md) |
| 运行保障 | 错误处理、恢复机制、日志、崩溃 | 框架容错机制的落地状态 | 错误报告、恢复登记、日志迁移和崩溃边界 | [运行保障](进度/运行保障.md) |
| 主题令牌分层 | 主题系统、颜色 token、代码分层 | 主题令牌跨层搬迁状态 | 主题令牌从绘图层归还 UI 层的当前差距 | [主题令牌分层](进度/主题令牌分层.md) |
| 性能与内存 | 帧率、内存占用、性能瓶颈 | 当前测量基线与优化证据 | 性能、内存、已知瓶颈与暂缓项 | [性能与内存](进度/性能与内存.md) |
| 已知缺陷 | Bug、窗口布局、交互问题 | 当前未关闭缺陷 | 已知缺陷清单及其状态 | [已知缺陷](进度/已知缺陷.md) |
