# 进度

> 当前重心：**Windows 生产可用（P6）**。契约见[架构](架构.md)；Linux / macOS / 移动端 / Agent 不计本 gate。

## 目标

把 Windows 桌面路径做到可交付生产：默认 Vulkan 真机可用、失败 typed、系统 IME（微软拼音）过关、**空闲真休眠/遮挡可停帧**、无 busy retry。上层无 OS 分叉。

**未闭合。** 已落地：DeepIdle、连续工作轮次 OS 事件公平分发、逐窗 one-shot、隐藏/最小化零唤醒、D3D11 bitblt 遮挡休眠、其余 Windows recipe typed `Unsupported` + 生命周期休眠、默认 Vulkan PixelUpload 真窗 present 与最小化/恢复、DeviceLost 有限恢复 → Auto 下一 recipe → Software/GDI 同帧提交与失败终态、`TerminalFailure` 吸收态、微软拼音真实前台 composition → commit、前台系统 Tab + Modal focus trap / 隐藏恢复组合。

### Gate（仅 Windows）

1. `cargo test --all-targets` 全过
2. 图形契约生效（[架构·图形](架构.md#图形-api与帧提交硬约束)）；下表图形证据关完
3. 失败 **typed**；禁静默换 GPU API / 伪成功
4. 微软拼音 composition → commit 真机过关
5. 上层无 OS 分叉
6. one-shot；隐藏 / **遮挡** / zero extent / 持续失败无 layout、present 或 busy retry（含默认 Vulkan 路径的遮挡停帧或诚实的 typed unsupported + 生命周期休眠）

### 推进顺序

`GFX-R5` → `GFX-R6`。

Metal/D3D12 扩展不进本 gate。

## 未关闭（挡 P6）

| 优先级 | ID | 缺口 | 下一动作 |
|--------|-----|------|----------|
| P2 | GFX-R5 · BUG-006/007/009 · RISK-002/006 | Win parity / fault / resize / 多驱动 / 稳定性 / 前台 capture | 真机矩阵 |
| P2 | GFX-R6 · RISK-005 | 共享 device + 逐窗 surface 长跑 | 正确性闭合后 profile |

| P2 | BUG-008 · DRAW-SYNC | 绘制裁剪同步：清空区（`begin_frame`）与录制管线裁剪区独立计算无编译期同步；不一致时父背景可越过 clip 覆盖未标脏兄弟节点像素 | 统一 dirty 并集来源，加编译期或运行时同步 |

## P6 后 / 不计本 gate

<a id="附能力不计-p6"></a>

| 类 | 项 | 时机 |
|----|-----|------|
| 三端债 | Wayland occlusion v6+、macOS/Wayland 真机（BUG-010–015、RISK-003、非 Win GFX-R0） | P6 后按需 |
| 附能力 | AGENT-R1：异账号 / 异 uid 拒绝证据 | 不挡 P6 |
| 暂缓 | 移动端 backend；Metal `GpuNative` / D3D12 完整扩展 | 图形契约闭合且 P6 后 |
| 不做 | 网络 / 同步；读屏桥（除非单独立项） | — |
| 性能 | PERF-001 · SCROLL-COPY | `ScrollCopy` 未接入 `render_frame`：当前滚动视口整窗清空重绘而非仅 exposed strip；接线后仅重绘暴露条带 | 图形契约闭合后 |
| 性能 | PERF-002 | InvalidationQueue 锁粒度：同一事件轮次中多次 paint/layout invalidation 各自独立 `.lock()`（`push_paint_invalidation` / `push_layout_invalidation` / `push_scroll_composite`），改为批量提交一次加锁 | P6 后 |
| 性能 | PERF-003 | InvalidationQueue::push Paint 合并 O(n²)：线性扫描找同节点已有 invalidation，脏节点多时退化。改用 HashMap 或 sort-first | P6 后 |
| 性能 | PERF-004 | WidgetTree::traverse() 命中缓存仍 `ids.clone()`，改为返回 `&[WidgetId]` | P6 后 |
| 性能 | PERF-005 | layout_traverse() 单次调用中多次 `HashSet::new` / `Vec::new` / `collect()`，合并为一次遍历+收集 | P6 后 |
| 性能 | PERF-006 | DisplayList `PaintOp::DrawText` 持有 `String`（owned），文本密集型 UI 累积 clone 开销；改用 `Arc<str>` 共享 | P6 后 |
| 性能 | PERF-007 | PaintContext::draw_text 每次 `text.to_string()` 复制，改为延迟 clone（仅在需存储时） | P6 后 |
| 性能 | PERF-008 | Debug overlay 每帧 6 个 `format!`，debug 关闭时仍求值；改为懒求值 | P6 后 |
| 性能 | PERF-009 | reconcile_requester() 每次 `Arc::new` 新闭包，改为缓存复用 | P6 后 |
| 图形 | GFX-R1 · BUG-004 | 圆角 Additive 仍 NotImplemented（`can_emit_native_additive_rect` 要求 `radius.is_none()`）；GPU native blend 未实现。裁决结论：不挡 P6——无组件依赖，失败形式为 typed `NotImplemented`，等有真实场景再做 | P6 后按需 |

> `Computed<T>` / `DesignTokens::from_primaries()` / `dynamic_label` 已在[使用文档](使用.md)中说明。
