# 使用

> 怎样用 UIX 写应用（不必关心框架内部）。

入口：`use uix::prelude::*`。可复制示例 → [README](../README.md#示例)；能力展示 → [`demo`](../demo/README.md)；导出一览 → [`prelude.rs`](../src/prelude.rs)。

> ⚠️ **Windows P6 gate 未闭合。** 下方为框架目标写法。已验证稳定：View + State + 组件声明式、布局、主题、多窗、定时器、`test-harness`、微软拼音 IME、Windows 前台系统 Tab、浮层 focus trap、隐藏/最小化及 D3D11 bitblt 遮挡休眠。验证中：图形 fault/recovery 与真机矩阵。详见[进度](进度.md)。

## 快速开始

`Cargo.toml`：

```toml
[dependencies]
uix = "0.1"
```

第一个应用：

```rust
use uix::prelude::*;

fn main() {
    App::new()
        .title("Hello UIX")
        .size(400, 300)
        .root(|| label("Hello, world!").font_size(32.0))
        .run();
}
```

带交互的计数器：

```rust
use uix::prelude::*;

fn main() {
    let count = State::new(0);

    App::new()
        .title("计数器")
        .size(360, 200)
        .root(move || {
            column((
                count.map_text(|n| format!("{n}")).font_size(48.0),
                row((
                    button("-1").on_click(&count, |c| c.update(|v| *v -= 1)),
                    button("+1").primary().on_click(&count, |c| c.update(|v| *v += 1)),
                )).gap(8.0),
            ))
            .gap(16.0)
            .padding(24.0)
        })
        .run();
}
```

---

## 核心概念

### State —— 响应式状态

```rust
let count = State::new(0);       // 创建
count.set(5);                     // 写入
count.update(|v| *v += 2);       // 基于旧值更新
let val: i32 = count.get();      // 读取
```

写入 `State` 会标记受影响的视图，由框架合并为至多一次重绘。高频写入不自动节流——用 `run_interval` 或动画驱动周期性变化。

### Computed —— 派生状态

自动追踪依赖，惰性求值，仅在依赖变化时重算：

```rust
let first = State::new("Ada");
let last  = State::new("Lovelace");
let full  = Computed::new(move || format!("{} {}", first.get(), last.get()));

// 映射到视图
full.map_text(|s| s.clone()).font_size(20.0);
```

`map` / `map_text` / `map_opt` 将 `Computed<T>` 转为响应式 View：

| 方法 | 输入 | 输出 |
|------|------|------|
| `.map(\|v\| view)` | `T` → `impl View` | 依赖变化时重建子视图 |
| `.map_text(\|v\| format!(…))` | `T` → `String` | 常用简写 |
| `.map_opt(\|v\| Option<impl View>)` | `T` → `Option<impl View>` | 条件渲染 |

### Effect —— 副作用

依赖变化时执行（不产生视图）：

```rust
Effect::new(move || {
    log::info!("count 变为 {}", count.get());
    // 自动追踪 count
});
```

Effect 在事件轮次末尾、重绘之前批量执行。

### View —— 组合模型

所有界面元素实现 `View` trait，通过组合构建 UI 树：

```rust
// 条件渲染
show(visible, || label("可见内容"))

// 列表渲染（少量项目）
column(items.iter().enumerate().map(|(i, item)| {
    label(item).automation_id(format!("item-{i}"))
}))

// 大量列表 → VirtualScroll（见后文）
```

`Option<impl View>` 自动处理 `None`（不渲染）和 `Some(view)`（渲染）。

---

## 组件

### 函数组件

最轻量：抽取函数返回 `impl View`：

```rust
fn user_card(name: &str, role: &str) -> impl View {
    column((
        label(name).font_size(16.0).font_weight(FontWeight::BOLD),
        label(role).font_size(12.0).fg(Color::gray()),
    ))
    .gap(4.0)
    .padding(12.0)
    .bg(Color::rgba(248, 248, 248, 255))
    .radius(8.0)
}
```

用于纯组合已有组件，无自定义绘制或事件。

### canvas —— 轻量自定义绘制

只需像素级绘制（无独立 measure / 事件），比 `component!` 更轻：

```rust
fn color_dot(color: Color, diameter: f32) -> impl View {
    canvas(diameter, diameter, move |frame, ctx| {
        let center = frame.center();
        ctx.fill_circle(center, diameter / 2.0, &color);
    })
}
```

### component! —— 完整自定义组件

需要 measure / render / on_event / 子节点时使用：

```rust
component! {
    /// 自定义进度环。
    pub struct ProgressRing {
        progress: f32,   // 0.0..1.0
        size: f32,
    }

    measure => (&self, constraints: Constraints) -> Size {
        constraints.clamp(Size::new(self.size, self.size))
    }

    render => (&self, frame: Rect, ctx: &mut PaintContext, _tree: &WidgetTree) {
        let color = ctx.tokens().color_primary();
        // 背景环
        ctx.stroke_circle(frame.center(), self.size / 2.0 - 4.0, 4.0,
            &color.with_alpha(0.2));
        // 进度弧
        let end = self.progress * 360.0;
        ctx.stroke_arc(frame.center(), self.size / 2.0 - 4.0, 4.0,
            0.0, end, &color);
    }
}

// 使用
ProgressRing { progress: 0.65, size: 80.0 }
```

`component!` 自动生成 reconciliation 逻辑。可定义的方法：

| 方法 | 用途 |
|------|------|
| `measure` | 给定约束，返回尺寸 |
| `render` | 像素绘制 |
| `build` | 返回子节点（`Vec<Box<dyn WidgetComponent>>`） |
| `on_event` | 接收 `SystemEvent`，返回是否消费 |

含子节点的组件：

```rust
component! {
    pub struct Card {
        children: WidgetChildren,
    }

    build => (&self) -> Vec<Box<dyn WidgetComponent>> {
        self.children.take()
    }

    render => (&self, frame: Rect, ctx: &mut PaintContext, _tree: &WidgetTree) {
        ctx.fill_rect(frame, &Color::WHITE, Radius::new(8.0));
    }
}
```

### 指针移动优化

同一 hit 目标上连续 `PointerMove` 不每帧触发重绘——仅 hover 目标变化时标脏。需要逐帧 PointerMove 的组件（Slider、拖拽选区）应 opt-in：

```rust
component! {
    pub struct MySlider {
        min: f32,
        max: f32,
    }

    wants_continuous_pointer_move => (&self) -> bool { true }
}
```

---

## 布局

### 容器

| 容器 | 行为 |
|------|------|
| `column(children)` | 纵向排列，横向填满 |
| `column_fit(children)` | 纵向排列，横向撑开到内容宽度 |
| `row(children)` | 横向排列，纵向填满 |
| `row_fit(children)` | 横向排列，纵向撑开到内容高度 |
| `stack(children)` | 层叠，子节点按顺序从底到顶 |

子节点用元组传入（`views!` 宏为兼容别名）：

```rust
column((header, body, footer))
    .gap(12.0)
    .padding(16.0);
```

### 尺寸与约束

```rust
view
    .size(200.0, 100.0)          // 固定尺寸
    .min_size(100.0, 50.0)       // 最小尺寸
    .max_size(f32::MAX, 200.0)   // 最大尺寸
    .expand(true)                // 在父容器中伸缩
```

### Grid

```rust
Grid::new()
    .columns(3)                   // 等宽 3 列
    .column_widths(&[100.0, -1.0, 80.0])  // -1.0 = 弹性
    .gap(8.0, 8.0)               // 列间距，行间距
    .children((
        label("A"), label("B"), label("C"),
        label("D"), label("E"), label("F"),
    ))
```

### Splitter

```rust
Splitter::horizontal()
    .first(sidebar, 240.0)       // 固定初始宽度
    .second(content)
    .min_first(180.0)            // 拖拽下限
```

### Affix / BackTop / Sider

```rust
// 吸顶
Affix::new().top(0.0).child(nav_bar)

// 回到顶部
BackTop::new().attach(scroll_view)

// 侧边栏布局
Layout::new()
    .sider(Sider::new().child(menu).width(240.0))
    .content(main_area)
```

### 滚动

普通滚动：

```rust
ScrollView::new()
    .size(-1.0, 400.0)
    .child(long_content)
```

虚拟列表（万级数据，只渲染可视区）：

```rust
VirtualScroll::new()
    .item_count(10_000)
    .item_height(32.0)
    .size(320.0, 480.0)
    .render(|index| label(format!("row-{index}")))
```

---

## 样式与主题

### 语义样式

优先语义，再覆盖细节：

```rust
button("保存").primary()       // 主色按钮
button("取消").secondary()     // 次色按钮
button("删除").danger()        // 危险操作
```

可用语义：`.primary()` `.secondary()` `.success()` `.warning()` `.danger()` `.info()` `.link()` `.ghost()` `.dashed()`

### 细粒度样式

```rust
view
    .bg(Color::rgba(240, 240, 240, 255))  // 背景色
    .fg(Color::BLACK)                      // 前景色（文本）
    .font_size(16.0)
    .font_weight(FontWeight::BOLD)
    .radius(8.0)                           // 圆角
    .border(1.0, Color::gray())            // 边框
    .gap(12.0)                             // 子节点间距
    .padding(16.0)                         // 内边距（四边）
    .padding_h(16.0)                       // 水平内边距
    .padding_v(8.0)                        // 垂直内边距
    .margin(8.0)                           // 外边距
    .opacity(0.8)                          // 透明度
    .shadow(4.0, Color::rgba(0,0,0,40))   // 阴影
```

### 主题

默认使用 Ant Design 风格亮色主题：

```rust
App::new()
    .theme(Theme::antd_light())          // 默认
    // .theme(Theme::antd_dark())
    .follow_system_theme(true)           // 跟随系统
    .run();
```

运行时切换：

```rust
ThemeToggle   // 内置切换开关组件
// 或手动（在 on_start 中取 AppHandle）
handle.set_theme(Theme::antd_dark());
```

通过 `ctx.tokens()` 获取当前主题的设计令牌：

```rust
render => (&self, frame: Rect, ctx: &mut PaintContext, _tree: &WidgetTree) {
    let primary = ctx.tokens().color_primary();
    let text    = ctx.tokens().color_text();
    let bg      = ctx.tokens().color_bg();
    // ...
}
```

### 自定义主题

从 12 色板生成：

```rust
let custom = DesignTokens::from_primaries([
    Color::hex("#1890ff"), // primary
    Color::hex("#52c41a"), // success
    Color::hex("#ff4d4f"), // danger
    // ... 共 12 色
], false); // is_dark

App::new()
    .theme(Theme::custom(custom))
    .run();
```

---

## 输入与表单

### 文本输入

```rust
let text = State::new(String::new());

// 单行
input()
    .placeholder("请输入用户名")
    .value(&text)

// 多行
Input::textarea()
    .placeholder("请输入描述")
    .value(&text)
    .rows(5)
    .max_length(500)

// 密码
Input::password()
    .placeholder("请输入密码")
    .value(&text)
```

### 选择器

```rust
let selected = State::new(String::new());
let options  = vec!["选项 A", "选项 B", "选项 C"];

Select::new()
    .options(&options)
    .value(&selected)
    .placeholder("请选择")
```

```rust
// 多选
Select::multiple()
    .options(&options)
    .value(&selected_set)  // State<HashSet<String>>
```

```rust
// 可搜索
Select::searchable()
    .options(&options)
    .value(&selected)
```

### 开关与勾选

```rust
let checked   = State::new(false);
let selected  = State::new(String::new());

Checkbox::new("同意协议").checked(&checked)
Switch::new().checked(&checked)
Radio::group("group1", &options, &selected)
Segmented::new(&options).value(&selected)
```

### 数值输入

```rust
let num    = State::new(0i32);
let val    = State::new(50.0f64);
let rating = State::new(3u32);

InputNumber::new().value(&num).min(0).max(100).step(1)
Slider::new(0.0..=100.0).value(&val)
Rate::new().count(5).value(&rating)
```

### 日期与颜色

```rust
let date  = State::new(Date::today());
let time  = State::new(Time::now());
let color = State::new(Color::RED);

DatePicker::new().value(&date)
TimePicker::new().value(&time)
ColorPicker::new().value(&color)
```

### 级联与自动完成

```rust
let tree_data = vec![
    TreeNode::new("北京").children(vec![
        TreeNode::new("朝阳"), TreeNode::new("海淀"),
    ]),
    TreeNode::new("上海").children(vec![
        TreeNode::new("浦东"), TreeNode::new("静安"),
    ]),
];

Cascader::new().options(&tree_data).value(&selected_path)
TreeSelect::new().options(&tree_data).value(&selected)
Mentions::new().suggestions(&users).value(&text)
AutoComplete::new().suggestions(&["Ada", "Alan", "Grace"]).value(&text)
```

### 上传

```rust
Upload::new()
    .accept(".jpg,.png,.pdf")
    .max_size(10 * 1024 * 1024)  // 10 MB
    .on_select(|files| { /* Vec<SelectedFile> */ })
```

### 表单

字段校验闭包直接定义，返回 `Result<(), String>`：

```rust
let form = Form::new()
    .field("user", "用户名")
        .default("")
        .required("请输入用户名")
        .custom(|v| if v.len() < 3 {
            Err("至少 3 个字符".into())
        } else { Ok(()) })
    .field("email", "邮箱")
        .default("")
        .required("请输入邮箱")
        .validate_email("邮箱格式不正确")
    .field("age", "年龄")
        .default(0i32)
        .validate_range(1..=120, "年龄范围 1-120")
    .build();

match form.validate() {
    Ok(values) => {
        let user  = values.get::<String>("user").unwrap();
        let email = values.get::<String>("email").unwrap();
        // 提交
    }
    Err(errors) => {
        // errors: Vec<(field_key, error_message)>
        for (field, msg) in &errors {
            eprintln!("{field}: {msg}");
        }
    }
}
```

内置校验规则：

| 方法 | 说明 |
|------|------|
| `.required(msg)` | 非空 |
| `.validate_email(msg)` | 邮箱格式 |
| `.validate_range(0..=100, msg)` | 数值范围 |
| `.validate_length(2..=20, msg)` | 字符串长度 |
| `.validate_pattern(regex, msg)` | 正则 |
| `.custom(\|v\| Result<(), String>)` | 自定义逻辑 |

---

## 事件

### 点击与语义事件

```rust
// 无状态点击
button("确定").on_click_fn(|| { println!("clicked"); })

// 携带状态
button("+1").on_click(&count, |c| c.update(|v| *v += 1))

// 语义事件（更通用）
view.on_semantic(SemanticKind::Click, |ev| {
    // ev.source, ev.target, ...
})
```

### 指针事件

```rust
view
    .on_pointer_down(|ev| { /* ev.position */ })
    .on_pointer_move(|ev| { /* 需 opt-in continuous */ })
    .on_pointer_up(|ev| {})
    .on_pointer_enter(|_| {})
    .on_pointer_leave(|_| {})
```

### 键盘与焦点

```rust
view
    .on_key_down(|ev| {
        match ev.key {
            Key::Enter => { /* 提交 */ }
            Key::Escape => { /* 取消 */ }
            _ => {}
        }
    })
    .on_focus(|_| {})
    .on_blur(|_| {})

// 编程式焦点
view.focus()
view.blur()
```

Tab / Shift+Tab 自动在可聚焦节点间循环；浮层内受 focus trap 约束。

### 滚动事件

```rust
ScrollView::new()
    .on_scroll(|offset| { /* offset.x, offset.y */ })
    .child(content)
```

---

## 浮层与反馈

框架自动维护 `OverlayStack`，应用不手管层级。

### Modal

```rust
Modal::show(move |ctx| {
    column((
        label("确认删除？").font_size(18.0),
        row((
            button("取消").on_click_fn(|| ctx.close()),
            button("确认").danger().on_click_fn(|| {
                // 执行删除
                ctx.close();
            }),
        )).gap(8.0),
    ))
    .gap(16.0)
    .padding(24.0)
})
.title("提示")
.width(400.0);
```

### Drawer

```rust
Drawer::show(move |ctx| {
    // 内容
})
.title("详情")
.placement(Placement::Right)
.width(480.0);
```

### Message / Notification

```rust
Message::success("保存成功")
Message::error("保存失败")
Message::warning("请先填写必填项")
Message::info("正在加载...")

Notification::new()
    .title("更新完成")
    .description("已更新到 v2.0")
    .duration(3.0)  // 秒，0 为不自动关闭
    .show()
```

### Popover / Tooltip

```rust
Popover::new()
    .content(|| label("弹出内容"))
    .trigger(Trigger::Click)  // 或 Trigger::Hover
    .attach(target_view)

Tooltip::new("提示文字").attach(target_view)
Popconfirm::new("确定删除？").on_confirm(|| { /* ... */ }).attach(delete_button)
```

### 进度与加载

```rust
ProgressBar::new().percent(65.0)         // 百分比
ProgressBar::new().indeterminate()       // 不确定进度
Spin::new().size(32.0)                   // 加载旋转
Skeleton::new().rows(3).animated(true)   // 骨架屏
```

### Alert

```rust
Alert::new(AlertType::Info)
    .title("注意")
    .description("这是一条提示信息")
    .closable(true)
```

---

## 数据展示

### Table

```rust
#[derive(Clone)]
struct User {
    name: String,
    age: u32,
    role: String,
}

let users = vec![
    User { name: "Ada".into(), age: 28, role: "Admin".into() },
    User { name: "Bob".into(), age: 35, role: "User".into() },
];

Table::new()
    .columns(vec![
        TableColumn::new("姓名", 120.0).render(|u: &User| u.name.clone()),
        TableColumn::new("年龄", 80.0).render(|u: &User| u.age.to_string()),
        TableColumn::new("角色", 100.0).render(|u: &User| u.role.clone()),
    ])
    .rows(users)
    .expandable(48.0, |row| {
        column((
            label(format!("姓名: {}", row.name)),
            label(format!("年龄: {}", row.age)),
        )).padding(12.0)
    })
    .sortable(true)       // 点击列头排序
    .selection(true)       // 行选择
    .bordered(true)
```

### List / Tree

```rust
List::new()
    .items(&data)
    .render(|item| label(item.name))
    .gap(4.0)

Tree::new()
    .data(&tree_nodes)
    .default_expand_all(true)
    .render(|node| label(node.label))
```

### 时间轴

```rust
Timeline::new()
    .items(vec![
        TimelineItem::new("2024-01-01", "项目启动"),
        TimelineItem::new("2024-03-15", "首次发布").color(Color::green()),
        TimelineItem::new("2024-06-01", "v2.0 上线"),
    ])
```

### 描述列表 / 标签

```rust
Descriptions::new()
    .item("用户名", "Ada Lovelace")
    .item("邮箱", "ada@example.com")
    .item("角色", tag("管理员").color(Color::blue()))
    .bordered(true)
```

---

## 导航

### Menu

```rust
Menu::new()
    .items(vec![
        MenuItem::new("首页").icon(Icon::Home),
        MenuItem::new("设置").icon(Icon::Settings).children(vec![
            MenuItem::new("个人资料"),
            MenuItem::new("安全设置"),
        ]),
    ])
    .selected_index(&selected)
    .mode(MenuMode::Inline)  // 或 Horizontal, Vertical
```

### Pagination

```rust
Pagination::new()
    .total(200)
    .page_size(20)
    .current(&page)
    .on_change(|p| page.set(p))
```

### Steps / Breadcrumb / Anchor

```rust
Steps::new()
    .current(1)
    .items(vec![
        Step::new("填写信息"),
        Step::new("确认订单"),
        Step::new("完成支付"),
    ])

Breadcrumb::new()
    .items(vec!["首页", "用户管理", "详情"])

Anchor::new()
    .items(vec![
        AnchorItem::new("section-1", "基本信息"),
        AnchorItem::new("section-2", "高级设置"),
    ])
```

### Tabs / Segmented

```rust
Tabs::new()
    .tabs(vec![
        Tab::new("标签1", || content_1()),
        Tab::new("标签2", || content_2()),
    ])
    .active(&active_tab)
```

### Dropdown

```rust
Dropdown::new()
    .items(vec![
        DropdownItem::new("编辑"),
        DropdownItem::new("删除").danger(true),
        DropdownItem::divider(),
        DropdownItem::new("导出"),
    ])
    .attach(button("操作"))
```

---

## 动画

```rust
let anim = Animation::new(0.0, 1.0, 0.3)  // from, to, seconds
    .easing(Easing::ease_out);

anim.value()  // f64，当前插值
anim.is_finished()

// 用于视图属性
label("淡入")
    .opacity(anim.value() as f32)

// 重启动画
anim.restart()
```

可用缓动：`Easing::linear` `Easing::ease_in` `Easing::ease_out` `Easing::ease_in_out` `Easing::bounce` `Easing::elastic`

动画由框架自动推进；窗口不可呈现时暂停、恢复首帧重基。**勿用 `run_interval` 模拟帧动画。**

---

## 多窗口

窗口客户区尺寸、布局、指针与 resize 都使用 logical 坐标；每窗 drawable 随所在显示器的 DPI 独立变化。应用无需监听 DPI 消息或手工缩放窗口内容。

在 `on_start` 中获取句柄：

```rust
App::new()
    .on_start(|handle| {
        // 打开第二个窗口
        let _win2 = handle.open_window(
            WindowConfig::new()
                .title("子窗口")
                .size(400, 300)
                .root(|| label("我是第二窗口"))
        );
    })
    .root(|| label("主窗口"))
    .run();
```

窗口间通信通过各自 `AppHandle`：

```rust
// 窗口 A
handle_a.run_after(Duration::from_secs(1), move || {
    // 向窗口 B 发消息
    handle_b.post_to_ui(|| { /* 更新 B 的状态 */ });
});
```

---

## 定时与异步

### 定时器

```rust
// 延时执行
let timer = handle.run_after(Duration::from_secs(2), || {
    // 2 秒后执行
});

// 周期执行
let timer = handle.run_interval(Duration::from_millis(100), || {
    // 每 100ms 执行
});

// drop 自动取消；需存活则 detach
timer.detach();       // 不受 drop 影响
timer.cancel();       // 显式取消
```

### 跨线程回 UI

从任意线程更新 UI 状态，只走 `post_to_ui`：

```rust
let handle_clone = handle.clone();
std::thread::spawn(move || {
    let result = heavy_computation();
    handle_clone.post_to_ui(move || {
        data.set(result);
    });
});
```

`post_to_ui` 绑目标窗口的 `WindowSession`，不会唤醒其他窗口。

---

## 图表

```rust
// 柱状图
BarChart::new()
    .data(&[( "Q1", 120.0), ("Q2", 200.0), ("Q3", 150.0), ("Q4", 180.0)])
    .size(400.0, 300.0)

// 折线图
LineChart::new()
    .series(vec![
        Series::new("收入", &[( "Jan", 100.0), ("Feb", 150.0), ("Mar", 130.0)]),
        Series::new("支出", &[( "Jan",  80.0), ("Feb",  90.0), ("Mar", 110.0)]),
    ])
    .size(400.0, 300.0)

// 饼图
PieChart::new()
    .data(&[("产品A", 40.0), ("产品B", 30.0), ("产品C", 30.0)])
    .size(300.0, 300.0)
```

---

## 配置

### Settings

```rust
App::new()
    .settings("app.settings.json")  // 可选，相对可执行文件路径
    .on_start(|handle| {
        let svc = handle.resolve::<SettingsService>();
        let theme: Option<String> = svc.get("theme");
        // 写入
        svc.set("theme", "dark");
    })
    .run();
```

KV 模型；默认不读盘（opt-in）；不进 UI 热路径。

### 图形后端

```rust
App::new()
    .graphics_backend(GraphicsBackend::Vulkan)        // 默认
    // .graphics_backend(GraphicsBackend::D3D11)
    // .graphics_backend(GraphicsBackend::Auto)       // 自动选择最佳
    .run();
```

失败 typed 诊断后回 Software/CPU；禁静默换 GPU API；仅 `Auto` 可跨 API。

### CLI 模式

```rust
App::new()
    .mode(AppMode::CLI)
    .on_start(|_| {
        println!("命令行模式运行");
    })
    .run();
```

---

## 测试

### TestApp —— 无窗测试

```rust
use uix::prelude::*;

#[test]
fn test_counter() {
    let count = State::new(0);
    let root_count = count.clone();

    let mut app = uix::ui::test_harness::TestApp::new((400.0, 300.0), move || {
        let count = root_count.clone();
        row((
            label(count.map_text(|n| format!("{n}"))).automation_id("counter.label"),
            button("+1").on_click(&count, |c| c.update(|v| *v += 1)).automation_id("counter.inc"),
        ))
    });

    assert_eq!(count.get(), 0);

    app.click("counter.inc").unwrap();
    app.settle();  // 等待 reconcile 完成

    assert_eq!(count.get(), 1);
    assert_eq!(app.text("counter.label").unwrap(), "1");
}
```

可用操作：`.click(id)` `.type_text(id, "text")` `.press_key(id, Key::Enter)` `.select(id, "option")` `.invoke(id)`（通用语义动作）

所有操作走真实 semantic / `SystemEvent` 路径，受可见性与 modal 约束。

### 真窗快照

设置环境变量 `UIX_AUTOMATION_DIR`，真窗运行时每窗输出 JSON 快照（`revision` / `presented_revision`），空闲不轮询。关闭写 `closed: true`。password 字段不导出。

---

## Agent Bridge (开发预览)

双门禁启用：`agent-control` feature + `.enable_agent_control()`。启用后发布本机端点，供同用户进程通过 JSON Lines 协议枚举窗口、读语义树、执行动作。空闲无轮询，敏感值不导出。

详见[架构](架构.md#大模型控制协议)；缺口 → [`AGENT-R1`](进度.md#附能力不计-p6)。

---

## 组件速查

### 通用
`Button` `Icon` `Label` `FloatButton` `Typography` `Divider` `Space`

### 布局
`Container`(`column`/`row`) `Grid` `Layout` `Splitter` `Affix` `BackTop` `Sider` `Footer` `Header` `Content` `ScrollView` `VirtualScroll`

### 输入
`Input` `InputNumber` `Checkbox` `Radio` `Select` `Switch` `Slider` `DatePicker` `TimePicker` `ColorPicker` `Cascader` `TreeSelect` `Mentions` `AutoComplete` `Rate` `Segmented` `Form` `Upload`

### 展示
`Avatar` `Badge` `Calendar` `Card` `Carousel` `Collapse` `Descriptions` `Empty` `Image` `List` `ResultView` `SelectableList` `Skeleton` `Table` `Tag` `Timeline` `Tree` `RichText` `QRCode` `Watermark` `Transfer`

### 反馈
`Alert` `Drawer` `Message` `Modal` `Notification` `Popconfirm` `Popover` `ProgressBar` `Spin` `Tooltip`

### 导航
`Anchor` `Breadcrumb` `Dropdown` `Menu` `Navigation` `Pagination` `Steps` `Tabs`

### 图表
`BarChart` `LineChart` `PieChart`

---

## 不必做

- 不手写 `request_redraw` / vsync 申请
- 不手写探活定时器
- 不直接操作框架内部 registry / `OverlayStack`
- 不为各桌面 OS 写两套上层 UI
- 不用 `run_interval` 模拟帧动画
- 现为 Rust API，无独立 UI 脚本语言
