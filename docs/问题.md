# 问题

## 状态与入账规则

- 状态流：`待确认 -> 已确认 -> 修复中 -> 待验证 -> 已关闭`；无法继续时使用 `阻塞`。
- `待确认` 需要直接证据；`已确认` 需要从已知基线完成最小复现。
- 已改产物只进入 `待验证`；原始复现和至少一个相邻场景通过后才进入 `已关闭`。
- 工具限制、环境/驱动问题和测试数据问题保留在单次报告的限制章节，不作为产品问题入账。
- 重复、非问题、无法复现或明确不修复的条目保留原 ID，并在关闭记录中注明原因。
- 严重度按用户/项目影响判断；禁止把单机或单后端结果外推为跨平台结论。

## 活跃问题

图形 API / 渲染器现有 6 项未关闭缺陷与 3 项交付风险；它们仍阻塞 REQ-006 与 P6 production。BUG-010 已以 Windows WARP 自动化关闭，不能外推为多驱动或 production 硬件证据。另有 2 项已有代码修复但仍缺指定 GUI 场景复验。整改契约 → [#181](决策.md#d181)–[#186](决策.md#d186)；排期与关闭门槛 → [P6.9](进度.md#p69-图形契约整改)。

<a id="已确认图形架构缺陷"></a>

## 已确认图形架构缺陷

| ID | 类型 / 严重度 | 置信度 / 状态 | 影响范围 | 最小复现；期望 / 实际 | 直接证据 | 下一动作 | 首次 / 最后确认 |
|----|---------------|---------------|----------|------------------------|----------|----------|-----------------|
| <a id="bug-20260712-004"></a>BUG-20260712-004 | Correctness / Critical | 高 / 修复中 | `FrameRenderer`、`NativeGpuBackend`、GL、Picture/offscreen、D3D11/12 | `begin_frame` 仅返回 `FrameReady`；external presenter 的结束状态为 `PresentPending`，仅窗口 `IPresenter` 成功后才认定最终 `Present`/记录指标/清 dirty，失败经 typed recovery hook 保留 dirty。main producer 已替换为私有 `FrameRecordingEngine`，root、非缓存 direct 与 overlay 不再写真实 backend canvas 或汇成单一完整 `FrameImage`：每个操作按画序进入 checked main `FrameEncoder` 的 `Clear`、受限 `Native(FillRect)`、精确 `CpuSegment` 或 `PictureBlit`。CPU target 逐项执行；NativeGpu main/已绑定 Picture target 按同一子集调用 native/alpha-tile 边界且自己不 present；producer 仍会清空并重建完整 target，因此 partial dirty、多 dirty 与 scroll 都强制 full redraw/full final damage，不再伪报 partial present。D3D11 WARP real-window readback 覆盖 main 与离屏 mixed 命令、修复离屏 target 错用主 viewport，并覆盖含 CPU fallback 的 `FrameRenderer` direct/Picture producer 到唯一 present 的实际路径；Windows WGL 自动化在 main `FrameEncoder` 执行后、`SwapBuffers` 前捕获真实 drawable，断言 direct/Picture/root-level overlay CPU fallback 的 top-left 像素画序并确认随后最终 `Present`，显式处理 bottom-left readback 与交换后 back buffer 不保证可读。path clip 现由 recorder 及 CPU/NativeGpu legacy main 与 checked Picture 路径在 present/flush 前 typed `NotImplemented` 拒绝；GPU legacy main/canvas 的 scroll copy 也记录同类错误，直接 `RenderBackend::present` 在 native 提交前消费；公开 `Canvas2D::scroll_region` 不再提供默认 no-op，CPU/recorder/GPU/Null/test 的语义均显式实现；不再静默降级。destination-dependent Additive、scroll copy 无等价 IR 时同样 typed failure，而非静默近似；非整数/有状态操作仍为 per-operation full-extent CPU source。缓存 Picture 的 offscreen render 仍为私有 CPU 存储，完整跨 API parity/原生 offscreen render 尚未证明；WGL 只是当前 Windows host 的 `automated`，不等同于多驱动 hardware 或 production 证据 | [`frame_recording`](../src/draw/pipeline/frame_recording.rs) · [`frame_encoder`](../src/draw/pipeline/frame_encoder.rs) · [`render_frame`](../src/draw/pipeline/render_frame.rs) · [`NativeGpuBackend`](../src/draw/backend/native_gpu.rs) · [`picture`](../src/draw/compositor/picture.rs) | 补 destination-dependent blend 与 scroll/copy 的等价 IR/回归，完成 CPU/NativeGpu/GL 的 root/direct/overlay/Picture cross-backend 像素 parity，再处理 retained/damage-aware recording 性能 | 2026-07-12 / 2026-07-13 |
| <a id="bug-20260712-005"></a>BUG-20260712-005 | Correctness / Critical | 高 / 修复中 | 所有 GPU/PixelUpload backend、event loop | `IGraphicsContext` resize/make-current/swap、默认 present 与 `GraphicsEngine::resize` 均可传播；主/副窗口 resize 失败保留 dirty；Picture 与 main `FrameEncoder` execute 的 checked `Result` 均可在 `end_frame` 前终止，后者的 `SurfaceLost` 注入已证明 `RecoveringGraphicsEngine` 将失败保留到下一帧并请求 surface rebuild；PresentUpload 回归确认失败不提交新 extent；external `IPresenter` failure 也按 surface→same recipe→next recipe→Software 替换引擎，OOM 终止，并将非法 end-frame ready 转为 typed failure；D3D11/12 HRESULT 的 device removed/reset/hung 与 OOM 已分别归类为 `GraphicsDeviceLost` / `GraphicsOutOfMemory` 并有纯函数回归；GDI resize、Wayland SHM 与 macOS CALayer 的创建/写入/无 surface/图像构造失败现返回 `Result`，不再静默沿用旧 DIB 或伪报提交成功；尚无实际原生 device/surface-loss 端到端证据 | [`render_frame`](../src/draw/pipeline/render_frame.rs) · [`recovering`](../src/draw/engine/recovering.rs) · [`present_upload`](../src/draw/engine/present_upload.rs) · [`D3D11Context`](../src/native/graphics/d3d11/platform/context.rs) · [`D3D12Context`](../src/native/graphics/d3d12/platform/context.rs) · [`GdiPresenter`](../src/native/backends/windows/gdi_presenter.rs) · [`WaylandPresenter`](../src/native/backends/linux/wayland/presenter.rs) · [`MacosPresenter`](../src/native/backends/macos/platform.rs) · [`application`](../src/app/shell/application.rs) · [`event_loop`](../src/app/event_loop/event_loop.rs) | 注入并验证真实平台 surface/device-loss、候选切换和 Software 终态 present；确认失败帧不清 dirty、指标不记成功 | 2026-07-12 / 2026-07-13 |
| <a id="bug-20260712-006"></a>BUG-20260712-006 | Platform correctness / High | 高 / 修复中 | Linux EGL / OpenGL ES | 现已禁用 partial caps 并删除未验证的 `eglSwapBuffersWithDamageKHR` ABI；partial 输入统一完整 swap，初始化失败和 shutdown 终止已初始化 display；native `OpenGlRasterPipeline` 使用 GLSL ES 3，EGL 仅选择/创建 GLES3，缺少 GLES3 config/context 时清理资源并 typed `NotImplemented`，不再以必然 shader 失败的 GLES2 冒充 fallback；EGL 仅声明其原生 DTO raster caps；仍未在 Wayland 真机验证 preservation/damage、resize 与错误路径 | [`egl.rs`](../src/native/graphics/opengl/platform/egl.rs) · [`OpenGlRasterPipeline`](../src/native/graphics/opengl/raster.rs) · `egl_native_pipeline_does_not_claim_an_unimplemented_gles2_fallback` · `cargo check --target x86_64-unknown-linux-gnu --all-features --tests` · [KHR swap_buffers_with_damage 规范](https://registry.khronos.org/EGL/extensions/KHR/EGL_KHR_swap_buffers_with_damage.txt) | 在 Wayland/GPU 真机补 lifecycle、damage、resize、错误传播与 GLES3 初始化拒绝证据；未验证前不得重新声明 partial redraw | 2026-07-12 / 2026-07-13 |
| <a id="bug-20260712-007"></a>BUG-20260712-007 | Resource / Critical | 高 / 修复中 | Linux Vulkan PixelUpload | acquire/record/submit 前保持 fence 可等待，且仅在即将 submit 时 reset；submit 失败替换为已 signal fence，swapchain/staging 采用先建后换、保留旧链、actual extent/usage/alpha/suboptimal 检查；acquire/present 的 out-of-date 或 suboptimal 在合规完成已 acquire 的 present 后重建 swapchain，并返回 `GraphicsSurfaceLost`，不再伪报 present 成功；Vulkan result 到 surface/device/OOM 的纯分类回归已随 Linux target 测试对象编译 | [`VulkanContext`](../src/native/graphics/vulkan/platform/context.rs) · `cargo check --target x86_64-unknown-linux-gnu --all-features --tests` | 在 Vulkan/Wayland 真机注入 out-of-date、record/submit 失败并验证下一帧恢复、resize 与无泄漏；Windows host 无 Linux linker，当前无目标测试运行证据 | 2026-07-12 / 2026-07-12 |
| <a id="bug-20260712-008"></a>BUG-20260712-008 | Architecture / High | 高 / 修复中 | `IGraphicsContext`、factory/registry、OpenGL native path | 同 API recipe 多行 probe、同 API 回退和 feature-off registry 拒绝创建均已自动化；API 模块及 `glow`/`ash`/`khronos-egl`/Windows Direct3D feature 已随 feature 裁剪，registry create function pointer、API create、Wayland `GpuPresenter` 与 draw raw context constructor/`RenderSession` setter 均已收为 crate-private，`native::create_gpu_context_with_backend` 与 registry raw context create 也仅 native factory bridge 内可见。bootstrap 与 runtime recovery 已复用单一 engine assembly/清理路径，且仅持有 `NativeSurfaceHandle`；factory 现为生产路径唯一的 `IGraphicsContext::initialize` 调用方，`PresentUploadEngine` 使用 factory 的实际 drawable 尺寸创建 CPU session，不再以 null surface 二次初始化；GpuNative startup 不会重建已创建的 swapchain，init-failure 回归从 native make-current 失败验证清理。GL loader、context、shader source、raster program/buffer/texture/FBO 已收回 `native/graphics/opengl`；WGL/EGL context 直接执行 API-neutral DTO，`GpuBackend`/`GpuCanvas2D`/runtime lease 已删除，`draw` 无 `glow` 或 native GL object，并有架构反向扫描及 WGL 有序 offscreen 自动化。factory bridge 外仍有历史 platform raw-pointer API，且其他 API 的 surface/presenter/raster 拆分与真机 lifecycle 证据未齐 | [`registry`](../src/native/factory/registry.rs) · [`factory bridge`](../src/native/factory/mod.rs) · [`graphics module`](../src/native/graphics/mod.rs) · [`bootstrap`](../src/draw/engine/bootstrap.rs) · [`present_upload`](../src/draw/engine/present_upload.rs) · [`present trait`](../src/native/traits/present.rs) · [`OpenGlRasterPipeline`](../src/native/graphics/opengl/raster.rs) · [`architecture guard`](../src/tests/architecture/boundaries.rs) | 逐 API 清除 factory bridge 外 raw surface 入口，并补 EGL/其他 API 的同等边界与平台 lifecycle 证据 | 2026-07-12 / 2026-07-13 |
| <a id="bug-20260712-009"></a>BUG-20260712-009 | Correctness / High | 高 / 修复中 | Native GPU hybrid、GL、CPU rasterizer、blend | CPU Additive 有精确回归；不具 destination 的 GL/NativeGpu CPU segment 现以 `NotImplemented` typed 拒绝，避免透明 buffer alpha-over 伪正确；GL source crop、clip 下 Picture 画序已有 WGL `automated`，GL CPU fallback 现跳过透明像素并逐行上传/合成紧包围盒，WGL real-window 回归断言传输量小于整张纹理；NativeGpu 现传递 API 中立的最小 alpha-visible `SoftFallbackTile`，D3D11/D3D12 只上传、复制和采样该 tile，未实现 partial tile 的 context typed 拒绝而不伪装为全帧成功；D3D11 与 GL soft tile 均以 premultiplied blend（`ONE`, `INV_SRC_ALPHA`）合成，避免半透明 RGB 二次乘 alpha；Software/D3D11 WARP/D3D12 WARP 共同 hybrid/clip/bounded-tile probe script、Software/D3D11 WARP Picture source-crop→post-blit clip script，以及 Software/WGL 半透明 soft tile probe 均已 `automated` 通过；这些 WARP/WGL 证据不等于多驱动硬件或 production，其他 API 与完整场景仍待覆盖 | [`NativeGpuBackend`](../src/draw/backend/native_gpu.rs) · [`IGraphicsContext`](../src/native/traits/present.rs) · [`D3d11Pipeline`](../src/native/graphics/d3d11/platform/pipeline.rs) · [`D3d12Pipeline`](../src/native/graphics/d3d12/platform/pipeline.rs) · [`OpenGlRasterPipeline`](../src/native/graphics/opengl/raster.rs) · [`SharedRasterizer`](../src/draw/engine/cpu/shared_rasterizer.rs) | 补其他 API 的 blend/clip/offscreen parity 与各 API 的传输量断言；不可将 WARP/WGL 自动化外推为硬件或 production | 2026-07-12 / 2026-07-13 |

## 待验证

| ID | 严重度 | 置信度 | 状态 | 影响范围 | 摘要 | 修复要点 | 来源 / 自动化证据 | 下一步 | 最后更新 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| <a id="bug-20260712-003"></a>BUG-20260712-003 | High | 高 | 待验证 | Windows 主窗（后端无关；D3D11 实机复现） | 最小化后再最大化客户区黑屏 | 主循环 `WindowMaximize` / 正尺寸 `WindowResize` 恢复 `window_visible` 并标脏；与副窗 `application` 对齐 | 上游提交 `9cd7331` 记录实机复现，但仓内未附可达截图；[`hidden_window_repaints_on_maximize`](../src/tests/app/event_loop/event_loop.rs) 已自动化 | 实机：最小化 → 任务栏最大化 / 还原，确认铺满重绘并归档证据 | 2026-07-12 |
| <a id="bug-20260711-002"></a>BUG-20260711-002 | High | 高 | 待验证 | Windows 输入页自绘 Input | Input 获得焦点后不接收文本，Tab 也不移动焦点 | `Input::sync_from` 保留 runtime value；PointerDown 不再本地抢焦点；remove 时派发 FocusOut | [首次复现](../test-reports/20260711-082208-uix-demo/report.md)；[复测](../test-reports/20260711-190000-uix-comprehensive/report.md)确认 ASCII 已恢复、Tab 仅有 FakePlatform 回归；[`event_loop`](../src/tests/app/event_loop/event_loop.rs) | 在真实 GUI 中复验 Tab 焦点遍历；系统 IME composition 另由 RISK-20260712-001 跟踪 | 2026-07-12 |

<a id="活跃风险与阻塞"></a>

## 活跃风险与阻塞

| ID | 类型 | 影响 | 状态 | 当前证据 | 下一动作 | 关联 |
|----|------|------|------|----------|----------|------|
| <a id="risk-windows-ime"></a>RISK-20260712-001 | P6 验收风险 | High | 已确认 | ASCII 输入、Tab 路由与 TSF 最小 TextStore 已自动化；[综合报告](../test-reports/20260711-190000-uix-comprehensive/report.md)未形成真实系统 IME 候选/提交证据 | 按 OPEN-005 准则完成 Microsoft Pinyin 前台 composition 会话 | [REQ-002a](产品.md#req-002a) · [OPEN-005](#open-ime-production) |
| <a id="risk-gpu-matrix"></a>RISK-20260712-002 | P6 验收风险 | High | 已确认 | D3D11 / Software / OpenGL ES 已有单 GPU 自动化与 GUI 证据；多驱动和前台 capture 未覆盖 | 在至少一个额外驱动/设备环境复跑初始化、present、resize、主题和关闭矩阵并保留前台证据 | [进度 · 证据矩阵](进度.md#平台与后端证据矩阵) |
| <a id="risk-macos-hardware"></a>RISK-20260712-003 | 平台交付风险 | Medium | 已确认 | AppKit / Metal / IME 路径已编码；本轮 `cargo check --target x86_64-apple-darwin --all-features --tests` 仅 cross-compile 通过 | 先裁决 OPEN-001 的验收准则，再在 macOS 真机执行启动、输入、resize、present 与关闭 smoke | [REQ-005](产品.md#req-005) · [OPEN-001](#open-macos-acceptance) |
| <a id="risk-graphics-thread-affinity"></a>RISK-20260712-004 | 内存安全风险 | Critical | 修复中 | 已删除 `RenderBackend`/关键 context 的无依据 `unsafe impl Send/Sync`，`RenderBackend` doctest 证明安全 API 不能将其作为 `Send` 传递；registry 与 D3D12 WARP 工厂已将生产/测试 context 统一包装为 `ThreadBoundGraphicsContext`（`!Send + !Sync`），其 `initialize`/present/offscreen 等 `Result` 操作在错误线程返回 `InvalidState` 且不触达 native object；readback 已收为唯一 `IGraphicsContext::read_pixels -> Result<Vec<u32>>`，thread-bound 在错误线程拒绝、owner-thread 注入 `GraphicsDeviceLost` 原样传播，WGL/EGL/D3D11/D3D12 不再用空像素吞 native 失败；D3D12 已覆写 public `try_shutdown` 以直接传播 terminal fence drain 的 `shutdown_result`，WARP real-window fault 由该入口验证收尾与幂等，不再被 void fallback 伪报成功；caps/native caps/width/height/DPR 改为 owner-thread snapshot，错误线程读取这些 legacy metadata 不再触达 native object；checked offscreen destroy 已经 native backend、engine wrapper、Picture cache 与 LayerTree orphan sweep 传播，注入失败时保留 target slot/handle、不可回收并可重试；公开 `PlatformWindowCore::with_gpu` 已收为 crate 内部，不能从公开窗口 API 注入裸 context；`RenderSession` 在 checked staged shutdown 失败时保留旧 context 并关闭 replacement，`PresentUploadEngine` 与 `RecoveringGraphicsEngine` shutdown/Drop 幂等；GL API objects 已全部留在 native `OpenGlRasterPipeline`，draw 架构扫描禁止其回流 | shutdown 仍是无返回值兼容 hook；错误线程 Drop、WGL/EGL teardown failure 和其他平台 API 生命周期仍缺目标平台证据；只有命令数据可跨线程 | [#183](决策.md#d183) · [#187](决策.md#d187) · [#191](决策.md#d191) · [#195](决策.md#d195) · [#198](决策.md#d198) · [P6.9 GFX-R3/R4](进度.md#p69-图形契约整改) |
| <a id="risk-graphics-resource-topology"></a>RISK-20260712-005 | 性能 / 资源风险 | High | 已确认 | 每窗口独建 device/queue/pipeline/cache；D3D11 每次 present 重建 RTV，native batch 临时分配，D3D12 device fault 以 clone+forget 保留资源图；4K soft fallback 可约 33 MiB/帧 | 正确性门槛闭合后拆 `GraphicsDevice`/`WindowSurface`、复用缓存与 scratch、局部上传并 profile 多窗/长跑；故障资源用可证明的延迟回收 | [#183](决策.md#d183) · [#185](决策.md#d185) · [P6.9 GFX-R6](进度.md#p69-图形契约整改) |
| <a id="risk-graphics-platform-evidence"></a>RISK-20260712-006 | 平台交付风险 | High | 已确认 | feature-off graphics API 模块与 `glow`/`ash`/`khronos-egl`/Direct3D dependencies 均已裁剪；WGL/D3D11/D3D12 已共享 logical client/drawable DPR，创建、resize 和 caps 由同一计算源驱动，192 DPI 纯回归、D3D11/D3D12 real-window caps 回归均通过；本次 D3D12 automatic real-window test 在 NVIDIA GeForce RTX 4070 Ti SUPER 上通过 hardware clear/draw/readback/present 一次，但不是高 DPI 或 production 证据。当前 Windows host 的只读 `GetDC`/`GetDeviceCaps(LOGPIXELSX)` 为 96（DPR 1.0），不能提供高 DPI 验收。native EGL raster 现明确要求 GLES3，缺少相等 pipeline 的 GLES2 配置/上下文返回 typed `NotImplemented`；Metal 名称为 CPU PixelUpload 且在调用 AppKit image 构造前共享校验像素长度；macOS `MetalPixelUploadContext` 与 `MacosPresenter` 的 CFData/provider/colorspace/image 创建失败现可传播，但 EGL/Vulkan Linux 与 Metal/AppKit macOS 本轮仍只有 target `compiled`，macOS caps/DPR、真实平台错误路径、resize 和硬件矩阵仍缺证据 | 在高 DPI WGL/D3D HWND 真机补 actual extent/DPR、Wayland resize/out-of-date、EGL lifecycle/damage/GLES3 reject、Vulkan fault、Metal present 失败真机矩阵 | [#186](决策.md#d186) · [证据矩阵](进度.md#平台与后端证据矩阵) |

## 已关闭

| ID | 摘要 | 关闭原因/修复 | 验证证据 | 关闭日期 |
| --- | --- | --- | --- | --- |
| <a id="bug-20260712-001"></a>BUG-20260712-001 | `InputNumber` 在 root reconcile 后回退默认值 | 仅显式 `.value(...)` 覆盖 runtime 值；未配置值保留并按边界夹紧 | [本轮 GUI 报告](../test-reports/20260712-100000-uix-demo-retest/report.md)；`uncontrolled_value_survives_reconcile`；全量测试 | 2026-07-12 |
| <a id="bug-20260712-002"></a>BUG-20260712-002 | 主题副窗口的 `Alt+F4` 被吞，无法关闭 | `WM_SYSKEYDOWN` / `WM_SYSKEYUP` 投递 UI 事件后交还 `DefWindowProcW` | [本轮 GUI 报告](../test-reports/20260712-100000-uix-demo-retest/report.md)：`Alt+F4` 与标题栏关闭实机通过 | 2026-07-12 |
| <a id="bug-20260712-010"></a>BUG-20260712-010 | D3D11 offscreen 忽略 source crop 且泄漏 scissor | source UV 由 `src` 传入 shader；blit 临时 raster state 后恢复，soft fallback 显式全纹理 UV | [`d3d11_warp_offscreen_crop_ignores_and_restores_previous_raster_state`](../src/native/graphics/d3d11/platform/context.rs) + [`d3d11_soft_blit_ignores_a_previous_native_scissor`](../src/native/graphics/d3d11/platform/context.rs)；Windows WARP `automated`，不是多驱动/production 硬件证据 | 2026-07-12 |
| <a id="bug-20260711-004"></a>BUG-20260711-004 | Transfer 首行点击命中偏移 | hit-test 与绘制统一减去 24px 列表头偏移 | [首次复现报告](../test-reports/20260711-082208-uix-demo/report.md) · [复测](../test-reports/20260711-190000-uix-comprehensive/report.md) | 2026-07-11 |
| <a id="bug-20260711-006"></a>BUG-20260711-006 | Upload 忽略 `.png,.jpg` accept | 模拟文件名改取 `accept` 的首个扩展名 | [首次复现报告](../test-reports/20260711-082208-uix-demo/report.md) · [复测](../test-reports/20260711-190000-uix-comprehensive/report.md) | 2026-07-11 |
| <a id="bug-20260711-007"></a>BUG-20260711-007 | 反馈页 Modal/Drawer 不可操作或被裁切 | 修正关闭态 hit-test、Overlay 拓扑/失效、Drawer surface 布局与 D3D11 soft-blit scissor | [首次复现报告](../test-reports/20260711-082208-uix-demo/report.md) · [复测](../test-reports/20260711-190000-uix-comprehensive/report.md) | 2026-07-11 |
| <a id="bug-20260711-001"></a>BUG-20260711-001 | 动态刷新交替出现完整帧与黑块/空白帧 | D3D11 重建 RTV；OpenGL ES 禁止 `clear_all` 继承 scissor | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)：D3D11 与显式 OpenGL ES 连续动态帧、OpenGL ES 副窗均完整 | 2026-07-11 |
| <a id="bug-20260711-003"></a>BUG-20260711-003 | 暗色主题固定色文字对比不足 | 主题敏感色改为 `ColorValue::Neutral` / `Palette` | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)：641×481 暗色应用能力页可读 | 2026-07-11 |
| <a id="bug-20260711-005"></a>BUG-20260711-005 | 640×480 Transfer/Upload 不可达 | Transfer 按约束/实际 frame 布局，页面改为纵向全宽 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；窄窗 GUI 回归 | 2026-07-11 |
| <a id="bug-20260711-008"></a>BUG-20260711-008 | D3D11 长会话 Present 内存过大 | Present 前释放 RTV 后重建；soft/offscreen 复用或按需分配 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)：15 分钟会话的工作集、私有字节、句柄与专用显存稳定 | 2026-07-11 |
| <a id="bug-20260711-010"></a>BUG-20260711-010 | Typography 向上跨节点拖选失效 | 树层在 pressed Move 期间协调同父级跨节点选区 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；实际渲染 glyph 回归 | 2026-07-11 |
| <a id="bug-20260711-011"></a>BUG-20260711-011 | 跨 Typography 选区复制不完整 | 聚合跨节点选择，Ctrl+C 走 `SystemEvent::Copy` | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；实际渲染 glyph + FakeClipboard 回归 | 2026-07-11 |
| <a id="bug-20260711-012"></a>BUG-20260711-012 | 窄短窗内容和滚动条不可达 | ScrollView 双轴溢出、尺寸约束和父级收缩修正 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)：641×481 首页横条与内容可达 | 2026-07-11 |
| <a id="bug-20260711-013"></a>BUG-20260711-013 | ScrollView 滑块不可拖动或首次跳变 | gutter、累计偏移和 grab offset 修正 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)：横、纵滑块实机拖动；29 项 ScrollView 回归 | 2026-07-11 |
| <a id="bug-20260711-014"></a>BUG-20260711-014 | D3D11 放大后旧尺寸黑边 | 每帧校正 surface/client，刷新 canvas、tree 和 clip | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)：641×481 至最大化均铺满客户区 | 2026-07-11 |
| <a id="bug-20260711-015"></a>BUG-20260711-015 | 无焦点 IME 事件仍产生额外工作 | `ensure_surface_matches_window` 在 resize 后仍无有效 canvas 尺寸时不再报告校正，避免每次 wake 强制 layout | 原始 IME 回归、相邻 `event_loop` 45 项、`cargo test --all-targets` lib 1162/1162 + demo 22/22 | 2026-07-11 |
| <a id="bug-20260711-016"></a>BUG-20260711-016 | 无失效键盘事件触发额外 layout | 同上：仅有效 surface 校正才进入布局路径 | 原始键盘回归、相邻 `event_loop` 45 项、`cargo test --all-targets` lib 1162/1162 + demo 22/22 | 2026-07-11 |
| <a id="bug-20260711-017"></a>BUG-20260711-017 | `GpuBackend` capability 的 offscreen 语义不一致 | non-partial context 回到 `gpu_full_redraw`；partial context 保持 `gpu_with_offscreen` | capability 双分支回归、相邻 present/offscreen caps 回归、`cargo test --all-targets` lib 1162/1162 + demo 22/22 | 2026-07-11 |
| <a id="bug-20260711-018"></a>BUG-20260711-018 | Interval 回调中 cancel 后被重新注册，零周期可同 tick 空转 | entry 共享 active 标记与 cancel epoch；零 interval 归一为 1 ms | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；回调 cancel、queue teardown、zero interval 回归 | 2026-07-11 |
| <a id="bug-20260711-019"></a>BUG-20260711-019 | WindowSession 关闭跳过组件 `inactive→unmount→detach→destroy` | `WindowSession::shutdown` 先执行幂等 `WidgetTree::shutdown`，再关闭引擎 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；生命周期完整序列回归 | 2026-07-11 |
| <a id="bug-20260711-020"></a>BUG-20260711-020 | 根窗退出时迟到的 `open_window` session 可遗留 timer、队列和 alive handle | `AppRuntime::shutdown_all` 封闭新 session、清空 live/pending session、timer、队列和 theme | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；root/child/pending/late request 回归 | 2026-07-11 |
| <a id="bug-20260711-021"></a>BUG-20260711-021 | Pagination 的 0 page size 或极大 current 触发除零/溢出 panic | page size 归一为 1，current 夹紧，next/可见范围饱和计算 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；0/`usize::MAX` 回归 | 2026-07-11 |
| <a id="bug-20260711-022"></a>BUG-20260711-022 | Descriptions 的 0 column 在布局/渲染时除零 panic | builder 和 reconcile 均将 column 归一为 1 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；0 column measure/snapshot 回归 | 2026-07-11 |
| <a id="bug-20260711-023"></a>BUG-20260711-023 | Image 缩放到零宽/高目标时除零 panic | rasterizer 对空目标提前 no-op | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；零宽/高像素不变回归 | 2026-07-11 |
| <a id="bug-20260711-024"></a>BUG-20260711-024 | VirtualList 极大/非有限 offset 或 overscan 可溢出并生成错误范围 | offset 归零、range 夹紧、overscan 饱和加法 | [本轮 GUI 报告](../test-reports/20260711-190000-uix-comprehensive/report.md)；`NaN`/无穷/`f32::MAX`/`usize::MAX` 回归 | 2026-07-11 |
| <a id="bug-20260711-009"></a>BUG-20260711-009 | 点击按钮后焦点环停在原屏位置，滚动后不随控件移动 | 先修 viewport 映射；后由 [#177](决策.md#d177) **取消全局焦点环**，问题不再适用 | `node_viewport_frame_follows_scroll_offset`；`visible_viewport_rect_*`；LayerTree 不再 stroke 焦点环 | 2026-07-11 |

<a id="待决"></a>

## 待决

| ID | 状态 | 影响 / 决策问题 | 所需证据或触发条件 | 下一动作与关联 |
|----|------|-----------------|--------------------|----------------|
| <a id="open-macos-acceptance"></a>OPEN-001 | 待裁决 | macOS 真机验收应达到何种证据级别与通过准则 | 可用真机、目标 OS/硬件范围及桌面平台最低 production gate | 产品裁决后转为 P6 验收项；若延期则另立阶段。[RISK-20260712-003](#risk-macos-hardware) |
| <a id="open-mobile-stack"></a>OPEN-002 | 待裁决 | 移动端首批 OS 与图形栈 | P6 退出、目标用户/设备优先级和可用原生 toolchain | 裁决后使用决策台账下一编号；[#166](决策.md#d166) · [REQ-005](产品.md#req-005) |
| <a id="open-fullstack-domain"></a>OPEN-003 | 待裁决 | 网络/同步进入 `data` 还是新域 | 首个真实用例、依赖方向、离线/同步边界与公开 API 草案 | 裁决前保持当前范围外；[#166](决策.md#d166) |
| <a id="open-accessibility-bridge"></a>OPEN-004 | 待裁决 | 屏幕阅读器平台桥的产品范围与平台顺序 | 目标辅助技术、语义树契约、事件回写与验收工具 | 裁决后新增阶段并使用决策台账下一编号；[#99](决策.md#d99) · [REQ-009](产品.md#req-009) |
| <a id="open-ime-production"></a>OPEN-005 | 待裁决 | Windows production 应覆盖哪些 Microsoft Pinyin 版本、场景与通过准则 | 至少覆盖 preedit、候选、commit、取消、焦点切换与 Tab 遍历的前台可观察证据 | 先完成最小真实会话，再由产品裁决准则；[RISK-20260712-001](#risk-windows-ime) · [REQ-002a](产品.md#req-002a) · [#75](决策.md#d75) |
| <a id="open-gpu-roadmap"></a>OPEN-006 | 待裁决 | Metal `GpuNative` 与 D3D12 完整能力扩展的先后顺序 | [P6.9](进度.md#p69-图形契约整改) 闭合、目标平台收益、缺失 caps 与真机环境 | 仅在 P6.9 契约关闭后裁决扩展顺序；[#169](决策.md#d169) · [#181–#186](决策.md#d181) |
| <a id="open-computed-capture"></a>OPEN-007 | 待裁决 | 源码公开支持 `Computed` capture fingerprint，但 [#142](决策.md#d142) 仅授权 State 与 `WindowId` | [`HandlerRegistration::with_computed_capture`](../src/ui/event.rs)、[`view`](../src/ui/view/mod.rs)、[`macros`](../src/ui/macros.rs) 已形成公开实现；缺持久裁决 | 接受现实现则用下一编号 `amends #142`；否则另开产品代码整改项，本轮不改代码 |
| <a id="open-continuous-pointer-shape"></a>OPEN-008 | 待裁决 | [#121](决策.md#d121) 要求独立 `WantsContinuousPointerMove` trait，源码实现为 [`EventHandler::wants_continuous_pointer_move`](../src/ui/traits/widget.rs) 方法 | 核对公开扩展面、对象安全与 opt-in 语义是否等价 | 接受现实现则用下一编号 `amends #121`；否则另开产品代码整改项，本轮不改代码 |
| <a id="open-brand-primaries-signature"></a>OPEN-009 | 待裁决 | [#57](决策.md#d57) 记为 `from_primaries([12])`，源码签名另含 `is_dark` | [`from_primaries([Color; PRIMARY_COUNT], is_dark)`](../src/ui/theme/design_tokens/presets.rs)；须确认历史写法是概念简称还是公开 API 约束 | 若为简称，用下一编号澄清；若为约束，另开产品代码整改项，本轮不改代码 |
