# 交付方向

[← 进度索引](../进度.md)

> **接口**：声明 UIX 当前的交付方向——更小（运行内存、使用方编译与产物）、更快（帧率）、更稳（跨平台）的目标、已做工作、当前瓶颈、已立项与未排期长期目标。依赖：[定位与原则](../产品/定位与原则.md)。导出：运行性能状态 → [性能与内存](性能与内存.md)，使用方构建状态 → [使用方按需编译](使用方按需编译.md)。

## 适用线索

当需要回答"现在在追什么目标、做了哪些优化、瓶颈在哪"时，本组件是权威来源。

## 权威范围

框架核心功能已收敛，当前聚焦三个交付方向——缩小运行内存与使用方构建成本、降低每帧耗时至稳定 60Hz、四平台同等视觉矩阵。

### 进度总览

| 状态 | 事项 |
|------|------|
| ✅ 已完成 | Windows 生产 GPU factory/default feature 切换到原生 D3D11 并移除 wgpu；生产只有一个 `draw::Renderer`；字形缓存收敛至 2048 项/8 MiB LRU、Picture 缓存 32 MiB 硬预算、DisplayList COW Arc 共享延迟分配、热路径 scratch 复用、布局失效优化、到期工作清退、动画注册原位同步、AppTimer 变更驱动、浮层修复（Modal/Drawer 布局+backdrop） |
| 🔄 进行中 | D3D11 原生绘制覆盖与完整视觉矩阵、MSDF 多通道字形视觉矩阵、当前生产路径的内存/帧耗时重新建立基线；代表性首帧真实视觉验证已闭合 |
| 🔄 进行中 | 使用方按需编译：locked 基线与能力映射已建立，`image-codecs`、`qrcode`、窄粒度 `form-pattern` 与演示专属 `demo-logging` 已完成专属依赖和对应源码门控，并通过八个正向 release 与三个禁用公开面 compile-fail 场景；schema v3 新增 Linux 最小正向 check，使十二场景分别绑定 Windows/Linux target，并验证 `windows`/`windows-core` 与 `libc`/`wayland-client` 只进入各自目标；`windows-core` 已收缩到 Windows target，零引用 `raw-window-handle` 与未使用的 `bytemuck` 直接边已纳入同一 resolved graph 防回归门禁，`bytemuck` 仅随 `image` 合法传递；其余能力及对应 compile-fail、Linux release、其他 target/backend 与产物矩阵仍待完成 |
| 🔄 进行中 | 图形后端收敛：已落地 `FramePlan` / `RenderPassPlan`、buffer/texture 资源上传、局部 `ClearRect`、同纹理 `TextureMove` 与 platform 私有薄 RHI 契约及记录型 adapter 验证；D3D11 与可选 `opengles` feature 下的 WGL/EGL OpenGL ES 已接入 surface 生命周期、solid mesh、SrcOver/Additive textured quad、SrcOver/Additive shape rect、gradient、仿射 R8 glyph coverage、RGBA8 MSDF glyph、轴对齐与变换圆角/描边矩形、原生扇形、仿射 box shadow、Picture texture 合成与两段 blur 的 device/pass/draw 子集；OpenGL ES WGL/EGL legacy queue 已补齐 native 描边矩形、box shadow、线性/径向渐变、扇形、solid mesh 与 BGRA 仿射 image blit，D3D11 legacy queue 已补齐 BGRA 仿射 image blit 与 affine linear/radial gradient，D3D11/OpenGL ES legacy offscreen texture blit 已补齐组 opacity 与 Additive，WGL/EGL 交换失败已映射为 typed surface/device recovery error；可表达的整条 `FrameEncoder`、`DrawSurface` scroll、原生 `Canvas2D::scroll_region`、主 surface native→soft sampled segment、Picture native→soft 离屏 segment 和 Picture source 接续主 surface soft 前缀已复用同一 ordered RHI plan，ClearRect/TextureMove 由 adapter capability/probe 门禁，MSDF 已加入带 gutter 和子区域上传的有界多页 atlas 并增加固定预算/shelf 单测，且大字号旋转 Watermark 已通过 D3D11/OpenGL ES 真窗 manifest 翻页、目标可见性和最终 present 专项；扇形 uniform ABI 已统一为共享 64 字节常量；WGL 已通过首帧前固定 pipeline/resource probe、正式 RHI surface present 与 1200×800 真窗截图；`GpuBackend::resize` 已改为 RHI surface 优先并在成功后刷新线程绑定 drawable 元数据，D3D11 RHI surface 与 OpenGL WGL/EGL surface 均已直接收口到各自 native drawable/RTV/pipeline resize helper，D3D11 Upload 视觉用例已通过 `1200×800 → 900×640 → 1200×800` 双向 resize，D3D11 与 OpenGL ES WGL RHI 最终提交前已接入 typed device/surface lower injection，均已通过三轮 teardown/rebuild 后续交互验收（第三轮交错另一类故障）；GPU 首帧可见性契约已修复，生产 `retained_framebuffer` 能力仍未开启，其他图元、专用大字号 WGC 像素快照、完整 Picture 语义、真实 surface lost/物理 device lost 及多 DPI/GPU/OS 视觉验证仍待完成；目标契约见[backend](../架构/graphics/backend.md) |
| 🔄 进行中 | **全面检查布局（最高优先级）**：共享有限几何、Flex/Grid 算法边界、ScrollView/VirtualScroll、声明协调与空子树状态、Flex 固有/溢出/换行细分边界、空子集账本、RichText 宽度测量缓存、Notification 窄窗留白、Table 固定列重叠命中、重叠句柄命中、选择列覆盖命中、展开控件覆盖命中、分组表头、跨层表头、行合并重绘固定区层级、普通文本跨固定区列合并几何、自定义 View 跨固定区多片段、Flex 未分行真实交叉轴、Popover/Popconfirm 同帧表面定位、Tooltip/Slider 表面约束提示几何、Select、Cascader、TreeSelect、AutoComplete、Mentions、DatePicker、DateRangePicker、TimePicker 与 ColorPicker 弹层同帧表面几何、Flex 单行溢出反向引导镜像、未分行溢出换行等价矩阵、实际多行溢出轴转置、正反向镜像、弹性冻结矩阵及负间距内容账本共五十一批已闭合；Flex 实际分行负 margin/gap 其余组合及上述矩阵以外的组合、表格其他合并组合的组件级布局一致性、其余组件失效分类、可变行高缓存及真窗视觉验证仍在推进，见[目标：全面检查布局](#目标全面检查布局) |
| ⏳ 待开始 | 复杂文本排版（已立项）：复杂脚本 shaping、双向文本、字素簇光标、完整 UAX#14 断行，范围与完成条件见[目标：复杂文本排版](#目标复杂文本排版) |
| ⏳ 待开始 | 丰富富文本组件（已立项）：Markdown 元素解析与段类型扩展、measure/render 测量统一、样式与内联富元素，见[目标：丰富富文本组件](#目标丰富富文本组件) |
| 📋 规划中 | 四平台同等视觉与图形矩阵；Windows 默认使用 D3D11，启用 `opengles` feature 时 WGL/EGL OpenGL ES 可进入构造路径，其他原生 API 与跨平台真机验证仍待闭合；窄 present 需要后端提供可靠的 retained/partial-present 事实能力 |

**活跃阻塞**：D3D11 与 OpenGL ES 仍通过逐 UI 操作的 `IGraphicsContext` 兼容门面承载尚未迁移的语义；OpenGL ES legacy queue 已收回当前生产 UI 所需的渐变、扇形、solid mesh、BGRA image blit、描边与阴影，D3D11 legacy queue 也已收回 BGRA affine image blit，D3D11/OpenGL ES legacy Picture blit 也已支持组 opacity 与 Additive；大字号旋转 Watermark 的真实 manifest/present 已通过，但专用 WGC 像素快照、完整图元/Picture parity、真实物理 surface/device lost 及多 DPI/GPU/OS 运行矩阵未闭合，当前 `test-harness` lower injection 已证明 D3D11 与 Windows WGL OpenGL ES 各三轮 teardown/rebuild 边界，第三轮交错另一类故障。D3D11 为 `FullOnly` present 且不保留主 framebuffer，生产 `retained_framebuffer` 仍关闭。

## 未排期目标：性能与资源持续优化

**状态：📋 规划中（未排期）。** 在不牺牲正确性、稳定性、可恢复性和跨平台一致性的前提下，持续降低使用方应用的启动与首帧时间、输入响应和稳定帧成本，并减少 CPU、GPU、内存、线程、磁盘 I/O、缓存与空闲能耗。使用方的编译成本和 release 产物体积由[使用方按需编译](使用方按需编译.md)目标持有。

本条只确立长期优化方向，不代表已经立项或正在执行；当前不指定开始时间、执行顺序、量化预算或绑定版本。未来决定执行时，先为目标平台和代表性场景建立可复现基线，再单独确定预算、优先级与验收门禁。

## 未排期目标：收敛后端图形 API

**状态：🔄 进行中（底层契约、两个 adapter 与首个绘制子集）。** 目标采用[通用 GPU Renderer + 薄原生 RHI](../架构/graphics/backend.md)：矩形、路径、字形、图片、Picture、离屏、模糊、混合、batch 与 atlas 等 UI 图形语义留在通用层，D3D11、D3D12、Vulkan、Metal、OpenGL adapter 只承担资源、pass、draw/copy、submit 与 present。当前已建立有序 `FramePlan`、资源上传契约，并由 D3D11 与可选 OpenGL ES WGL/EGL adapter 执行 solid mesh、SrcOver/Additive textured quad、仿射 linear/radial gradient、仿射 R8 glyph coverage、RGBA8 MSDF glyph、轴对齐与变换圆角/描边矩形、原生扇形、仿射 box shadow、Picture texture 合成与两段 blur 子集；MSDF 已加入带 gutter 和子区域上传的有界多页 atlas，且大字号旋转 Watermark 已通过两个 adapter 的真实 manifest/present 专项；扇形 uniform ABI 已统一为共享 64 字节常量；WGL 已完成首帧 probe 与实际 RHI submit 运行验证；生产路径仍保留兼容高层绘制调用。

当前已有共享的 `RenderBackend` / `GpuBackend`，但 `IGraphicsContext` 和 `NativeRasterCaps` 仍按逐 UI 操作组织尚未迁移的原生能力，且其他完整旋转/仿射图元、专用大字号 WGC 像素快照、Picture 的完整语义 parity、resize/lost 运行矩阵尚未闭合，因此目标尚未完成。本条不指定开始时间、执行顺序、版本或交付承诺。

完成声明需要 D3D11 参考 adapter 和至少第二个原生 adapter 复用同一帧计划与资源策略、bootstrap 能拒绝不满足 GPU 基线的实现，并以真实 adapter 覆盖图片、Picture、opacity、additive、offscreen、blur、resize、surface/device lost 及一次最终 present 的视觉或像素验证；当前已闭合代表性 Demo 的首帧最终 present 与 D3D11 双向 resize，尚未闭合其余矩阵。

## 目标：全面检查布局

**状态：🔄 进行中（最高优先级，主人 2026-08-05 决定）。** 布局是组件正确性的地基：历史缺陷（测量/绘制/命中分叉、越界、负区域、未请求布局收缩等）分散记录在[组件稳定性](组件稳定性.md)的逐组件修复条目中，现正系统检查 `src/ui/layout/`（`engine`/`flex`/`grid`）与各组件自有布局辅助。本目标覆盖：

- **不变量审计**：对照[架构 · layout](../架构/ui/layout.md)不变量逐条核验——结果尺寸与 frame 有限非负、measure/paint/hit-test 对同一组件使用同一实际 frame、无界轴只走约束语义而不把 `f32::MAX` 写入实际 frame、子 frame 收敛后同步父级可见性、布局不执行业务 I/O / 任意应用 callback / present / 跨窗 wake
- **算法边界**：Flex/Grid 的 grow/shrink/wrap/gap/span/align 在负剩余空间、极小/极大约束、非有限输入与空子集下的行为；margin 推开兄弟、border/padding 收敛到 content rect、阴影不影响 content box 的语义
- **组件级一致性**：浮层、表格、富文本、通知等自带布局辅助的组件与共享 `LayoutEngine` 收敛；各组件修复记录中的历史缺陷类别全量回归复核
- **增量与缓存**：Layout dirty 标记边界（paint-only 变化不触发、几何动画触发）、布局 scratch 与输出快照的树版本/约束失效、虚拟滚动物化范围边界
- **验证**：真窗视觉矩阵（含既有 92 组件回归）、行为断言与像素验证，并在当前 D3D11 生产路径确认

**首批进展（2026-08-05，`7de6fcd8`）**：共享 BoxModel/Flex/Grid 已在公开布局边界归一非有限值、负尺寸与 `f32::MAX` 测量哨兵；margin 保留有限负值语义，border/padding 与 Grid gap 收敛为有限非负值；共享入口未新增 track 归一化副本，纯 Grid 求解器继续借用 columns 并沿用既有 implicit row 缓冲。`tests/layout_invariants.rs` 固化五项契约：修复前四个病理输入用例均可复现失败，修复后五项全通过，且正常 Flex/Grid 精确几何保持不变。本批只闭合有限几何与盒模型入口。

**第二批进展（2026-08-05，`7e03fe41`）**：Flex 现先落实 min/max 再分行，以无额外分配的有界迭代把 grow/shrink/Stretch 在子项触顶或触底后的剩余空间转交给仍可伸缩的兄弟；wrapped 零尺寸 bootstrap 保持固有主轴自然尺寸，单行与 wrapped 行账本均把 margin 纳入交叉轴总量，Center/End 在扣除非对称 margin 后对齐，固有反向主轴按实际内容镜像。六个新增聚焦断言在修复前稳定失败；修复后公开布局契约 9/9、内部 Flex 边界 4/4、库测试 101/101，并通过公开 API、使用门面、无默认特性库与全特性全目标检查。本批仍不代表 Grid、Flex 其余组合、组件、缓存或真窗验证已经完成。

**第三批进展（2026-08-05，`33721e4a`）**：Grid 现从子项的有限测量尺寸与 margin 解析 `Auto` 列/行，不再把 `Auto` 当作等权 `1fr` 填满容器；`Px` 与 `Auto` 先锁定，正权重 `Fr` 只分配剩余空间，不含正权重 `Fr` 的跨格子项会扣除已知轨道与内部 gap 后在所覆盖 `Auto` 轨道间均分缺口。修复前三个列轴聚焦断言稳定失败；修复后新增行轴对称覆盖，公开布局契约 13/13、库测试 101/101、公开 API 2/2、使用门面 5/5，无默认特性库与全特性全目标检查均为 0 错误。显式超大 `grid_cell`/`row_span` 仍可推高稠密 occupied 矩阵的资源需求，与 Grid/Flex 其余组合、组件、缓存及真窗矩阵一同留待后续批次。

**第四批进展（2026-08-05，`f2ccba05`）**：Grid 放置层现以单轴 4096 条轨道和总计 65,536 个稠密占用单元格建立双重预算，超限列/行、`grid_cell`、column/row span 均确定性收敛，预算耗尽的自动子项保留零 frame；空闲矩形搜索改为复用二维前缀和，消除无界扩行循环与 span 面积级重复探测。两个 5000 行级安全断言修复前失败；修复后公开布局契约 16/16、内部放置边界 6/6（含 4608 组小矩阵穷举对照）、库测试 107/107、公开 API 2/2、使用门面 5/5，无默认特性库与全特性全目标检查均为 0 错误。本批闭合上一批登记的 Grid 资源风险，但仍不代表 Grid/Flex 其余组合、组件、缓存或真窗验证已完成。

**第五批进展（2026-08-05，`40e66b65`）**：Grid 公开入口现把子项 `align_self` 传给单元格求解器，不再被容器默认 Stretch 覆盖；Flex `overflow_content` 在保留自然主轴尺寸的同时复用标准 justify 间距/偏移，在扣除两侧 margin 的交叉轴可用区内执行 Center/End，并以正向逻辑顺序加容器主轴镜像统一 RowReverse/ColumnReverse。Grid 覆盖、溢出交叉轴、反向与 justify 四个聚焦断言在修复前稳定失败；修复后公开布局契约 20/20、库测试 107/107、公开 API 2/2、使用门面 5/5，无默认特性库与全特性全目标检查均为 0 错误。本批仍不代表 Grid 其余 justify/span、Flex overflow 与 wrap/固有轴组合、组件、缓存或真窗验证已经完成。

**第六批进展（2026-08-05，`e7ad9d55`）**：Flex `overflow_content` 现与标准路径一致，在固有反向主轴上按自然内容长度镜像，并让零交叉轴 bootstrap 的 Stretch 至少保留子项自然尺寸及两侧 margin。组件接线同时收敛：Space 不再丢弃子项 margin，Container/Space 共用一次无分配的子 frame 外尺寸扫描，把正右/下 margin 纳入缓存且不让负尾侧 margin 裁掉可见 frame。两个公开 Flex 与两个组件缓存断言在修复前稳定失败；修复后公开布局契约 22/22、组件缓存 2/2、库测试 109/109、公开 API 2/2、使用门面 5/5，无默认特性库与全特性全目标检查均为 0 错误。本批仍不代表 Grid 其余 justify/span、Flex overflow 与 wrap 组合、其他组件、增量缓存或真窗验证已经完成。

**第七批进展（2026-08-05，`b5c4f152`）**：ScrollView 现让四侧 margin 参与纵向/双向纵列及横向单行的放置、兄弟推进、滚动条首轮判断和内容范围；非滚动轴填充先扣两侧 margin，正尾侧 margin 保留在物理内容末端。双轴经典滚动条同时出现时，滚动坐标范围补入对侧 8px 沟槽，最大距离因此按真实内容视口计算；病理 frame、自然尺寸与 margin 也在组件边界收敛为有限几何。三个聚焦断言修复前稳定失败；修复后内部 ScrollView 4/4、公开布局契约 22/22、库测试 113/113、公开 API 2/2、使用门面 5/5、使用示例 11/11，无默认特性库与全特性全目标检查均为 0 错误，文档测试 47 通过/23 忽略。静态追踪确认当前生产调用没有组合 `overflow_content` 与 wrap，因此本批未改变该语义，也不把该组合、其他组件、增量缓存或真窗验证宣称为完成。

**第八批进展（2026-08-05，`5b45c0d9` + `cca439f9`）**：VirtualScroll 的动态 renderer 现先声明带业务 key 的行，缺省时补入绝对索引后备 key，再由 keyed reconcile 原位复用窗口重叠行与 renderer 更新后的既有组件，不再以 `set_children` 破坏状态；固定行高范围同时拒绝非正/非有限度量、夹取恢复 offset，以有限总高度和滚动状态输出有限 frame，并用 4096 行硬预算让可见内容优先于 overscan。三项身份聚焦断言与四项病理资源断言修复前稳定失败，正常范围断言始终通过；修复后内部 VirtualScroll 8/8、公开布局契约 22/22、库测试 121/121、公开 API 2/2、使用门面 5/5、使用示例 11/11，无默认特性库与全特性全目标检查均为 0 错误，文档测试 47 通过/23 忽略。本批闭合固定行高 VirtualScroll 的行身份、物化窗口与有限几何，不代表可变行高测量缓存、其他增量缓存、Grid/Flex 剩余组合或真窗矩阵已经完成。

**第九批进展（2026-08-05，`32a5cbf6`）**：声明树协调不再把任意组件配置变化合并成单一布尔值并同时请求 Layout/Paint；组件 patch 现返回双通道影响，只抓取一次新旧公开快照，并为 `Label`、`Container`、`Grid` 精确区分颜色、背景等纯绘制字段与文本、字号、固定尺寸、盒模型、Flex/Grid 属性、响应断点及轨道等布局字段。未审计组件仍保守请求 Layout。标签颜色与容器背景两个聚焦门禁修复前均得到错误的 Layout，修复后含 Grid 绘制/轨道与保守回退在内的协调分类 6/6、完整库测试 127/127、公开布局/API/门面/示例 40/40，无默认特性库与全特性全目标检查均为 0 错误，文档测试 47 通过/23 忽略。本批闭合三个基础组件的声明协调失效边界，不代表其余组件、`ProviderContext`、布局 scratch/输出缓存、可变行高或真窗矩阵已经完成。

**第十批进展（2026-08-05，`8530d29`）**：树布局 Phase 1 会跳过没有直接子节点的节点，因此组件在 `layout_children([])` 内提供的清理分支无法处理“有子树 → 空子树”协调，Container/Space/Affix 会保留旧固有尺寸，ScrollView 会保留旧内容范围与偏移，Carousel 会保留旧幻灯片数量。组件核心现提供直接子结构通知，并由建树、移除和声明重排三个入口统一发送；上述组件在最后内容移除时立即清理派生状态，ScrollView 同步回写受控零偏移，Carousel 还排除自定义箭头。审计同时确认 `ProviderContext` 变化继续保守请求 Layout，布局 scratch 每轮清空，遍历缓存以树版本为键，且当前没有跨帧 `LayoutOutput` 语义快照。新增六项结构状态门禁后适配器 12/12、完整库测试 133/133、集成测试 91 通过/1 忽略，无默认特性库与全特性全目标检查均为 0 错误，文档测试 47 通过/23 忽略。本批闭合当前树核心与五类组件的空子树派生状态，不代表 Grid/Flex 剩余组合、其余组件级一致性、其余组件失效分类、可变行高缓存或真窗矩阵已经完成。

**第十一批进展（2026-08-06，`b7f7e5c7`）**：固有主轴在已经获得非零实际 frame 时，justify 先按容器主轴定位，RowReverse/ColumnReverse 却仍按更短的自然内容长度镜像，会把子项移到负坐标；`overflow_content + wrap` 还会被独立流式分支截获而完全忽略换行。单行溢出、标准固有主轴和溢出换行现统一遵守“零尺寸 bootstrap 才按自然内容长度镜像，非零 frame 按实际主轴镜像”；溢出换行进入共享 Flex 分行求解器并冻结 grow/shrink，Stretch 保持自然尺寸。三个聚焦断言修复前分别复现负坐标或未换行，修复后公开布局契约 25/25、完整库测试 133/133、公开 API/使用门面/使用示例 18/18，无默认特性库与全特性全目标检查均成功。本批闭合已确认的固有反向边界与首个溢出换行组合，不代表 Grid 其余 justify/span、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

**第十二批进展（2026-08-06，`b1aefee`）**：多个同跨度子项重叠覆盖相邻 `Auto` 轨道时，旧求解器会在遍历单个子项后立即改写轨道，使后续子项从不同快照计算缺口；同一 cell/span 约束只交换声明顺序，末列起点会从 125px 变成 100px。跨轨道贡献现按 span 升序分批，同跨度子项先基于统一快照登记每条 `Auto` 轨道的最大计划增量，再整批写回；较短 span 继续优先提供基础尺寸，引用表与计划缓冲都受既有放置/轨道预算约束。聚焦断言在修复前稳定失败，修复后公开布局契约 26/26、完整库测试 133/133，无默认特性库与全特性全目标检查均成功。本批闭合已确认的 Grid 重叠同跨度顺序依赖，不代表 Grid 其余 justify、不同 span/Fr 组合、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

**第十三批进展（2026-08-07，`9d86578c`）**：子项跨越 `Auto + Fr`、而 span 外还有其他 Fr 竞争同一剩余空间时，旧求解器完全跳过该自然尺寸约束；一百像素容器中的七十像素子项因此只获得六十像素。混合 span 现按全局 Fr 权重计算 span 外可让份额，在不扩大父级轨道容量的前提下转入 span 内 Auto；多个约束按 span、起点与自然外尺寸确定性排序，并以最多 64 轮单调松弛处理相互影响。聚焦断言修复前稳定失败，修复后子项宽度与末轨起点均为 70px，公开布局契约 27/27、完整库测试 133/133，无默认特性库与全特性全目标检查均成功。本批闭合有外部 Fr 可转移的混合跨度竞争，不代表 Grid 其余 justify、覆盖全部 Fr 或没有 Auto 可转移的组合、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

**第十四批进展（2026-08-07，`d81dbe5b`）**：公开 `Grid::justify` 原先被接到单元格内子项对齐，Center 因此在百像素容器、两条二十像素固定列与十像素 gap 下把首项放到 5px，而不是把五十像素轨道组居中后的 25px。Grid 现把整组列轨内容对齐与单元格内 `with_justify` 拆为独立通道；Center/End 移动轨道组，SpaceBetween/SpaceAround/SpaceEvenly 分配轨道间及两端剩余空间，Stretch 只扩展 Auto 列。修复后六种固定轨道模式、Auto-only Stretch 与双通道兼容契约全部通过，公开布局契约 28/28、完整库测试 135/135、文档测试 47 通过/23 忽略，无默认特性库与全特性全目标检查均成功。本批闭合 Grid 已公开的水平内容 justify 语义，不代表覆盖全部 Fr 或没有 Auto 可转移的 span/Fr 组合、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

**第十五批进展（2026-08-07，`41158b3f`）**：CodeGraph 审计确认不可转移的 Fr span 已遵守架构规则，无需改写生产求解器：`Auto + Fr` span 覆盖全部 Fr 时没有外部份额可转移，一百二十像素自然宽度在一百像素父级内收敛到一百；纯 Fr 的列/行 span 没有 Auto 承接份额，三条等权轨道继续各占三十像素，八十像素自然尺寸收敛到前两轨的六十像素。新增三项契约直接通过，公开布局契约 31/31、完整库测试 135/135、文档测试 47 通过/23 忽略，无默认特性库与全特性全目标检查均成功。本批闭合 Grid 覆盖全部 Fr 与无 Auto 可转移的列/行跨度边界，不代表 Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

**第十六批进展（2026-08-07，`518adc67`）**：Flex 的统一 `compute_justify` 已支持负剩余空间下的 Center/End，但单行溢出、标准固有主轴与换行超宽行三条调用路径都提前把负差值钳为零，导致内容真正溢出时分布退化为 Start。现仅在零尺寸 bootstrap 固定零偏移，实际 frame 将有限正负差值交给统一分布器；Center/End 分别向主轴起点外溢半量/全量，Start 与 Space* 保持既有语义。三项聚焦断言修复前均失败，修复后公开布局契约 34/34、完整库测试 135/135、文档测试 47 通过/23 忽略，无默认特性库与全特性全目标检查均成功。本批闭合 Flex 负剩余主轴分布，不代表其余 overflow/wrap/固有轴组合、组件一致性、缓存或真窗矩阵已经完成。

**第十七批进展（2026-08-07，`6fe836a4`）**：wrapped 求解器只以自然最大子项高度建立行盒，默认 Stretch 因而无法消费实际容器的交叉轴剩余空间；即使最终只有一行，启用 wrap 也会把 non-wrap 的填充结果退回自然高度。现先保留自然行高与固定 gap，再把正剩余空间均分到每一行并同步后续行起点；逐项 `align_self` 继续只覆盖扩展行盒内的项尺寸。两项聚焦断言修复前分别得到 10px 而非 30px 的单行高度，以及 14px 而非 22px 的第二行自然起点；修复后拆分的布局契约目标 23/23 与 Flex 高阶目标 13/13、完整库测试 135/135、文档测试 47 通过/23 忽略，无默认特性库与全特性全目标检查均成功。原 1117 行契约文件同步拆为 804/360 行，恢复单文件 900 行门禁。本批闭合 Flex 换行交叉轴 Stretch，不代表其余组合、组件一致性、缓存或真窗矩阵已经完成。

**第十八批进展（2026-08-07，`952912e4`）**：wrapped 分行器以累计主轴占用 `> 0` 代替“当前行已有项目”，零宽首项因此会让后续满宽项连同 gap 错误留在首行。现以当前索引是否超过行起点判断行内项目存在性，尺寸为零的项目仍参与行收集和 gap 边界。聚焦断言修复前把第二项放在首行 `(x=9px, y=6px)`，修复后在八像素首行与五像素行距之后另起第二行 `(x=4px, y=19px)`；拆分的布局契约目标 23/23 与 Flex 高阶目标 14/14、完整库测试 135/135、文档测试 47 通过/23 忽略，无默认特性库与全特性全目标检查均成功。本批闭合 Flex 零尺寸项目分行，不代表其余组合、组件一致性、缓存或真窗矩阵已经完成。

**第十九批进展（2026-08-07，`bdccced7`）**：Flex 虽在自然尺寸阶段解析子项交叉轴 min/max，最终 Stretch 却会用容器或扩展行盒的可用高度直接覆盖该结果；过小容器可把子项压破最小值，宽裕容器和 wrapped 扩展行盒也可拉破最大值。单行与 wrapped 现都先计算 Stretch 候选，再用既有有序交叉轴上下限完成最终钳制；行盒尺寸和行起点仍按容器剩余空间结算。两个内部聚焦门禁修复前分别得到 10px 而非 20px 的单行最小高度、20px 而非 12px 的换行子项高度；修复后新增门禁 2/2、内部 Flex 6/6、拆分的布局契约目标 23/23 与 Flex 高阶目标 14/14、完整库测试 137/137，无默认特性库与全特性全目标检查均成功。本批闭合 Flex 交叉轴 Stretch 上下限，不代表其余组合、组件一致性、缓存或真窗矩阵已经完成。

**第二十批进展（2026-08-07，`9cd3ae5a`）**：wrapped Flex 对交叉轴 Center 已保留负剩余空间，却在 End 分支把同一负差值钳为零；自然行组高于实际容器时因此退化为 Start，末行末端越过容器。End 现与主轴溢出和单行逐项对齐保持一致，直接使用有限交叉轴差值；两行各 10px、行距 5px、容器高 15px 的聚焦契约修复前让首行错误停在 `y=6px`，修复后整体向起点外偏移 10px 到 `y=-4px`，末行底边贴住 `y=21px` 的容器底边。拆分的布局契约目标 23/23 与 Flex 高阶目标 15/15、完整库测试 137/137，无默认特性库与全特性全目标检查均成功。本批闭合 Flex 换行交叉轴 End 溢出，不代表其余组合、组件一致性、缓存或真窗矩阵已经完成。

**第二十一批进展（2026-08-07，`af35d11b`）**：第二十批恢复实际交叉轴 End 的负溢出后，继续审计确认 wrapped Center/End 未区分零交叉轴 bootstrap，都会围绕不存在的零尺寸范围移动自然行组。两种偏移现仅在交叉轴大于 bootstrap 阈值时启用；零高度首次布局固定从自然起点开始，仍由两行与固定 gap 撑开 25px 输出高度。聚焦契约修复前 Center 把首行从 `y=6px` 移到 `y=-6.5px`，修复后 Center/End 都从 `y=6px` 开始，同时上一批 15px 实际容器的负 End 偏移继续通过。拆分的布局契约目标 23/23 与 Flex 高阶目标 16/16、完整库测试 137/137，无默认特性库与全特性全目标检查均成功。本批闭合 Flex 换行交叉轴 bootstrap 对齐，不代表其余组合、组件一致性、缓存或真窗矩阵已经完成。

**第二十二批进展（2026-08-07，`382dd1df`）**：公开 `FlexLayout` 的空子集早返回始终复制完整 frame，跳过了非空路径中 `intrinsic_main || overflow_content` 的自然主轴账本；因此空水平固有布局在 100×20px frame 内错误报告 100×20px。空布局现先保留固定尺寸账本，再仅在固有主轴或溢出模式下按方向把自然主轴收敛为零，交叉轴继续保留父级分配值；水平、垂直与溢出契约分别得到 0×20px、100×0px 与 0×20px。拆分的布局契约目标 23/23 与 Flex 高阶目标 17/17、完整库测试 137/137，无默认特性库与全特性全目标检查均成功。本批闭合 Flex 空子集固有主轴，不改变底层 Flex/Grid padding 账本，也不代表其余组合、组件一致性、缓存或真窗矩阵已经完成。

**第二十三批进展（2026-08-07，`d91d9831`）**：RichText 的估算测量只在内容脏或缓存高度为空时重排，没有把当前宽度约束纳入缓存键；同一实例先在 400px 宽度建立 21px 高缓存、再缩到 80px 时，测量宽度虽被约束夹取，高度仍错误保持 21px，而绘制阶段会按 80px 重新折行，形成 measure/paint 分叉与裁切风险。现有 `last_layout_width` 同时作为有效测量宽度键；约束变化会重算估算高度、清除旧行坐标和代码复制命中区域、清除真实颜色键，下一次绘制再以同一宽度和真实字体度量重建缓存。聚焦契约修复后窄布局高度大于宽布局；布局契约目标 24/24 与 Flex 高阶目标 17/17、完整库测试 137/137，无默认特性库与全特性全目标检查均成功。本批闭合 RichText 宽度约束下的测量缓存一致性，不代表真实字体/DPI 全矩阵、其他组件一致性、可变行高或真窗矩阵已经完成。

**第二十四批进展（2026-08-07，`646f33de`）**：Notification 直接以 `min(frame.w, 384px)` 生成条目宽度；400px 窗口不足以同时容纳 384px 条目和双侧各 24px 留白时，TopRight 条目因此落在 `x=0`，牺牲了左侧留白，也与 Message 先扣 inset 的窄窗语义分叉。条目宽度现先扣除双侧 `HORIZONTAL_INSET`，再受 384px 上限约束；400px frame 修复后稳定得到 `x=24,w=352`，绘制、Overlay/dirty bounds 与命中继续共用 `notification_rects`，聚焦契约也确认动画后命中边界等于同一矩形。组件聚焦契约 1/1、完整库测试 138/138，无默认特性库与全特性全目标检查均成功。本批闭合 Notification 窄窗水平留白，不代表浮层/表格等其他组件一致性、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第二十五批进展（2026-08-07，`b14313ca`）**：Table 的表头、分组表头与表体按 Middle → Left → Right 绘制列区，但 `column_at` 按 Left → Right → Middle 查找，左右固定列在窄视口重叠时会返回被右列遮挡的左列。现以 `COLUMN_PAINT_ORDER` 统一三个绘制入口，并让命中按共享层级逆序执行。100px 视口中左右固定列各宽 80px，聚焦契约在 `x=50px` 修复前得到左列索引 0，修复后得到视觉最上层右列索引 1，左右非重叠区仍分别返回索引 0 与 1。几何聚焦契约 1/1、完整库测试 139/139，无默认特性库与全特性全目标检查均成功。本批闭合 Table 固定列重叠时的绘制/命中优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第二十六批进展（2026-08-07，`d51d5b0e`）**：Table 的列宽调整线随各列区绘制，但 `resize_handle_at_point` 把所有可见句柄合并后只按指针距离选择；不同固定区边缘重合时会保留声明顺序更早、视觉上已被覆盖的句柄。100px 视口中左固定列宽 80px、右固定区两列各宽 20px，左列和右内列的右边缘都落在 `x=80px`；聚焦契约修复前返回左列索引 0。句柄选择现按共享绘制层级逆序执行 Right → Left → Middle，仅在同层内比较距离，修复后返回最后绘制的右内列索引 1。交互几何契约 1/1、完整库测试 140/140，无默认特性库与全特性全目标检查均成功。本批闭合 Table 固定列重叠调整线的绘制/命中优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第二十七批进展（2026-08-07，`cd4e9a95`）**：Table 的表头与表体先绘制 32px 选择列，再按 Middle → Left → Right 绘制列区；但 `action_at_point` 在 `x < 32px` 时无条件先返回选择动作。100px 视口中一个 90px 右固定列覆盖 `x=10..32px`，表头 `x=20px` 视觉上显示右列，修复前却返回 `ToggleAll`。动作解析现先通过共享列几何获取视觉最上层列，仅在没有列覆盖时进入选择动作；修复后表头同点返回 `SortColumn(0)`，表体同点返回 `SelectRow(0)`，未覆盖的 `x=5px` 仍返回 `ToggleAll`。交互聚焦契约 1/1、完整库测试 141/141，无默认特性库与全特性全目标检查均成功。本批闭合 Table 选择列被固定列覆盖时的绘制/动作优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第二十八批进展（2026-08-07，`52f99f94`）**：Table 的展开箭头在选择列、普通列和合并单元格之后绘制，但 `action_at_point` 原先先处理选择列；当 24px 极窄视口让行末 32px 展开交互区覆盖选择列时，表体 `x=10px` 视觉上属于展开控件却错误返回 `ToggleRow(0)`。动作解析现先解析物理数据行，并让表体最上层展开控件优先于选择列和普通单元格；修复后同点返回 `ToggleExpand(0)`，表头同点仍返回 `ToggleAll`，展开内容区仍不产生展开动作。两项相邻层级交互契约 2/2、完整库测试 142/142，无默认特性库与全特性全目标检查均成功。本批闭合 Table 展开控件覆盖选择列时的绘制/动作优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第二十九批进展（2026-08-07，`625b0c31`）**：Table 叶表头与命中测试都以 Middle → Left → Right 为全局固定区层级，但有标题分组片段原先以分组声明为外层循环；100px 视口中先声明 80px 右固定组、后声明 80px 左固定组时，两区在 `x=20..80px` 重叠，旧分组绘制访问顺序为 Right → Left，而 `x=50px` 命中右固定列。分组片段现以共享 `COLUMN_PAINT_ORDER` 为外层、分组声明为内层，无中间集合分配；修复后顺序为 Left → Right，最后绘制的右组与命中结果一致。绘制层级聚焦契约 1/1、完整库测试 143/143，无默认特性库与全特性全目标检查均成功。本批闭合有标题分组在固定区重叠时的全局层级，不代表跨两层单列、合并单元格等表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第三十批进展（2026-08-07，`a4dada8f`）**：Table 虽已分别让有标题分组与叶表头遵守 Middle → Left → Right，却仍先绘制全部分组标题、再绘制全部叶表头；无标题单列跨越两层，因此 100px 视口中先声明 80px 右固定组、后声明 80px 左固定单列时，`x=50px,y=10px` 的旧片段顺序为“右分组 → 左单列”，视觉被左列覆盖，而列命中仍返回右固定列索引 0。表头现以共享列区为最外层，在每区内依次完成分组标题与叶表头阶段；修复后同点顺序为“左单列 → 右分组”，视觉与命中一致。新聚焦契约修复前稳定失败，修复后表头层级契约 2/2、完整库测试 144/144，无默认特性库与全特性全目标检查均成功。本批闭合跨两层无标题单列与有标题分组在固定区重叠时的全局层级，不代表合并单元格等表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第三十一批进展（2026-08-07，`d4999ab3`）**：Table 为避免后续物理行背景切断跨行单元格，会在全部普通行之后重绘合并锚点；但该阶段仍使用完整列区裁剪。100px 视口中左右固定列各宽 80px 时，左固定行合并锚点会在右列绘制完后用 `x=0..80px` 再次覆盖重叠区，而 `x=50px` 命中右列索引 1。合并重绘现使用最终可见裁剪：普通按层绘制仍保留左区 80px，末尾重绘则扣除右固定区并只保留 `x=0..20px`；右区继续完整覆盖 `x=20..100px`。新聚焦契约修复前稳定得到错误的 80px，修复后几何层级契约 2/2、完整库测试 145/145，无默认特性库与全特性全目标检查均成功。本批闭合左固定行合并锚点末尾重绘时的固定区层级，不代表跨固定区列合并、其他合并组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第三十二批进展（2026-08-07，`3348a7f4`）**：Table 的 `col_span` 仍以逻辑列索引判断覆盖，却以锚点物理 x 加覆盖列宽度之和构造矩形；100px 视口中先声明 60px 右固定锚点、后声明 40px 左固定覆盖列时，命中会把视觉左列回落到同一锚点，但旧矩形错误落在 `x=40,w=100`，漏掉 `x=0..40px` 并越出视口。列几何现联合全部覆盖列的真实物理边界得到 `x=0,w=100`，末尾合并重绘按 Middle → Left → Right 分别提取左片段 `x=0..40px` 与右片段 `x=40..100px`；`x=10px` 先命中左列索引 1，再稳定回落到锚点 `(0,0)`。同一失败契约修复前稳定得到错误起点 40px，修复后跨区几何/命中契约 2/2、Table 聚焦测试 12/12、完整库测试 147/147，无默认特性库与全特性全目标检查均成功。本批闭合普通文本单元格反向跨固定区列合并的矩形与分片重绘，不代表自定义 View 单元格跨区多片段、其他合并组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第三十三批进展（2026-08-07，`5a3c0c1f`）**：Table 自定义 View 单元格虽然复用第三十二批的完整跨区几何，旧布局仍把唯一子树收缩到锚点所属的单一固定区；同一 100px 视口中先声明 60px 右固定锚点、后声明 40px 左固定覆盖列时，旧 View frame 只有 `x=40,w=60`。现把唯一有状态 View 根节点按 `x=0,w=100` 布局一次，并在节点上保存左区 `x=0..40px`、右区 `x=40..100px` 两个父级片段；LayerTree 在子节点自身变换前逐片重放同一子树，带片段节点不提升为 Picture，空片段隐藏整棵子树。2D 命中、指针捕获与可见区域沿祖先链消费相同片段集合，独立间隙契约确认左右片段命中同一子树而中间空隙回落父节点。失败契约修复前稳定得到错误的 60px 宽 frame，修复后回归契约 2/2、Table 14/14、完整库测试 149/149，无默认特性库与全特性全目标检查均成功。本批闭合自定义 View 单元格跨固定区多片段，不代表表格其他合并组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

**第三十四批进展（2026-08-07，`09354738`）**：wrapped Flex 只在容器默认 Stretch 且交叉轴存在正剩余空间时扩展行盒；开启 wrap 但实际仍为一行时，较小容器与逐项 `align_self` 因而继续使用自然行高。八像素高容器中，带上下各两像素 margin 的逐项 Stretch 子项错误保留 10px 自然高，逐项 End 子项错误落在 `y=17px`，而关闭 wrap 的同一契约分别为 4px 与 `y=11px`。现只要最终为一行且已获得实际交叉轴，唯一行盒就直接采用容器交叉尺寸，逐项对齐在真实行盒内完成；自然外尺寸仍保留在内容账本，真正多行路径不变。主轴 justify 纯计算同步拆入独立模块，使核心 `flex.rs` 从 899 行降至 881 行。失败契约修复后布局契约 24/24、Flex 高阶契约 18/18、内部 Flex 6/6、完整库 149/149、文档测试 47 通过/23 忽略，无默认特性与全特性全目标检查均成功。本批闭合 Flex 未分行真实交叉轴一致性，不代表其余 overflow/wrap/固有轴组合、组件一致性、缓存或真窗矩阵已经完成。

**第三十五批进展（2026-08-07，`f41fede8`）**：Popover/Popconfirm 的绝对气泡矩形只以触发器 frame 判断上一帧缓存是否可用；窗口逻辑表面缩小但触发器保持不动时，布局阶段会先把旧位置登记进 OverlayStack，绘制阶段才按新表面收敛，命中与实际画面因而分叉。渲染能力现提供向后兼容的 `overlay_entry_for_surface`：默认继续委托既有入口，组件树重建浮层时显式传入当前根表面，两个边界敏感组件刷新表面后再调用同一 placement 解析器。失败契约修复前 Popover 横坐标为 150px、期望 20px，Popconfirm 为 200px、期望 60px；修复后契约 2/2、布局 24/24、反馈门面 4/4、完整库 151/151、文档测试 47 通过/23 忽略，两套特性检查均成功。本批闭合这两个反馈气泡在同 frame、不同 surface 下的登记/绘制一致性，不代表 Tooltip、选择类弹层等其余浮层、Flex 剩余组合、可变行高或真窗 resize 矩阵已经完成。

**第三十六批进展（2026-08-07，`5fefe2ed`）**：反馈 Tooltip 与 Slider 拖动提示复用的共享原语只接收文字、方向和目标 frame，不知道当前 logical surface；作者指定顶部后，即使目标靠近窗口上边缘也继续生成负坐标气泡，超长文字还会让气泡宽于窄表面，绘制、dirty 与 overlay bounds 一起越界。共享解析器现归一目标和表面，比较作者方向与主轴反向候选的总越界量，再把尺寸与坐标收敛到表面；绘制按解析后的真实方向放置箭头并锚向目标中心，文字裁在最终气泡内。Tooltip 与 Slider 均通过 `overlay_entry_for_surface` 接收同帧表面，旧入口只保留有限回退。失败契约修复后顶部气泡翻转为 Bottom 并完整留在 200×100px 表面，长提示宽度不超过 80px 窄表面；原语 2/2、组件集成 2/2、布局 24/24、反馈门面 4/4、公开 API 2/2、完整库 155/155、两套文档测试均为 47 通过/23 忽略，两套特性检查均成功。本批闭合 Tooltip/Slider 提示的表面约束一致性，不代表选择类弹层等其余浮层、Flex 剩余组合、可变行高或真窗 resize/长文案视觉矩阵已经完成。

**第三十七批进展（2026-08-07，`d288e6c5`）**：Select 原先直到绘制阶段才读取 logical surface 并决定弹层上下方向，同帧更早执行的自定义选项布局、滚动视口、dirty、命中与 OverlayStack 登记会消费旧 `dropdown_rect`；上下空间都不足时不缩高，窄表面也不收窄。共享解析器现横向约束宽度与起点，纵向优先完整向下、其次完整向上、最后选择空间更大的一侧并缩高；布局从树根、绘制从画布、显式登记从参数取得同帧表面，最终矩形贯通所有消费端，过滤前后翻转方向不同时 dirty 合并两侧。修复前 84px 弹层仍从 `y=80px` 控件向下越出 120px 表面，120px 宽弹层也越出 100px 窄表面；修复后分别翻到上方并缩为 80px、高度与宽度均留在表面内。四项聚焦契约、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 159/159、两套文档测试 47 通过/23 忽略及两套特性检查全部通过。本批只闭合 Select，不代表 Cascader、TreeSelect、AutoComplete、Mentions、日期类等其余选择弹层、Flex 剩余组合、可变行高或真窗 resize/过滤/滚动矩阵已经完成。

**第三十八批进展（2026-08-07，`c386f992`）**：Cascader 原先固定向下生成 200px 高弹层、每列至少 200px，多列总宽无视当前 logical surface，绘制、dirty、命中、OverlayStack 登记和滚动又各自按旧常量重算。私有共享解析器现横向约束总宽与起点、按可见列数等分最终列宽，纵向优先完整向下、其次完整向上、最后选择较大空间缩高；显式登记与绘制记录同帧表面，事件、滚动、键盘显露、绘制裁剪、dirty、命中和 bounds 复用最终几何。修复前底边弹层越界、100px 窄表面被撑破、78px 实际视口仍只滚到 120px；修复后向上缩高、横向收敛，滚动上限修正为 242px，两列总宽压到 300px 后第二列按 150px 实际列宽命中。四项聚焦契约、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 163/163、两套文档测试 47 通过/23 忽略及两套特性检查全部通过。本批只闭合 Cascader，不代表 TreeSelect、AutoComplete、Mentions、日期类等其余选择弹层、Flex 剩余组合、可变行高或真窗 resize/多列/滚动矩阵已经完成。

**第三十九批进展（2026-08-07，`250ba978`）**：TreeSelect 原先固定向下生成最多 280px 高、至少 200px 宽的弹层，事件命中始终按触发器下方坐标映射，虚拟滚动与键盘显露始终按 280px 自然视口计算，绘制、dirty 与 OverlayStack 登记也没有共享同帧 surface；底边、窄表面、缩高滚动和 resize 后历史脏区因此分叉。私有共享解析器现横向收敛宽度与起点，纵向优先完整向下、其次完整向上、最后选择较大空间缩高；组件缓存同帧 surface、绝对锚点、相对弹层与历史绝对脏区，事件、虚拟行、滚动显露、绘制裁剪、dirty、命中和 bounds 复用最终几何。修复前底边弹层越界、100px 窄表面被撑破、80px 实际视口仍只滚到 280px，翻转区域点击无法命中；修复后弹层翻到上方并缩为 80px，横向完整收敛，滚动上限修正为 480px，负向本地坐标正确映射首行，表面改变时旧弹层尾部也进入 dirty。五项聚焦契约、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 168/168、两套文档测试 47 通过/23 忽略及两套特性检查全部通过。本批只闭合 TreeSelect，不代表 AutoComplete、Mentions、日期类等其余选择弹层、Flex 剩余组合、可变行高或真窗 resize/展开/滚动矩阵已经完成。

**第四十批进展（2026-08-07，`5117f4d7`）**：AutoComplete 原先固定向下生成最多 280px 高、至少 200px 宽的弹层，事件命中始终按触发器下方坐标映射，虚拟滚动与键盘显露始终按自然视口计算，绘制、dirty 与 OverlayStack 登记也没有共享同帧 surface；底边、窄表面、缩高滚动和 resize 后历史脏区因此分叉。私有共享解析器现横向收敛宽度与起点，纵向优先完整向下、其次完整向上、最后选择较大空间缩高；组件缓存同帧 surface、绝对锚点、相对弹层与历史绝对脏区，事件、虚拟行、滚动显露、绘制裁剪、dirty、命中和 bounds 复用最终几何。连续输入只在首次呈现时清空缓存，过滤改变候选数时当前 bounds 按新行数重算而 dirty 保留旧区域。修复前底边弹层越界、100px 窄表面被撑破、80px 实际视口仍只滚到 280px，翻转区域点击无法命中；修复后弹层翻到上方并缩为 80px，横向完整收敛，滚动上限修正为 480px，负向本地坐标正确映射首行，20 行过滤为 1 行后当前 bounds 为 28px 而 dirty 继续覆盖旧 280px 弹层。六项聚焦契约、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 174/174、两套文档测试 47 通过/23 忽略及两套特性检查全部通过。本批只闭合 AutoComplete，不代表 Mentions、日期类等其余选择弹层、Flex 剩余组合、可变行高或真窗 resize/过滤/滚动矩阵已经完成。

**第四十一批进展（2026-08-07，`ed758bb3`）**：Mentions 原先固定向下生成最多 280px 高、至少 200px 宽的弹层，事件命中始终按触发器下方坐标映射，虚拟滚动与键盘显露始终按自然视口计算，绘制、dirty 与 OverlayStack 登记也没有共享同帧 surface；底边、窄表面、缩高滚动和 resize 后历史脏区因此分叉。私有共享解析器现横向收敛宽度与起点，纵向优先完整向下、其次完整向上、最后选择较大空间缩高；组件缓存同帧 surface、绝对锚点、相对弹层与本次 `@` 查询呈现周期的历史绝对脏区，事件、虚拟行、滚动显露、绘制裁剪、dirty、命中和 bounds 复用最终几何。仅在从非建议状态进入有效活动查询时清空缓存，光标查询改变候选数时当前 bounds 按新行数重算而 dirty 保留旧区域。修复前底边弹层越界、100px 窄表面被撑破、80px 实际视口仍只滚到 280px，翻转区域点击无法命中，20 行过滤为 1 行后的 OverlayStack bounds 还错误包含输入框而达到 60px；修复后弹层翻到上方并缩为 80px，横向完整收敛，滚动上限修正为 480px，负向本地坐标正确映射首行，当前 bounds 为 28px 而 dirty 继续覆盖旧 280px 弹层。六项聚焦契约、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 180/180、两套文档测试 47 通过/23 忽略及两套特性检查全部通过。本批只闭合 Mentions，不代表日期类等其余选择弹层、Flex 剩余组合、可变行高或真窗 resize/光标查询/滚动矩阵已经完成。

**第四十二批进展（2026-08-07，`677e5a7d`）**：DatePicker 原先固定向下生成 250px 高、至少 160px 宽的月历，月份导航、日期命中、绘制、dirty 与 OverlayStack 登记各自从触发器重算自然矩形；底边和窄表面会越界，登记还错误包含输入框，resize 后旧面板尾部可能漏出脏区。若受限高度只做裁切，后排日期会永久不可选。私有解析器现横向收敛宽度与起点，纵向保留 2px 间隙并优先完整向下、其次完整向上、最后选择较大空间缩高；组件缓存同帧 surface、绝对锚点、相对面板与当前打开周期的历史绝对脏区，绘制、dirty、命中和 bounds 复用最终几何。共享月历按最终矩形同步缩放标题、星期、日期网格、导航和字号，旧自然尺寸包装接口保留给 DateRangePicker 后续独立审计。修复前底边与窄表面越界、78px 面板中的 2026-08-01 无法命中、登记从输入框顶边开始、缩小表面 dirty 仍从 `y=80px` 开始；修复后五项契约 5/5，布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 185/185、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批只闭合 DatePicker，不代表 DateRangePicker、TimePicker、ColorPicker 等其余日期时间颜色类弹层、Flex 剩余组合、可变行高或真窗 resize/月份导航/日期选择视觉矩阵已经完成。

**第四十三批进展（2026-08-07，`5bbfd02f`）**：DateRangePicker 实际由单个月历与可选命名预设页脚组成；旧实现按 250px 月历、2px 间隙、8px 页脚上留白和每项 24px 行高向下拼接，三项预设时自然组合高度达到 332px。月份导航、日期与预设命中、绘制、dirty 和 OverlayStack 登记分别重算自然矩形，底边与窄表面会越界，登记错误包含输入框，resize 后旧页脚尾部也可能漏出脏区。私有解析器现先确定受表面约束的唯一组合矩形，再按同一纵向比例划分月历、间隙和预设页脚；组件缓存同帧 surface、绝对锚点、相对面板与当前打开周期的历史绝对脏区，全部消费者复用最终分区。78px 受限组合面板仍能选择 2026-08-01 与第二个预设，证明内部不是简单裁切；迁移完成后删除了已无消费者的四个共享自然尺寸过渡入口。修复后聚焦契约 6/6、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 191/191、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批只闭合 DateRangePicker，不代表 TimePicker、ColorPicker 等其余日期时间颜色类弹层、Flex 剩余组合、可变行高或真窗 resize/翻月/范围与预设选择视觉矩阵已经完成。

**第四十四批进展（2026-08-07，`fa3a6595`）**：TimePicker 原先固定向下生成 200px 高、至少 120px 宽的小时/分钟双列面板，指针与滚轮命中、键盘显露、绘制、dirty 和 OverlayStack 登记分别从触发器重算自然矩形；底边与窄表面会越界，登记错误包含输入框，resize 后旧面板尾部也可能漏出脏区。更关键的是受限高度仍以 200px 计算滚动上限，78px 视口下分钟列最大偏移停在 1720px，第 59 分钟不可达，23:59 当前高亮也落在视口外。私有解析器现确定唯一的表面内时间面板；两列保持 32px 固定行高，不压扁内容，滚动上限、当前高亮显露、指针/滚轮/键盘、绘制、dirty 与 bounds 都复用实际视口高度和同帧几何。78px 视口下分钟滚动上限修正为 1842px，末项可命中且 23:59 两列当前值均保持可见。修复后聚焦契约 7/7、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 198/198、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批只闭合 TimePicker，不代表 ColorPicker 等其余颜色选择弹层、Flex 剩余组合、可变行高或真窗 resize/滚动/键盘选择视觉矩阵已经完成。

**第四十五批进展（2026-08-07，`1e5cb989`）**：ColorPicker 原先固定向下生成 208×88px 的 8 列、24 色预设面板，绘制、指针命中、dirty 与 OverlayStack 登记分别从触发器重算自然矩形；底边和 100px 窄表面会越界，登记错误包含触发器，surface 缩小后旧面板尾部可能漏出脏区，关闭动画期间也没有可靠的受限几何。私有解析器现保留 4px 触发间隙，横向收敛宽度与起点，纵向优先完整向下、其次完整向上、最后选择较大空间缩高；共享 `ColorPanelGeometry` 从最终矩形同步派生内边距、色块槽位与命中映射，绘制、点击、hover、dirty、命中和 bounds 复用同帧几何。底边场景可翻到上方并缩为 76px，受限网格点击仍映射正确；关闭动画继续保留表面内面板，重新打开则重置上一呈现周期的损伤缓存。修复后聚焦契约 7/7、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 205/205、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批闭合日期、时间与颜色选择弹层这一子序列，不代表 Flex 剩余组合、表格其他合并组合、可变行高或真窗 resize/hover/点击/关闭动画视觉矩阵已经完成。

**第四十六批进展（2026-08-07，`89bc5708`）**：公开 `FlexLayout` 的单行 `overflow_content` 路径原先仍要求调用方另设 `intrinsic_main`，反向方向在零主轴 bootstrap 下否则会围绕零长度镜像，把两个自然尺寸子项推到负坐标；这与溢出模式自身保留自然主轴的契约不一致。溢出求解器现只要实际主轴不大于 bootstrap 阈值，就直接使用已求得的自然内容总长作为反向镜像边界；非零实际 frame 仍围绕容器主轴镜像，与 justify 继续共享同一边界。强化契约修复前稳定失败、修复后通过，聚焦溢出契约 18/18、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 205/205、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批只闭合单行溢出反向零主轴 bootstrap，不代表 Flex 其余 wrap/overflow/固有轴组合、表格其他合并组合、可变行高或真窗视觉矩阵已经完成。

**第四十七批进展（2026-08-07，`144b3224`）**：CodeGraph 审计确认 `overflow_content && !wrap` 与 `overflow_content && wrap` 进入两套独立求解器，而既有未分行证据只覆盖一个交叉轴场景。新增矩阵使用两个自然尺寸不同且四侧 margin 非对称的子项，枚举 Row/RowReverse/Column/ColumnReverse、全部七种 JustifyContent、四种 AlignItems，并为每组分别执行实际 frame、零主轴和零交叉轴，共 336 个组合；wrapped 与流式溢出路径的子项矩形和自然尺寸账本首轮全部相同，因此无需改写生产求解器。聚焦溢出契约增至 19/19，布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 205/205、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批闭合未实际分行的溢出路径等价边界，不代表 Flex 其余实际分行 overflow/wrap/固有轴组合、表格其他合并组合、可变行高或真窗视觉矩阵已经完成。

**第四十八批进展（2026-08-07，`ce51b1fe`）**：CodeGraph 审计确认 wrapped 溢出在真正分行后仍分别处理行内分布、行组交叉轴分布、逐项对齐、反向镜像和 bootstrap。新增矩阵用四个不同自然尺寸、四侧非对称 margin、逐项 Start/End/Stretch 且稳定形成两行的子项，比较 Row/Column 与 RowReverse/ColumnReverse，并枚举全部七种 JustifyContent、四种 AlignItems，以及交叉轴正剩余、负剩余和零尺寸 bootstrap，共 168 个组合；交换 frame、子项尺寸和 margin 两轴后，子项矩形与总尺寸账本首轮全部转置一致，因此无需修改生产求解器。聚焦溢出契约增至 20/20，布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 205/205、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批闭合实际分行溢出的轴转置对称边界，不代表 Flex 其余实际分行组合、表格其他合并组合、可变行高或真窗视觉矩阵已经完成。

**第四十九批进展（2026-08-07，`d5db70cf`）**：CodeGraph 审计确认第四十八批轴转置矩阵没有直接证明同一主轴的正反向镜像，而反向分支会单独选择物理尾侧 margin 并在逐行分布后围绕主轴边界镜像。新增矩阵用四个不同自然尺寸、四侧非对称 margin、逐项 Start/End/Stretch 且稳定形成二加一加一三行的子项，比较 Row/RowReverse 与 Column/ColumnReverse，并枚举全部七种 JustifyContent、四种 AlignItems，以及交叉轴正剩余、负剩余和零尺寸 bootstrap，共 168 个组合；反向输入交换主轴起止 margin 后，每个子项矩形均围绕真实容器主轴精确镜像，总尺寸账本保持不变，首轮全部通过，因此无需修改生产求解器。新增镜像契约 1/1、聚焦溢出契约 20/20，布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 205/205、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批闭合实际分行溢出的正反向物理镜像边界，不代表 Flex 其余实际分行组合、表格其他合并组合、可变行高或真窗视觉矩阵已经完成。

**第五十批进展（2026-08-07，`fe2acc63`）**：CodeGraph 审计确认 `overflow_content` 的自然主轴承诺依赖公开布局入口在进入 wrapped 求解器前把所有子项 grow/shrink 置零，而既有多行矩阵没有直接证明调用方的弹性声明不会泄漏。新增矩阵用四个不同自然尺寸、四侧非对称 margin、逐项 Start/End/Stretch 且稳定形成二加一加一三行的子项，比较零弹性基准与包含普通正值、大权重、`f32::MAX`、负值、正无穷和 NaN grow/shrink 的输入，并枚举四种方向、全部七种 JustifyContent、四种 AlignItems，以及交叉轴正剩余、负剩余和零尺寸 bootstrap，共 336 个组合；分行、每个子项矩形与总尺寸账本首轮全部相同，因此无需修改生产求解器。新增冻结契约 1/1、镜像契约 1/1、聚焦溢出契约 20/20，布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 205/205、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批闭合实际分行溢出的弹性冻结边界，不代表 Flex 其余实际分行组合、表格其他合并组合、可变行高或真窗视觉矩阵已经完成。

**第五十一批进展（2026-08-07，`c7586072`）**：CodeGraph 审计确认 wrapped Flex 的自然交叉轴总量只读取最终行游标；有限负 gap 使行盒重叠时，较矮末行可能早于较高前行结束。新增水平与垂直失败契约用 30px 高/宽首行和 5px 高/宽末行、负 8px gap 稳定复现：旧账本均返回 27px，漏掉首行/首列最后 3px。求解器现逐行计算起点加行高，并以所有有限物理末端的最大值作为自然总量；两轴结果均恢复为 30px。负 gap 契约 2/2、弹性冻结 1/1、镜像 1/1、聚焦溢出 20/20、布局 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11、完整库 205/205、无默认特性库和全特性全目标检查均成功，两套文档测试分别为 47 通过/23 忽略和 47 通过/25 忽略。本批只闭合负 gap 下最远行末端账本，不代表负 margin/gap 其余组合、Flex 其余实际分行组合、表格其他合并组合、可变行高或真窗视觉矩阵已经完成。

**依赖与边界**：检查发现的缺陷直接修复并按既有组件稳定性流程记录；本目标不扩展布局 API 能力，只保证既有语义正确；不指定开始时间、量化预算或绑定版本。

**完成声明**：不变量逐条有对应断言或真窗验证；历史缺陷类别全部回归通过；检查与修复记录登记到[组件稳定性](组件稳定性.md)与[已知缺陷](已知缺陷.md)文档。

## 目标：复杂文本排版

**状态：⏳ 待开始（已立项，未排期）。** 目标让文本层按 Unicode 标准提供完整排版语义，覆盖四个子项：

- **复杂脚本 shaping**：Indic、阿拉伯文等需要组合与上下文替换的脚本，按正确 glyph 顺序与定位进入测量、布局与绘制。当前 `ab_glyph` 仍是逐 Unicode 标量的 advance/kerning 与 CPU 轮廓提取（见[性能与内存](性能与内存.md)瓶颈），只保证拉丁、CJK 等简单文本顺序正确。
- **双向文本（UAX#9）**：混合 LTR/RTL 段落按 Unicode 双向算法重排，逻辑顺序、视觉顺序与命中顺序一致。
- **字素簇光标（UAX#29）**：光标移动、文本选择与命中按字素簇边界进行，组合字符与 emoji 序列不中途断开。
- **完整断行（UAX#14）**：按 Unicode 断行规则换行，替换当前逐标量断行。

**依赖与边界**：文本测量/布局路径（`ab_glyph` 的替换或扩展）与 R8/MSDF 字形管线、富文本、输入控件、无障碍文本语义需随各子项同步验证；字形缓存预算与现有性能基线保持一致。本目标不包含可变字体轴、复杂合字排版、移动端 backend 或完整 OpenType 排版引擎承诺；不指定开始时间、执行顺序、量化预算或绑定版本。

**完成声明**：四个子项均有真实窗口视觉/像素验证（含既有 92 组件视觉矩阵回归）与行为断言（双向段落重排、字素簇光标移动、断行边界），并在当前 D3D11 生产路径建立 shaping/断行后的测量与性能基线；实现前先为代表文本场景建立可复现基线，再单独确定预算、优先级与验收门禁。

## 目标：丰富富文本组件

**状态：⏳ 待开始（已立项，未排期）。** 当前 `RichText`（`src/ui/widgets/other/rich_text/`）已具备混合样式段落显示：Text/Code/Link/NewLine 四类段、Markdown 风格解析（仅围栏代码块与内联反引号）、字符级布局与折行、链接键盘导航、文字选择与代码复制。目标扩展其能力边界，让富文本从「混合样式段落显示」走向「Markdown 级排版与内联富元素」，覆盖：

- **解析与段类型扩展**：`parse_rich_text` 目前只识别围栏代码块与内联反引号，需补齐标题、粗体/斜体标记、Markdown 链接语法、无序/有序列表、引用块与分割线等元素，并新增对应 `RichTextSegment` 变体
- **测量统一**：measure 路径与公开测量辅助 `layout_rich_text` / `layout_rich_text_segments` 使用启发式字符宽度估算（按字符类别比例，不依赖 FontService），与 render 路径 `layout_rich_text_real` 的真实字形测量存在精度差距；需统一为真实测量，保证命中、选择与像素一致
- **样式与排版**：段级字体族/字号变体（当前粗体为 x+0.6 二次偏移模拟，`italic` 仅为数据标志、渲染无字形变体）、行高与段间距、水平对齐、缩进与上下标
- **内联富元素**：段落内嵌图片/图标（`Image` 目前是独立组件，无法嵌入段落）、内联按钮与自定义 widget 槽位
- **选择与无障碍同步**：选择/命中/无障碍语义与结构化快照随新段类型与内联元素同步扩展

**依赖与边界**：折行与光标质量依赖[目标：复杂文本排版](#目标复杂文本排版)的 shaping/断行成果，两个目标共享文本测量与字形管线改动；`RichText` 保持只读显示定位，不包含富文本编辑器；本目标不指定开始时间、执行顺序、量化预算或绑定版本。

**完成声明**：新段类型与内联元素均有公开使用示例（`uix-compile` 锚点）、行为断言与真窗视觉/像素验证；measure 与 render 宽度一致；既有 92 组件视觉矩阵与无障碍 manifest 回归通过。

## 更小 · 更快 · 更稳

### 更小：缩小内存占用

- **当前生产基线**：wgpu 已移除；Windows 默认 feature 与 factory 使用原生 D3D11，`opengles` feature 可构造 WGL/EGL OpenGL ES adapter。`draw::Renderer` / `GpuBackend` 仍共享，逐 UI 操作的原生 pipeline 与资源实现正在收敛到 thin RHI；OpenGL ES legacy compatibility 的真实窗口 traversal 已通过，WGC 已确认硬件窗口捕获到非空完整 UI，但完整像素矩阵仍未闭合
- **历史实测**：Windows/NVIDIA release、1200×800、中文 Demo 首屏稳定 5 秒后的 Vulkan/D3D12，以及 2026-08-01 AMD RX 5700 内存批次均来自已移除的 wgpu 路径，只保留为历史对照，不能作为当前 D3D11 基线；原始数字见[性能与内存](性能与内存.md)
- **已做**：字形缓存 LRU 2048 项/8 MiB、Picture 32 MiB 硬预算、DisplayList COW Arc 共享、主画布 scratch 1×1、Label/Input 直接移交单次绘制的新 glyph layout、1920×1080 resize 不再分配未用 CPU 主表面（约 −7.91 MiB）
- **瓶颈**：当前 D3D11 的驱动、shader、GPU 资源与中文字体常驻成本尚未建立同口径实测；native adapter 在 RHI 收敛前仍承担重复 pipeline/资源实现
- **使用方构建**：目标是未选择且非传递必需的能力不进入应用编译图，已选择但不可达的代码不进入 release 产物；当前九个公开 capability、`agent-control`、演示专属 `demo-logging` 与五种 backend 公开选择面已有正反门禁，`windows-core` 已收缩到 Windows target，schema v6 的 38 场景双目标矩阵还加入了排除内部 `test-harness` 的全能力外部 fixture，并在同条件下记录最小/完整 release 产物 8,293,442 B / 19,957,355 B。整体门禁仍缺其余能力分区、生产 backend/其他 target 及产物符号与资源断言，详见[使用方按需编译](使用方按需编译.md)
- **目标**：逼近同体量 GPU-only 框架内存水平，并让应用只承担实际选择能力的构建与产物成本

### 更快：降低每帧耗时

- **已做**：生产只有一个 `draw::Renderer`；相邻兼容 op 在 API 中立层合批；失效队列、布局热路径与到期工作已完成一批分配/遍历优化
- **历史实测**：Vulkan 首帧约 14.6 ms、稳定后采样帧约 4.7 ms 来自已移除的 wgpu 路径，不代表当前 D3D11；D3D11 的首帧与稳定帧需要重新测量
- **热路径 scratch 复用**：Layout 遍历复用扩展签名/已调整子节点集合；Flex/Grid/溢出流式不分配主轴尺寸副本；Overlay 新旧签名流式比较；Timer discovery 复用 route map
- **布局失效优化**：同节点 Layout 队列项去重但 revision 仍逐请求推进；向上传播复用每树祖先链缓冲
- **到期工作清退**：registry 在一次有序 retain 扫描中处理；动画注册原位同步不逐帧重建 desired map
- **瓶颈**：`ab_glyph` 仍是逐 Unicode 标量的 advance/kerning 与 CPU 轮廓提取；MSDF RHI lowering、带硬预算的多页 atlas 和大字号 Watermark 的真实 present 已落地，但专用 WGC 像素快照与更宽的字号/DPI 视觉矩阵未闭合；完整复杂文本排版（shaping/双向文本/字素簇光标/UAX#14 断行）已立项为[目标：复杂文本排版](#目标复杂文本排版)
- **目标**：稳定 60Hz

### 更稳：跨平台真机现状

- **当前生产路径**：Windows 默认原生 D3D11；启用 `opengles` feature 时 WGL/EGL 走同一薄 RHI host；D3D11 present coherency 为 `FullOnly`，`retained_framebuffer=false`，D3D11 与 OpenGL ES/WGL 已有真实 context、正式 RHI surface present 和 1200×800 首帧视觉验证，D3D11 另有 1200×800↔900×640 双向 resize 验证，大字号 Watermark 已有两个 adapter 的真实 present 专项，但仍缺专用 WGC 像素快照、完整 surface/device lost 与跨环境矩阵
- **历史数据**：Windows Auto→Vulkan、强制 Vulkan、最小化/隐藏恢复与 wgpu device-lost 验证来自已移除路径，不能替代当前 D3D11 验收
- **数据缺口**：D3D11 下最低支持 build 26100、96/144/192 DPI、NVIDIA/AMD/Intel、最小化/隐藏恢复、surface/device lost，以及 Wayland/macOS 生产 backend 均无闭合的当前报告；既有组件视觉矩阵需按当前 backend 重新确认
- **目标**：四平台同等视觉/图形矩阵
