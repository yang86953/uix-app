# 组件稳定性

[← 返回进度 索引](../进度.md)

> **接口**：声明 UIX 各组件的已知修复、风险记录、回归验证覆盖与真窗验证状态。依赖：[首发状态](首发状态.md)、[组件功能缺口](组件功能缺口.md)。导出：无。（本组件是交付状态的终点报告）

> 当前组件修复、规格与历史 / 工作树专项验证。下文"Windows 真窗 ✅"只对应其记录时的具体组件场景，不代表当前 92 项库存的完整状态。

> **分母审计（S0-02）**：`42 项`是历史专项修复与真窗验证工作项的汇总计数，不是 92 项组件库存，也不是 847 条视觉验证行；当前文档没有独立的 42 行机器台账，后续不得从其他分母换算该数字。

### 进度总览

| 状态 | 事项 |
|---|---|
| ✅ 已完成 | 42 项组件修复与真窗验证（Typography/Label/Button/Tag/Badge/Tooltip/Checkbox/Switch/Radio/Avatar/Calendar/Card/Carousel/Collapse/Descriptions/Empty/Image/List/ResultView/SelectableList/Skeleton/Table·受限渲染/Table·指针交互/Tag/Timeline/Tree/RichText/Alert/Drawer/Message/Notification/Popover·Popconfirm/ProgressBar·Spin/Modal/Input/InputNumber/Select/Segmented/Form·FormItem/Slider/Rate/DatePicker/DateRangePicker/TimePicker/ColorPicker/Cascader/TreeSelect/AutoComplete/Mentions） |
| 🔄 进行中 | 全面检查布局：共享有限几何、Flex/Grid 算法边界、ScrollView/VirtualScroll、声明协调与空子树状态、Flex 固有/溢出/换行细分边界、空子集账本、RichText 宽度测量缓存、Notification 窄窗留白、Table 固定列重叠命中、重叠句柄命中、选择列覆盖命中、展开控件覆盖命中、分组表头、跨层表头、行合并重绘固定区层级、普通文本跨固定区列合并几何、自定义 View 跨固定区多片段、Flex 未分行真实交叉轴、Popover/Popconfirm 同帧表面定位、Tooltip/Slider 提示约束、Select、Cascader 与 TreeSelect 弹层同帧表面几何共三十九批已闭合；Flex 其余组合、其余选择类浮层、表格其他合并组合的组件级一致性、其余组件失效分类、可变行高缓存与真窗矩阵待继续 |
| ⏳ 待开始 | AMD/Intel/多 DPI/Wayland/macOS 多平台验证 |
| 📋 规划中 | — |


## 概述

- **修复**：
  - Typography：单字体长段不折行、CJK 测量少行、显式换行少高、闭合标点落行首。
  - Label：多行按单行高、CJK 量宽偏小、Style 单位未进测量。
  - Button：CJK 宽按 `0.55em` 欠量、字号回退不一致。
  - Tag：UTF-8 字节量宽、关闭符号退化。
  - Badge：按 count 量宽、固定字符估算。
  - Tooltip：UTF-8 字节量宽、dirty damage padding 不一。
  - Checkbox：UTF-8 字节量宽。
  - Switch：滑块纵偏 2/4px。
  - Radio：UTF-8 字节量宽、无焦点图形、方向键首尾停。
- **规格**：
  - Typo 112px=4 行。
  - Label 三行=54px。
  - Button 六字 CJK 14px=114px。
  - Tag 三字 12px 可关闭=72×20。
  - Badge 新消息=45×20 / 99+=30×20。
  - Tooltip 四字=64×26。
  - Checkbox 中号=74×32、空=16×32。
  - Switch S/M/L=28×24/40×32/52×40、滑块 φ12/18/24。
  - Radio 双字 S/M/L≈135×24/168×32/200×40、三项组=120×96。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## 共享 LayoutEngine · 有限几何不变量

- **修复**：BoxModel、空 Flex/Grid 与病理测量输入可把非有限值、负尺寸或 `f32::MAX` 哨兵写入实际输出；共享入口现统一归一 frame、总尺寸、子测量、margin、border/padding、gap 与弹性因子。
- **规格**：实际坐标必须有限且不得为无界哨兵，实际尺寸必须有限非负；有限负坐标与 margin 保留既有语义，border/padding 和 Grid gap 收敛为非负；正常 Flex/Grid 精确几何不得因防御性归一化改变。
- **验证**：`7de6fcd8`；`cargo test --locked --test layout_invariants` 5/5，`cargo test --locked --test ui_public_api` 2/2，`cargo test --locked --lib` 97/97，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误。修复前四个病理输入用例均可复现失败。
- **备注**：本条是跨组件布局审计的首批记录，不计入上方 42 项历史组件修复；完整 Flex/Grid 边界、measure/paint/hit-test 一致性、增量缓存及真窗视觉矩阵仍未闭合。

## 共享 FlexLayout · 弹性分配与换行边界

- **修复**：grow/shrink/Stretch 遇到 min/max 后只固定轮数重试或不再分配，可能留下尾部空白或整行越界；wrapped 在落实 min/max 前分行和计算行高，可能错分行或让相邻行重叠；零主轴 bootstrap 会把固有 wrapped 子项压成零；Center/End 未先扣非对称交叉轴 margin，单行总尺寸也漏记 margin。
- **规格**：min/max 先于分行生效；触顶/触底项冻结后，剩余空间持续交给仍可伸缩兄弟，全部到界才允许留白或溢出；无真实主轴约束时 wrapped 保持自然尺寸；对齐在扣除 margin 的可用交叉轴内计算，单行、多行、固有尺寸与反向镜像复用同一行账本。
- **验证**：`7e03fe41`；六个新增聚焦断言修复前失败；修复后 `cargo test --locked --test layout_invariants` 9/9、`cargo test --locked --lib ui::layout::flex::tests` 4/4、完整 lib 101/101、公开 API 2/2、使用门面 5/5，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误。
- **备注**：常规无钳制路径保持线性遍历且不新增分配，只有子项实际触及 min/max 时进入至多“子项数加一”轮的有界再分配。本条仍不代表 Grid、Flex 其余方向/justify/align 组合、组件级布局、增量缓存或真窗矩阵已经闭合。

## 共享 GridLayout · 自动轨道与 span

- **修复**：`Auto` 与 `Fr` 被合并为弹性权重，导致纯 `Auto` 网格无条件填满容器、`Auto + Fr` 错分剩余空间；轨道解析未读取子项测量尺寸与 margin，跨多条 `Auto` 的子项也不会撑开 span。
- **规格**：`Auto` 在列/行轴分别取子项有限外宽/外高，`Px` 与 `Auto` 优先确定，正权重 `Fr` 仅分配剩余空间；不含正权重 `Fr` 的 span 先扣除已知轨道与内部 gap，再将缺口均分给所覆盖的 `Auto`。
- **验证**：`33721e4a`；三个列轴聚焦断言在修复前失败，修复后补入行轴对称覆盖；`cargo test --locked --test layout_invariants` 13/13、完整 lib 101/101、公开 API 2/2、使用门面 5/5，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误。
- **备注**：显式超大 `grid_cell`/`row_span` 的资源风险已由下一批有界放置闭合；Grid/Flex 其余方向、justify/align/span 组合、组件级布局、增量缓存与真窗矩阵仍未闭合。

## 共享 GridLayout · 有界放置与资源预算

- **修复**：显式 `grid_cell`/`row_span` 可按请求行号直接扩张稠密 occupied、rows 与 positions，乘法/加法溢出与过量分配可在 frame 求解前发生；自动放置没有总行上限，且每次扩一行后会从头重扫 span 面积。
- **规格**：单轴最多 4096 条轨道、稠密占用账本最多 65,536 个单元格；超限轨道/cell/span 收敛到窗口边界，显式重叠保留，自动放置仍按行优先稠密回填，耗尽时留零 frame。二维前缀和让矩形占用查询为常数时间，扩行尝试至多一次。
- **验证**：`f2ccba05`；两个 5000 行级安全聚焦断言修复前失败，修复后补入 `usize::MAX`/`u32::MAX`、预算耗尽、行优先回填与 4608 组小矩阵穷举对照；公开布局契约 16/16、内部放置边界 6/6、完整 lib 107/107、公开 API 2/2、使用门面 5/5，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误。
- **备注**：本条闭合 Grid 放置层的已知资源/算术风险，不代表 Grid/Flex 其余方向、justify/align/span 组合、组件级布局、增量缓存或真窗矩阵已闭合。

## 共享 Grid/FlexLayout · 逐项对齐与溢出方向

- **修复**：Grid 公开入口把子项 `align_self` 固定丢为 `None`，容器默认 Stretch 会覆盖逐项 End/Center；Flex 溢出路径忽略 justify，反向游标从错误起点继续递减，Center/End 也未先扣除非对称交叉轴 margin。
- **规格**：Grid 逐项交叉轴覆盖必须进入单元格求解；溢出 Flex 只保留自然主轴尺寸，不取消 justify、align、gap 或方向语义。正剩余空间复用标准分布函数，交叉轴在扣除两侧 margin 后对齐，反向方向先按逻辑顺序放置再沿容器主轴镜像。
- **验证**：`40e66b65`；Grid 覆盖、溢出交叉轴、反向与 justify 四个聚焦断言在修复前失败；修复后 `cargo test --locked --test layout_invariants` 20/20、完整 lib 107/107、公开 API 2/2、使用门面 5/5，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误。
- **备注**：溢出路径没有新增主轴尺寸副本或增长/压缩阶段，只复用已有 justify 计算并原位镜像结果；Grid 其余 justify/span、Flex overflow 与 wrap/固有轴组合、组件级布局、增量缓存与真窗矩阵仍未闭合。

## FlexLayout / Container / Space · 固有溢出与内容缓存

- **修复**：固有 ColumnReverse/RowReverse 溢出仍沿零主轴镜像，子项落到负坐标；零交叉轴 bootstrap 的默认 Stretch 把自然尺寸压成零；Space 转换 `LayoutChild` 时未传 margin；Container/Space 缓存只取可见 frame 末端，漏掉正右/下 margin。
- **规格**：固有反向主轴按自然内容总长镜像，固定主轴仍按父级分配镜像；零交叉轴以最大自然外尺寸 bootstrap，Stretch 至少保留自然尺寸。Space 与 Container 都让 margin 推开兄弟，共享的缓存扫描保留可见末端并只补入正尾侧 margin。
- **验证**：`e7ad9d55`；两个公开 Flex 与两个组件缓存聚焦断言在修复前失败；修复后 `cargo test --locked --test layout_invariants` 22/22、组件缓存 2/2、完整 lib 109/109、公开 API 2/2、使用门面 5/5，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误。
- **备注**：内容缓存只做一次位置/子项并行扫描，不新增尺寸副本；Grid 其余 justify/span、Flex overflow 与 wrap 组合、其他组件级布局、增量缓存与真窗矩阵仍未闭合。

## ScrollView · 外边距与双轴滚动范围

- **修复**：ScrollView 完全忽略 `LayoutChild.margin`，使子项从内容原点重叠既定外边距、兄弟只按内容尺寸推进、首轮滚动条判断漏掉外尺寸；双轴滚动条同时出现时，`content_bounds` 按扣沟槽视口排布，却仍让 `max_scroll` 减完整外视口，两个方向各少一个对侧沟槽。
- **规格**：纵向/双向纵列与横向单行都让 margin 推开兄弟，子 frame 从前侧 margin 后开始，非滚动轴填充先扣两侧 margin，物理范围保留正尾侧 margin；双轴内容范围补入对侧沟槽，使外视口相减等价于按真实内容视口计算。实际 frame、内容范围和最大距离必须保持有限非负。
- **验证**：`b5c4f152`；纵向 margin、横向 margin 与双轴沟槽三个聚焦断言修复前失败，修复后内部 ScrollView 4/4、`cargo test --locked --test layout_invariants` 22/22、完整 lib 113/113、公开 API 2/2、使用门面 5/5、使用示例 11/11，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误，文档测试 47 通过/23 忽略。
- **备注**：组件直接复用共享布局有限化规则，没有新增尺寸缓存；当前生产调用未组合 `overflow_content` 与 wrap，本条不代表该组合、其他组件级布局、增量缓存或真窗矩阵已经闭合。

## VirtualScroll · 行身份与有界物化

- **修复**：窗口滑动和 renderer 声明更新原先都会用 `set_children` 销毁全部物化行，重叠项因而丢失组件身份、内部状态与焦点；无显式 key 的行没有确定身份。非法行高/viewport、超限恢复 offset 和极大 overscan 还可生成错误范围或无界物化，总高度、滚动状态和最终 frame 可出现 NaN/无穷。
- **规格**：renderer 的业务 key 原样参与 keyed reconcile；缺省时按绝对索引补入 `virtual-scroll-item:{index}`，只适用于顺序不可变数据。范围改变时重叠行原位协调，进入/离开行增删；固定行高和 viewport 必须有限且为正，offset 按内容夹取，单次物化不超过 4096 行且可见内容优先于 overscan，所有总高度、偏移、比例与行 frame 均保持有限非负尺寸。
- **验证**：`5b45c0d9` + `cca439f9`；重叠身份、后备 key 与 renderer 更新三项断言修复前失败；非法度量、资源预算、滚动状态与最终 frame 四项病理断言修复前失败，正常范围精确断言始终通过。修复后内部 VirtualScroll 8/8、`cargo test --locked --test layout_invariants` 22/22、完整 lib 121/121、公开 API 2/2、使用门面 5/5、使用示例 11/11，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均为 0 错误，文档测试 47 通过/23 忽略。
- **备注**：当前 renderer 在刷新时仍会重新声明有界窗口内的 `ViewNode`，keyed reconcile 只保证重叠组件不被重建；本条只闭合固定行高 VirtualScroll，不代表可变行高测量缓存、其他增量缓存或真窗视觉矩阵已经完成。

## ViewAdapter · 声明协调失效分类

- **修复**：组件原位 patch 原先只返回单一 `config_changed`，协调器会把颜色、背景等任意声明变化同时升级为 Layout/Paint；同一轮还会为特殊配置比较与普通比较重复抓取公开快照。
- **规格**：patch 结果分别报告 Paint 与 Layout；`Label` 忽略纯颜色差异，`Container`/`Grid` 的统一样式只把盒模型、固定尺寸、Flex/Grid、字号和可见性视为布局字段，Grid 断点/列声明仍触发布局。未审计组件必须保守请求 Layout；新旧公开快照每轮各抓取一次。
- **验证**：`32a5cbf6`；标签颜色、容器背景两个聚焦断言修复前失败，修复后连同文本、内边距、Grid 背景/轨道及未分类按钮回退共 6/6；完整 lib 127/127，布局/API/门面/示例 40/40，`cargo check --no-default-features --lib` 与 `cargo check --all-features --all-targets` 均为 0 错误，文档测试 47 通过/23 忽略。
- **备注**：本条只闭合 `Label`、`Container`、`Grid` 的声明协调边界；其余组件字段分类、可变行高与真窗视觉矩阵仍未闭合，`ProviderContext` 与布局 scratch/输出缓存由下一批单独审计。

## WidgetTree · 空子树派生状态

- **修复**：Phase 1 对空直接子集提前跳过，导致组件的 `layout_children([])` 清理分支在声明协调移除最后子项后不可达；Container/Space/Affix 继续暴露旧固有尺寸，ScrollView 继续暴露旧范围/偏移，Carousel 继续暴露旧幻灯片数量。
- **规格**：直接子节点增加、移除与重排完成后由树核心通知父组件；最后内容移除时清除上述派生状态，ScrollView 终止滑块交互并把受控偏移写回零，Carousel 从直接子节点数中排除自定义箭头。通知只发生在结构变更入口，不给每轮叶节点布局增加分发。
- **验证**：`8530d29`；新增 Container、Space、Affix、ScrollView、Carousel 空状态及自定义箭头共 6 项门禁，适配器模块 12/12、完整 lib 133/133、集成测试 91 通过/1 忽略，`cargo check -p uix --lib --no-default-features` 与 `cargo check -p uix --all-features --all-targets` 均为 0 错误，文档测试 47 通过/23 忽略。
- **备注**：审计确认 `ProviderContext` 变化继续保守请求 Layout，布局 scratch 每轮清空、遍历缓存受 `tree_version` 约束且当前没有跨帧 `LayoutOutput` 快照；其余组件失效分类、可变行高缓存与真窗视觉矩阵仍未闭合。

## FlexLayout · 固有反向与溢出换行

- **修复**：固有主轴在非零实际 frame 内先按容器 justify 定位，却按自然内容长度执行反向镜像，导致 RowReverse/ColumnReverse 子项落入负坐标；`overflow_content` 的独立流式分支还无条件跳过 wrap。
- **规格**：零或近零主轴 bootstrap 才使用自然内容长度完成反向镜像；一旦父级分配非零实际 frame，正向定位和反向镜像必须使用同一容器主轴。溢出换行复用共享 Flex 分行器，冻结 grow/shrink，并让 Stretch 与单行溢出一致地保留自然尺寸。
- **验证**：`b7f7e5c7`；单行溢出、标准固有主轴与溢出换行三个聚焦断言修复前分别得到负坐标或未换行，修复后 `cargo test --locked --test layout_invariants` 25/25、完整 lib 133/133、公开 API 2/2、使用门面 5/5、使用示例 11/11，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均成功。
- **备注**：非换行溢出继续使用既有无增长/压缩流式路径；本条只闭合已确认的固有反向边界与首个溢出换行组合，不代表 Grid 其余 justify/span、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

## GridLayout · 重叠同跨度顺序稳定性

- **修复**：多个同跨度子项重叠覆盖相邻 `Auto` 轨道时，逐项分摊并立即写回会让后续子项读取被前项修改的尺寸；相同 cell/span 约束交换声明顺序后产生不同列起点。
- **规格**：跨轨道贡献按 span 升序分批；同跨度子项必须基于批次开始时的同一轨道快照计算缺口，为每条 `Auto` 轨道登记最大计划增量，并在整批完成后统一写回。较短 span 继续先建立基础尺寸，声明顺序不参与轨道结果。
- **验证**：`b1aefee`；聚焦断言修复前正序/反序末列起点分别为 125px/100px，修复后 `cargo test --locked --test layout_invariants` 26/26、完整 lib 133/133，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均成功。
- **备注**：排序引用表不复制子项/轨道数据，计划增量缓冲与轨道数等长，二者均受既有放置预算约束；本条不代表 Grid 其余 justify、不同 span/Fr 组合、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

## GridLayout · 混合跨度弹性竞争

- **修复**：子项同时跨越 `Auto + Fr`、而 span 外还有其他 Fr 时，旧求解器跳过该自然尺寸约束；外部 Fr 分走剩余空间后，七十像素子项只获得六十像素。
- **规格**：在父级轨道容量内，按全局 Fr 权重计算 span 外可让份额并转入 span 内 Auto；约束按 span、起点与自然外尺寸确定性排序，以最多 64 轮单调松弛处理相互影响。没有 Auto、覆盖全部 Fr 或剩余空间耗尽时不伪造额外容量。
- **验证**：`9d86578c`；聚焦断言修复前得到 60px 子项宽度，修复后子项宽度与末轨起点均为 70px；`cargo test --locked --test layout_invariants` 27/27、完整 lib 133/133，`cargo check --locked --no-default-features --lib` 与 `cargo check --locked --all-features --all-targets` 均成功。
- **备注**：混合 span 只复用有界引用表与 Auto 尺寸账本，松弛次数固定为 64；本条不代表 Grid 其余 justify、覆盖全部 Fr 或没有 Auto 可转移的组合、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

### Grid 内容主轴对齐（第十四批，2026-08-07）

- **修复**：公开 `Grid::justify` 原先直接写入单元格内 `justify_items`，Center 只会在单格中移动子项，SpaceBetween/SpaceAround/SpaceEvenly 还会退化为逐项 Stretch；轨道组本身始终贴在内容起点。
- **规格**：`Grid::justify` 与低层 `GridLayout::with_content_justify` 控制整组列轨的水平内容分布，低层 `with_justify` 保留单元格内子项对齐。Center/End 分配前置空间，Space 系列只增加分布空间，Stretch 只均分正剩余空间给 Auto 列；固定/Fr 轨道不被内容 Stretch 改写。
- **验证**：`d81dbe5b`；Center 聚焦断言修复前得到首项 x=5px，修复后为 25px；六种固定轨道模式、Auto-only Stretch 与双通道兼容契约全部通过，`cargo test --locked --test layout_invariants` 28/28、完整 lib 135/135、文档测试 47 通过/23 忽略，两套特性组合检查均成功。
- **备注**：分布阶段复用既有列宽数组，不复制子项或轨道；本条闭合已公开的水平内容 justify，不代表覆盖全部 Fr 或没有 Auto 可转移的 span/Fr 组合、Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

### Grid 不可转移 Fr 跨度契约（第十五批，2026-08-07）

- **审计**：混合 span 覆盖全部 Fr 时没有外部 Fr 份额可转移；纯 Fr 的 span 内没有 Auto 轨道承接转移。两类路径都应服从父级容量和作者声明权重，不能用子项自然尺寸伪造轨道最小值。
- **规格**：`Auto + Fr` 覆盖全部 Fr 时保留既有 Auto 基础尺寸，并把其余容量按 Fr 权重分配；纯 Fr 的列/行 span 继续按全局权重解析，子项宽高收敛到实际单元格，列轴与行轴对称。
- **验证**：`41158b3f`；新增全 Fr 覆盖父容量、无 Auto 列跨度权重、无 Auto 行跨度权重三项契约并直接通过，`cargo test --locked --test layout_invariants` 31/31、完整 lib 135/135、文档测试 47 通过/23 忽略，两套特性组合检查均成功。
- **备注**：本批没有改写生产求解器，只把 CodeGraph 审计确认的既有正确边界固化为回归证据；Grid 当前登记的 span/Fr 范围已闭合，不代表 Flex 其他组合、组件一致性、缓存或真窗矩阵已经完成。

### FlexLayout · 负剩余主轴分布（第十六批，2026-08-07）

- **修复**：统一 justify 已实现负剩余空间下的 Center/End 偏移，但单行溢出、标准固有主轴与换行超宽行都在调用前把负差值钳为零，内容超过实际主轴时因此退化为 Start。
- **规格**：零尺寸 bootstrap 从自然起点开始；获得实际 frame 后，有限正负剩余空间都进入统一分布器。Center 使用半量差值、End 使用全量差值，Start 与 Space* 在负空间下保持起点和基础 gap；自然内容尺寸账本不因对齐偏移改变。
- **验证**：`518adc67`；单行溢出 Center、标准固有主轴 End、换行溢出超宽行 Center 三项聚焦断言修复前均停在容器起点，修复后 `cargo test --test layout_invariants` 34/34、完整 lib 135/135、文档测试 47 通过/23 忽略，`cargo check --lib --no-default-features` 与 `cargo check --all-features --all-targets` 均成功。
- **备注**：三条路径只在分布定位阶段复用既有有限差值，不改变 grow/shrink、换行拆分或内容总尺寸；本条不代表 Flex 其余组合、组件一致性、缓存或真窗矩阵已经完成。

### FlexLayout · 换行交叉轴 Stretch（第十七批，2026-08-07）

- **修复**：wrapped 求解器以自然最大子项高度建立行盒后直接定位，默认 Stretch 只能填满自然行高；单行因此与 non-wrap 分叉，多行也无法消费容器交叉轴正剩余空间。
- **规格**：先确定每行自然外尺寸并保留固定行间 gap；容器默认 Stretch 在存在实际正剩余空间时将其均分到每个行盒，并按前置行累计扩展量推进后续行起点。只有一行时等价于 non-wrap 的交叉轴填充；`align_self` 只覆盖子项在扩展行盒内的对齐。
- **验证**：`6fe836a4`；单行带 margin Stretch 与两行 `align_self` 覆盖门禁修复前分别得到 10px 自然高度和 14px 第二行起点，修复后 `cargo test --test layout_invariants` 23/23、`cargo test --test flex_overflow_invariants` 13/13、完整 lib 135/135、文档测试 47 通过/23 忽略，两套特性组合检查均成功。
- **备注**：行高和行起点沿用既有两个等长缓冲原位更新，不复制子项；原 1117 行布局契约同步拆为 804 行基础契约与 360 行 Flex 高阶契约。本条不代表 Flex 其余组合、组件一致性、缓存或真窗矩阵已经完成。

### FlexLayout · 零尺寸项目分行（第十八批，2026-08-07）

- **修复**：wrapped 分行器以累计主轴占用是否大于零判断当前行非空；零宽首项虽已被收集，占用仍为零，后续满宽项连同 gap 超过上限时也不会另起一行。
- **规格**：当前行是否已有项目由索引范围决定，不由累计尺寸推断；零主轴尺寸项目仍占据行内顺序，后续项目必须连同基础 gap 接受正常换行判断。
- **验证**：`952912e4`；聚焦断言修复前把第二项放在首行 `(x=9px, y=6px)`，修复后移到第二行 `(x=4px, y=19px)`；`cargo test --test layout_invariants --test flex_overflow_invariants` 为 23/23 + 14/14，完整 lib 135/135，文档测试 47 通过/23 忽略，两套特性组合检查均成功。
- **备注**：实现仅把现有分行非空判据从浮点占用改为索引存在性，不新增分配或额外遍历。本条不代表 Flex 其余组合、组件一致性、缓存或真窗矩阵已经完成。

### FlexLayout · 交叉轴 Stretch 上下限（第十九批，2026-08-07）

- **修复**：自然尺寸阶段虽已应用交叉轴 min/max，单行与 wrapped 最终 Stretch 仍会用容器或行盒可用尺寸直接覆盖，导致子项压破最小值或拉破最大值。
- **规格**：Stretch 先以扣除 margin 后的容器/行盒可用区生成候选，再以子项有序交叉轴上下限钳制；行盒继续消费实际剩余空间，受最大值限制的子项保持在行盒起点。
- **验证**：`bdccced7`；两个内部聚焦门禁修复前分别得到 10px 而非 20px 的单行最小高度、20px 而非 12px 的换行子项高度，修复后新增门禁 2/2、内部 Flex 6/6、公开布局契约 37/37、完整 lib 137/137，两套特性组合检查均成功。
- **备注**：实现复用既有 `child_cross_bounds`，没有新增分配或额外遍历。本条不代表 Flex 其余组合、组件一致性、缓存或真窗矩阵已经完成。

### FlexLayout · 换行交叉轴 End 溢出（第二十批，2026-08-07）

- **修复**：wrapped 行组的交叉轴 End 分支把负剩余空间钳为零；自然行组高于容器时会退化为 Start，末行末端越过容器。
- **规格**：获得实际交叉轴后，Center/End 都保留有限负差值并分别使用半量/全量偏移；End 让自然行组末端贴住容器末端，内容从交叉轴起点外溢。
- **验证**：`9cd3ae5a`；两行自然高度 25px、容器高度 15px 的聚焦契约修复前首行停在 `y=6px`，修复后位于 `y=-4px` 且末行底边贴住 `y=21px`；公开布局契约 38/38、完整 lib 137/137，两套特性组合检查均成功。
- **备注**：实现只移除 End 分支的正值钳制，不改变行高、行距、主轴分布或内容尺寸账本。本条不代表 Flex 其余组合、组件一致性、缓存或真窗矩阵已经完成。

### FlexLayout · 换行交叉轴 bootstrap 对齐（第二十一批，2026-08-07）

- **修复**：wrapped Center/End 未区分零交叉轴 bootstrap，会围绕尚不存在的零尺寸范围移动自然行组并生成负坐标。
- **规格**：只有获得实际交叉轴后才按有限正负剩余空间执行 Center/End；bootstrap 固定零偏移，从自然原点排列并让全部自然行高与固定 gap 撑开输出。
- **验证**：`af35d11b`；两行自然高度 25px、零高度 frame 的聚焦契约修复前让 Center 首行从 `y=6px` 移到 `y=-6.5px`，修复后 Center/End 都从 `y=6px` 开始；公开布局契约 39/39、完整 lib 137/137，两套特性组合检查均成功。
- **备注**：实现只给既有 Center/End 行组偏移增加 bootstrap 守卫，不改变实际容器的正负对齐、行账本或逐项 `align_self`。本条不代表 Flex 其余组合、组件一致性、缓存或真窗矩阵已经完成。

### FlexLayout · 空子集固有主轴（第二十二批，2026-08-07）

- **修复**：公开空子集早返回不再让 `intrinsic_main` 与 `overflow_content` 无条件继承完整 frame 主轴。
- **规格**：固定尺寸空 Flex 继续占用父级分配的宽高；固有主轴或溢出模式按方向只把零个自然子项对应的主轴收敛为零，交叉轴保留分配尺寸，反向方向与正向方向对称。
- **验证**：`382dd1df`；100×20px frame 的水平固有契约修复前错误返回 100×20px，修复后水平、垂直与溢出入口分别返回 0×20px、100×0px 与 0×20px；公开布局契约 40/40、完整 lib 137/137，两套特性组合检查均成功。
- **备注**：实现只修公开 `FlexLayout` 空子集账本，不改变底层 Flex/Grid padding、非空布局或位置求解。本条不代表 Flex 其余组合、组件一致性、缓存或真窗矩阵已经完成。

### RichText · 宽度约束测量缓存（第二十三批，2026-08-07）

- **修复**：估算测量缓存不再跨不同宽度约束复用旧折行高度，也不让旧行坐标与代码复制区域继续参与新宽度命中。
- **规格**：有效宽度是固有高度缓存键；约束变化先以估算字体重排行高并清除旧绘制/命中几何，绘制再以相同宽度、真实字体与当前主题颜色重建缓存。
- **验证**：`d91d9831`；同一文本先在 400px 宽度测得 21px 高，再缩到 80px，修复前仍错误返回 21px，修复后高度随折行增加；公开布局契约 41/41、完整 lib 137/137，两套特性组合检查均成功。
- **备注**：实现只闭合宽度约束下的估算测量缓存，不宣称真实字体/DPI 全矩阵、复杂文本排版、其他组件一致性、可变行高或真窗矩阵已经完成。

### Notification · 窄窗水平留白（第二十四批，2026-08-07）

- **修复**：固定 384px 条目宽度不再在窗口不足 432px 时牺牲一侧 24px 留白，也不再与 Message 的窄窗宽度语义分叉。
- **规格**：条目宽度先从规范化 frame 扣除双侧 `HORIZONTAL_INSET`，再以 `WIDTH` 封顶；绘制、浮层/脏区边界与动画后命中统一消费 `notification_rects`，不得各自重算窄窗几何。
- **验证**：`646f33de`；400×200px frame 的 TopRight 条目修复前为 `x=0,w=384`，修复后为 `x=24,w=352`，命中边界等于同一矩形；组件聚焦契约 1/1、完整 lib 138/138，两套特性组合检查均成功。
- **备注**：实现只闭合 Notification 窄窗水平留白，不代表浮层/表格等其他组件一致性、Flex 剩余组合、可变行高或真窗矩阵已经完成。

### Table · 固定列重叠命中（第二十五批，2026-08-07）

- **修复**：左右固定列在窄视口中重叠时，命中不再返回被最后绘制的右固定列遮挡的左固定列。
- **规格**：表头、分组表头与表体按共享 `COLUMN_PAINT_ORDER` 执行 Middle → Left → Right 绘制；`column_at` 按 Right → Left → Middle 逆序命中，重叠区域必须返回视觉最上层列。
- **验证**：`b14313ca`；100px 视口中左右固定列各宽 80px，`x=10/50/90px` 修复后分别命中索引 0/1/1，其中重叠点修复前错误返回索引 0；几何聚焦契约 1/1、完整 lib 139/139，两套特性组合检查均成功。
- **备注**：实现只闭合固定列重叠时的绘制/命中优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

### Table · 固定列重叠句柄命中（第二十六批，2026-08-07）

- **修复**：不同固定区的列宽调整线重合时，句柄命中不再因声明顺序返回被上层列区遮挡的列。
- **规格**：句柄候选先按共享 `COLUMN_PAINT_ORDER` 的逆序 Right → Left → Middle 分层；最高可见层存在候选时立即返回，只在同一层内比较指针到列右边缘的距离。
- **验证**：`d51d5b0e`；100px 视口中左固定列宽 80px、右固定区两列各宽 20px，`x=80px` 修复前返回左列索引 0，修复后返回最后绘制的右内列索引 1；交互几何聚焦契约 1/1、完整 lib 140/140，两套特性组合检查均成功。
- **备注**：实现只闭合固定列重叠调整线的绘制/命中优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

### Table · 选择列覆盖命中（第二十七批，2026-08-07）

- **修复**：右固定列侵入 32px 选择区时，表头与表体动作不再响应视觉上已被覆盖的复选框。
- **规格**：选择列先绘制，Middle → Left → Right 列区随后覆盖；动作解析必须先调用 `column_at_x` 获取视觉最上层列，仅当选择区内没有可见列时才返回 `ToggleAll` 或 `ToggleRow`。
- **验证**：`cd4e9a95`；100px 视口中 90px 右固定列覆盖 `x=10..32px`，表头 `x=20px` 修复前返回 `ToggleAll`，修复后返回 `SortColumn(0)`；同点表体返回 `SelectRow(0)`，未覆盖的 `x=5px` 仍返回 `ToggleAll`。交互聚焦契约 1/1、完整 lib 141/141，两套特性组合检查均成功。
- **备注**：实现只闭合选择列被固定列覆盖时的绘制/动作优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

### Table · 展开控件覆盖命中（第二十八批，2026-08-07）

- **修复**：极窄表体中最后绘制的展开箭头覆盖选择列时，动作不再响应其下方的行复选框。
- **规格**：展开箭头在选择列、普通列和合并单元格之后绘制，其行末 32px 交互区在真实数据行内优先于 `ToggleRow` 与 `SelectRow`；表头和展开内容区不产生展开动作。
- **验证**：`52f99f94`；24px 视口中 `x=10px` 的表体动作修复前返回 `ToggleRow(0)`，修复后返回 `ToggleExpand(0)`，表头同点仍返回 `ToggleAll`。相邻层级聚焦契约 2/2、完整 lib 142/142，两套特性组合检查均成功。
- **备注**：实现只闭合展开控件覆盖选择列时的绘制/动作优先级，不代表表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

### Table · 分组表头固定区层级（第二十九批，2026-08-07）

- **修复**：有标题分组不再因声明顺序让较低固定区覆盖最后应可见的右固定组。
- **规格**：分组标题片段以共享 `COLUMN_PAINT_ORDER` 的 Middle → Left → Right 为外层遍历，同一列区内才保持分组声明顺序；绘制访问器不建立中间集合。
- **验证**：`625b0c31`；100px 视口中先声明 80px 右固定组、后声明 80px 左固定组，修复前片段顺序为 Right → Left，修复后为 Left → Right，`x=50px` 命中右固定列。绘制层级聚焦契约 1/1、完整 lib 143/143，两套特性组合检查均成功。
- **备注**：实现只闭合有标题分组之间在固定区重叠时的全局层级，不代表跨层单列、合并单元格等其他组合已经完成；跨层单列随后由第三十批闭合。

### Table · 跨层表头固定区层级（第三十批，2026-08-07）

- **修复**：跨两层无标题单列不再因处于全局叶表头阶段而覆盖更高层右固定分组标题。
- **规格**：两层表头以共享 `COLUMN_PAINT_ORDER` 的 Middle → Left → Right 为最外层，在每个列区内依次完成有标题分组与叶表头阶段；跨层单列不能绕过该列区层级。
- **验证**：`a4dada8f`；100px 视口中先声明 80px 右固定组、后声明 80px 左固定单列，`x=50px,y=10px` 的片段顺序修复前为“右分组 → 左单列”，修复后为“左单列 → 右分组”，列命中仍返回右固定列索引 0。表头层级契约 2/2、完整 lib 144/144，两套特性组合检查均成功。
- **备注**：实现只闭合跨两层无标题单列与有标题分组在固定区重叠时的全局层级，不代表合并单元格等表格其他组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

### Table · 行合并重绘固定区层级（第三十一批，2026-08-07）

- **修复**：左固定行合并锚点在全部普通行之后补绘时，不再反向覆盖已经绘制的右固定列。
- **规格**：普通 Middle → Left → Right 绘制仍使用完整列区裁剪；末尾合并重绘必须扣除更高列区覆盖，只提交锚点所属列区最终仍可见的片段。
- **验证**：`d4999ab3`；100px 视口中左右固定列各宽 80px，左区普通裁剪保持 `x=0..80px`，合并重绘裁剪修复前同为 80px、修复后收敛为 `x=0..20px`，右区完整保持 `x=20..100px`，`x=50px` 命中右列索引 1。几何层级契约 2/2、完整 lib 145/145，两套特性组合检查均成功。
- **备注**：实现只闭合左固定行合并锚点末尾重绘时的固定区层级，不代表跨固定区列合并、其他合并组合、浮层、Flex 剩余组合、可变行高或真窗矩阵已经完成。

### Table · 普通文本跨固定区列合并几何（第三十二批，2026-08-07）

- **修复**：逻辑上连续、物理上被固定区重排的 `col_span` 不再从锚点位置累加声明宽度，避免漏绘左区并越出视口。
- **规格**：合并边界必须并集所有逻辑覆盖列的真实物理位置；普通文本按 Middle → Left → Right 列区顺序分别裁剪实际相交片段，命中仍将任一覆盖列归一到同一锚点。
- **验证**：`3348a7f4`；100px 视口中先声明 60px 右固定锚点列、再声明 40px 左固定覆盖列，合并矩形修复前为 `x=40,w=100`，修复后为 `x=0,w=100`，绘制片段分别为左区 `x=0..40px`、右区 `x=40..100px`；`x=10px` 命中索引 1 后返回锚点 `(0,0)`。跨区聚焦契约 2/2、Table 12/12、完整 lib 147/147，两套特性组合检查均成功。
- **备注**：普通文本跨固定区列合并已闭合；该批尚未覆盖自定义 View 单元格的跨固定区多片段布局。

### Table · 自定义 View 跨固定区多片段（第三十三批，2026-08-07）

- **修复**：跨固定区的自定义 View 单元格不再只占锚点所属列区，也不会为各区复制有状态子树。
- **规格**：唯一 View 根节点按全部逻辑覆盖列的真实物理联合矩形布局一次；父布局按 Middle → Left → Right 保存最终可见片段，合成、2D 命中、指针捕获与可见区域共同消费该集合，空片段隐藏整棵子树，片段间隙不能穿透完整 frame 包围盒。
- **验证**：`5a3c0c1f`；100px 视口中先声明 60px 右固定锚点列、再声明 40px 左固定覆盖列，旧 View frame 为 `x=40,w=60`，修复后唯一 frame 为 `x=0,w=100`，父级片段为左区 `x=0..40px` 与右区 `x=40..100px`；独立间隙契约确认左右片段命中同一子树、中间空隙回落父节点。回归契约 2/2、Table 14/14、完整 lib 149/149，两套特性组合检查均成功。
- **备注**：自定义 View 跨固定区多片段已闭合；表格其他合并组合、浮层、Flex 剩余组合、可变行高和真窗矩阵仍待核验。

### FlexLayout · 未分行真实交叉轴（第三十四批，2026-08-07）

- **修复**：wrapped 求解器在 wrap 开启但最终只有一行时，较小交叉轴或逐项 `align_self` 仍使用自然行高，导致和 non-wrap 的同一布局分叉。
- **规格**：最终只有一行且已获得实际交叉轴时，唯一行盒直接采用完整容器交叉尺寸；Stretch 在扣除 margin 后伸缩，Center/End 相对真实行盒定位，自然外尺寸继续保留在内容账本。实际多行与零交叉轴 bootstrap 语义不变。
- **验证**：`09354738`；八像素高容器中，逐项 Stretch 子项修复前错误保留 10px 自然高、逐项 End 子项错误位于 `y=17px`，修复后分别为 4px 与 `y=11px`。布局契约 24/24、Flex 高阶契约 18/18、内部 Flex 6/6、完整 lib 149/149、文档测试 47 通过/23 忽略，两套特性组合检查均成功。
- **备注**：主轴 justify 纯计算拆入 68 行独立模块，核心 `flex.rs` 从 899 行降至 881 行；公开 API 与实际多行分配不变。Flex 其余组合、组件一致性、缓存和真窗矩阵仍待核验。

### Popover / Popconfirm · 同帧表面定位（第三十五批，2026-08-07）

- **修复**：触发器 frame 未变化而窗口逻辑表面缩小时，浮层栈重建仍读取上一帧绘制缓存，导致绘制已经按新表面收敛、overlay bounds 与命中却继续停留在旧位置。
- **规格**：`WidgetRender::overlay_entry_for_surface` 在布局阶段显式接收当前根表面；默认实现委托既有 `overlay_entry`，只有依赖窗口边界的组件覆盖。Popover/Popconfirm 在同一次 OverlayStack 重建中刷新表面，再由统一 placement 解析器生成 dirty、登记与命中几何；触发器 frame 相同也不得复用旧表面下的绝对气泡矩形。
- **验证**：`f41fede8`；Popover 的窄窗横坐标修复前停在 `x=150px`、应为 `20px`，Popconfirm 停在 `x=200px`、应为 `60px`，两项失败契约修复后 2/2。完整 lib 151/151、布局不变量 24/24、反馈门面 4/4、文档测试 47 通过/23 忽略，无默认特性与全特性全目标检查均成功。
- **备注**：目标文件 rustfmt 与差异检查通过；全局 `cargo fmt --all -- --check` 仍先被既有 `src/native/backends/macos/platform/cocoa.rs:773` 多余闭合分隔符阻断，并继续报告仓库既有格式差异。Tooltip、选择类弹层等其余浮层组合和真窗 resize 矩阵仍待核验。

### Tooltip / Slider · 表面约束提示几何（第三十六批，2026-08-07）

- **修复**：共享提示气泡原语原先只按作者指定的 Top/Bottom/Left/Right 生成固定矩形，不感知当前 logical surface；靠近边缘时反馈 Tooltip 与 Slider 拖动提示会越出窗口，超长文字还会生成比窄表面更宽的绘制、脏区与 overlay bounds。
- **规格**：共享解析器先归一目标与表面，按作者方向及主轴反向候选的总越界量择优，再把气泡宽高和坐标收敛到表面；绘制使用解析后的真实方向放置箭头，箭头锚点指向触发中心，文字裁在最终气泡内。Tooltip 与 Slider 均覆盖 `overlay_entry_for_surface`，登记、dirty 与绘制消费当前帧同一表面；首次独立调用旧入口时只使用有限回退，不制造无界矩形。
- **验证**：`5fefe2ed`；顶部气泡靠近 `y=4px` 时修复前越出表面，修复后翻转为 Bottom 并完整留在 200×100px 表面；超长提示在 80px 窄表面内宽度不再超过 80px；Tooltip 与 Slider 两项显式表面登记契约均与共享解析器一致。共享原语 2/2、显式表面集成 2/2、完整 lib 155/155、布局不变量 24/24、反馈门面 4/4、公开 API 2/2、文档测试两套均为 47 通过/23 忽略，无默认特性与全特性全目标检查均成功。
- **备注**：三个目标文件 rustfmt 与差异检查通过；全局 `cargo fmt --all -- --check` 仍先被既有 `src/native/backends/macos/platform/cocoa.rs:773` 多余闭合分隔符阻断，并继续报告仓库既有格式差异。选择类弹层等其余浮层组合和真窗 resize/长文案视觉矩阵仍待核验。

### Select · 弹层同帧表面几何（第三十七批，2026-08-07）

- **修复**：Select 原先只在绘制阶段读取逻辑表面并决定上下方向，自定义选项布局、滚动视口、dirty、命中与 OverlayStack 登记会先消费旧 `dropdown_rect`；上下空间都不足时不缩高，控件宽于或靠近窄表面右边界时也不收窄，过滤前后行数导致翻转方向变化时保守 dirty 还可能遗漏当前短弹层。
- **规格**：共享解析器归一控件与 surface，横向把宽度和起点收敛到表面；纵向优先完整向下、否则完整向上，两侧都不足时选择空间更大的一侧并缩高。布局从 `WidgetTree` 根 frame、绘制从 `PaintContext`、显式登记从 `overlay_entry_for_surface` 取得当前表面，最终矩形统一供自定义选项、裁剪、滚动、键盘揭示、绘制、命中、dirty 与 bounds 使用；状态切换 dirty 合并当前实际与过滤前后保守矩形。
- **验证**：`d288e6c5`；修复前底边场景仍把 84px 弹层放在控件下方，窄表面场景仍保留 120px 宽并越出右边界；修复后底边弹层翻到上方并按 80px 可用空间缩高，右侧弹层收敛到 100px 表面。底边、窄表面、自定义选项布局与反向 dirty 契约 4/4，完整 lib 159/159、布局不变量 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11，两套文档测试均为 47 通过/23 忽略，无默认特性与全特性全目标检查均成功。
- **备注**：五个目标文件 rustfmt 与差异检查通过；全局 `cargo fmt --all -- --check` 仍先被既有 `src/native/backends/macos/platform/cocoa.rs:773` 多余闭合分隔符阻断，并继续报告仓库既有格式差异。Cascader、TreeSelect、AutoComplete、Mentions 与日期类选择弹层及真窗 resize/过滤/滚动视觉矩阵仍待核验。

### Cascader · 多列弹层同帧表面几何（第三十八批，2026-08-07）

- **修复**：Cascader 原先固定向下生成 200px 高弹层，每列至少 200px，多列总宽、滚动视口、绘制、dirty、命中与 OverlayStack 登记各自重算无表面几何；靠近底边时越界，窄表面会被多列总宽撑破，缩高后滚动上限与列命中仍按旧尺寸计算。
- **实现**：组件私有共享解析器以当前 logical surface 横向收敛总宽和起点，纵向依次尝试完整向下、完整向上与较大可用空间缩高；最终总宽按可见列数等分。显式登记与绘制记录同帧表面及相对弹层缓存，事件、滚动、键盘显露、绘制裁剪、dirty、命中和 bounds 共同复用最终矩形与实际列宽，不扩展公开 trait。
- **验证**：`c386f992`；修复前底边弹层仍向下越界，100px 窄表面仍被单列撑破，78px 实际视口的滚动上限仍停在 120px、而应为 242px；修复后底边翻到上方并缩高，窄表面完整收敛，两列总宽压到 300px 后第二列命中按 150px 实际列宽映射。聚焦契约 4/4、完整 lib 163/163、布局不变量 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11，两套文档测试均为 47 通过/23 忽略，无默认特性库与全特性全目标检查均成功。
- **备注**：三个目标文件 rustfmt 与差异检查通过；全局 `cargo fmt --all -- --check` 仍先被既有 `src/native/backends/macos/platform/cocoa.rs:773` 多余闭合分隔符阻断，并继续报告仓库既有格式差异。TreeSelect、AutoComplete、Mentions、日期类选择弹层及真窗 resize/多列/滚动视觉矩阵仍待核验。

### TreeSelect · 树选择弹层同帧表面几何（第三十九批，2026-08-07）

- **修复**：TreeSelect 原先固定向下生成最多 280px 高、至少 200px 宽的弹层，窄表面与底边场景会越界；事件命中、虚拟滚动、键盘显露、绘制、dirty 与 OverlayStack 登记又按自然尺寸或触发器下方坐标各自计算，表面缩放时旧弹层尾部也可能漏出脏区。
- **实现**：私有共享解析器横向收敛宽度与起点，纵向优先完整向下、其次完整向上、最后选择较大空间并缩高；组件缓存同帧 surface、触发锚点、相对弹层和历史绝对脏区，事件、虚拟行、滚动显露、绘制裁剪、dirty、命中与 bounds 复用最终几何。节点展开或过滤改变行数时继续使用同一表面和绝对锚点重算，不回退到固定自然视口。
- **验证**：`250ba978`；修复前底边弹层仍向下越界，100px 窄表面仍被 200px 最小宽度撑破，80px 实际视口的末行显露只滚到 280px、而应为 480px，翻转区域命中返回空；修复后弹层翻到上方并缩为 80px，横向完整收敛，滚动上限和命中映射均复用实际视口，表面变化后的 dirty 也覆盖旧弹层尾部。聚焦契约 5/5、完整 lib 168/168、布局不变量 24/24、公开 API 2/2、使用门面 5/5、使用示例 11/11，两套文档测试均为 47 通过/23 忽略，无默认特性库与全特性全目标检查均成功。
- **备注**：四个目标文件 rustfmt 与差异检查通过；全局 `cargo fmt --all -- --check` 仍先被既有 `src/native/backends/macos/platform/cocoa.rs:773` 多余闭合分隔符阻断，并继续报告仓库既有格式差异。AutoComplete、Mentions、日期类选择弹层及真窗 resize/展开/滚动视觉矩阵仍待核验。

## Avatar

- **修复**：长 fallback 固定字号溢出、受限 frame 按名义尺寸绘、默认文字对比不足、低分辨率先遮罩后放大→边缘退化、方形未沿用 fallback 圆角。
- **规格**：文字按实际居中正方形测量缩放，图片按 DPR 居中裁切后抗锯齿遮罩，位图复用 generation 递增防旧 handle 复效。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Calendar

- **修复**：绘制/命中用名义 `cell_size`→外溢误触、标题/星期/日期固偏未居中、标题硬编码中文、箭头争空间、首日遗漏边框、星期低对比色、焦点环与填充同色。
- **规格**：标题/星期/日期按实际几何测量缩放，全程裁剪，命中复用最近渲染几何。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前专项回归与 Windows Vulkan/NVIDIA 真窗场景

## Card

- **修复**：标题/action 固定字号越界、正文缺 body 裁剪、窄卡负宽装饰条、过大 padding 负几何、短卡标题 action 争空间、`AfterChildren` 重绘背景覆正文。
- **规格**：标题/action 保留 11/10px 下限按实宽省略，卡片/标题/action/正文分别按实 frame/body 裁剪。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Carousel

- **修复**：非当前幻灯仅归零直接子 frame→后代仍参与绘制/命中/焦点、交互幻灯遮后绘控件、子控件焦点时方向键误冒泡切页、窄视口命中失配、reconcile 误读运行态。
- **规格**：父级可见性条件隐藏非当前子树，控件区先捕获再聚焦 Carousel，箭头/圆点/命中/裁剪共用安全几何，提供 `.size()` 首选视口。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Collapse

- **修复**：固有宽 0、正文只按显式换行计高、长标题/正文越界、字符箭头、绘制/命中分叉、按下即切换且移出仍激活、缺 hover/pressed、动画完未请求布局收缩、reconcile 误读焦点/展开状态。
- **规格**：按受限宽测量换行，标题省略/正文/箭头/焦点/命中共用裁剪几何，完整点击序列才激活，动画后传播布局失效。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan 真窗场景

## Descriptions

- **修复**：宽不足仍强制列数、标签可占满格、标题/label/value 不裁剪不换行、固定行高测量、零宽标签区 1px 虚增高。
- **规格**：每列 ≥140px 自适应收敛列数保留 span 契约，标题省略，字段按各自可用宽换行以同行最大内容高测量，bordered/非 bordered 全程裁剪。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan 真窗场景

## Empty

- **修复**：固定 160×100 测量→长文本单行越界、图标/文字百分比定位受限偏移、纯文本被压下区、四级色对比不足、手绘图标与 Lucide 分叉。
- **规格**：按本地化描述在 160–320px 首选宽内测量，受限换行随内容计高，内容块按实 frame 居中，复用公共 Lucide 图标。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## Image

- **修复**：加载后 `alt` 绘到 frame 外、占位/fallback/焦点/预览缺统一裁剪、长 fallback 不换行、四级色对比不足、emoji 预览与关闭图标、非有限尺寸/圆角污染几何、`radius` 只作用背景位图保持直角。
- **规格**：`alt` 保持语义名称，fallback 实 frame 内居中换行，缩略图按 DPR/尺寸/圆角/fit 生成缓存抗锯齿圆角位图。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## List

- **修复**：空列表无 empty→整块空白、header/item/footer/load-more 受限越界、窄 frame 负分隔线、换行破坏固定行高、静态条目未入语义快照。
- **规格**：保留 ConfigProvider 自定义空态优先并回退 Empty，固定高度行按 Unicode 单行省略，文字/分隔线/边框统一裁剪于归一化 frame。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## ResultView

- **修复**：图标/标题/副标题/按钮按 400×300 固定偏移受限外绘、长文本/按钮不换行不省略、按钮命中依赖缓存重排前后错位、框外释放仍提交、404 低对比四级色、默认本地化未入语义。
- **规格**：按实 frame 为图标/标题/副标题和 36px 操作区分配共享裁剪，窄高优先保留标题/副标题各一行，按钮 Unicode 省略，命中同步当前 frame 操作区，完整点击才提交。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## SelectableList

- **修复**：行命中只查纵坐标、按下即提交且移出仍激活、缺 pressed 反馈、滚轮在 header/footer 被吞、受限 frame 负几何、长文字越界、固定图标偏移窄行溢出、reconcile 按旧索引粘到重排后项。
- **规格**：同一归一化几何驱动 header/视口/footer/二维命中/滚动，仅同一目标内完成按下释放后提交，Unicode 单行省略/图标缩放/绘制/焦点框统一受实 frame 裁剪，非受控选择按 item id 跨重排保留。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## Skeleton

- **修复**：负数/NaN/无穷尺寸进入测量与快照、Text 按配置高而非实 frame 计行高受限外溢、极窄 Rect 圆角和 Circle 半径未收敛。
- **规格**：Rect 圆角/Circle 半径/Text 两行高与间距全部按真实可用尺寸计算并统一裁剪。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## Table · 受限渲染

- **修复**：非法列宽/视口尺寸/行高进入测量几何与虚拟滚动、短表头 frame 虚增一行表体视口可命中、长列标题/分组标题/单元格/空态跨列或跨 frame 绘、外焦点框被裁、展开行用字符箭头。
- **规格**：规范化普通/typed/expandable 三条路径尺寸，表头/单元格/空态/表体/边框/焦点框统一按实 frame/列区裁剪，固定单行按 Unicode 省略，展开指示复用 Lucide。
- **验证**：Windows 真窗 ✅
- **备注**：交互闭环见下行；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## Table · 指针交互

- **修复**：排序表头/全选/行复选/普通行/展开按钮 PointerDown 立即提交→移出仍激活、缺 pressed 反馈、header/frame 外滚轮仍滚表体、无变化 PointerMove 吞事件。
- **规格**：统一目标枚举驱动二维命中/按下/释放；同一目标完整序列才改变排序/勾选/当前行/展开，移出/PointerLeave/FocusOut 取消；行与排序表头分别 pressed 背景，hover 仅目标变化时重绘，滚轮只真实表体生效。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染、交互与语义问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## Tag

- **修复**：主体/关闭区在 PointerDown 立即提交、移出仍激活、关闭命中比可见槽宽 8px、重复按键可能重复切换、缺 hover/pressed、窄 frame 产生负文字区并让文字/图标/焦点框越界、浅色自定义背景固定白字。
- **规格**：统一实际几何驱动主体、14px 勾选槽、精确 20px 关闭槽、绘制与二维命中；指针和 Enter/Space 仅匹配释放时提交，离开/失焦取消；Unicode 单行省略、全部内容与内嵌描边统一裁剪，自定义背景按明暗选择黑/白前景。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染、交互与语义问题；仅覆盖当前回归、完整 lib 回归与 Windows Vulkan/NVIDIA 真窗场景

## Timeline

- **修复**：固定 400×N×60 坐标直接绘制导致受限 frame 内文字、节点和连接线越界，长标签/说明不省略，节点外环固定白色在深色容器中分叉，reverse 每帧分配引用 Vec，组件语义退化为 Generic。
- **规格**：归一化实际 frame 统一计算 28–60px 行距、节点/连接线/文字槽和可见行，标签与说明按 Unicode 单行省略并双层裁剪，节点外环使用容器背景；reverse 零临时分配，List `value_text` 按视觉顺序发布说明并把 pending 保持末尾。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与语义问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Tree

- **修复**：勾选/展开/选择 PointerDown 立即提交且移出仍激活、绘制与命中槽位分叉、横向越界仍命中、缺 hover/pressed、滚轮不校验 frame、深层缩进/长标题硬裁、极小 frame 焦点框外溢、展开未请求布局、无障碍把业务 key 当用户值、flatten 每次克隆整棵节点树。
- **规格**：统一实际 frame 行几何驱动有上限缩进/勾选/Lucide 展开/图标/标题/二维命中，完整同目标释放才提交，离开/失焦取消；Unicode 单行省略与全部绘制统一裁剪，展开行数变化发一次 Layout，请求语义发布可见标题与位置，flatten 直接构建临时行表。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染、交互与语义问题；仅覆盖当前回归、Windows Vulkan/NVIDIA 真窗语义交互与浅/深色后台精确窗口验证

## RichText

- **修复**：链接与代码复制 PointerDown 立即提交且移出仍激活、框外按下启动选择、绘制/代码复制区未统一裁剪、无效 frame 仍布局、默认固定灰色导致浅色正文低对比、主题切换复用旧色布局缓存、QA 缺代码/折行/受限/完整释放覆盖。
- **规格**：归一化实际 frame 统一裁剪字形背景/文字 run/选择/链接/代码按钮与二维命中；链接/复制使用稳定 segment 目标并仅同目标释放提交，离开/失焦/reconcile 取消；默认正文随 `color_text`，显式 `.color(...)` 保持覆盖，解析后颜色进入缓存键。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染、选择与语义问题；仅覆盖当前回归、Windows Vulkan/NVIDIA 真窗语义交互与浅/深色后台精确窗口验证

## Alert

- **修复**：PointerDown/Enter/Space 立即关闭且移出无法取消、关闭命中与绘制分叉、未归一化/裁剪导致极窄负几何和文字越界、消息使用状态色对比不稳、关闭无 hover/pressed、关闭后 measure 未回收、说明未入无障碍值。
- **规格**：归一化实际 frame 统一分配强调线/图标/消息/说明/关闭槽，消息与说明 Unicode 单行省略，正文随 `color_text`；同一关闭目标完整释放才提交，离开/失焦/reconcile 取消，关闭返回零测量并请求布局。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查反馈组件的渲染、交互与语义问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Drawer

- **修复**：闭合触发器/关闭按钮 PointerDown 立即提交且不可键盘访问、拉伸 frame 的触发视觉与命中错位、标题/extra/关闭槽固定坐标重叠、超 surface 与非法尺寸产生负几何、内容子树打开态不裁剪、关闭无 hover/pressed、闭合态仍发布 Dialog、空标题触发器缺可访问名称。
- **规格**：遮罩面板归一化并收敛至真实 surface，header/body/footer/子树共享非负几何和裁剪，标题/extra Unicode 单行省略；闭合 Button 与打开 Dialog 发布 expanded 和非空名称，触发器和关闭槽完整释放才提交，离开/失焦或含 mask 在内的交互几何改变时取消，overlay focus trap/Escape 保持。
- **验证**：Windows 真窗 ✅
- **备注**：footer 内置确认仍是视觉槽，业务提交继续由内容子树真实 Button 承担；仅覆盖当前回归、Windows Vulkan/NVIDIA 真窗右侧面板与现有 overlay 性能验证

## Message

- **修复**：关闭 PointerDown 立即提交、长内容覆盖关闭槽/越卡、四项 184px 栈越出 160px surface、临时字符图标、无 hover/pressed、快照落入 Generic 且无内容。
- **规格**：有限非负 frame 决定最新可见容量，40px 卡片/48px 步长与动画后几何共同驱动裁剪、Lucide 状态/关闭图标、Unicode 单行省略、二维命中和 dirty/overlay bounds；同一稳定项完整释放才关闭，离开/失焦/frame/placement 改变取消，非空队列发布 Alert 内容且不改隐藏较早项的公开队列、ID 和计时。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查反馈组件；仅覆盖 Message，不扩展到 Notification

## Notification

- **修复**：关闭 PointerDown 立即提交、长标题/说明覆盖关闭槽或越卡、受限 surface 中通知栈外溢、临时字符图标、无 hover/pressed、非零原点 frame 命中坐标失配、非空快照落入 Generic 且无内容。
- **规格**：有限非负 frame 按 48/66px 实际卡片高和 12px 间距决定最新可见后缀，Lucide 状态/关闭图标、标题/说明 Unicode 单行省略、二维命中和 dirty/overlay bounds 共用裁剪几何；同一稳定项完整释放才关闭，离开/失焦/frame/placement 改变取消，非空队列发布 Alert 标题与说明且不改隐藏较早项的公开队列、ID 和计时。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查反馈组件；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Popover / Popconfirm

- **修复**：触发子树缺布局、PointerDown/KeyDown 立即开关或确认、拉伸 frame 仍按固定 80×28 命中、固定浮层越出 surface、长标题/正文/按钮越界、字符警告图标、箭头固定居中、dirty/overlay bounds 漏阴影与箭头、Popover 快照退化为 Generic。
- **规格**：触发子树铺到归一化实际 frame，声明方向与主轴反向候选按越界量择优，220×100 / 200×110 浮层及阴影/动画 sweep 收敛并裁在 logical surface；箭头锚定触发中心，Lucide 警告图标与全部文字按实际非负槽位 Unicode 单行省略；同一稳定目标完整 PointerDown/PointerUp 或 KeyDown/KeyUp 才打开、取消或确认，离开/失焦/几何配置变化取消；Popover 发布 Button、expanded 与内容值，Popconfirm 发布当前操作且仅确认产生 Submit。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查反馈组件；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## ProgressBar / Spin

- **修复**：Component QA 把 45%/64%/100% 误写为 `45.0`/`64.0`/`100.0` 并全部裁成满进度，`round(false)` 仍用 token 圆角；ProgressBar 与 Spin 在 Content / AfterChildren 两轮重复绘制，非法或紧凑实际 frame 可产生越界；Spin 包装遮罩先于子树绘制、停止时仍留提示、长 tip 不省略且无默认可访问名称。
- **规格**：进度继续只接受 `0.0..=1.0`，线形/圆形/不确定形态统一归一化并裁在实际 frame，方角真实使用零圆角；普通 Spin 只在 Content 绘制，包装模式把子树铺满并只在 AfterChildren 绘制主题自适应遮罩、按可用尺寸缩放转子和 Unicode 单行省略 tip，停止时透明，默认发布 Status"加载中"。
- **验证**：Windows 真窗 ✅
- **备注**：包装模式保持零固有占位，调用方需由 Grid/容器分配明确 frame；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Modal

- **修复**：闭合触发器、关闭槽与 mask 在 PointerDown 立即提交，拉伸 frame 的触发视觉与命中错位；声明尺寸未归一/收敛导致对话框越出 surface，固定 header/body/footer 几何在紧凑尺寸下产生负区域，长标题覆盖关闭槽，内容子树与关闭反馈未统一裁剪，关闭使用临时字符且缺 hover/pressed，闭合态仍发布 Dialog；Paint-only zoom 首帧缩放曾错误写入子树布局，令紧凑浮层内容永久压缩；闭合态建树缓存的 `tab_index=1` 曾在打开后继续把 Dialog 宿主混入 focus trap。
- **规格**：有限非负尺寸收敛到 surface，header/body/footer/内容子树共享裁剪，标题在 Lucide 关闭槽前 Unicode 单行省略，子树始终使用最终对话框几何；闭合 Button 与打开 Dialog 发布 expanded 和非空名称，触发器、关闭槽与 mask 同一目标完整释放才提交，离开/失焦或交互几何变化取消，Enter/Space、overlay focus trap 与 Escape 保持；无显式覆盖时 Tab 资格实时跟随组件当前默认值。
- **验证**：Windows 真窗 ✅
- **备注**：内置 footer 仍是视觉槽，业务操作继续由内容子树真实 Button 承担；仅覆盖当前回归、Windows Vulkan/NVIDIA 真窗与现有 overlay 性能验证

## Input

- **修复**：CJK addon 按 UTF-8 字节分配宽、textarea 丢末尾空行与滚动命中错行、CRLF/CR 破坏行索引、空 placeholder→越界光标、密码按明文宽定位光标、clearable 无动作、附件按钮坐标分叉。
- **规格**：双字 CJK addon=122×32；clearable=100×32；password=104×32；两行 textarea=80×60；S/L=80×24/80×40。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## InputNumber

- **修复**：小数固定两位→聚焦丢精度、连续步进累积尾差、重复点击/步进丢弃缓冲、受限高度仍按名义判上下区、光标固定 7px 宽、禁用复用普通背景、焦点描边遗漏步进区、步进区无外框、三档总宽固定 80px 挤压 Large。
- **规格**：S/M/L=104×24/112×32/120×40。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Select

- **修复**：可搜索方向键提前提交、空选项/无匹配不显空态、下拉视口外仍可命中、Large 受限仍按名义高命中、底部空不足不上翻、光标固定字符宽、Unicode 按 UTF-8 估算、长文字覆箭头、多选标签溢出缺关闭槽、快速重开残留查询、下拉被后绘覆盖。
- **规格**：S/M/L 高=24/32/40；下拉行高 28、视口上限 280。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Segmented

- **修复**：CJK 按 UTF-8 字节分配宽、长文字/选中块越出覆盖相邻、Large 20px 受限仍按 40px 绘/命中、整体禁用选中块与容器同色、方向键首尾停、实 frame 宽于固有→不可点尾部、窄 frame 按名义排挤残片、边界仍可命中、无变化 hover 吞事件、options 重排按旧索引变非受控值。
- **规格**：四双字 CJK Medium=200×32；S/M/L 高=24/32/40。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Form / FormItem

- **修复**：标签/帮助文字未裁剪越界覆控件、Vertical 帮助与子控件共区、无帮助 Horizontal 浪费 16px、Inline 遗漏状态条与帮助、带帮助自然高未含帮助行、NaN/无穷 gap→重叠归零。
- **规格**：Horizontal/Inline 无帮助=44px 有帮助=60px；Vertical=56/72px；帮助行=16px。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Slider

- **修复**：方向键从离格值直加减未对齐区间起点、十进制步进→尾差、受限宽高用名义轨道/滑块→溢出、框外按下仍启拖动、`PointerLeave` 提前终止捕获。
- **规格**：S/M/L=200×24/200×32/200×40。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Rate

- **修复**：半星左半用空态色且右半被背景擦除、自定义 Unicode 固定 24px 格宽互相覆盖、受限仍用名义字号/命中格→溢出、框外按下误选首项、半星无障碍最大值按整星发布。
- **规格**：五颗星 S/M/L=96×24/120×32/144×40。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## DatePicker

- **修复**：星期标题与首行日期共垂直区绘制/命中错位、月份标题区误触翻月、窄触发框七列压至不可读、固字号/图标受限外溢、长占位覆图标、月历被后绘覆盖截获点击、Tab 不进入焦点态、无障碍缺展开状态、清除 hover 不请求重绘、无关按键被吞。
- **规格**：S/M/L=160×24/160×32/160×40；最小弹层宽 160。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## DateRangePicker

- **修复**：月历与预设按普通树序被后绘覆盖截获点击、范围文字/图标按名义高受限外溢、长占位覆图标、长预设标签越出 footer、Tab 不进入焦点态、无障碍缺展开状态、清除 hover 不请求重绘、翻月残留 hover、无关按键被吞。
- **规格**：S/M/L=280×24/280×32/280×40；最小月历宽 160。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## TimePicker

- **修复**：分钟列五分步长把实际分钟误作行索引、晚时间不滚到当前/指针无法到 23:59、滚轮无效、键盘只滚小时不能切列/提交、固字号/图标受限外溢、长占位覆图标、滚动后半行/高亮越出弹层、列文字偏未居中、弹层被后绘覆盖、Tab 不进入焦点态、无障碍缺展开、离开未清预览、无关按键被吞。
- **规格**：S/M/L=120×24/120×32/120×40。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## ColorPicker

- **修复**：受限仍强制 ≥8px 色块描边越出 frame、触发命中只查纵坐标用名义高、色板右 padding/边界错预览下行色、透明色显示空白、预设无选择标记、浅色 hover 白框不可辨、键盘只能开关不能导航/提交、弹层被后绘覆盖、无障碍缺展开、无变化移动/离开吞事件。
- **规格**：S/M/L=24×24/32×32/40×40。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Cascader

- **修复**：只绘末级→鼠标无法返回/切换祖先、弹层命中忽略横坐标、长列表仅前六项可达无滚轮、触发框按名义 32px 命中、长占位/选项覆箭头或越界、弹层被后绘覆盖、缺 hover/FocusIn/Home/End/展开状态/叶子 Change、reconcile 重置打开中子级到根列。
- **规格**：触发框=120×32；弹层每列 ≥200×200。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## TreeSelect

- **修复**：触发框/弹层命中只查纵坐标、视口下不可见行可点击、受限高仍按名义 32px 绘、长占位/值覆箭头、窄触发器层级压至不可读、长/深标题缺可见文字、空树不绘空态、弹层被后绘覆盖、无变化移动吞事件、hover 与键盘高亮互清、缺 Home/End、reconcile 重置高亮、退出动画跳回顶部、重开长列表未定位选中项。
- **规格**：触发框=200×32；弹层 ≥200 宽/28 行高/视口 ≤280。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## AutoComplete

- **修复**：触发框/弹层命中只查纵坐标、受限高仍按名义 32px 绘、长文字越界、光标固定字符宽只能末尾编辑、查询提交不发 Change、长候选无滚动、过滤空无空态、缺候选 hover、弹层被后绘覆盖、reconcile 重置键盘候选到首项。
- **规格**：触发框=200×32；弹层 ≥200 宽/28 行高/视口 ≤280。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景

## Mentions

- **修复**：光标固定字符宽只能末尾编辑、活动查询只取最后 `@` 不看光标、提交截断查询后正文、输入提交不发 Change、候选只绘前五项键盘可入不可见、缺滚轮空态、命中忽略二维边界、长占位/正文/候选越界、缺 hover、弹层被后绘覆盖、reconcile 重置候选。
- **规格**：触发框=200×32；弹层 ≥200 宽/28 行高/视口 ≤280。
- **验证**：Windows 真窗 ✅
- **备注**：继续按组件审查渲染与文字问题；仅覆盖当前回归与 Windows Vulkan/NVIDIA 真窗场景
