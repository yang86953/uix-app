# 运行保障

[← 返回进度 索引](../进度.md)

> **接口**：声明 UIX 运行保障的交付状态——错误处理、恢复登记、日志迁移和已知差距。依赖：[交付方向](交付方向.md)。导出：运行保障的架构真相 → [架构 · 诊断与运行保障](<file:///C:/data/note/我的项目/软件/UIX App/架构/platform/diagnostics.md>)。

### 进度总览

| 状态 | 事项 |
|---|---|
| ✅ 已完成 | typed Error / Result 核心、一级 diagnostics pub(crate) 重新导出、native 与 draw 类型迁移已编译验证、Snapshot / Recovery 公开 API 在 module doc 及其 8 项使用契约（📎） |
| ✅ 已完成 | Diagnostics 首个 SMC 纵切：公开 `Diagnostics` System、私有 `reporting` / `recovery` Module，以及报告构建、容量存储、tracing 发射 Component 边界；定向构建与 8 项公开契约通过 |
| ✅ 已完成 | pending failure queue 首个纵切：Diagnostics runtime 共享固定 64 容量队列，按 graphics context source 隔离；wgpu uncaptured/device-lost callback 与 offscreen destroy void 失败只入队，owner-thread `ensure_active` 取出 typed Error 交给既有 `RecoveryDriver`；S1-02 已让 wgpu teardown 先走 checked offscreen 销毁，再关闭 source |
| ✅ 已完成 | 旧日志轮子迁移与删除：`core::log::*`（info/warn/error/debug `_fn`）全部迁移为 `tracing::*`，唯一日期工具 `days_to_date` 搬入 UI 日期组件，旧 `core/log` 与 `core/diagnostic` 全目录删除，无桥接双写 |
| ✅ 已完成 | native bool / 空值失败语义清理（S2-01 批次 1）：`IClipboard` / `ICursor` / `ITimer` / `IFileDialog` / `IFileSystem` / `IDisplay` 及其 `SpecialDirProvider` 全部改为 typed `Result`；文件对话框 `Ok(None)` 表示取消、`Err` 表示对话框失败，不再与空串混淆；Wayland/macOS 未实现能力返回 `Err(NotImplemented)`，不再用 `(0,0)` / 空串 / 0 冒充结果 |
| ✅ 已完成 | native bool / 空值失败语义清理（S2-01 批次 2）：`ISystemInfo` 七个查询（`os_info` / `cpu_count` / `memory_info` / `hostname` / `username` / `up_time` / `process_memory`）与 `default_font_paths` 全部改为 typed `Result`；Windows RtlGetVersion/GlobalMemoryStatusEx/GetComputerNameW 等失败转 `PlatformError`，Linux /proc 读取失败转 `IoError`，不再返回 "Windows (Unknown)" / 全 0 / 空串冒充结果；字体探测类 `probe_*` 保留 `Option`（找不到是合法语义） |
| ✅ 已完成 | Windows `wnd_proc` 第二个纵切：DPI、窗口尺寸/移动和 IMM 读取失败只入队，App owner-thread 任务边界统一取出并交给 Diagnostics |
| ✅ 已完成 | Windows DWM frame pacer 纵切：worker 的 `DwmFlush` / `PostMessageW` 失败只入队，当前 ticket 仍有效时由 WindowsPlatform owner-thread 取出并交给 Diagnostics |
| ✅ 已完成 | Windows custom chrome 纵切：`GetWindowLongW`、`SetWindowPos`、DWM frame/圆角和 `WM_NCHITTEST` 失败统一转 typed Error；`wnd_proc` 每条 custom chrome 消息只入队，owner-thread 再取出 |
| ✅ 已完成 | native trait 失败语义清理 S2-01 批次 3：`INotification::show` 与 `IConsole` 六个写方法全部返回 `Result<()>`；`NotificationService` 接入 `Diagnostics::report` |
| ✅ 已完成 | S2-07 device-lost 真实恢复注册：`drain_platform_pending_failures` 成为 owner-thread 恢复安全点（先 `attempt_recovery`，Recovered 不再 report，未处理才最终报告一次）；App 组合根对 `Errc::GraphicsDeviceLost` 注册真实 handler，经共享 `RebuildRequest` 触发 `RecoveryDriver` 下个帧边界执行既有有界恢复序列；契约测试证明 `report` 不自动执行恢复 |
| ✅ 已完成 | CompileFail deny 证据：`uix::native` 根 / backends / graphics / shared / traits / test_harness、旧 `core::log` / `core::diagnostic` / `prelude::create_platform`、Diagnostics 私有 Module（reporting / recovery / pending / report / config / crash）对外不可达，共 15 个 compile-fail doctest 契约锁定 System / Module 边界 |
| ✅ 已完成 | CrashReport primitive（S3-01～S3-03）：私有 crash Module 含字段有界、脱敏、行式渲染的 `CrashReport` 模型；临时文件 + fsync + rename 原子写入（冲突 replace、失败清理、磁盘失败转 typed `IoError`）；`App::run` 在配置加载前安装 panic hook，配置 `crash_report_directory` 时原子写入 crash report，hook 内递归 guard 且始终转发 previous hook 不吞 panic |
| ✅ 已完成 | SMC-01 core 定级审计：core 确认为模式外基础依赖（不提升为 System），四目标模块边界落地（`identity` / `geometry` / `damage` / `error`），`damage → geometry` 为唯一窄契约依赖；`glyph_outline` 保持私有、`perf_probe` 按跨 System 共享保留公开；新增 3 个 compile-fail deny 契约锁旧平铺路径与私有基础算法，doctest 23/23 通过 |
| ✅ 已完成 | SMC-02 platform 系统迁移：`src/native` 重组为 capabilities / windowing / presentation / agent-transport 四个私有 Module（diagnostics 已落地），`traits/` 目录删除，264 处契约引用按归属迁移；`factory`（组合根）、`platform`/`present`（跨 Module 共享契约）、`backends`（OS 适配层）归 System 私有边界；兄弟隔离 rg 审计零违规，`presentation → windowing::window` 为唯一窄契约；新增 3 个 compile-fail deny 契约，doctest 26/26 通过 |
| ✅ 已完成 | SMC-03 graphics 系统迁移：六 Module 物理边界（painting / geometry / scene / renderer / backend / resources）；CPU 光栅化器上移 `draw/raster/`（模式外基础算法），`NodeId` 归 scene、`RenderOutcome`/`GraphicsFailure` 上移 `draw/outcome/`、`RenderTarget`/`UpdateStrategy`/`GraphicsCapabilities` 上移 `draw/target/`（拆 scene⇄renderer 双向）；能力端口经构造注入；兄弟隔离 rg 审计与 SMC 定义一致（painting/scene/resources→renderer/backend 反向边全 0）；新增 4 个 compile-fail deny 契约，doctest 30/30 通过 |
| ✅ 已完成 | SMC-04 ui 系统迁移：十二 Module 物理边界（component / view / reactive / layout / event / animation / accessibility / theme / overlay / form / virtualization / widgets）；`component ⇄ view` 经 `ViewChildrenProvider` 能力端口拆环、`component → widgets` 集中到 System 私有边界 `tree_widget_hooks/`、`LayoutEngine` 收归 layout、`apply_style` 上移 `style_paint`；兄弟隔离 rg 审计全部单向、无任何被引用反向边；新增 9 个 compile-fail deny 契约，doctest 39/39 通过 |
| ✅ 已完成 | SMC-05 data 系统迁移：settings 保持私有 Module，公开面由 data 根收口（`SettingsService`），跨 System 依赖仅 `settings → core` 失败模型；新增 1 个 compile-fail deny 契约，doctest 40/40 通过 |
| ✅ 已完成 | SMC-06 app 系统迁移：四 Module 物理边界（application / event_loop / window / agent）+ 组合根 `session_runtime`；拆环消除 `window → event_loop` 反向依赖（8 处）：调度组件（active_work_registry / app_timer / main_thread_queue / clock）上移 System 私有边界 `queues/`、`frame_scheduler` 归 window；兄弟隔离 rg 审计（agent 零引用、全部单向下行、queues 无 app 内依赖）；新增 7 个 compile-fail deny 契约，doctest 47/47 通过 |
| ✅ 已完成 | SMC-07 跨层强制规则全量闭合：全仓跨 System 依赖与[系统列表](<file:///C:/data/note/我的项目/软件/UIX App/架构/系统列表.md>)「允许依赖」列一致（platform→graphics 零依赖，消除 2 处 rustdoc 链接）；ui→native 43 处全部为输入契约类型（绘制入口规则遵守）；兄弟隔离全量重扫 + 无环抽查反向边全 0；deny 契约覆盖 lib.rs 47 个 + diagnostics 5 个全 System 闭合 |
| ✅ 已完成 | SMC 完成度独立审计与 panic hook 测试竞态修复（2026-08-01）：跨 System 依赖与系统列表允许依赖列逐项核对一致（platform→graphics/ui/app/data 零引用、data 仅 core、ui 无 app/data、ui→draw 全为 scene/resources/geometry/painting + renderer 失效句柄）；五 System 兄弟 Module 反向边 rg 全量扫描全 0（`presentation → windowing::window::PlatformWindow` 保持 SMC-07 白名单窄契约；`app/agent → window_semantics` 为根级粘合非 window Module）；doctest 47/47、公开 API 契约 7 组、workspace all-targets check、Windows GNU check 通过。修复：`diagnostics::crash` hook 测试与 `wnd_proc` / TSF thunk 故意 panic 测试并行时，全局 panic hook 捕获他测试 panic 写入崩溃目录导致偶发失败（实测 3 次中 1 次）；新增 `diagnostics::PANIC_HOOK_TEST_LOCK` 共享测试锁，crash 两 hook 测试与两故意 panic 测试串行；修复后 lib 测试 12 次连续通过 |
| 🔄 进行中 | 平台运行保障的 callback → owner-thread 失败队列全覆盖（Windows `wnd_proc` 核心分支、frame pacer、custom chrome、TSF、Win32 ABI panic 与 S1-06 queue/source 压力证据已完成，Wayland / macOS callback 和其他图形 teardown 仍待接入） |
| ⏳ 待开始 | native failure queue 跨平台贯通、补 CompileFail 阻断证据 |
| 📋 规划中 | — |

目标契约见[架构 · 诊断与运行保障](<file:///C:/data/note/我的项目/软件/UIX App/架构/platform/diagnostics.md>)与[架构 · 系统列表](<file:///C:/data/note/我的项目/软件/UIX App/架构/系统列表.md>)。

### 支持文档

- **交付方向**：[交付方向](交付方向.md)（运行保障六项原则：最终责任边界 / 不能恢复的才报告 / 可观察且安全 / 类型化 / …）
- **架构**：[架构 · 诊断与运行保障](<file:///C:/data/note/我的项目/软件/UIX App/架构/platform/diagnostics.md>)（错误架构与恢复组件模型）

## 当前结论

当前工作树已有 typed Error 核心与 `Diagnostics` 公开 API；native 和 draw 的旧类型已迁移。Diagnostics 首个 SMC 纵切已经落地：System 只编排自己的私有 Module，报告和恢复实现不再由公开入口直接持有。

系统仍未覆盖整个平台层，也不能标记完成：

- native / graphics / app 的旧日志调用已全部迁移为 `tracing::*`，旧 `core/log` 与 `core/diagnostic` 目录已删除。
- framework 没有实际登记或触发恢复 handler。
- Windows `wnd_proc` 的 DPI、尺寸/移动、IMM、custom chrome（含 DWM frame/圆角）和 ABI panic，以及 DWM frame pacer worker 的 `DwmFlush` / `PostMessageW` 失败，已改为只入队并在 App owner-thread 交给 Diagnostics；TSF composition/text-store callback 和生成 COM thunk panic 也已返回安全 HRESULT 并保留 typed cause。Wayland / macOS FFI callback 仍直接日志或丢失错误原因。wgpu uncaptured/device-lost 与 offscreen destroy void 失败已改为只入队，`WgpuContext` teardown 已通过 checked offscreen 销毁并在成功后关闭 source；其他图形后端的完整 teardown 仍未覆盖。
- CrashReport primitive 尚未实现，配置中的 crash directory 还没有消费者。
- native 平台 trait 的 bool / 空值/void 失败信号已全部清理完毕：`IClipboard` / `ICursor` / `ITimer` / `IFileDialog` / `IFileSystem` / `IDisplay` / `ISystemInfo` / `INotification` / `IConsole` 及 `SpecialDirProvider` 共 12 个接口全部返回 typed `Result`；`NotificationService` 已接入 `Diagnostics::report`。
- framework 已有真实恢复注册：App 组合根对 `Errc::GraphicsDeviceLost` 注册 handler，owner-thread 安全点（`drain_platform_pending_failures`）先 `attempt_recovery`，只有未处理 / 恢复失败的 Error 才最终 `report` 一次；恢复 handler 只通过共享 `RebuildRequest` 请求 `RecoveryDriver` 在下个帧边界执行既有有界恢复序列，领域算法仍归 graphics。
- 崩溃处理已实现：`App::run` 在配置加载前安装绑定 Diagnostics runtime 的 panic hook；配置 `crash_report_directory` 时把有界 `CrashReport` 原子写入（tmp + fsync + rename，冲突 replace，失败清理不残留伪完整报告），未配置时 hook 行为等同默认；hook 内递归 guard 防二次失败，且始终转发 previous hook——不吞 panic，unwind/abort 语义保持。

## 已落地事实

| 范围 | 当前工作树事实 |
|---|---|
| Error 核心 | `core::error::Error` 含 Errc/ErrorSeverity/summary/code/caller/source chain/typed cause；支持 `Error::downcast_ref` 精确访问原始类型化 cause |
| Diagnostics | 公开 `Diagnostics` 含 `new(report_capacity)`、`report(Error) → ReportId`、`snapshot() → DiagnosticsSnapshot`、`on_error(code, handler) → RecoverySubscription` 和 `attempt_recovery(Error) → RecoveryOutcome`；System 私有持有 `reporting` 与 `recovery` Module，报告构建、容量存储和 tracing 发射为 reporting 内部 Component；module doc 含 8 项使用契约 |
| Pending failure queue | `Diagnostics` 私有 runtime queue；固定容量 64，`PendingFailureSource` 按 graphics context / Windows platform owner 隔离；wgpu uncaptured/device-lost callback、offscreen destroy void 失败、Windows `wnd_proc`（含 custom chrome/ABI panic）、TSF callback、生成 COM thunk 与 DWM frame pacer callback/worker 只入队，owner-thread 取出原始 typed Error；`WgpuContext` checked teardown 失败时保留 owner/source 以便重试，成功 teardown 后关闭旧 source，队列溢出转为一次 `InsufficientResources`；S1-06 已补并发 producer、FIFO、单次 overflow、source/generation 隔离和 replacement 容量回归 |
| 旧类型迁移 | `core::Error`（含 `Errc` / `ErrorSeverity`）已获结论，不再继续携带旧字段；已跨原生层、/draw 和 /app 使用。遗留项：无——旧 Collector、Logger、Retry、Middleware 与旧 `core/log`、`core/diagnostic` 目录均已删除 |
| native trait 失败语义 | `IClipboard` / `ICursor` / `ITimer` / `IFileDialog` / `IFileSystem` / `IDisplay` / `ISystemInfo` / `INotification` / `IConsole` 与 `SpecialDirProvider` 共 12 个接口全部改为 typed `Result`（S2-01 批次 1/2/3）；`IFileDialog` 用 `Ok(None)` 区分用户取消；Wayland/macOS 未实现能力返回 `Err(NotImplemented)`；字体探测类 `probe_*` 保留 `Option`（找不到是合法语义）；`ITextInput` / `IFileSystem::read_file` / 窗口与呈现系接口此前已是 Result；`NotificationService` 在最终责任边界通过 `Diagnostics::report` 提交观察 |
| 恢复原型 | 已有 `RecoveryRegistry` 与 `attempt_recovery` 全链路，但 framework 未实际使用。接口选择跳过有争议设计（见直接切换约束） |
| 使用契约 | `types.rs` 的 8 项 `Diagnostics` 公开 API 契约以 `📎` 标注，记录在 module doc |

## 已知差距

| 优先级 | 差距 | 完成条件 |
|---|---|---|
| P0 | 平台运行保障未全覆盖 | App 内部 native owner 显式持有 runtime diagnostics；wgpu uncaptured/device-lost/offscreen destroy void、Windows `wnd_proc` 核心失败分支、custom chrome、TSF、Win32 ABI panic 和 DWM frame pacer worker 已只投递统一有界 pending failure，ObjC / Wayland 仍需接入，owner thread 执行领域恢复、registry 恢复、返回或最终报告（device-lost 恢复注册与 owner-thread 安全点已落地，见 S2-07 快速模型） |
| P0 | ABI panic 边界未全覆盖 | Win32 `wnd_proc` 与 Windows-rs TSF COM thunk 已捕获 panic 并返回 `DefWindowProcW` / `E_FAIL`；ObjC thunk、Wayland callback 与其他生成 FFI thunk 仍需覆盖 |
| P1 | 框架无实际恢复注册 | ~~至少一个真实可失败能力（如 device-lost）注册恢复 handler，并有契约证明 typed Error 可触发对应恢复逻辑~~ ✅ 已完成：`Errc::GraphicsDeviceLost` 已注册，`attempt_recovery` 在 owner-thread 安全点触发 `RebuildRequest` → `RecoveryDriver` 有界重建；`report` 不自动执行恢复有契约测试 |
| P1 | 崩溃处理未实现 | ~~CrashReport 序列化、panic hook 覆盖、crash dump 写入配置路径完成，并验证安全终止而非静默吞 panic~~ ✅ 已完成（S3-01～S3-03）：有界脱敏模型、原子写入（失败清理 + replace）、`App::run` 安装 hook（配置目录时写报告，未配置等同默认），hook 递归 guard 且不吞 panic；demo 提供 `--crash-dir` 配置消费者 |

## 直接切换约束

- 不考虑兼容：`Diagnostics::report` / `attempt_recovery` 不自动执行恢复 handler，避免意外副作用。
- 扩展禁止：首版不引入 wildcard recovery、priority、async handler、cancel、auto-report 或 filter 等复杂机制。静态类型化是 UIX 的核心承诺。
- 仅 owner 报告：只有类型化的最终责任边界掉用 `diagnostics.report`；未被报告的 Error 携带 cause chain 继续上抛，不自动 fallback。
- 固定容量：使用固定容量循环缓冲，不引入动态淘汰策略。
- 不对内暴露 Diagnostics 给后端复杂跑批量报告。
- 区分 origin：应用调用 `report`，框架使用 `report_with_origin`。
- Callback 只入队：callback 不调用 subscriber、recovery handler 或用户代码；owner thread 才能取出 pending failure。

## 快速模型 S1-03：Windows custom chrome callback 入队

S1-03 已完成，代码提交 `64016c2db188eaab821229579a6ef683fc6b5728` 已推送到 `origin/main`。`custom_chrome` 不再在 `DwmExtendFrameIntoClientArea` / `DwmSetWindowAttribute` 失败时直接写日志，也不再静默丢弃 `GetWindowLongW`、`SetWindowPos`、`GetWindowRect` 失败；这些路径统一生成 typed `Error`。`WM_NCCALCSIZE`、`WM_NCHITTEST`、`WM_SIZE` 只执行安全返回或入队，`WM_SIZE` 的帧重算与 DWM 操作最多保留一个失败，owner-thread 再取出 pending failure。窗口 owner API 的 DWM 失败仍通过 `Result` 返回。

验证证据：

- `cargo check --target x86_64-pc-windows-msvc`：通过。
- `cargo check --target x86_64-pc-windows-gnu`：通过。
- `cargo test --lib`：7 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib custom_chrome_failure_is_deferred_to_owner_boundary -- --nocapture`：1 passed、0 failed；invalid HWND 回归证明 custom chrome callback 失败进入 owner source 且同一消息不重复消费。
- `cargo test --target x86_64-pc-windows-msvc --lib --no-run`：通过，含 Windows 测试目标编译。
- `git diff --check`：通过；全仓格式检查的既有无关差异未触碰。
- 静态审计：`custom_chrome.rs` / `wnd_proc.rs` 不含 `core::log`、tracing、subscriber、recovery 或 report 调用；真实 DWM/窗口硬件运行证据仍未冒充本阶段结果。

下一直接任务为 S1-04：Windows TSF callback 入队；本次未提前处理 ABI panic、队列压力或其他平台 callback 范围。

## 快速模型 S1-04：Windows TSF callback 入队

S1-04 已完成，代码提交 `a5771a0d37790d297070188e1a69f936ea310c53` 已推送到 `origin/main`。TSF composition 事件只进入共享队列；`PostMessageW`、`OnLockGranted` 与 text-store 借用冲突保留 typed cause，由 owner-thread 消费。S1-04 本身不处理 ABI panic，随后由 S1-05 闭合。

## 快速模型 S1-05：Win32 ABI panic 边界

S1-05 已完成，代码提交 `525e58aa` 已推送到 `origin/main`。显式 `wnd_proc` 外层以 `catch_unwind` 包裹窗口消息与 custom chrome，panic 后由 owner source 入队 `PlatformError` 并返回 `DefWindowProcW`；诊断投递与 fallback 也有二次防线。TSF 两个 windows-rs `#[implement]` 接口的 trait 方法统一捕获 panic，COM 返回 `E_FAIL`，owner source 保留 typed failure。callback 不执行 tracing、report、subscriber、recovery 或应用用户代码。

验证证据：

- `cargo check --target x86_64-pc-windows-msvc`、`cargo check --target x86_64-pc-windows-gnu`：通过。
- `cargo test --target x86_64-pc-windows-msvc --lib`：11 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib --no-run`：通过。
- 两项新增 ABI 回归各 1 passed、0 failed：wnd_proc safe fallback；generated TSF thunk `E_FAIL` + owner failure。
- `git diff --check` 和静态 callback 审计通过；真实 Windows 窗口、TSF/TIP、硬件、Wayland/macOS 仍未验证。

## 快速模型 S1-06：pending queue 容量与 source 压力测试

S1-06 已完成。生产 queue 语义未改变，测试证据覆盖 owner-thread drain、四个并发 callback producer、固定 64 容量、overflow 单次信号、旧 generation 关闭后的晚到 callback 拒绝以及 replacement source 容量复用。项目根目录同时提交通用 SMC 设计模式文件副本，程序开发知识库文件仍为权威定义。

验证证据：

- `cargo test --lib pending -- --nocapture`：4 passed、0 failed。
- `cargo test --target x86_64-pc-windows-msvc --lib pending -- --nocapture`：4 passed、0 failed。
- `cargo check --target x86_64-pc-windows-msvc`、`cargo check --target x86_64-pc-windows-gnu`：通过。
- `git diff --check`：通过。

下一直接任务为 S2-01：清理 native bool / 空值失败语义；Wayland/macOS callback、其他图形 teardown、旧日志和 CrashReport 仍未闭合。

## 快速模型 S2-旧日志迁移：core::log::* 全部迁往 tracing 并删除旧目录

已完成。`core::log::*`（`info_fn` / `warn_fn` / `error_fn` / `debug_fn`）在 app、draw、native、ui 的全部调用迁移为 `tracing::info!` / `warn!` / `error!` / `debug!`；唯一日期工具 `days_to_date` 由 `core::diagnostic::timestamp` 搬入 `ui/widgets/input/date_picker.rs`（与 `first_weekday` / `days_in_month` 同族）；旧 `src/core/log` 与 `src/core/diagnostic` 全目录删除，不建立桥接双写。`core/mod.rs` 不再声明 `log` / `diagnostic` 模块，`days_to_date` 以外的旧模块无单元测试可迁移。

验证证据：

- `cargo check --workspace --all-targets`：通过。
- `cargo test --quiet`：全部通过（含 22 项 GUI ignored 入口未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过；全仓既有无关格式差异（demo CLI、demo main、prelude、diagnostics_public_api、rasterizer core）未触碰。
- 静态审计：`src/`、`demo/`、`tests/` 不再含 `core::log` / `core::diagnostic` / `error_fn` / `info_fn` / `warn_fn` / `debug_fn` / `trace_fn` 引用，`Cargo.toml` 无 `log` crate 依赖，`src/diagnostics/` 内部已全部使用 tracing。

下一直接任务为 S2-01：清理 native bool / 空值失败语义；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 S2-01 批次 1：native bool / 空值失败语义清理（六个 trait）

已完成。`IClipboard`、`ICursor`、`ITimer`、`IFileDialog`、`IFileSystem`、`IDisplay` 六个 crate-private trait 及 `SpecialDirProvider` 的全部 bool / 空字符串 / 空 Vec / 0 失败信号改为 typed `Result`：

- `IClipboard`：`text/set_text/has_text → Result`；Windows OpenClipboard/GetClipboardData/EmptyClipboard/SetClipboardData 失败转 `PlatformError`，Wayland 锁失败/无 serial 转 `InvalidState`。
- `ICursor`：全方法 `→ Result`；Windows LoadCursorW/GetCursorPos/SetCursorPos/ClipCursor/SetCapture/ReleaseCapture 失败转 `PlatformError`；Wayland 未实现能力（位置/约束/捕获）返回 `Err(NotImplemented)`，不再返回 `(0,0)` 冒充位置。
- `ITimer`：`set → Result<u32>`、`clear → Result<()>`；Windows 无 hwnd 时不再返回幽灵 id（`InvalidState`）、SetTimer/KillTimer 失败转 `PlatformError`；Linux 定时器线程通道关闭转 `IoError`；macOS stub 返回 `Err(NotImplemented)`。
- `IFileDialog`：`open/save/open_folder → Result<Option<...>>`；`Ok(None)` = 用户取消，`Err` = 对话框本身失败。Windows 用 `CommDlgExtendedError()` 区分取消与内部错误，Linux spawn 失败转 `IoError`、非零退出视为取消，macOS stub 返回 `Err(NotImplemented)`。
- `IFileSystem` 与 `SpecialDirProvider`：`→ Result<String>`；SHGetKnownFolderPath 失败转 `PlatformError`、HOME 缺失转 `NotFound`、未知目录转 `InvalidArgument`、current_dir/current_exe 失败转 `IoError`。
- `IDisplay`：全方法 `→ Result`；Windows 注册表与显示器枚举失败转 `PlatformError`，Wayland 无输出转 `NotFound`、锁失败转 `InvalidState`；删除 Windows 虚拟 fallback 显示器与 Wayland 虚构 1080p 显示器。
- 调用方迁移：`ui/foundation/clipboard.rs` 公开 API 签名不变（widget 调用面零改动），失败记 `tracing::error!`；`application.rs` / `event_loop.rs` 主题查询失败降级 light 并 `tracing::warn!`；`window_driver.rs` 最大化显示器查询失败跳过 resize；`platform/imp/{linux,macos}.rs` 的 `displays()` 透传 `Err`。

验证证据：

- `cargo check --workspace --all-targets`：通过（0 error，警告数与基线一致 28 个）。
- `cargo test --quiet`：25 passed、0 failed（22 项 GUI ignored 入口未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过。
- 静态审计：src 内不再有以 bool/空串/0/void 表达失败的 I* trait 方法（`ISystemInfo` 查询已由批次 2 收口，`INotification` / `IConsole` 已由批次 3 收口）。

下一直接任务为 S2-01 批次 2：`ISystemInfo` 七个查询改 `Result` 并迁移 font_service 调用方；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 S2-01 批次 2：ISystemInfo 查询改 typed Result

已完成。`ISystemInfo` 的 `os_info` / `cpu_count` / `memory_info` / `hostname` / `username` / `up_time` / `process_memory` 与 `default_font_paths` 全部改为 typed `Result`；字体探测类 `probe_cjk_font_path` / `probe_family_font_path` / `scan_fallback_font_path` / `probe_cjk_font_paths` 保留 `Option` / `Vec`（"找不到"是合法语义，不是失败信号）。

- Windows：`RtlGetVersion` / `GlobalMemoryStatusEx`（含 total==0）/ `GetComputerNameW` / `GetUserNameW` / `GetProcessMemoryInfo` 失败转 `PlatformError`，不再返回 "Windows (Unknown)" / 全 0 / 空串；`cpu_count` 为 0 视为失败。
- Linux：`/proc/sys/kernel/*`、`/proc/meminfo`（缺 MemTotal）、`/proc/uptime` 读取或格式失败转 `IoError` / `FormatError`；`available_parallelism` 失败转 `PlatformError`；username 通过 `id -un` 兜底，结果为空转 `NotFound`。
- macOS：`HOSTNAME` / `USER` 缺失转 `NotFound`，`up_time` 返回 `NotImplemented`（原为假的 0）。
- Fake：全部 `Ok(...)`。
- 调用方迁移：`font_service` 的 `default_font_paths()` 失败记 `tracing::warn!` 后按空列表走既有兜底（BitmapFont 降级不变，但失败不再无声）；`native/factory::available_memory_bytes` 失败回退 0。

验证证据：

- `cargo check --workspace --all-targets`：通过（0 error，警告数与基线一致）。
- `cargo test --quiet`：25 passed、0 failed（22 项 GUI ignored 未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过。

下一直接任务：剩余旧失败语义审计；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 S2-01 批次 3：INotification / IConsole 失败语义改为 typed Result

已完成。`INotification::show` 返回 `Result<()>`：Windows `Shell_NotifyIconW` 失败转 `PlatformError`、Linux `notify-send` spawn/exit 失败转 `PlatformError`、macOS 返回 `NotImplemented`。`IConsole` 六个写方法（`write` / `write_line` / `set_color` / `reset_color` / `show_terminal_cursor` / `set_terminal_title`）全部返回 `Result<()>`：Windows `WriteConsoleA` / `SetConsoleTextAttribute` / `SetConsoleCursorInfo` / `SetConsoleTitleW` 返回值不再忽略、Linux stdout 写入失败转 `PlatformError`、macOS 移除 `print!` panic 改为 `stdout().write_all()`；`capabilities()` 签名不变。

同时 `NotificationService` 接入 `Diagnostics`：新增 `diagnostics` 字段与 `with_diagnostics()` 构建器；`show()` 失败在最终责任边界通过 `Diagnostics::report` 提交观察，满足 `core/error` 模块「最终责任边界交给平台 Diagnostics 观察」的不变量。

验证证据：

- `cargo check --workspace --all-targets`：通过（0 error，警告数与基线一致）。
- `cargo test --quiet`：25 passed、0 failed（22 项 GUI ignored 未执行）。
- `cargo check --target x86_64-pc-windows-msvc --lib`、`cargo check --target x86_64-pc-windows-gnu --lib`：通过。
- 改动文件 `git diff --check`：通过。
- 静态审计确认改动文件无 `core::log` / `_fn` 旧日志引用。

下一直接任务：代码库中剩余的旧失败语义审计；Wayland/macOS callback、其他图形 teardown 和 CrashReport 仍未闭合。

## 快速模型 S2-07：device-lost 真实恢复注册与 owner-thread 安全点

已完成，代码提交 `9baca6c6`（本地，未推送）。App owner-thread 安全点 `drain_platform_pending_failures` 先对每个取出的失败调用 `attempt_recovery`：注册 handler 返回 `Recovered` 时不再 report（仅 tracing 观察），`Unhandled` / `Failed` 才最终 `report` 一次，符合「不能恢复的才报告」。App 组合根对 `Errc::GraphicsDeviceLost` 注册真实恢复 handler：handler 只通过共享 `RebuildRequest` 请求窗口引擎在下个帧边界执行既有有界恢复序列，领域算法仍归 graphics 的 `RecoveryDriver`（`with_rebuild_request` 帧边界消费，请求一次性且不覆盖首个未处理失败）。契约测试证明 `report` 不自动执行恢复 handler。

验证证据：

- `cargo check --workspace --all-targets` 与 `--features test-harness`：通过（0 error）。
- `cargo test --quiet`：19 passed、0 failed（22 项 GUI ignored 未执行）。
- `cargo check --lib --target x86_64-pc-windows-msvc`、`--target x86_64-pc-windows-gnu`：通过。
- 定向测试：`app::shell::application::runtime::tests` 3/3（drain 安全点恢复不报告 / report 不触发 handler / device-lost 注册触发 RebuildRequest）；`draw::renderer::recovery_driver` 2/2（请求触发有界重建一次 / 请求并入既有 pending 不双恢复）；doctest 19/19（含 14 个新增 compile-fail deny 契约）。
- 改动文件 `git diff --check`：通过；全仓既有无关格式差异（rasterizer core、prelude）未触碰。

下一直接任务：CrashReport primitive（S3-01～S3-03）；Wayland/macOS callback、其他图形 teardown 仍按延后事项不进入 0.0.1。

## 快速模型 S3-01～S3-03：CrashReport 有界模型、原子写入与 panic hook

已完成，代码提交 `de306d50`（本地，未推送）。`src/diagnostics/crash.rs` 是 Diagnostics System 的私有 crash Module：

- **S3-01 有界模型**：`CrashReport` 字段固定（schema_version / runtime_id / occurred_at / thread / panic_message / panic_location / retained_reports），构造与渲染双处有界（线程 256B、消息 2048B、位置 512B 截断），控制字符替换为空格，行式 key=value 渲染不可注入；非字符串 panic payload 以固定标记代替。
- **S3-02 原子写入**：`create_dir_all` → 唯一 tmp → write + fsync → rename；rename 冲突删除旧报告后重试（replace 语义）；任何失败清理 tmp，不残留伪完整报告；磁盘失败转 typed `Errc::IoError` 并保留 io cause。
- **S3-03 panic hook**：`App::run` 在 settings 加载前安装（settings / CLI / GUI 任一段 panic 均覆盖）；配置 `crash_report_directory` 时写 crash report，未配置时行为等同默认；始终转发 previous hook，不吞 panic、unwind/abort 语义保持；`PanicHookGuard` 防 hook 内递归，二次失败仍安全终止。demo 新增 `--crash-dir <path>` 启动参数作为真实配置消费者。

验证证据：

- `cargo check --workspace --all-targets`：通过（0 error，警告数与基线一致）。
- `cargo test --quiet`：20 passed、0 failed（22 项 GUI ignored 未执行）。
- `cargo test --doc`：20 passed（含 15 个 compile-fail deny 契约）。
- `cargo check --lib --target x86_64-pc-windows-msvc`、`--target x86_64-pc-windows-gnu`：通过。
- `diagnostics::crash` 定向测试 7/7：渲染脱敏与行式格式 / 有界截断 / 原子写成功与同名 replace 且无 tmp 残留 / 写入失败 typed Error 与清理 / 非字符串 payload / panic hook 写报告且不吞 panic / hook 写失败仍不吞 panic。
- 改动文件 `git diff --check`：通过。

下一直接任务：运行保障 P0/P1 剩余项（平台回调覆盖、ABI 边界、图形 enum 双轨收口等）或完整构建矩阵。

## 接手实施顺序

1. 检查 typed Error 迁移现状，移走未完成依赖，补 CompileFail 阻断使用旧类型——✅ 已完成（本次提交）：`uix::native` 根 / backend SPI / 厂商对象 / raw handle、旧 `core::log` / `core::diagnostic` / `prelude::create_platform` 与 Diagnostics 私有 Module 共 14 个 compile-fail 契约）。
2. 保持当前 Diagnostics 公共核心，不扩展 wildcard、priority、async handler 或自动 report/recover，并重跑已有 8 项契约。
3. 已完成 wgpu device-lost / uncaptured / offscreen destroy void 与 `WgpuContext` checked teardown/source close、Windows `wnd_proc` 核心失败分支、DWM frame pacer worker、custom chrome、TSF、Win32 ABI panic 和 S1-06 队列容量/source 压力的统一 pending native failure queue 纵切；S2-01 native trait 失败语义清理（12 个 trait）与 S2-旧日志迁移均已完成；下一步覆盖 Wayland/macOS callback 与其他图形后端 teardown。
4. 将平台可失败接口改为 typed Result；owner 仍可返回时绝不提前报告——✅ 已完成（S2-01 批次 1/2/3，12 个 trait 全部收口）。
5. 接通 framework recovery registration 与安全点 `attempt_recovery`；领域恢复算法留在 graphics / platform 自己——✅ 已完成（本次提交，S2-07）：owner-thread 安全点先 `attempt_recovery`，未处理才最终报告；`Errc::GraphicsDeviceLost` 真实注册经 `RebuildRequest` 触发 `RecoveryDriver` 有界重建。
6. 按 `platform → graphics → ui/data → app` 迁移旧日志到 tracing，消除 `error_fn(text)`——✅ 已完成（本次提交）。
7. 搬走唯一日期工具，删除旧 core log/diagnostic 全目录——✅ 已完成（本次提交）。
8. 最后实现 crash primitive、补齐最终边界和完整构建矩阵——✅ S3-01～S3-03 CrashReport primitive 已完成（本次提交）；剩余为完整构建矩阵与发布门禁。

## 接手时禁止的捷径

- 不让 `Diagnostics::report` 自动执行恢复 handler。
- 不加独立 outcome 类型（与 typed Error 共存的不协调混合体）。
- 不通过放宽`系统列表`的依赖方向或废除架构划分来通过编译。
- 不增添非限定、递归、wildcard 或异步恢复注册。
- 不把完成判据与具体实现绑定——如果新设计能通过全部完成判据并保留 typed Error 核心，就可以替换。

## 快速模型 S2-08：Wayland callback failure queue

Wayland 第一段 callback → owner-thread 失败投递已落地：运行时 source 从 `PendingFailureQueue` 注入 `LinuxPlatform` / `WaylandBackend`；`Dispatch` 适配层捕获 callback panic；event loop 的 Wayland flush、poll、display fd、read、dispatch、wake pipe，以及 clipboard pipe / nonblocking I/O 失败统一进入该 source。Linux `Platform` 实现 `take_pending_failure`，应用安全点继续执行 `attempt_recovery` 后再决定最终 `report`，callback 本身不调用 subscriber、recovery handler 或用户代码。

验证：`cargo check --workspace --all-targets`、`cargo test --quiet`（110 passed、31 ignored）、`python -m unittest discover -s tests -p 'test_*.py' -q`（15 passed）、文档链接 4/4、测试布局 3/3 和 `git diff --check` 通过。边界仍明确：无真实 Wayland compositor 运行证据；macOS ObjC callback、其他图形 teardown 与未知协议 child dispatch 分支仍待后续批次。
## 快速模型 S2-09：macOS ObjC callback failure queue 首段

`MacosPlatform` 现在接收运行时 `PendingFailureQueue` 并在 owner-thread 实现 `take_pending_failure`。window delegate 的 close/resize/focus/occlusion/dealloc 与 CADisplayLink frame/dealloc callback 均在 ABI 边界捕获 panic，转为 typed `PlatformError` 入队；delegate/display-link context 携带 source，平台销毁时关闭 source，迟到 callback 返回 `Closed`。callback 仍不调用 Diagnostics subscriber、recovery handler 或用户代码。

验证：macOS target check、workspace all-targets check、host `cargo test --quiet`（110 passed、31 ignored）通过。边界：未有真实 macOS AppKit/CADisplayLink 运行证据，text-input view callback、其他 ObjC thunk 和图形 teardown 尚未覆盖。
## 快速模型 S2-10：macOS text-input view callback failure queue

`TextInputContext` 现在携带 `PendingFailureSource`。`UixContentView` 的 dealloc、first-responder、marked/selected range、marked/insert/unmark text 与 keyDown callback 均在 ABI 边界捕获 Rust panic；失败只入队 `Errc::PlatformError`，返回型 callback 返回保守 fallback，owner-thread 通过 `Platform::take_pending_failure` 统一处理。dealloc 的 Rust context 清理和 superclass dealloc 各自受保护，避免 panic 穿过 Objective-C ABI。

验证：macOS target、Windows MSVC target、host workspace check、host Rust 测试（110 passed、31 ignored）及 `tests` Python 门禁（17/4/3）通过。边界：没有真实 macOS AppKit/IME runtime 证据，不宣称输入法/TIP 行为或其余图形 teardown 已闭合。
## 快速模型 S2-11：Wayland event_created_child owner-thread failure boundary

`WaylandBackend::dispatch_pending_checked` 现在保护 Wayland 0.31 event queue 的 dispatch panic；因此 `event_created_child` 对未知 child opcode 的显式失败会在 owner-thread 转为 `Errc::PlatformError`，进入 pending source 并关闭 backend。已知 `wl_data_device` 的 `wl_data_offer` child 仍走原有 `QueueHandle::make_data`，没有把未知协议伪装成合法对象。

验证：host/Linux target check、host Rust 测试（110 passed、31 ignored）、Wayland rustfmt、`tests` Python 门禁（19/4/3）和文档链接检查通过。边界：没有真实 compositor 运行证据，未知协议真实消息与其他图形 teardown 仍待后续门禁。
