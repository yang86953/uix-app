# 组件功能缺口

[← 返回进度 索引](../进度.md)

> **接口**：声明 UIX 第 4～8 批 110 项组件功能的逐批实现状态与证据缺口——真窗截图、无障碍复核、多 DPI 验证等。依赖：[首发状态](首发状态.md)、[功能账本](功能账本.md)。导出：组件稳定性 → [组件稳定性](组件稳定性.md)。

### 进度总览

| 状态 | 事项 |
|---|---|
| ✅ 已完成 | Rust fence 示例 214/214 通过、第 4～8 批 110 项功能全部进入行为路径、定向回归测试 110/110 覆盖、图标收敛为单一组件管线、Table/Tree/Carousel/ImageGroup/FloatButtonGroup/Calendar 各有定向测试通过 |
| 🔄 进行中 | 92 项组件视觉矩阵（847 条证据/329 张原图，标记 UNVERIFIED） |
| ⏳ 待开始 | 110 项逐项真窗截图、110 项无障碍复核、144/192 DPI 验证、AMD/Intel 验证、长稳报告 |
| 📋 规划中 | — |


## 当前可证明基线（2026-07-22）

- Cargo 版本与发布状态
  - `Cargo.toml` 与 lockfile 根包版本已是 `0.0.1`
  - 这不代表已经发布，仅是版本元数据
- Rust fence 示例清单
  - `docs/使用/**/*.md` 有 214 个带唯一 `uix-compile` 的 Rust fence
  - 目标 `text` fence 为 0
  - Markdown 清单按声明 feature 验证为 214 / 214 通过
  - 其中 211 个基础、3 个 opt-in
- 第 4～8 批 110 项范围
  - 已从 `uix::prelude` 公开
  - 已完成配置消费、关键交互 / reconcile 与边界行为的定向回归
  - 当前未发现仍然只保存配置而不形成约定行为的范围项
  - 现有结果不包含 110 项逐项真窗数据
- 图标组件
  - 已收敛为单一组件管线
  - 应用 View 使用 `Icon::new(...)`，内置紧凑槽位使用 `Icon` 自有嵌入入口
  - 图表数据标记和控件几何不计作图标
- Table 与 Tree
  - `Table` 的远程分页、行点击与合并单元格进入测量、绘制、命中、回调和 reconcile 路径
  - `Tree` 的搜索、拖放、懒加载与节点图标进入测量、绘制、命中、回调和 reconcile 路径
  - Table 有 43 个定向测试，Tree 有 18 个定向测试
  - 暂无逐项真窗数据
- Carousel
  - autoplay、手动暂停 / 恢复、hover 暂停、Fade 动画 / 绘制、自定义双向箭头已进入真实组件路径
  - reconcile 已进入真实组件路径
  - 有 18 个定向测试，暂无逐项真窗数据
- ImageGroup
  - 已具备主图 / 缩略图导航、键盘、全 surface 模态预览
  - 遮罩关闭、防穿透、语义与重复路径 reconcile 已进入
  - 有 11 个定向测试，暂无逐项真窗数据
- FloatButtonGroup
  - 已具备 Hover / Click / Focus / ContextMenu 展开、键鼠匹配与取消
  - 动画布局、可见性 / 命中、语义及 reconcile 已进入
  - 有 11 个定向测试，暂无逐项真窗数据
- Calendar
  - `Calendar::date_cell` 已物化稳定日期子树并随月份 / 选中态刷新
  - `events` 以日期数据标记进入真实绘制
  - Calendar 相关 19 个测试通过，第 5、6、7、8 批也有定向行为回归
- 功能账本
  - [0.0.1 功能账本](功能账本.md)覆盖 110 / 110 项
  - 记录公开入口、实现位置、测试位置与当前工作树的 `targeted-verified` 状态

## 批次状态

| 批次 | 当前事实 | 证据缺口 |
|------|----------|----------|
| 第 4 批：表格与数据展示 | 范围项已进入真实行为路径并有定向交互 / reconcile / 边界测试 | 缺少逐项真窗数据 |
| 第 5 批：反馈组件 | 范围项已进入真实触发、状态、焦点、计时与回调路径并有定向测试 | 缺少无障碍复核与逐项真窗数据 |
| 第 6 批：导航组件 | 范围项已进入真实受控状态、键鼠、溢出 / 嵌套与 reconcile 路径并有定向测试 | 缺少逐项真窗数据 |
| 第 7 批：图表系统 | 图表增强与 9 个入口已消费绘制、布局及通用交互配置并有定向测试 | 缺少主题 / 交互状态矩阵与逐项真窗数据 |
| 第 8 批：排版与基础设施 | 范围项已进入真实布局、生命周期、动画、搜索与回调路径并有定向测试 | 缺少逐项真窗数据 |

## 共同证据缺口

- 真窗与视觉 QA 缺口
  - 当前工作树组件分母为 92 项（87 Widget、3 Composite、2 Provider）
  - 新 wgpu Vulkan 报告含 847 条证据行、329 张原始 PNG 与 22 张联系表
  - 人工视觉 QA 未发现 P0 / P1，报告标记为 `UNVERIFIED`
- 平台与 DPI 组合缺口
  - Windows x64 的 144 / 192 DPI、AMD / Intel、真实 compositor occlusion、多窗与 DeviceLost 组合没有当前报告
- 长稳与性能缺口
  - 长稳和性能没有当前完整报告
  - 旧的 88 组件 / 412 状态报告只属于其日期和旧渲染基线
