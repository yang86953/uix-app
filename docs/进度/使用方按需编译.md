# 使用方按需编译

[← 返回进度索引](../进度.md)

> **接口**：记录“框架使用方只编译实际使用部分”的当前状态、差距、实施阶段与验收门禁。依赖：[架构/使用方按需编译](../架构/使用方按需编译.md)。导出：完成后更新对应[使用文档](../使用.md)中的依赖声明与 capability feature 用法。

## 当前结论

**状态：🔄 进行中（2026-08-04）。** ODC-01 锁定基线、ODC-02 首轮能力映射、`image-codecs`、`qrcode`、`form-pattern` 三个公开 capability 的正反门禁、`demo-logging` 根二进制正向对照、首个无调用点死依赖清理，以及 schema v3 的 Windows release + Linux check 双目标矩阵已建立；`windows-core` 也已收缩到 Windows target。但 app/ui/draw/data 的整体能力分区与其他 backend/target、非 Windows release 产物完整矩阵仍未完成，尚未满足完整的使用方按需编译契约。使用文档不得在总门禁通过前把本目标描述为已交付能力。

## 已有基础

- `ash`、`glow`、`khronos-egl`、`serde`、`serde_json` 中的一部分已是 optional dependency，并由 backend、`settings-serde` 或 `agent-control` feature 接入。
- Windows、Unix、Wayland 等平台依赖已有 target `cfg` 边界，非目标平台依赖不会无条件进入同一目标构建。
- release profile 已配置 `opt-level = "s"`、`strip = "symbols"`、fat LTO 与单 codegen unit，有利于最终不可达代码裁剪。
- `agent-control`、`settings-serde`、测试门面以及部分 backend 实现已有条件编译。

以上只说明存在基础设施，不等于使用方应用已经做到“只编译所用部分”。

## ODC-01 首批基线入口

首批 fixture 位于 [`fixtures/usage-build`](../../fixtures/usage-build/README.md)，采集脚本为 [`scripts/measure_usage_build.py`](../../scripts/measure_usage_build.py)。从仓库根目录运行：

```powershell
python scripts/measure_usage_build.py --dry-run
python scripts/measure_usage_build.py --locked
```

采集器对每个 fixture 默认执行 `cargo clean`、`cargo metadata`、依赖存在性断言、正向 build/check 或预期失败 check、`cargo clean`，以保证不同入口之间不复用构建产物，且正常结束后不留下 Rust `target`。schema v3 会从 `rustc -vV` 解析 host target，向 metadata 传入 `--filter-platform <target>`，并让 build/check 显式使用同一个 `--target`，避免把其他目标的条件依赖混入当前 resolved graph；场景可显式覆盖目标，也可对没有目标链接器的跨目标入口执行真实正向 `cargo check`，但这种场景不生成或冒充 release 产物。仅在显式传入 `--keep-build-artifacts` 时保留后置构建产物；运行环境没有 Cargo 或无法确认 rustc host target 时只能使用 `--dry-run`，不会生成虚假的基线证据。

2026-08-04 在提交 `ceceff33b28e352d9120713a2ed6506b911cef7a` 上完成首轮 locked clean-build。环境为 `rustc 1.97.1 (8bab26f4f 2026-07-14)`、`cargo 1.97.1 (c980f4866 2026-06-30)`、默认 host/target `x86_64-pc-windows-gnu`；三个场景均通过 metadata、release build 与后置清理。该轮使用的 schema v1 尚未向 metadata 传入 `--filter-platform`，因此下表的 resolved package 同时包含不同 target 条件分支，只保留为改造前历史计数：

| 场景 | feature 入口 | resolved package | clean release build | release 产物 |
|---|---|---:|---:|---:|
| `minimal` | `default-features = false` | 84 | 98.330 s | 17,808,892 B |
| `d3d11-default` | 改造前默认 `d3d11` | 84 | 111.146 s | 18,149,551 B |
| `settings-serde` | `default-features = false, features = ["settings-serde"]` | 91 | 108.287 s | 17,798,435 B |

其中 build 耗时与 release 产物仍绑定上述工具链、Windows target、feature 集和提交，可用作改造前同机历史对照；resolved package 数未绑定单一 target，不能证明 Windows 依赖边界，也不能与 schema v2/v3 的目标过滤计数直接比较。采集 JSON 位于忽略目录 `test-reports/usage-build-baseline.json`，后续 capability 变更必须在对应干净提交上重新生成，不得沿用本表冒充新证据。

## ODC-02 首轮能力映射

首轮事实盘点见[使用方按需编译能力映射](../架构/使用方按需编译能力映射.md)。当前结论是：backend、`settings-serde`、`agent-control`、`image-codecs`、`qrcode`、`form-pattern` 与 `demo-logging` 已有 feature 边界，Windows 与 Linux 目标的 resolved graph 已按各自 target 条件隔离；唯一 `#[implement]` 宏归属 Windows TSF 路径，因此 `windows-core` 已随其收缩到 Windows target。但 app/ui/draw/data 的整体 capability 仍未独立裁剪，字体与基础绘制依赖仍需逐项判断。`tracing-subscriber` 已确认只归属演示程序并完成 optionalize，库的基础 `tracing` 事件契约不受影响；`raw-window-handle` 经源码调用点与反向依赖树核对后确认仅为主 crate 的无引用直接依赖，已删除且不再归入候选 capability。`image-codecs` 只裁剪第三方压缩格式解码与路径型公开方法，核心 RGBA 位图、裁剪、离屏和预加载句柄绘制仍属于基础契约；`form-pattern` 只裁剪正则规则，基础表单模型和非正则校验仍属于常驻能力。该状态不等于已完成使用方按需编译门禁。

## ODC-03/05/06/07 能力门控与死依赖收敛

提交 `533059cf252454c7a33593ae468e47ad20d4940c` 将第三方 `qrcode` 依赖改为 optional，并新增同名 capability feature。关闭该 feature 时，二维码实现模块、模块重导出、`prelude::QRCode`、内建组件 patch、组件快照分派和 `SnapshotFields::QRCode` 同步消失，不保留绕过 feature 的公开类型路径。

提交 `2b880a4375d747215f0a93e844b8fdba0d275ff4` 进一步将第三方 `regex` 依赖改为 optional，并新增窄粒度 `form-pattern` capability。关闭该 feature 时仅隐藏 `FormBuilder::validate_pattern` 与 `FieldRule::Pattern`；基础 `Form`、必填、长度、范围和自定义校验继续可用。

提交 `e0f07c0958a6ad1442c32c8f54748b84fcb3c12b` 将第三方 `image` 依赖改为 optional，并新增 `image-codecs` capability。关闭该 feature 时，压缩字节/路径解码模块、`ImageService::load_from_bytes` / `load_from_path` / `ensure_loaded`、`Image::src`、`Avatar::src`、`ImageGroup::images` 与 `Upload::preview_image` 同步隐藏；基础 `ImageService`、`BitmapHandle`、RGBA 槽位、派生裁剪、离屏合成及 `Image::slot` 继续可用。

提交 `172b7fae75c11d4e9d701e3e9b7be0579dd91d1b` 删除源码零引用、反向依赖树中仅由 `uix` 直接引入的 `raw-window-handle`，同步更新主锁文件与九份 fixture 锁文件，并把该 package 纳入所有场景的禁用依赖断言，防止后续无意重新引入。

提交 `c8c431db914688f3ff1cd103b1c25e101c6ffdd0` 将只由 `uix-demo::init_tracing` 使用的 `tracing-subscriber` 改为 optional，并新增默认启用的 `demo-logging` feature 以保持既有演示日志行为。关闭该 feature 时，订阅器初始化实现与调用同步不编译；基础 `tracing` 事件仍保留。两个根二进制场景使用完全相同的 `d3d11 + image-codecs + qrcode + form-pattern` 组合，仅切换 `demo-logging`，避免把演示所需的其他 capability 误当作日志依赖。当前兼容默认集合为 `default = ["d3d11", "image-codecs", "qrcode", "form-pattern", "demo-logging"]`。

通用 compile-fail 门禁由提交 `cfa3e488d269aa2901f3e8405a6f869bad0190b1` 建立。提交 `5689f0d58177c2c977ce9a0ddad5a364e745799c` 将采集报告升级为 schema v2：解析 rustc host target，metadata 使用 `--filter-platform`，build/check 使用相同的显式 `--target`，并把实际 target 写入每个场景结果。提交 `deaaa2e4c99d4c3d488a34a239453b076b53a640` 又确认唯一 `#[implement]` 位于多层 Windows `cfg` 链下的 TSF 路径，将 `windows-core` 依赖与 crate 根导入同步限定为 Windows target，并把报告升级为 schema v3，新增显式 `x86_64-unknown-linux-gnu` 的正向 check 场景。2026-08-04 在该干净提交上，使用 `rustc 1.97.1 (8bab26f4f 2026-07-14)`、`cargo 1.97.1 (c980f4866 2026-06-30)` 与 Windows host 完成十二场景 locked 双目标矩阵：

| 场景 | feature 入口 | resolved package | 专属 package 断言 | 构建门禁 | release 产物 |
|---|---|---:|---|---:|---:|
| `minimal` | `default-features = false`，Windows host target | 33 | `image`、`qrcode`、`regex`、`tracing-subscriber`、`raw-window-handle` 缺席 | clean release 66.564 s | 9,510,211 B |
| `minimal-linux` | 与 `minimal` 相同 feature 集，显式 Linux GNU target | 45 | `libc`、`wayland-client` 存在；`windows`、`windows-core` 及未选 capability 缺席 | clean check 29.952 s | — |
| `d3d11-default` | 默认 `d3d11 + image-codecs + qrcode + form-pattern + demo-logging` | 64 | 四项专属 package 存在，`raw-window-handle` 缺席 | clean release 79.740 s | 18,143,389 B |
| `demo-logging-disabled` | 演示基础组合，不启用 `demo-logging` | 53 | `image`、`qrcode`、`regex` 存在，`tracing-subscriber`、`raw-window-handle` 缺席 | clean release 131.696 s | 5,278,208 B |
| `demo-logging` | 相同演示基础组合，启用 `demo-logging` | 63 | `image`、`qrcode`、`regex`、`tracing-subscriber` 存在，`raw-window-handle` 缺席 | clean release 137.374 s | 6,189,056 B |
| `settings-serde` | `default-features = false, features = ["settings-serde"]` | 41 | 四项专属 package 与 `raw-window-handle` 缺席 | clean release 70.925 s | 9,512,329 B |
| `image-codecs` | `default-features = false, features = ["image-codecs"]` | 48 | `image` 存在，其余三项专属 package 与 `raw-window-handle` 缺席 | clean release 70.671 s | 14,708,568 B |
| `qrcode` | `default-features = false, features = ["qrcode"]` | 34 | `qrcode` 存在，其余三项专属 package 与 `raw-window-handle` 缺席 | clean release 65.295 s | 9,649,320 B |
| `form-pattern` | `default-features = false, features = ["form-pattern"]` | 38 | `regex` 存在，其余三项专属 package 与 `raw-window-handle` 缺席 | clean release 67.461 s | 12,275,753 B |
| `qrcode-disabled` | `default-features = false`，故意导入 `QRCode` | 33 | 四项专属 package 与 `raw-window-handle` 缺席 | E0432 compile-fail 39.929 s | — |
| `form-pattern-disabled` | `default-features = false`，故意调用 `validate_pattern` | 33 | 四项专属 package 与 `raw-window-handle` 缺席 | E0599 compile-fail 40.476 s | — |
| `image-codecs-disabled` | `default-features = false`，故意调用 `load_from_bytes` | 33 | 四项专属 package 与 `raw-window-handle` 缺席 | E0599 compile-fail 42.015 s | — |

十二场景的必需 package 缺失数、禁用 package 误入数与必需诊断缺失数均为 0，且前后清理全部通过。十一个 Windows 场景均记录 target `x86_64-pc-windows-gnu`，实际 resolve 节点包含精确 package `windows`、`windows-core`，不含 `libc`、`wayland-client`；`minimal-linux` 记录 `x86_64-unknown-linux-gnu`，要求并实际包含 `libc`、`wayland-client`，同时强制排除 `windows`、`windows-core`。`raw-window-handle` 在全部十二场景缺席。Linux 场景只证明目标过滤后的解析图与源码类型检查，不具备 Linux 链接器、release 产物或生产 backend 验收，不能外推为 Linux 完整交付。两个日志场景在相同 feature 基线上由 53 个 package、5,278,208 B 变为 63 个 package、6,189,056 B，并由精确 `tracing-subscriber` 正反断言证明订阅器只在启用后进入；基础组合中的 `regex` 会共享部分传递节点，不能把 10 个 package 的净差机械解释成订阅器的全部依赖数。三个公开面禁用场景分别命中 `unresolved import` + `QRCode`、`no method named` + `validate_pattern` 与 `no method named` + `load_from_bytes`。这里的 `image`、`regex` 与 `tracing-subscriber` 断言均针对精确 package，基础 RGBA 位图与 `tracing` 事件契约继续存在；产物大小与耗时只作同机、同提交记录，不单独承担证明。

## 当前差距

| 编号 | 状态 | 差距 |
|---|---|---|
| ODC-01 | ✅ 已完成 | 已建立最小、默认 D3D11 与 settings-serde 三个独立 fixture、锁文件及基线采集器；提交 `ceceff33b28e352d9120713a2ed6506b911cef7a` 的 locked clean-build 时间与产物绑定工具链、target 和 feature 集，旧 resolved package 数仅作未过滤历史计数；当前目标依赖结论以 schema v3 证据为准 |
| ODC-02 | 🔄 进行中 | 已完成首轮 capability、依赖、源码、平台与 SMC 所有权映射，并用 `qrcode`、`form-pattern`、`image-codecs` 与 `demo-logging` 验证组件级、单方法级、服务/组件方法级及根二进制私有实现级四种粒度；其余候选能力仍待逐项判断 |
| ODC-03 | 🔄 进行中 | `qrcode` 的模块/重导出/prelude/patch/快照、`form-pattern` 的 builder 方法/规则变体、`image-codecs` 的解码模块/服务方法/路径型组件方法及 `demo-logging` 的私有初始化实现/调用已同步门控；主 crate 其余 app、data、draw、ui 及组件族仍以无条件 module 为主 |
| ODC-04 | ⏳ 待开始 | `wgpu` 已从依赖图移除；backend feature 与真实依赖图、使用方 capability 边界仍未完全一致 |
| ODC-05 | 🔄 进行中 | `image`、`qrcode`、`regex`、`tracing-subscriber` 已 optionalize 并通过正反 resolved graph 断言；零引用直接依赖 `raw-window-handle` 已删除并由十二场景缺席断言防回归；`windows-core` 已按唯一 Windows TSF 宏所有权收缩到 Windows target；`ab_glyph`、`earcut`、`bytemuck` 仍需按绘制基础所有权判断 |
| ODC-06 | 🔄 进行中 | `qrcode-disabled`、`form-pattern-disabled` 与 `image-codecs-disabled` 已分别证明关闭 feature 后公开类型、builder 方法和解码服务方法不可用；其余 `prelude` 与各 System 公开面仍未形成完整 compile-fail 契约 |
| ODC-07 | 🔄 进行中 | 已建立九个独立 fixture、两个根二进制配置与一个复用最小 fixture 的 Linux 目标配置，覆盖八个正向 release、一个跨目标正向 check、四个单能力/私有实现边界和三个禁用公开面门禁；schema v3 已在十二场景隔离 Windows/Linux 目标依赖并断言已删除死依赖不得回归，其余 capability、Linux release、其他 target/backend、产物符号/资源对比仍待补齐 |

## 实施顺序

1. **基线**：固定工具链、目标平台、clean-build 条件和三个代表性使用方应用，得到未经改造的编译与产物验证。
2. **能力图**：从第三方依赖和大体积组件开始划 capability feature，写明传递依赖与不兼容组合。
3. **编译隔离**：先 optionalize 专属依赖，再对 module 加 `cfg`；同 crate 隔离仍不足时才拆独立 crate。
4. **公开面收缩**：同步门面、`prelude`、示例和未启用能力的编译错误，不保留绕过 feature 的公开路径。
5. **使用方门禁**：执行最小、单能力、backend / target、全能力矩阵，检查依赖图、clean-build 计时与 release 产物。
6. **交付**：门禁通过后再修改使用文档和默认 feature 策略，并记录兼容性影响与迁移方式。

## 完成门禁

只有同时满足以下条件，状态才能改为“✅ 已完成”：

- 至少一个最小 GUI 使用方 fixture 能在关闭默认 feature 后，仅启用明确列出的能力完成 clean release build。
- 未选能力的专属依赖不出现在该 fixture 的 Cargo resolved graph 与构建日志中。
- 每个公开 capability feature 都有独立或声明组合的编译验证；默认、无默认、单 backend 与全能力矩阵通过。
- 至少选取图像/二维码、富文本/正则、Settings serde、Agent 和两个图形 backend 做“启用前后”产物对比，证明专属内容只在启用后出现。
- 使用方最小集与完整集在同条件下记录 clean-build 时间和 release 二进制大小；结论绑定工具链、target、feature 集与 commit。
- 使用文档明确说明 feature 选择、默认集合和迁移方式，不把运行时开关写成编译期裁剪。

## 风险

- Cargo feature 会在整个依赖图中合并；任一传递依赖重新启用能力，都会使其进入构建。
- 过细 feature 会放大组合数量与维护成本，过粗 feature 又无法减少编译成本；粒度必须用基线数据决定。
- 修改默认 feature 或条件导出属于使用方可见兼容性变化，实施时需单独评估版本与迁移策略。
- 二进制变小不能单独证明未用能力没有参与编译；必须同时检查 resolved graph、clean build 和产物。
