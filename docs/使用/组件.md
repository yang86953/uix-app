# 组件

[← 使用索引](../使用.md)

> **接口**：声明 UIX 自定义组件的三种开发方式（函数组件、canvas、`component!` 宏）及 `PaintContext` 绘制入口。依赖：[响应式基础](响应式基础.md)、[事件](事件.md)。导出：组件系统架构 → [架构 · 组件系统](../架构/ui/component.md)，内置组件 → [组件速查](组件速查.md)。

## 适用线索

当需要"创建自定义组件——从简单组合到完整自定义绘制/事件/动画"时，本组件是权威来源。

## 函数组件

优先用普通 Rust 函数组合 `ViewNode`。状态放在最近的业务拥有者，列表子项提供稳定 key；展示函数不直接持有平台或渲染资源。

```rust uix-compile=component-function
use uix::prelude::*;

// 函数组件：输入参数 + 返回 ViewNode，无私有状态
fn user_card(name: &str, role: &str) -> ViewNode {
    column((
        label(name).font_size(16.0),
        label(role).font_size(12.0).color(Color::gray()),
    ))
    .gap(4.0)
    .padding(12.0)
    .bg(Color::WHITE)
    .radius(8.0)
}
```

## Canvas

需要自定义绘制但不需要完整生命周期时使用 canvas/绘制闭包。绘制只消费解析后的尺寸、主题和业务快照，不在 painter 中执行 I/O、计时或状态写入。

```rust uix-compile=component-canvas
use uix::prelude::*;

// 固定尺寸画布：绘制只消费 frame 与 PaintContext
canvas(120.0, 40.0, |frame, ctx| {
    let color = ctx.tokens().color_primary();
    ctx.fill_rounded_rect(frame, color, Radius::uniform(8.0));
});
```

## `component!`

需要私有状态、精细事件、动画或资源租约时使用完整组件。组件在挂载时登记资源，在更新时保留稳定身份，在卸载时取消任务、事件捕获、动画与订阅。

```rust uix-compile=component-full
use uix::prelude::*;

component! {
    pub struct Counter { pub count: u32 }
    @new -> Self { Self { count: 0 } }

    measure => (&self, constraints: Constraints) -> Size {
        constraints.clamp(Size::new(120.0, 36.0))
    }

    on_event => (&mut self, event: &SystemEvent) -> EventResult {
        match event {
            SystemEvent::PointerDown { .. } => {
                self.count += 1;
                EventResult::Handled
            }
            _ => EventResult::NotHandled,
        }
    }

    render => (&self, frame: Rect, ctx: &mut PaintContext) {
        let bg = ctx.tokens().color_primary_bg();
        let color = ctx.tokens().color_primary();
        ctx.fill_rect(frame, bg, None);
        ctx.draw_text(
            &format!("Count: {}", self.count),
            Point::new(frame.x + 8.0, frame.y + 8.0),
            color,
            14.0,
        );
    }
}
```

`component!` 支持 `@new` 构造与 `measure` / `on_event` / `render` / `dirty_rect` 等生命周期与能力方法；能力由框架按方法名自动登记（布局 / 渲染 / 事件 / 生命周期 / 动画）。

## PaintContext

`PaintContext` 是 UI 到 graphics 的规范绘制入口（UI-owned，包装底层 draw 上下文并持有主题令牌作用域）；组件记录绘制意图，不直接选择 GPU backend、surface 或 present 策略。

```rust uix-compile=component-paint-context
// render 内使用令牌与绘制原语
render => (&self, frame: Rect, ctx: &mut PaintContext) {
    // 语义色来自主题令牌，不硬编码
    let primary = ctx.tokens().color_primary();
    let bg = ctx.tokens().color_primary_bg();

    // 绘制原语：圆角矩形、文本、圆形、阴影
    ctx.fill_rounded_rect(frame, bg, Radius::uniform(8.0));
    ctx.fill_circle(frame.x + 20.0, frame.y + 20.0, 10.0, primary);
    ctx.draw_text("hello", Point::new(frame.x + 8.0, frame.y + 8.0), primary, 14.0);
}
```

绘制命令逐一转发给底层 draw 上下文；`with_opacity` 闭包接收底层 draw 上下文（令牌需提前解析）。`dirty_rect` 声明自绘区域的脏矩形，帮助框架最小化重绘（资源优先）。

## 理想使用方式

按产品定义（组合优先、资源优先、容错可观测）：

```rust uix-compile=component-timer-badge
use uix::prelude::*;

// 组件显式声明状态、绘制副作用与卸载清理
component! {
    pub struct TimerBadge {
        pub seconds: u32,
        pub pulse: bool,
    }
    @new -> Self { Self { seconds: 0, pulse: false } }

    measure => (&self, constraints: Constraints) -> Size {
        constraints.clamp(Size::new(160.0, 48.0))
    }

    dirty_bounds => (&self, frame: Rect) -> Rect {
        // 只声明需要重绘的脉冲区域
        Rect::new(frame.x, frame.y, 48.0, frame.h)
    }

    render => (&self, frame: Rect, ctx: &mut PaintContext) {
        let color = if self.pulse { ctx.tokens().color_warning() } else { ctx.tokens().color_primary() };
        ctx.draw_text(&format!("{}s", self.seconds), Point::new(frame.x + 8.0, frame.y + 8.0), color, 16.0);
    }
}
```

原则：

- 组合优先：先函数组件 + 内置组件拼装；只有私有状态/精细事件/动画才升级 `component!`。
- 绘制无副作用：painter 中不执行 I/O、计时或状态写入。
- 卸载清理：任务、事件捕获、动画与订阅在卸载时取消，不留泄漏。
- 令牌优先：颜色、尺寸、字体来自主题令牌，不硬编码平台常量。
