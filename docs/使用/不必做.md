# 不必做

→ [使用总览](../使用.md)

以下操作由框架自动处理，手动干预会破坏框架调度：

- 不手写 `request_redraw` / vsync 申请
- 不手写探活定时器
- 不直接操作框架内部 registry / `OverlayStack`
- 不为各桌面 OS 写两套上层 UI
- 不用 `run_interval` 模拟帧动画（替代方案：[动画系统](动画.md)）
- 现为 Rust API，无独立 UI 脚本语言
