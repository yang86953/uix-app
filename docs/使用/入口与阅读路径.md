# 入口与阅读路径

[← 返回使用 索引](../使用.md)

> **接口**：声明 UIX 的 crate 入口（`uix::prelude::*`）、当前可用状态、读者阅读路径和权威参考入口。依赖：无（入口文档）。导出：快速开始 → [快速开始](快速开始.md)，教程 → [教程](教程.md)。

> 怎样用 UIX 写应用（不必关心框架内部）。详细文档见 **[教程](教程.md)**。

入口：`use uix::prelude::*`。可复制示例 → [README](../../README.md#示例)；能力展示与可执行测试 → [`demo`](../../demo/README.md)；导出一览 → [`prelude.rs`](../../src/prelude.rs)。

> **当前状态**：Windows P6 功能基线已闭合，`0.0.1` 范围内 110 项已完成工作树定向实现与行为验证，版本仍未发布。现有真窗数据来自 Windows / NVIDIA / 96 DPI 工作树；AMD / Intel、144 / 192 DPI、长稳、性能和正式内部包暂无当前报告。详见[`0.0.1 首发状态`](../进度/首发状态.md#当前状态)。

## 阅读路径

**新手**：从教程开始，跟着写一个应用，再查响应式基础了解原理。

**用过类似框架**：直接看响应式基础 → 组件 → 组件速查，把 UIX 当 Ant Design 用。

**遇到问题**：按分类查对应文档，或搜组件速查定位。
