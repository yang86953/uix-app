# 定位与原则

[← 返回产品索引](../产品.md)

> **接口**：声明 UIX 的产品定位、目标用户、差异化对比与设计原则。依赖：[产品](../产品.md)。导出：原则 → [Gitea 0.0.1 产品完成父 Issue #1](http://100.79.245.29:3000/admin/uix-app/issues/1)，按需编译原则 → [使用方按需编译架构](../架构/使用方按需编译.md)。

## 适用线索

需要回答“UIX 定位在哪、和其他方案相比如何取舍、设计上遵循什么原则”时，以本文为准。

## 权威范围

本文持有产品定位事实，包括目标用户、方案取舍和五条设计原则。后续任务与带时点的实施状态不属本文范围，由 Gitea 跟踪。

## 定位

UIX 是一个跨平台原生应用框架，目标是让同一套界面与应用逻辑运行在多个平台的原生窗口上。当前 `0.0.1` 首发范围是 Windows 与 Linux；这一定义不构成对 macOS 或移动端当前完成度的承诺。

UIX 不把浏览器或 WebView 作为界面运行时。界面推荐用 uix-lang 描述并在编译时转换为 Rust，也可以直接使用 Rust 声明式 API；框架通过自身图形管线绘制到原生窗口。“全栈”只表示 UI、应用壳和本地数据能力，不包含网络或同步。

**界面描述方式**（产品方向声明）：

- **推荐**：uix-lang——用 `.uix` 文档描述界面结构与样式，编译时由框架自动转换为 Rust（规范见 [uix-lang 语言规范](../uix-lang/README.md)）。
- **支持**：Rust 声明式 API——直接书写界面代码；uix-lang 已登记能力映射到相同的 Rust 语义。`uix_app!` 可从 `<App>` 根文档生成 `App` builder，Rust 组合根继续持有最终 `.run()` 与窗口生命周期。公开 `uix!` 可把内嵌源码或 `.uix` 文件编译为 `ViewNode`；未登记能力与宏边界见 [uix-lang 语言首页](../uix-lang/README.md#状态声明)。

## 目标用户

- 当前需要用 Rust 构建 Windows 或 Linux 桌面应用的开发者
- 希望复用大部分界面与应用逻辑，并把平台差异收敛在框架边界内的团队
- 在乎应用体积和资源消耗
- 想要声明式 UI，但不要 WebView 或 JavaScript

当前不适合作为首选方案的场景：需要复用现有 Web 前端、依赖框架内建网络/云同步、立即交付移动端，或需要以开源许可向外部分发 UIX 本身。相关边界见[能力与边界](能力.md)和[交付与许可](交付与许可.md)。

## 方案取舍

| 对比方向 | UIX 的选择 | 需要同时考虑 |
|---|---|---|
| Electron | 不捆绑浏览器内核，使用 UIX 自身的布局与绘制管线 | 实际体积、内存和启动速度取决于应用内容、资源、后端与构建配置，不承诺固定倍数 |
| Tauri | 不使用 HTML/CSS/JavaScript 作为界面技术栈，界面由 uix-lang 或 Rust 描述 | 无法直接复用 Web 组件生态；UIX Lang 只映射 UIX 自身语义 |
| Qt 等跨平台原生框架 | 以 Rust 类型和 Cargo 工具链为中心，提供声明式界面与统一平台契约 | UIX 尚处于 `0.0.1` 开发阶段，生态、平台覆盖和成熟度需单独评估 |
| Win32 等平台 API | 将平台差异封装在框架边界内，减少应用层条件分支 | 需要精确使用平台专属能力时，仍受 UIX 当前公开能力范围限制 |

## 原则

1. **资源优先**——空闲时进入休眠；重绘、缓存和后台工作必须有明确触发条件与生命周期。
2. **Rust 优先**——uix-lang 在编译时转换为 Rust，运行期不引入第二套语言解释器；可静态检查的错误尽量在编译期暴露。
3. **原生优先**——上层契约不散落平台条件分支，平台差异由明确边界吸收；平台本身不支持的能力返回可识别错误。
4. **容错可观测**——失败有类型和责任边界，恢复有明确路径，最终失败可以形成报告。
5. **使用方按需编译**——未选择且非传递必需的能力不进入使用方编译图；已选择能力中最终不可达的代码和数据由 release 链接裁剪。运行时关闭能力不能替代编译期裁剪。

## 后续方向

→ 后续方向（更小 · 更快 · 更稳）由 [Gitea 0.0.1 产品完成父 Issue #1](http://100.79.245.29:3000/admin/uix-app/issues/1) 跟踪。
