# 交付与许可

[← 返回产品 索引](../产品.md)

> 分类：交付与许可  
> 权威范围：平台优先级、专有许可和内部交付边界

## 当前阶段

**Windows 优先**做到生产可用。P6 功能基线已经闭合；`0.0.1` 范围内 110 项已完成工作树定向实现与行为验证，版本当前仍未发布。现有新 wgpu 真窗数据来自工作树，AMD / Intel、144 / 192 DPI、长稳、性能和正式内部包暂无当前报告，详见[`0.0.1 首发状态`](../进度/首发状态.md#当前状态)。平台差异只在 `native` backend；上层只见 trait，禁按 OS 分叉（`#[cfg(OS)]` 仅限 `native/`）。

UIX `0.0.1` 是 `yangyanhui` 持有版权的**专有闭源、仅限获授权内部交付**版本，不向 crates.io 发布，也不公开提供源码或二进制制品；法律边界见 [`LICENSE`](../../LICENSE)，交付形态见[首发状态](../进度/首发状态.md#内部交付形态)。
