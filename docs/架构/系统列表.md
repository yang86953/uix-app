# 系统列表

[← 返回架构索引](../架构.md)

> **接口**：适用于把通用 SMC 定义应用到 UIX，决定能力属于哪个领域边界和目标实现单元。本文档权威持有 UIX 的候选 System、目标 Module 目录和跨层规则；Module 内部 Component 契约由各模块文档持有。依赖：[产品](../产品.md)。导出：[架构索引](../架构.md)中的全部模块正文。

## 设计阶段的三层粒度

| 层级 | 设计定义 | 代码重构后的落点 | 文档责任 |
|---|---|---|---|
| System | 能向产品或其他领域独立承诺完整结果的边界，拥有公开契约、领域状态、策略、失败语义和生命周期 | 通常由一个顶层 Rust module 提供公开门面；可跨多个物理单元 | 本文声明候选边界、公开责任和跨 System 依赖 |
| Module | 只在所属 System 内有意义、围绕同一变化原因隐藏多项协作决策的私有能力 | 通常落在私有或 `pub(crate)` Rust module，可由一个或多个目录/文件承载 | 对应架构正文声明职责、私有契约、所有权和不变量 |
| Component | 从 Module 中拆出的单一职责原子行为，具有窄契约、替换价值与独立验证边界 | 可由类型、窄 trait、函数组合或其他语言机制实现；不要求一组件一文件 | 模块正文声明输入、输出、错误、并发、实例所有权和替换边界 |

**三层角色不是物理目录的别名。** 本目录描述重构完成后应形成的逻辑架构；当前路径只作为定位遗留实现的迁移线索，不得反向决定 System、Module 或 Component 的数量、命名和职责。一个现有文件可能包含多个角色的实现，多个现有目录也可能共同承载一个逻辑角色。没有独立契约、替换价值或变化原因的普通函数、基础算法、生成代码和外部资源不强行归入三层。

## 术语语义基线：component 只指 SMC 的 Component

「component」在本仓库代码与架构文档中只指 SMC 三层中的 **Component**——单一职责、窄契约、独立替换边界明确的原子代码单位。UI 实体一律称为 widget：`Widget`（主契约）、`WidgetId`（身份）、`WidgetHandle` / `WidgetConfig` / `WidgetOverrides`（句柄/配置）、`widget_snapshot`（派生快照）、widgets 库实现以及 UIX Lang `Widget` 声明均不属于 SMC Component 术语。

ui System 的组件运行时框架由 `widget_runtime` 私有 Module 承载（WidgetTree / WidgetNode / 契约 / 管理器 / 配置 / i18n / 焦点陷阱）；该命名与 SMC Component 层保持区分。原 `component` Module 的迁移记录由 [Gitea Issue #4](http://100.79.245.29:3000/admin/uix-app/issues/4) 持有，任何新代码与文档不得再引入「component 指模块」的用法。

系统与模块目录只以本文列出的目标模块为准。为保持其他三类文档在分阶段迁移中可导航，少量旧文件名可暂存“迁移路由（非模块）”；这些路由不进入目标架构，引用迁移完毕后删除。

## 候选领域边界

下表声明当前目标领域边界，不以历史“六系统”命名或物理目录数量作为定级依据。`core` 与 `bus`（`src/bus/`）当前只有基础值与失败契约 / 无领域状态、策略和生命周期，因此暂按模式外基础依赖处理（待定级）；其余候选 System 必须同时持有公开结果、领域状态、策略、失败语义和生命周期。`bus` 承载通用 SMC 定义的进程内 EventBus（可复用 Component 定义），由各 Module/System 创建自己的实例（定义见内部知识库《System Module Component 设计模式》）。

| 系统 | 核心职责 | 允许依赖 |
|---|---|---|
| `core`（基础依赖，待定级） | 无平台、图形和 UI 语义的基础值与失败模型 | — |
| `bus`（模式外基础设施，待定级） | 进程内同步类型化事件分发 | — |
| `diagnostics` | runtime 级错误观察、恢复协调、callback 失败归队和崩溃记录 | `core` |
| `platform` | OS 窗口、输入、呈现能力和本机传输 | `core`、`diagnostics`、`bus` |
| `graphics` | 绘制录制、场景、资源、后端与提交 | `core`、`diagnostics`、`platform`、`bus` |
| `ui` | 组件、声明式视图、状态、交互和通用组件 | `core`、`platform`、`graphics`、`bus` |
| `data` | 不进入每帧热路径的应用数据服务 | `core`、`bus` |
| `app` | 组装并调度所有下层系统 | `core`、`diagnostics`、`platform`、`graphics`、`ui`、`data`、`bus` |

这是目标代码依赖的有向无环图，不要求每个 System 依赖排序中紧邻的边界。例如 data 只依赖 core；ui 不得为了调用 AppHandle 而反向依赖 app，所需能力由组合根或上层 System 注入窄契约。

```text
core ─ diagnostics ─ platform ─ graphics ─ ui ─┐
  └─ data ─────────────────────────────────────┤
bus（独立基础设施，由所有者按需实例化）────────┤
                                              └─ app
```

## core：基础契约（System 定级待定）

core 只拥有跨系统稳定、无运行时策略的值类型。它不包含日志器、性能探针、字体轮廓等上层机制。

| 目标模块 | 目标职责 | 代表组件 | 文档 |
|---|---|---|---|
| identity | 稳定身份与代际失效 | `WidgetId`、`WindowId` | [identity](core/identity.md) |
| geometry | logical 坐标、尺寸、矩形和约束 | `Point`、`Size`、`Rect`、`Constraints` | [geometry](core/geometry.md) |
| damage | 增量失效与提交前后真相 | `DirtyFlags`、`DamageRegion`、`PresentDamageTracker` | [damage](core/damage.md) |
| error | typed error 和统一结果语义 | `Error`、`Errc`、`Result<T>` | [error](core/error.md) |

## diagnostics：运行保障系统

diagnostics 对框架与应用宿主提供单一 runtime 级运行保障公开面。它拥有报告容量、恢复登记、callback 失败归队和崩溃记录生命周期；不负责具体资源重建、业务补偿、远程上传或应用展示。

| 目标模块 | 目标职责 | 代表组件 | 文档 |
|---|---|---|---|
| reporting | typed error 脱敏、有界存储、快照和结构化事件 | `ReportDraftBuilder`、`ReportStore`、emission guard | [diagnostics](platform/diagnostics.md) |
| recovery | 精确错误码恢复登记、递归保护和结果保真 | `RecoverySubscription`、`RecoveryGuard` | [diagnostics](platform/diagnostics.md) |
| crash | panic 最小安全记录、原子落盘和 hook 生命周期 | `CrashReport`、panic hook、atomic writer | [diagnostics](platform/diagnostics.md) |

> **迁移路由**：文档暂保留在 `platform/diagnostics.md`，只为避免迁移期链接失效；该路径不表示 diagnostics 属于 platform 的私有 Module。

## platform：操作系统适配系统

platform 对上提供 owned 值和线程亲和能力，对内收敛 `#[cfg(target_os = ...)]` 与原生资源。分派用的 trait 是组件或实现手段，不单独形成 traits 模块。

| 目标模块 | 目标职责 | 代表组件 | 文档 |
|---|---|---|---|
| capabilities | 显示器、主题、硬件与轻量系统查询 | `Platform`、`OsInfo`/`DisplayInfo`、`SystemNotification` | [capabilities](platform/capabilities.md) |
| windowing | 原生窗口、事件源、输入、剪贴板与 IME | `PlatformWindow`、`EventSource`、`TextInputSession` | [windowing](platform/windowing.md) |
| presentation | surface、图形 recipe 与 presenter | `GraphicsRecipe`、`GraphicsSurface`、`Presenter` | [presentation](platform/presentation.md) |
| agent-transport | 可选同用户本机 IPC 和 discovery | `AgentListener`、`PeerIdentity`、`DiscoveryPublisher` | [agent-transport](platform/agent-transport.md) |

## graphics：图形与呈现系统

graphics 接收解析后的视觉值并完成录制、场景组织、资源管理、backend 执行与提交。Theme、组件状态和窗口事件不属于本系统。

| 目标模块 | 目标职责 | 代表组件 | 文档 |
|---|---|---|---|
| geometry | 图形领域的颜色、路径和空间变换 | `Color`、`Path`、`Transform` | [geometry](graphics/geometry.md) |
| painting | 规范绘制 API、命令录制与帧编码 | `PaintContext`、`DisplayList`、`FrameEncoder` | [painting](graphics/painting.md) |
| scene | retained layer、picture 和场景差异 | `LayerTree`、`LayerNode`、`Picture` | [scene](graphics/scene.md) |
| renderer | 帧编排、RenderTarget、提交结果和恢复 | `Renderer`、`RenderTarget`、`GraphicsRecovery` | [renderer](graphics/renderer.md) |
| backend | CPU/GPU 执行、能力协商和 fallback | `RenderBackend`、`GpuBackend`、`FramePlan` | [backend](graphics/backend.md) |
| resources | 字体、文本布局、图像和代际句柄 | `FontService`、`ImageService`、resource handle | [resources](graphics/resources.md) |

## ui：声明式界面系统

ui 拥有从 View 输入到持久组件树、布局、交互和语义的完整界面模型。它通过 graphics/painting 录制，不直接接触原生 surface 或 backend。

| 目标模块 | 目标职责 | 代表组件 | 文档 |
|---|---|---|---|
| widget_runtime | 组件运行时框架：持久组件树、能力分派和树级运行态 | `WidgetTree`、`Widget`、`WidgetManagers` | [widget_runtime](ui/widget_runtime.md) |
| view | 声明式节点、组合器和 reconcile 输入 | `View`、`ViewNode`、`ViewAdapter` | [view](ui/view.md) |
| reactive | 状态、派生值、依赖捕获和副作用 | `State`、`Computed`、`Effect` | [reactive](ui/reactive.md) |
| layout | 测量、盒模型、Flex/Grid 与有限几何 | `LayoutEngine`、`FlexLayout`、`GridLayout` | [layout](ui/layout.md) |
| event | 系统事件、语义事件、传播和 handler | `SystemEvent`、`SemanticEvent`、`HandlerTable` | [event](ui/event.md) |
| animation | 插值、过渡、活动源和暂停 | `Animation`、`Animated`、`TransitionPlayer` | [animation](ui/animation.md) |
| accessibility | 无障碍快照、语义树和动作 | `AccessibilitySnapshot`、`SemanticNode`、`SemanticAction` | [accessibility](ui/accessibility.md) |
| theme | 主题令牌、组件配置、本地化和 Provider | `Theme`、`ConfigProvider`、`LocaleProvider` | [theme](ui/theme.md) |
| overlay | 窗口内浮层栈、focus trap 和进退场 | `OverlayStack`、`OverlayEntry`、`FocusTrap` | [overlay](ui/overlay.md) |
| form | 字段模型、校验、提交和动态行 | `FormModel`、`FieldError`、field adapter | [form](ui/form.md) |
| virtualization | 大数据视图物化、稳定 key 和测量缓存 | `VirtualViewport`、`MaterializedRange`、`ItemKey` | [virtualization](ui/virtualization.md) |
| widgets | 内置通用与复合组件库 | `Button`、`Input`、`Table`、`Modal` | [widgets](ui/widgets.md) |

## data：应用数据服务系统

data 持有明确由应用触发的冷路径数据能力，不感知窗口、主题、组件树或图形 backend。

| 目标模块 | 目标职责 | 代表组件 | 文档 |
|---|---|---|---|
| settings | 设置状态、编解码与可恢复原子保存 | `SettingsService`、codec、sidecar protocol | [settings](data/settings.md) |

## app：应用运行时系统

app 是唯一允许组装全部下层系统的顶层。它拥有进程与窗口生命周期、等待/唤醒和逐窗推进，但不重新定义 UI 语义或图形执行。

| 目标模块 | 目标职责 | 代表组件 | 文档 |
|---|---|---|---|
| application | 应用组装、共享服务、运行模式和关闭 | `App`、`AppHandle`、`Container` | [application](app/application.md) |
| event-loop | 平台等待、活动工作、timer 和帧机会 | `EventLoop`、`ActiveWorkRegistry` | [event-loop](app/event-loop.md) |
| window | 窗口创建、逐窗会话、单帧驱动和 IME | `Window`、`WindowSession`、`WindowDriver`、`FrameScheduler` | [window](app/window.md) |
| agent | 可选本机控制面、协议和语义动作桥 | `AgentProcessBridge`、`AgentProtocolSession`、`AgentWindowRegistration` | [agent](app/agent.md) |
| extensions（目标，未实现） | 标准 Lisp 软件扩展、活动代、能力登记、状态迁移与回收 | 解释器私有适配、候选加载、注册集与配额管理；公开名称待冻结 | [extensions](app/extensions.md) |

`AgentCommandQueue`、`WindowAgentState`、`AgentCommandExecutor` 与 `AgentSemanticsPort` 是 app System 私有的跨 Module 契约和状态边界，不归任一 Module 私有。`session_runtime` 组合根创建并注入队列、执行器与语义端口；`agent` 提供执行和发布端口实现，`event-loop` / `window` 只消费 System 私有契约。

动态扩展也由 app System 组装：`extensions` 不直接持有 `application`、`window`、`event-loop` 或 `agent` Module，通过 app 私有契约取得窗口提交、任务与关闭能力。ui 只消费声明数据，不反向依赖 Lisp 引擎；具体执行与所有权见[目标设计](app/extensions.md#4-职责执行位置与状态权威)，不表示当前源码已增加对应 Module。

## 系统内业务流向

下列箭头只表示由 System 编排时的领域数据流，不授予 Module 之间的代码依赖。每个表项中的 Module 必须保持兄弟隔离：System 调用左侧、接收结果并转换后，再调用右侧。

| System | 由 System 编排的主业务流 |
|---|---|
| core | `geometry → damage`；identity、geometry、error 彼此独立 |
| diagnostics | `typed error → recovery`；未恢复错误再进入 reporting；panic 独立进入 crash；三个 Module 互不直连 |
| platform | `windowing → presentation`；`windowing → agent-transport`；capabilities 独立 |
| graphics | `geometry → painting → scene`；`painting → backend`；`{geometry, backend} → resources`；`{scene, backend, resources} → renderer` |
| ui | `widget_runtime → reactive → view → theme`；`widget_runtime → layout → event → accessibility`；`{widget_runtime, reactive} → animation`；`{event, animation} → overlay`；`{widget_runtime, reactive, event} → form`；`{widget_runtime, layout, view} → virtualization`；上述能力最终供 widgets 组合 |
| data | settings 当前无系统内模块依赖 |
| app | `window → {event-loop, agent} → application`；event-loop 与 agent 互不依赖 |

> **当前实现映射**：上表只表达 System 编排的数据流。`event_loop` / `window` 不直接引用 `agent` Module；Agent 命令协议、队列与每窗状态已由 app System 私有边界持有，具体执行器和语义发布端口由 `session_runtime` 在组装期注入。兄弟 Module 禁止重新直连；这是内部架构审查规则，不建立项目测试。

禁止任何 Module 直接引用、持有、发现或回调同一 System 的兄弟 Module；事件总线、容器查询和异步通道也不能用于规避该规则。

## 当前实现迁移线索

下表只帮助后续重构定位遗留代码，不是目标模块定义。

| 目标系统 | 当前主要实现线索 |
|---|---|
| core | `src/core/` |
| bus（模式外） | `src/bus/` |
| diagnostics | `src/diagnostics/` |
| platform | `src/platform/`、`src/native/` |
| graphics | `src/draw/` |
| ui | `src/ui/` |
| data | `src/data/` |
| app | `src/app/` |

实际迁移应以“目标组件归属”逐项搬运：先建立新边界和单向依赖，再迁移组件，最后删除旧入口。不得为了维持当前目录而新增目标模块，也不得在文档中把临时兼容层提升为长期架构。

## 跨层强制规则

| 规则 | 设计契约 |
|---|---|
| OS 隔离 | OS 专属实现和条件编译收敛在 platform；上层只依赖 owned 描述值和明确能力 |
| 绘制入口 | UI 和内置组件只使用 graphics/painting；raw backend、可写像素和原生上下文不进入组件 API |
| 主题归属 | Theme、DesignTokens、组件配置和 Locale 属于 ui；graphics 只接收已解析视觉值 |
| 单一 UI 管线 | View → WidgetTree → layout/event/paint 是唯一可写界面路径；无障碍与 Agent 都是派生或动作桥 |
| 资源身份 | 长寿命窗口、组件和图形句柄使用 generation；旧 generation 只能失效，不能误命中新资源 |
| 逐窗隔离 | timer、动画、帧请求、surface 状态、IME、Agent 队列和语义 revision 归所属窗口 |
| 热路径 | 设置 I/O、服务解析和阻塞系统调用不得进入 reconcile/layout/paint/present 热路径 |
| 失败语义 | 内层传播 typed error；最终责任边界只观察/报告一次，不把失败伪装成成功 |
| 诊断隔离 | diagnostics 只观察、登记和协调；具体资源重建、业务补偿与用户展示仍由资源所有者或应用负责 |
| 深度休眠 | 无事件、到期工作、队列或帧机会时进入 DeepIdle；禁止固定 tick 和无条件轮询 |
| 公开边界 | 公开 API 由产品与使用文档约束；源码内部 module graph 不自动成为兼容承诺 |
| 兄弟隔离 | 同一 System 的 Module 互不依赖；所有跨 Module 流程由 System 编排 |
| 组件所有权 | Module 持有自己的 Component 实例；定义可以复用，可变实例不得跨 Module 共享 |
| 组合根 | 最终应用选择和注入 System；框架不提供固定的全系统聚合对象 |

## 关键数据流

```text
platform event
  → app/event-loop 定位目标 app/window
  → ui/event 更新 ui/reactive 或组件运行态
  → ui/view reconcile → ui/layout
  → graphics/painting → graphics/scene
  → graphics/renderer/backend
  → platform/presentation
```

settings、theme、accessibility 和 agent 不建立旁路：settings 只在应用冷路径读写；theme 只改变 UI 解析上下文；accessibility 从组件树派生；agent 动作重新进入目标窗口的正常事件路径。
