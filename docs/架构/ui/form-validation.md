# 表单校验

[← 架构索引](../../架构.md)

> **接口**：声明 ui 系统中**表单校验**的内部设计。所属系统：`ui`（位于 `foundation/` 模块中）。依赖：[组件核心模块](core.md)。导出：表单用法 → [使用 · 输入与表单](../../使用/输入与表单.md)。

## 模块定位

表单校验（`src/ui/foundation/` 中表单相关部分）负责字段值的类型化管理、校验规则触发和级联重验。Form 只管理布局不持有数据，字段值与校验规则由应用侧 `FormModel` 管理。
