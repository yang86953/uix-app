# 输入与表单

## 文本输入

```rust uix-compile=text-inputs
let text = State::new(String::new());

// 单行
input()
    .placeholder("请输入用户名")
    .value(&text);

// 多行
Input::textarea()
    .placeholder("请输入描述")
    .value(&text)
    .rows(5)
    .max_length(500);

// 密码
Input::password()
    .placeholder("请输入密码")
    .value(&text);
```

`.value(&State<String>)` 使用 controlled 模式：输入事件立即回写 State，外部 `set` 自动 reconcile；业务读取 `text.get()`，不从组件实例维护第二份值。`Input::textarea()` / `Input::password()` 直接声明模式；`.rows()` 至少为 1，`.max_length()` 按 Unicode 字符而非 UTF-8 字节计数。

## 选择器

```rust uix-compile=single-select
let selected = State::new(String::new());
let options  = vec!["选项 A", "选项 B", "选项 C"];

Select::new()
    .options(&options)
    .value(&selected)
    .placeholder("请选择");
```

```rust uix-compile=multiple-select
// 多选
use std::collections::HashSet;

let options = vec!["选项 A", "选项 B", "选项 C"];
let selected_set = State::new(HashSet::new());

Select::multiple()
    .options(&options)
    .value(&selected_set); // State<HashSet<String>>
```

```rust uix-compile=searchable-select
// 可搜索
let selected = State::new(String::new());
let options = vec!["选项 A", "选项 B", "选项 C"];

Select::searchable()
    .options(&options)
    .value(&selected);
```

`searchable()` 聚焦后直接输入即可按大小写不敏感的包含关系过滤；`Backspace` 修改查询，方向键在过滤结果中移动，`Enter` 选择并关闭。查询只属于组件运行态，不写入绑定 State。

Select 以选项文本而非索引作为外部真值：单选绑定 `State<String>`，多选绑定 `State<HashSet<String>>`；用户选择立即回写，外部值不在选项中时显示 placeholder。只需非受控初始索引时使用 `default_selected(index)`。

## 开关与勾选

```rust uix-compile=boolean-and-choice-inputs
let checked   = State::new(false);
let selected  = State::new(String::new());
let options = vec!["选项 A", "选项 B", "选项 C"];

Checkbox::new("同意协议").checked(&checked);
Switch::new().checked(&checked);
Radio::group("group1", &options, &selected);
Segmented::new(&options).value(&selected);
```

`checked(&State<bool>)` 以外部 State 为真值并在用户切换时回写；只需非受控初值时使用 `default_checked(bool)`。

`Radio::group(name, options, &State<String>)` 创建具名受控单选组：外部值按选项文本匹配，点击、方向键与语义选择立即回写，组名进入无障碍语义。链式写法可使用 `Radio::new().group_name(...).options(...).value(&state)`；只需非受控初值时使用 `default_selected(index)`，当前值与索引分别由 `current_value()`、`current_index()` 返回。

`Segmented::new(options).value(&State<String>)` 使用相同的选项文本契约；点击、方向键与语义选择会跳过 `disable_option(index)` 标记的项并立即回写。组件默认进入 Tab 顺序并显示焦点框，获得焦点后可直接用方向键选择。只需非受控初值时使用 `default_selected(index)`，当前值与索引同样由 `current_value()`、`current_index()` 返回。

## 数值输入

```rust uix-compile=numeric-inputs
let num    = State::new(0i32);
let val    = State::new(50.0f64);
let rating = State::new(3u32);

InputNumber::new().value(&num).min(0.0).max(100.0).step(1.0);
Slider::new(0.0..=100.0).value(&val);
Rate::new().count(5).value(&rating);
```

InputNumber 通过 `value(&State<T>)` 托管标准整数或浮点数，接收系统 `TextInput` / IME commit 与粘贴中的 ASCII 数字、负号和小数点；`Enter` 或失焦提交，无法解析的缓冲恢复为上一个有效值。`Up` / `Down` 及右侧步进按钮按 `step` 立即回写；整数 State 会先将结果四舍五入，再同步组件显示。外部越界值按 `min` / `max` 显示且构建过程不改写 State，下一次用户操作会回写合法值。只需非受控初值时使用 `default_value(T)`，占位文本用 `.placeholder(...)`，当前缓存值统一由 `current_value()` 返回。

Slider 使用 `RangeInclusive<f64>` 声明区间，`value(&State<f64>)` 以外部 State 为真值并在拖动、方向键或语义增减时回写。外部越界值按区间端点显示且构建过程不改写 State；下一次用户操作会回写合法值。只需非受控初值时使用 `default_value(f64)`。

Rate 通过 `value(&State<u32>)` 托管评分，点击、方向键与语义增减立即回写；组件默认进入 Tab 顺序并显示焦点框。外部超出 `count` 的值按上限显示，下一次用户操作会写回合法值。只需非受控初值时使用 `default_value(u32)`，当前缓存值由 `current_value()` 返回。启用 `allow_half()` 后，State 使用半星步数：`1` 表示半星、`2` 表示一星。

## 日期、时间与颜色

```rust uix-compile=date-time-color-inputs
let date  = State::new(Date::today());
let time  = State::new(Time::now());
let color = State::new(Color::RED);

DatePicker::new().value(&date);
TimePicker::new().value(&time);
ColorPicker::new().value(&color);

// 范围选择
let (start, end) = (State::new(Date::today()), State::new(Date::today()));
DateRangePicker::new()
    .start(&start).end(&end)
    .presets(vec![
        ("今天",     PresetDate::today()),
        ("本周",     PresetDate::this_week()),
        ("本月",     PresetDate::this_month()),
        ("最近 7 天", PresetDate::last_days(7)),
    ])
    .disabled_date(|d| d.weekday().is_weekend()); // 周末禁用

// 其他选择模式
DatePicker::new().mode(PickerMode::Week); // 周 / Month 月 / Quarter 季
```

基础 DatePicker 以 `State<Date>` 为外部真值，用户选日立即回写；`Date::today()` 返回当前 UTC 日期，年月日构造会归一到合法公历日期。只需非受控初值时使用 `default_value(Date)`，占位文本用 `.placeholder(...)`。`disabled_date` 禁止命中日期提交；Week / Month / Quarter 模式分别把周一、月初、季初写回 State，便于业务使用稳定代表值。

DateRangePicker 以 `start` / `end` 两个 `State<Date>` 为外部真值；第一次命中只建立待选起点，第二次命中按日期排序后一次提交范围，禁用端点不会提交。非受控初值用 `default_range(start, end)`。`PresetDate::today()` / `this_week()` / `this_month()` / `last_days(n)` 在组件构建时按当前 UTC 日期生成稳定范围；选择预设同样按起止端点执行禁用规则。范围快照与无障碍值统一为 `YYYY-MM-DD / YYYY-MM-DD`。

TimePicker 以 `State<Time>` 为外部真值，选中小时或分钟时立即回写；`Time::now()` 返回当前 UTC 时间并精确到分钟，`Time::new(hour, minute)` 将字段收敛到 `23:59` 内。只需非受控初值时使用 `default_value(Time)`，占位文本用 `.placeholder(...)`，当前缓存值由 `current_value()` 返回；`00:00` 是有效值而非空值。

ColorPicker 以 `State<Color>` 为外部真值，选中预设色时立即回写，外部更新会重绘色块并同步无障碍值。只需非受控初值时使用 `default_value(Color)`，当前缓存值由 `current_value()` 返回；未配置时从 `Color::default()`（黑色）开始。

## 级联与自动完成

```rust uix-compile=cascader-and-suggestions
let tree_data = vec![
    TreeNode::new("北京", "beijing").children(vec![
        TreeNode::new("朝阳", "chaoyang"), TreeNode::new("海淀", "haidian"),
    ]),
    TreeNode::new("上海", "shanghai").children(vec![
        TreeNode::new("浦东", "pudong"), TreeNode::new("静安", "jingan"),
    ]),
];

let cascader_options = vec![
    CascaderOption::new("北京", "beijing").children(vec![
        CascaderOption::new("朝阳", "chaoyang"),
        CascaderOption::new("海淀", "haidian"),
    ]),
];

let cascader = Cascader::new(cascader_options, "请选择地区");
let tree_select = TreeSelect::new().nodes(tree_data).placeholder("请选择节点");
let mentions = Mentions::new("请输入 @ 用户").options(vec!["Ada", "Alan", "Grace"]);
let autocomplete = AutoComplete::new().options(vec!["Ada", "Alan", "Grace"]);
```

`Cascader::selected()` 返回当前标签和值路径；`TreeSelect::value()` / `value_key()`、`Mentions::value()` 与 `AutoComplete::value()` 返回各组件当前缓存值。

`AutoComplete` 是真实文本输入目标：点击或聚焦后可接收系统 `TextInput` / IME commit 与粘贴，`Backspace` 修改查询，按大小写不敏感的包含关系过滤；`Up` / `Down` 移动候选，`Enter` 提交，`Escape` 或失焦关闭弹层。`.value()` 始终返回当前已键入文本或最终候选值。

`Mentions` 在最后一个 `@` 后按大小写不敏感方式过滤；查询字符会立即进入 `.value()`，失焦只关闭建议而不丢文本。`Up` / `Down` 移动候选，`Enter` 或点击提交，并把当前 `@query` 替换为 `@候选 `。

`TreeSelect` 可用 `Down` / `Enter` / `Space` 打开；`Up` / `Down` 在扁平树中移动并跳过 `TreeNode::disabled(true)`，`Enter` / `Space` 提交，`Escape` 或失焦关闭。长列表会自动滚动到键盘高亮项，`.value()` / `.value_key()` 与结构化快照同步返回当前选择。

`Cascader` 可用 `Down` / `Enter` / `Space` 打开；`Up` / `Down` 在当前层跳过禁用项，`Right` / `Enter` 进入子级或提交叶子，`Left` 返回父级，`Escape` 关闭。`.selected()` 与结构化快照同步返回已选择的 label / value 路径。

## 上传

```rust uix-compile=upload-config
let mut upload = Upload::new()
    .accept(".jpg,.png,.pdf")
    .multiple(true)
    .max_count(10);
assert!(upload.try_add_file("report.pdf"));
upload.update_progress(0, 0.5);
let queued = upload.files();
```

`accept` 声明允许的扩展名，`multiple` 控制一次拖放可接收的文件数，`max_count` 限制队列数量；Windows 原生 `FileDrop` 会把命中组件且符合过滤条件的真实文件名加入队列。应用通过 `files()` 读取队列，并可把文件对话框结果交给 `try_add_file`：返回 `false` 表示过滤或数量限制拒绝了文件；兼容的 `add_file` 会忽略该返回值。`update_progress` / `complete_file` 更新展示状态，成功完成会把进度归一为 `1.0`；`remove_file` 返回被移除项，`clear_files` 清空队列。结构化快照与可访问性列表同步报告队列状态。组件不读取或传输文件，实际 I/O 仍由应用服务负责。

---

## 表单

### 基础表单

字段校验闭包直接定义，返回 `Result<(), String>`：

```rust uix-compile=form-validation
let form = Form::new()
    .field("user", "用户名")
        .default("")
        .required("请输入用户名")
        .custom(|v| if v.len() < 3 {
            Err("至少 3 个字符".into())
        } else { Ok(()) })
    .field("email", "邮箱")
        .default("")
        .required("请输入邮箱")
        .validate_email("邮箱格式不正确")
    .field("age", "年龄")
        .default(0i32)
        .validate_range(1..=120, "年龄范围 1-120")
    .build();

match form.validate() {
    Ok(values) => {
        if let (Some(user), Some(email)) = (
            values.get::<String>("user"),
            values.get::<String>("email"),
        ) {
            println!("submit: {user} <{email}>");
        }
    }
    Err(errors) => {
        for error in &errors {
            eprintln!("{}: {}", error.field(), error.message());
        }
    }
}
```

### FormItem —— 声明式字段绑定

字符串、选项、布尔与标准数值字段可直接绑定为对应的 `FormItem + controlled component` View：

```rust uix-compile=form-input-item
use std::collections::HashSet;

let email = State::new(String::new());
let age = State::new(18i32);
let plan = State::new(String::new());
let channels = State::new(HashSet::new());
let accepted = State::new(false);
let notifications = State::new(true);
let volume = State::new(50.0_f64);
let rating = State::new(3_u32);
let delivery = State::new("标准".to_string());
let density = State::new("舒适".to_string());
let form = Form::new()
    .field("email", "邮箱")
        .default("")
        .required("请输入邮箱")
        .validate_email("格式不正确")
        .validate_trigger(Trigger::OnBlur)
    .field("age", "年龄")
        .default(18i32)
        .validate_range(18..=120, "年龄范围 18-120")
        .validate_trigger(Trigger::OnChange)
    .field("plan", "方案")
        .default("")
        .required("请选择方案")
        .validate_trigger(Trigger::OnBlur)
    .field("channels", "渠道")
        .default(HashSet::new())
        .required("请至少选择一个渠道")
        .validate_trigger(Trigger::OnChange)
    .field("accepted", "条款")
        .default(false)
        .custom(|value| if value == "true" { Ok(()) } else { Err("请接受条款".into()) })
        .validate_trigger(Trigger::OnChange)
    .field("notifications", "通知")
        .default(true)
    .field("volume", "音量")
        .default(50.0_f64)
        .validate_range(0.0..=100.0, "音量范围 0-100")
        .validate_trigger(Trigger::OnChange)
    .field("rating", "评分")
        .default(3_u32)
        .validate_range(1..=5, "评分范围 1-5")
        .validate_trigger(Trigger::OnBlur)
    .field("delivery", "配送")
        .default("标准")
        .required("请选择配送方式")
    .field("density", "密度")
        .default("舒适")
    .build();

let content = column((
    form.input_item("email", &email)
        .map(|item| item.placeholder("name@example.com").show_error(true)),
    form.input_number_item("age", &age)
        .map(|item| item.min(0.0).max(120.0).step(1.0)),
    form.select_item("plan", &plan)
        .map(|item| item.options(["基础版", "专业版"]).searchable()),
    form.select_item("channels", &channels)
        .map(|item| item.options(["邮件", "短信", "站内信"])),
    form.checkbox_item("accepted", &accepted)
        .map(|item| item.label("我已阅读并接受")),
    form.switch_item("notifications", &notifications),
    form.slider_item("volume", &volume, 0.0..=100.0)
        .map(|item| item.step(5.0)),
    form.rate_item("rating", &rating)
        .map(|item| item.count(5).clearable()),
    form.radio_item("delivery", &delivery)
        .map(|item| item.options(["标准", "加急"]).group_name("delivery")),
    form.segmented_item("density", &density)
        .map(|item| item.options(["紧凑", "舒适", "宽松"]).disable_option(2)),
));

let submitted = form.submit();
```

`input_item(field, &State<String>)`、`select_item(field, &State<T>)` 与 `input_number_item(field, &State<T>)` 对已声明字段分别返回 `FormInputItem`、`FormSelectItem<T>` 与 `FormInputNumberItem<T>`；未知字段返回 `None`，`Option<View>` 可直接放入 `column` / `row`。`FormSelectItem` 支持单选 `State<String>` 与多选 `State<HashSet<String>>`，并可设置 `options`、`optgroups`、`searchable`、`placeholder`、`size` 与 `disabled`；选项文本就是 typed 真值。多选集合向文本规则投影时会先排序、再以换行连接，`Values::get::<HashSet<String>>()` 仍返回原始集合。数值绑定支持 `InputNumberValue` 覆盖的标准整数与浮点数，并可继续设置 `placeholder`、`min`、`max`、`step`、`size` 与 `disabled`。

`checkbox_item(field, &State<bool>)` 与 `switch_item(field, &State<bool>)` 返回 `FormCheckboxItem` / `FormSwitchItem`；前者可用 `label` 设置复选框自身说明，两者都支持 `size`、`disabled` 与 `show_error`。布尔值的规则文本为 `"true"` / `"false"`，所以“必须开启”应使用 `.custom(...)`；`.required(...)` 只判断文本是否为空，不会拒绝 `false`。

`slider_item(field, &State<f64>, range)` 返回 `FormSliderItem`，区间仍由 Slider 归一，`step` 与 `size` 保留原组件语义。`rate_item(field, &State<u32>)` 返回 `FormRateItem`，可设置 `count`、`allow_half`、`clearable`、`character`、`size` 与 `disabled`；启用半星后 State 的一个单位仍表示半星。

`radio_item(field, &State<String>)` 返回 `FormRadioItem`，支持 `options`、`group_name`、`vertical`、`size` 与 `disabled`；未指定组名时使用字段名。`segmented_item(field, &State<String>)` 返回 `FormSegmentedItem`，支持 `options`、`disable_option`、`size` 与 `disabled`。两者都以选项文本为 typed 真值，键盘导航和禁用选项跳过规则仍由原组件负责。

Date / Time / Color 使用各自的 typed State：

```rust uix-compile=form-picker-items
let date = State::new(Date::new(2026, 7, 16));
let range_start = State::new(Date::new(2026, 7, 16));
let range_end = State::new(Date::new(2026, 7, 20));
let time = State::new(Time::new(9, 30));
let color = State::new(Color::blue());
let form = Form::new()
    .field("date", "日期")
        .default(Date::new(2026, 7, 16))
    .field("range_start", "开始日期")
        .default(Date::new(2026, 7, 16))
    .field("range_end", "结束日期")
        .default(Date::new(2026, 7, 20))
        .depends_on("range_start", |_, values| {
            let start = values.get::<Date>("range_start").copied().unwrap_or_default();
            let end = values.get::<Date>("range_end").copied().unwrap_or_default();
            (start <= end).then_some(()).ok_or_else(|| "结束日期不能早于开始日期".to_string())
        })
    .field("time", "时间")
        .default(Time::new(9, 30))
    .field("color", "颜色")
        .default(Color::blue())
    .build();

let pickers = column((
    form.date_picker_item("date", &date)
        .map(|item| item.placeholder("选择日期").mode(PickerMode::Date)),
    form.date_range_picker_item("range_start", &range_start, "range_end", &range_end)
        .map(|item| item.label("日期范围").placeholder("选择日期范围")),
    form.time_picker_item("time", &time)
        .map(|item| item.placeholder("选择时间")),
    form.color_picker_item("color", &color),
));
```

`date_picker_item`、`time_picker_item` 与 `color_picker_item` 分别返回 `FormDatePickerItem`、`FormTimePickerItem` 与 `FormColorPickerItem`。`date_range_picker_item(start_field, &start, end_field, &end)` 返回一个共享 `FormItem` 的 `FormDateRangePickerItem`；两个字段分别保留规则、错误和 typed `Date`，Change / Blur 同步作用于两者，任一字段成为提交首错时都会聚焦同一个范围控件。Date 文本投影为 `YYYY-MM-DD`，Time 为 `HH:MM`，Color 为 `#RRGGBBAA`；文本规则读取该稳定表示，`Values` 仍以 `Date` / `Time` / `Color` 返回。日期选择粒度、范围排序、预设与禁用日期继续由对应 Picker 负责。

输入修改同时回写 State 与 `FormModel`，`OnChange` 立即校验，`FocusOut` 激活 `OnBlur`；字段经 Blur 或 Submit 激活后，后续修改会持续重验。错误状态自动 reconcile 为红色字段状态和首条内联文本，`show_error(false)` 只隐藏文本，不清除错误状态。

`submit()` 执行整表校验并返回 typed `Values` 或有序 `Vec<FieldError>`；失败时会尽力通过该字段的 `FocusHandle` 聚焦首错。`FormModel::clone()` 共享值、错误与焦点句柄，可安全移入同一 UI 线程的事件闭包。

### 表单联动

表单与普通 View 组合使用；提交按钮的回调可捕获 `form.clone()` 后调用 `submit()`，业务层只处理成功 values 或错误列表。字段依赖与动态列表仍沿用下文现有模型 API。

### 内置校验规则

| 方法 | 说明 |
|------|------|
| `.required(msg)` | 非空 |
| `.validate_email(msg)` | 邮箱格式 |
| `.validate_range(0..=100, msg)` | 数值范围 |
| `.validate_length(2..=20, msg)` | 字符串长度 |
| `.validate_pattern(regex, msg)` | Rust regex；非法表达式按该字段失败 |
| `.validate_url(msg)` | URL 格式 |
| `.validate_phone(msg)` | 手机号格式 |
| `.custom(\|v\| Result<(), String>)` | 自定义逻辑 |

### 校验触发时机

```rust uix-compile=form-validation-triggers
let form = Form::new()
    .field("email", "邮箱")
        .default("")
        .required("请输入邮箱")
        .validate_email("格式不正确")
        .validate_trigger(Trigger::OnBlur)    // 失焦即校验
    .field("username", "用户名")
        .default("")
        .required("必填")
        .validate_trigger(Trigger::OnChange)  // 输入即校验
    .build();

form.set_value("username", "Ada"); // 执行 username 的 OnChange 规则
form.set_value("email", "ada@example.test");
form.blur("email");                 // 执行 email 的 OnBlur 规则
let visible_errors = form.errors();
```

### 字段间依赖校验

被依赖字段变更后级联校验：

```rust uix-compile=form-dependent-validation
let form = Form::new()
    .field("password", "密码")
        .default("")
        .required("请输入密码")
    .field("confirm", "确认密码")
        .default("")
        .depends_on("password", |v, deps| {
            let Some(pwd) = deps.get::<String>("password") else {
                return Err("缺少密码字段".into());
            };
            if v != pwd { Err("两次密码不一致".into()) } else { Ok(()) }
        })
    .build();
```

依赖源经 `set_value()` 变化后，直接和传递依赖各按声明顺序重验一次；循环依赖由访问集截断，不会递归忙循环。提交校验时，依赖闭包读取的是本轮全部字段的 typed `Values`。

### 动态表单（Form::List）

增删表单行：

```rust uix-compile=dynamic-form-list
let mut list = Form::new()
    .list("items", |items| {
        items
            .field("name", "名称").default("").required("必填")
            .field("qty", "数量").default(1i32).validate_range(1..=100, "1-100")
    })
    .build();

if let Ok(item_id) = list.add_item() { // ID 耗尽时返回 typed FormListError
    list.set_value(item_id, "name", "主机");
}
let rows = column(list.render_rows(|item_id, index| {
    label(format!("第 {} 行 · {}", index + 1, item_id.get()))
}));
list.remove_item(0);
```

`render_rows()` 在 View 构建期读取列表结构，并按 `FormListItemId` 为每行写入稳定 key；`add_item()` / `remove_item(index)` / `clear()` 只更新该结构 `State`，自动请求所属根 View reconcile，未删除行保留组件实例。字段值和校验结果逐行隔离；`validate()` 成功返回 `FormListValues`，失败按当前行顺序、再按字段声明顺序返回 `Vec<FormListFieldError>`。
