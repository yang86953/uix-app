//! 自 src/draw/geometry/spatial/physical_box.rs 迁出的文档测试样例；原检查语义保留，仅源仓 RUSTDOCFLAGS
//! --cfg uix_repo_doc_contract 下的 cargo test --doc 收集，不进发布包。


/// 迁出的文档样例 1（原 src/draw/geometry/spatial/physical_box.rs:11）：
/// ```rust
/// use uix_app::draw::geometry::spatial::{PhysicalBox, PhysicalUnitExt};
/// let pb = PhysicalBox::new(10.0.mm(), 5.0.mm(), 0.0.mm(), 5.0.cm(), 3.0.cm(), 1.0.mm());
/// ```
pub struct DocExample1;
