//! 自 src/draw/painting/paint_context/mod.rs 迁出的文档测试样例；原检查语义保留，仅源仓 RUSTDOCFLAGS
//! --cfg uix_repo_doc_contract 下的 cargo test --doc 收集，不进发布包。


/// 迁出的文档样例 1（原 src/draw/painting/paint_context/mod.rs:69）：
/// ```ignore
/// let fs = resolve_font_size(self.font_size, self.font_size_unit, ctx.dpi());
/// ```
pub struct DocExample1;
