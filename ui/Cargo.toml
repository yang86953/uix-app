[package]
name = "uix-ui"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
uix-graphics = { path = "../graphics" }
uix-platform = { path = "../platform" }

[features]
# 启用平台层测试框架（FakePlatform），用于 UI 层集成测试
test-harness = ["uix-platform/test-harness"]

[lints]
workspace = true
