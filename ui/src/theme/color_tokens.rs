//! Color token trait — brand, neutral, semantic, shadow, and link colors.
//!
//! `ShadowToken` 与 `IColorTokens` 定义在 `uix_graphics::painting`。

pub use uix_graphics::painting::{IColorTokens, ShadowToken};
