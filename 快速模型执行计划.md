# UIX App：0.0.1 快速模型分阶段执行计划

> 本计划只定义阶段、原子任务、依赖和验收，不安排日历。每次只派发一个任务 ID 给快速模型；任务通过并回写证据后，才能派发它的直接后继。

## 执行目标

完成 UIX `0.0.1` 的 Windows x64 内部交付。当前 110 项功能已进入真实行为路径，执行重点不是继续扩功能，而是闭合：

1. callback → owner-thread 运行保障、teardown 和 ABI panic 边界。
2. typed failure、真实恢复、旧日志、CrashReport 和架构反向依赖。
3. 110 项真窗与无障碍、92 项组件视觉矩阵。
4. NVIDIA / AMD / Intel、96 / 144 / 192 DPI、最低 Windows build 26100、DeviceLost、多窗和长稳证据。
5. 内部 ZIP、许可材料、校验和、解包与回滚。

## 快速模型执行合同

### 每次派发只包含一个任务

快速模型不得自动继续下一个任务。一个任务只能有一个主要目标、一个受控修改范围和一组验收证据。

### 每次执行前必须读取

1. `C:\data\note\AGENTS.md`
2. `C:\data\note\我的项目\软件\UIX App\项目上下文索引.md`
3. 本计划中对应任务行
4. 任务所属的权威架构文档
5. 任务所属的源码进度文档
6. 当前正式 WorkItem；没有 WorkItem 时先创建，不直接改代码

项目任务召回总数仍遵守知识库的 10 项硬上限。

### 每次执行必须遵守

- 开始前检查 Git 分支、状态和最新提交。
- 只修改任务明确允许的模块、测试和进度文档。
- 不恢复 `pub mod native`，不增加兼容 facade、旧路径 alias 或新旧 DTO 双轨。
- 不扩大公开 API；确需改变时停止并标记 `needs-decision`。
- 不顺手处理兄弟任务、跨平台延期项或无关警告。
- 失败必须保留 typed cause；不能用 bool、空字符串、null 或日志冒充成功。
- 没有真实硬件、视觉或长稳证据时标记 `blocked`，不能降级成 pass。
- 默认不提交、不推送；只有派发任务明确授权时执行。
- 完成后同步 WorkItem、对应 `docs/进度/` 和必要的知识库权威文档。

### 快速模型回报格式

```text
任务 ID：
状态：done | blocked | needs-decision
修改范围：
完成内容：
验证命令与结果：
证据路径：
未覆盖范围：
下一可派发任务：
```

## 执行者分类

| 执行者 | 适合任务 | 不应承担 |
|---|---|---|
| 快速模型 | 单模块代码纵切、定向测试、脚本、清单和文档回写 | 产品取舍、跨阶段大重构、主观视觉放行 |
| 视觉模型 / 人工 | 原分辨率截图、对比度、裁切、重叠、焦点可见性 | 用肉眼结论替代行为和语义测试 |
| 目标硬件执行者 | GPU、DPI、最低 OS、DeviceLost、长稳 | 用模拟器或其他 GPU 代替真实目标 |
| 主人 | 产品范围、例外、许可、包内容和最终发布 | 普通代码任务的逐项完成确认 |

## 阶段总览

| 阶段 | 目标 | 入口条件 | 出口门禁 |
|---|---|---|---|
| 阶段 0 | 冻结范围、基线、分母和外部条件 | 项目上下文可读取 | 任务与证据路由明确，外部阻塞可见 |
| 阶段 1 | 闭合 callback、teardown 和 ABI P0 | 阶段 0 通过 | release path callback 只入队，owner-thread 唯一处理 |
| 阶段 2 | 统一失败语义、真实恢复和日志 | 阶段 1 通过 | 无 bool / 空值失败双轨，无旧日志双写 |
| 阶段 3 | CrashReport、theme 分层与公开面冻结 | 阶段 2 通过 | 运行保障和架构门禁通过，公开行为冻结 |
| 阶段 4 | 组件、视觉、文档示例和无障碍证据 | 阶段 3 通过 | 110 / 110 与 92 项矩阵均绑定 clean 候选 |
| 阶段 5 | 硬件、DPI、性能、DeviceLost 与长稳 | 阶段 4 通过 | GFX-R5 和 G5 必需类全部 pass |
| 阶段 6 | 打包、解包、回滚和内部发布 | 阶段 5 通过 | clean 包可复验，全部门禁通过 |

## 阶段 0：范围与基线

### 可派发给快速模型

| ID | 原子任务 | 允许范围与产出 | 依赖 | 验收 |
|---|---|---|---|---|
| S0-01 | 生成源码基线清单 | 只读检查 Git、Cargo、测试、脚本和已知失败；输出基线报告 | 无 | commit、工作树、工具链、特性和失败项均可追溯 |
| S0-02 | 建立证据分母映射 | 映射 110 功能、92 组件、663 状态、847 视觉证据、42 项专项修复 | S0-01 | 每个数字有唯一语义和来源，不互相换算 |
| S0-03 | 建立门禁与证据路径表 | 把代码测试、真窗、无障碍、GFX-R5、G5、打包映射到阶段出口 | S0-01 | 每个门禁有命令、证据路径和失败状态 |
| S0-04 | 审计当前文档门禁 | 运行并记录 `check_docs_links.py` 的真实失败，不修改内容 | S0-01 | 区分迁移后脚本缺口与真正失效链接 |
| S0-05 | 审计当前打包门禁 | 只读检查 `build_internal_release.ps1` 的 payload 与仓库实际文件 | S0-01 | 缺失负载和过期假设形成清单 |

### 只能由主人或外部条件完成

| ID | 决策 / 外部任务 | 建议默认值 | 对后续影响 |
|---|---|---|---|
| D0-01 | Windows system notification 是否进入本版 | 保持明确 `unsupported` | 若要求实现，必须新增独立设计任务 |
| D0-02 | 内部包内容与代码签名 | `.crate`、demo、资产、LICENSE、第三方声明、变更记录、SHA256；签名单独决定 | 决定阶段 6 的 payload |
| D0-03 | AMD / Intel / 多 DPI / 最低 OS 环境 | 必须提供真实目标 | 缺失时阶段 5 保持 blocked |

### 阶段出口

- S0-01～S0-05 全部完成。
- D0-01～D0-03 已有结论或明确 blocked。
- 项目总进度仍不计算；只使用阶段任务和证据分母。

## 阶段 1：callback、teardown 与 ABI

| ID | 原子任务 | 允许范围与产出 | 依赖 | 验收 |
|---|---|---|---|---|
| S1-01 | wgpu offscreen destroy 失败入队 | graphics context、Diagnostics pending source、定向测试 | S0-03 | callback 只入队；typed reason 在 owner-thread 处理 |
| S1-02 | graphics teardown 与 source close | teardown、generation、replacement context 测试 | S1-01 | 晚到 callback 不污染 replacement context；重复 teardown 安全 |
| S1-03 | Windows custom chrome callback 入队 | custom chrome 回调和 Windows owner 安全点 | S1-02 | 不直接 tracing / report / subscriber；最终只处理一次 |
| S1-04 | Windows TSF callback 入队 | TSF / IME callback 和 owner 安全点 | S1-03 | 保留 typed cause；不跨线程调用用户逻辑 |
| S1-05 | Win32 ABI panic 边界 | wnd_proc、custom chrome、TSF 和生成 FFI thunk | S1-03、S1-04 | unwind 不跨 ABI；各回调使用安全返回值 |
| S1-06 | pending queue 容量与 source 压力测试 | 固定容量、overflow、drop、generation、线程亲和测试 | S1-01～S1-05 | 64 容量不变；overflow 只产生一次 `InsufficientResources` |

### 阶段出口

- release path 的 wgpu、wnd_proc、frame pacer、custom chrome 和 TSF callback 全部只入队。
- offscreen、teardown、晚到 callback、queue overflow 和 ABI panic 均有回归证据。
- callback 不执行 recovery handler、subscriber 或用户代码。

## 阶段 2：typed failure、真实恢复与日志

| ID | 原子任务 | 允许范围与产出 | 依赖 | 验收 |
|---|---|---|---|---|
| S2-01 | 审计 bool / 空值 / null 失败 | 只读扫描 crate-private platform release path | 阶段 1 | event loop、timer、display、clipboard、cursor、dialog 清单完整 |
| S2-02 | event loop 失败改为 typed Result | event loop trait、实现和定向测试 | S2-01 | 暂无事件与真实失败不再共用返回值 |
| S2-03 | timer 失败改为 typed Result | timer trait、实现和定向测试 | S2-01 | 取消、到期、平台失败语义分离 |
| S2-04 | display 查询失败改为 typed Result | display trait、DTO 转换和测试 | S2-01 | 空显示器集合与查询失败语义分离 |
| S2-05 | clipboard 失败改为 typed Result | clipboard trait、Windows 实现和测试 | S2-01 | 空剪贴板、取消和失败语义分离 |
| S2-06 | cursor / dialog 失败改为 typed Result | cursor、dialog trait 和测试 | S2-01 | 取消不冒充失败，失败不冒充空结果 |
| S2-07 | 注册真实 device-lost recovery | graphics recovery handler 和 owner-thread 安全点 | S1-02、S2-02 | typed device-lost 触发领域恢复；`report` 不自动 recovery |
| S2-08 | platform 旧日志迁移 | platform 中真实使用的 `core::log::*` | S2-02～S2-07 | 可返回错误继续上抛；final owner 才报告 |
| S2-09 | graphics 旧日志迁移 | graphics 中真实使用的旧日志 | S2-07、S2-08 | 无 tracing / typed Error 双写 |
| S2-10 | ui / data 旧日志迁移 | ui、data 中真实使用的旧日志 | S2-09 | 热路径不提前物化日志字符串 |
| S2-11 | app 旧日志迁移 | app 中真实使用的旧日志 | S2-10 | App 最终责任边界只报告一次 |
| S2-12 | 删除旧运行保障轮子 | Collector、Logger、Retry、Middleware 和旧 diagnostic 目录 | S2-08～S2-11 | 源码与公开面不可再引用；compile-fail 阻断旧入口 |

### 阶段出口

- release path 不再用 bool、空字符串或 null 表达失败。
- device-lost 有一个真实恢复注册和安全点证据。
- 旧日志和重复运行保障实现已删除，不存在桥接双写。

## 阶段 3：CrashReport、theme 分层与公开面

| ID | 原子任务 | 允许范围与产出 | 依赖 | 验收 |
|---|---|---|---|---|
| S3-01 | 定义 CrashReport 有界数据模型 | diagnostics 私有模型、脱敏与序列化测试 | 阶段 2 | 字段有界，不写敏感值，损坏输入返回 typed Error |
| S3-02 | 实现 CrashReport 原子写入 | 配置目录、临时文件、replace 和故障测试 | S3-01 | 磁盘失败有 typed cause；不留下伪完整报告 |
| S3-03 | 接入 panic hook 与安全终止 | panic hook、递归 guard、最终终止测试 | S3-01、S3-02 | 不吞 panic；hook 内二次失败仍安全终止 |
| S3-04 | 把 ThemeSnapshot 归还 UI | theme snapshot 类型、UI 绘制接入和调用点 | 阶段 2 | graphics 不持有 UI theme 类型 |
| S3-05 | 拆分 UI-owned 与 graphics PaintContext | `tokens()`、patch scope、解析后视觉值边界 | S3-04 | graphics 只接收已解析颜色、尺寸和绘制值 |
| S3-06 | 闭合 TokenPatch scope 生命周期 | UI subtree scope、正常返回与 panic 展开测试 | S3-05 | scope 不泄漏到兄弟节点 |
| S3-07 | 增加 graphics → ui 依赖门禁 | 架构测试、`rg` 规则或 compile-fail | S3-04～S3-06 | `src/draw` 不引用 `crate::ui` |
| S3-08 | 增加 `uix::native` 公开面 deny | native 根、SPI、raw handle、context、厂商对象 compile-fail | 阶段 2 | 所有内部实现对外不可达 |
| S3-09 | 修复文档链接门禁 | 只修改 `check_docs_links.py` 及其测试 | S0-04 | 理解知识库权威外链；真正失效链接仍失败 |
| S3-10 | 修复打包 payload 门禁 | 只修改打包脚本及脚本测试 | S0-05、D0-02 | 缺失文件在构建前明确失败；payload 与包契约一致 |
| S3-11 | 生成工程冻结报告 | 汇总公开 API、布局、主题、渲染和门禁状态 | S3-01～S3-10 | 后续除 P0 / P1 修复外不再改变视觉与公开行为 |

### 阶段出口

- CrashReport、panic hook、写入失败和安全终止有完整证据。
- `src/draw` 不反向依赖 `crate::ui`。
- `uix::native` 和 backend SPI 对外不可达。
- 文档和打包脚本与当前知识库边界一致。
- 公开 API、布局、主题和渲染结果冻结。

## 阶段 4：组件、视觉、示例与无障碍

| ID | 原子任务 | 执行者 | 允许范围与产出 | 依赖 | 验收 |
|---|---|---|---|---|---|
| S4-01 | 为组件矩阵增加 clean commit manifest | 快速模型 | 测试执行器、环境、文件哈希和 manifest | 阶段 3 | dirty 工作树不能生成发布级 pass |
| S4-02 | 固化 110 功能 ↔ 92 组件映射 | 快速模型 | 功能账本和证据索引 | S0-02 | 每个功能有行为、真窗、无障碍证据槽 |
| S4-03 | 固化无障碍证据 schema | 快速模型 | role、name、state、value、actions、focus、keyboard | S4-02 | 缺字段不能标记通过 |
| S4-04 | 适配知识库 Rust fence 验证 | 快速模型 | fence 清单工具和定向测试 | S3-09 | 当前权威使用文档中的 Rust fence 可重复编译 |
| S4-B1 | 第 1 批证据闭合 | 快速模型 | 只处理第 1 批场景、测试和缺陷 | S4-01～S4-04 | 本批行为、真窗、无障碍全部通过 |
| S4-B2 | 第 2 批证据闭合 | 快速模型 | 只处理第 2 批场景、测试和缺陷 | S4-B1 | 本批行为、真窗、无障碍全部通过 |
| S4-B3 | 第 3 批证据闭合 | 快速模型 | 只处理第 3 批场景、测试和缺陷 | S4-B2 | 本批行为、真窗、无障碍全部通过 |
| S4-B4 | 第 4 批证据闭合 | 快速模型 | 只处理第 4 批场景、测试和缺陷 | S4-B3 | Table、Tree 和数据展示边界通过 |
| S4-B5 | 第 5 批证据闭合 | 快速模型 | 只处理第 5 批场景、测试和缺陷 | S4-B4 | 浮层、焦点 trap、关闭和恢复通过 |
| S4-B6 | 第 6 批证据闭合 | 快速模型 | 只处理第 6 批场景、测试和缺陷 | S4-B5 | 导航、受控状态、键鼠和 reconcile 通过 |
| S4-B7 | 第 7 批证据闭合 | 快速模型 | 只处理图表场景、测试和缺陷 | S4-B6 | 主题、布局、交互和绘制状态通过 |
| S4-B8 | 第 8 批证据闭合 | 快速模型 | 只处理排版和基础设施场景 | S4-B7 | 生命周期、动画、搜索和回调通过 |
| S4-13 | 重跑 92 项完整视觉矩阵 | 快速模型 | clean 候选、329 原图、联系表、CSV、日志 | S4-B1～S4-B8 | 92 项、663 状态、847 证据均绑定同一 commit |
| S4-14 | 原分辨率视觉复核 | 视觉模型 / 人工 | 329 原图和联系表 | S4-13 | 裁切、重叠、对比度、缺字、焦点和展开态无 P0 / P1 |
| S4-15 | 汇总阶段证据 | 快速模型 | manifest、功能账本、组件稳定性和进度回写 | S4-14 | 110 / 110 真窗、110 / 110 无障碍、92 / 92 视觉可追溯 |

### 阶段出口

- Rust fence、行为测试、真窗、无障碍和视觉矩阵全部绑定同一 clean 候选。
- 视觉模型 / 人工已完成原分辨率复核。
- 没有开放 P0 / P1 组件缺陷。

## 阶段 5：目标硬件、DPI、性能与长稳

| ID | 原子任务 | 执行者 | 依赖 | 验收 |
|---|---|---|---|---|
| S5-01 | GFX-R5 inventory 与 preflight | 快速模型 | 阶段 4 | runner、ignored tests、vendor 参数和证据 schema 一致 |
| S5-02 | NVIDIA vendor profile | 目标硬件执行者 | S5-01 | resize、present、readback、surface recovery 全部 pass |
| S5-03 | AMD vendor profile | 目标硬件执行者 | S5-01 | 同上，证据识别真实 AMD adapter |
| S5-04 | Intel vendor profile | 目标硬件执行者 | S5-01 | 同上，证据识别真实 Intel adapter |
| S5-05 | 144 DPI 往返 | 目标硬件执行者 | S5-01 | logical extent 保持，drawable 与 DPI 对应 |
| S5-06 | 192 DPI 往返 | 目标硬件执行者 | S5-01 | logical extent 保持，drawable 与 DPI 对应 |
| S5-07 | 最低 Windows build 冒烟 | 目标硬件执行者 | S5-01 | 启动、resize、输入法、主题、浮层、关闭通过 |
| S5-08 | 多窗、最小化 / 隐藏与 DeviceLost | 目标硬件执行者 | S5-02～S5-04 | 窗口隔离、恢复、replacement context 和 typed cause 通过 |
| S5-09 | G5 performance | 目标硬件执行者 | 阶段 4 | ≥10,000 presented 帧，p95 ≤16.7 ms，5 类各 1,000 次闭环 |
| S5-10 | G5 soak | 目标硬件执行者 | S5-09 | 连续 8 小时，内存增长 ≤64 MiB，idle p95 ≤0.25 core |
| S5-11 | 验证并汇总硬件证据 | 快速模型 | S5-02～S5-10 | 所有 manifest、文件哈希、环境和 runner 可重新验证 |

### 阶段出口

- NVIDIA、AMD、Intel、144 / 192 DPI 和最低 OS 均有真实证据。
- GFX-R5 必需 profile 全部 pass。
- G5 performance 与 soak 均为 pass；`blocked` 不能作为通过。

## 阶段 6：内部包与发布

| ID | 原子任务 | 执行者 | 允许范围与产出 | 依赖 | 验收 |
|---|---|---|---|---|---|
| S6-01 | 生成第三方许可声明 | 快速模型 | Cargo.lock、依赖许可、资产许可 | D0-02、阶段 5 | 内容来自当前依赖，不恢复过期副本 |
| S6-02 | 生成当前变更记录 | 快速模型 | 当前版本的提交与交付事实 | 阶段 5 | 只写可验证变更和已知限制 |
| S6-03 | 验证打包脚本失败前置 | 快速模型 | 打包脚本和脚本测试 | S3-10、S6-01、S6-02 | 缺 payload、dirty tree、版本不符均在复制前失败 |
| S6-04 | 构建 clean 内部包 | 快速模型 | `.crate`、demo、资产、许可、变更记录、SHA256 | S6-03 | ZIP 绑定 clean commit，payload 完整 |
| S6-05 | 干净机器解包冒烟 | 目标硬件执行者 | 只使用发布 ZIP | S6-04 | demo、字体、shader、资产、Vulkan 和 Software 回退通过 |
| S6-06 | 升级与回滚演练 | 目标硬件执行者 | 当前包与上一可用包 | S6-05 | 新旧包可并存，回滚不依赖隐式状态 |
| S6-07 | 发布证据总审计 | 快速模型 | G0～G5、manifest、哈希、缺陷和限制 | S6-04～S6-06 | 所有门禁 pass，无开放 P0 / P1 |
| D6-01 | 最终内部发布决定 | 主人 | 发布包与总审计 | S6-07 | 主人明确放行后才更新为已发布 |
| S6-08 | 同步项目状态 | 快速模型 | 项目根、WorkItem、源码进度和发布记录 | D6-01 | 状态、证据、版本和下一步一致 |

## 通用验证命令

每个代码任务至少执行最小充分验证：

```powershell
cargo check --workspace --all-targets
cargo test --tests
cargo fmt --all -- --check
git diff --check
```

按影响追加：

```powershell
cargo check --lib --target x86_64-pc-windows-gnu
cargo check --lib --no-default-features
cargo check --lib --no-default-features --features vulkan
python scripts\check_docs_links.py
```

冻结候选使用：

```powershell
python scripts\run_release_g5.py --mode performance
python scripts\run_release_g5.py --mode soak
python scripts\run_windows_gfx_r5.py --profile all --vendor nvidia --device-fault false
python scripts\run_windows_gfx_r5.py --profile all --vendor amd --device-fault false
python scripts\run_windows_gfx_r5.py --profile all --vendor intel --device-fault false
powershell -ExecutionPolicy Bypass -File scripts\build_internal_release.ps1
```

`device-fault` 参数必须与目标 GPU 的真实能力一致。

## 进度回写规则

每次只更新以下状态：

| 字段 | 规则 |
|---|---|
| 当前任务 | 使用唯一任务 ID |
| 状态 | `ready / in-progress / blocked / needs-decision / done` |
| 阶段覆盖 | 已完成原子任务数 / 本阶段任务数 |
| 证据 | 命令、结果、路径、commit 和环境 |
| 下一任务 | 只列依赖已经满足的直接后继 |

不得输出项目整体百分比，直到所有实施叶子 WorkItem 正式落档并把 `pm_task_tree_status` 改为 `complete`。

证据分母保持独立：

- 功能：110。
- 组件库存：92。
- 声明状态：663。
- 视觉证据行：847。
- 真窗功能：110。
- 无障碍功能：110。
- GPU × DPI 冒烟组合：9。
- GFX-R5：按 runner case 和 vendor 分别统计。
- G5：只使用 `blocked / fail / pass`。

## 快速模型派发模板

```text
任务：<任务 ID + 标题>

目标：
只完成该任务，不自动继续后继。

必须读取：
- C:\data\note\AGENTS.md
- C:\data\note\我的项目\软件\UIX App\项目上下文索引.md
- 本计划对应任务
- 对应架构权威文档
- 对应 docs\进度文档
- 正式 WorkItem

允许修改：
<明确目录、文件或模块>

禁止：
- 扩大公开 API
- 新增兼容层或双轨
- 修改兄弟任务
- 无证据标记完成
- 未授权提交或推送

验收：
<从任务行复制完成条件和命令>

完成后：
- 更新 WorkItem 与审计证据
- 更新对应进度文档
- 返回状态、修改范围、验证、未覆盖范围和下一可派发任务
```

## 延后事项

以下事项不进入 `0.0.1`，不得由快速模型顺手实施：

- Wayland / macOS 同等交付与完整 callback 覆盖。
- D3D11、D3D12、OpenGL / OpenGLES、Metal 正式交付构建。
- Agent Linux / macOS 异 uid 拒绝。
- 复杂脚本 shaping、双向文本、字素簇光标和完整 UAX #14。
- 移动端 backend。
- 窄 present。
- 全仓一次性完成所有 System / Module / Component 目录迁移。
